Write the audit report to exactly one file and write nothing outside this directory: `{repo}/.worc/security-reports/{task_id}/report.md`. Cover each verified finding — location (file/line or dependency), severity, impact, and a concrete remediation — and list separately the threats dismissed as false positives with the reason. This is a private control-workspace report: it stays under the gitignored `.worc/` home and is never committed. Do not modify any source file. Return the typed structured result required by the output schema.
