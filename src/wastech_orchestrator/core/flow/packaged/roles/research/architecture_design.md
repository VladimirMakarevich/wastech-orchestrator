From the repository and external evidence, work out the answer's structure: the options, trade-offs, and a recommended approach grounded in what the evidence supports. Capture the reasoning so the synthesis can present it with citations. You may organize notes only under {repo}/docs/research/{task_id}/; never modify source files outside that directory. Return the typed structured result required by the output schema.
