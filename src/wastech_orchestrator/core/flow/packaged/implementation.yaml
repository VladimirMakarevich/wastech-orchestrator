# Packaged built-in flow: implementation (P1 parity shape — the executable flow the engine drives).
#
# This is the pre-refactor pipeline expressed as data: refinement -> planning -> implementation ->
# testing(checks) -> review(evaluator) -> fixing -> summary -> publish, with the two fix loops and
# decomposition, and WITHOUT supervisor / hybrid testing_quality / durable sessions. Those are the
# target features layered on in P2; the TARGET shape (supervise_impl / testing_quality / final-
# handoff summary) is preserved in docs/backlog/flows/co-design/implementation.yaml + flow-contract
# §7, and P2.5 grows this packaged file back to it.
flow:
  name: implementation
  task_type: implementation
  permission_ceiling: workspace-write
  output_policy: code_change
  publishing: pull_request

  nodes:
    - id: refinement
      kind: agent
      role_file: roles/refinement.md
      session_scope: fresh_disposable
      permission_profile: read-only
      output_artifact: enriched_spec
      hitl: { allow_question: true, allow_approval: false }
      when: { fact: derived.needs_refinement }
    - id: planning
      kind: agent
      role_file: roles/planning.md
      session_scope: fresh_disposable
      permission_profile: read-only
      output_artifact: plan
      hitl: { allow_question: true, allow_approval: true }
      when: { fact: config.planning_enabled }
    - id: implementation
      kind: agent
      role_file: roles/implementation.md
      session_scope: editing_lineage
      permission_profile: workspace-write
    - id: testing
      kind: checks
      checker: command_profile
      discovery: { mode: auto, approve_command_changes: true }
      when: { fact: config.testing_enabled }
    - id: review
      kind: evaluator
      role: review
      role_file: roles/review.md
      session_scope: fresh_disposable
      permission_profile: read-only
      when: { fact: config.review_enabled }
    - id: fixing
      kind: agent
      role_file: roles/fixing.md
      session_scope: editing_lineage
      lineage_affinity: implementation
      permission_profile: workspace-write
      when: { fact: config.fixing_enabled }
    - id: summary
      kind: agent
      role_file: roles/summary.md
      session_scope: fresh_disposable
      permission_profile: read-only
      output_artifact: summary
      best_effort: true
      when: { fact: config.summary_enabled }
    - id: publish
      kind: publish
      policy: pull_request

  edges:
    - { from: refinement, to: planning }
    - { from: planning, to: implementation }
    - { from: implementation, to: testing }
    - { from: testing, to: review, outcome: pass }
    - { from: testing, to: fixing, outcome: fail, loop: test_fix }
    - { from: review, to: summary, outcome: accept }
    - { from: review, to: fixing, outcome: rework, loop: review_fix }
    - { from: fixing, to: testing }
    - { from: summary, to: publish }

  budgets:
    global_fix_iterations: 30
    test_fix: 15
    review_fix: 15

  decomposition:
    proposed_by: planning
    gate: { min: 2, max: 8, linear_depends_on: true }
    sub_flow: [implementation, testing, review, fixing]
    commit_each_subtask: true
    shared_budget: global_fix_iterations
