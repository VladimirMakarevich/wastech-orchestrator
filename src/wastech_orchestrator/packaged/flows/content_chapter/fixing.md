Apply the outstanding findings to the chapter in the working tree, resuming the editor's lineage. There are two sources:

- The story critic's findings at {review_path} — each names a `path`, a `what`, and a suggested `fix`.
- The deterministic prose gate's report at {constraints_path} — structural violations (heading hierarchy, ≤1 title per page, missing Purpose/Emotional point, banned AI patterns, service-label headings; this flow's gate does not cap page length).

Make the smallest edits that resolve each finding. Preserve the author's voice and every fact grounded in the project's continuity reference, keep this book's voices/registers in their own `###` blocks, and do not introduce a new violation while fixing another — your edit re-enters the deterministic gate next. Do not rewrite passages that already work.
