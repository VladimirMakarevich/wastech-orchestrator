Apply the outstanding findings to the article in the working tree, resuming the editor's lineage. There are two possible sources:

- The tone-of-voice critic's findings at {review_path} — each names a `path`, a `what`, and a suggested `fix`.
- The deterministic length gate's report at {length_path} — a JSON `data.violations` map telling you which article is below its character or paragraph floor, and by how much.

Make the smallest edits that resolve each finding. If a finding says a passage was needlessly rewritten, restore it toward what worked before rather than editing the rewrite further. If the length gate flagged the piece as too short, grow it by deepening the existing argument — add a real example, follow a thread further, meet another angle — never by padding with filler, repetition, or throat-clearing (that would fail the tone gate). Preserve the author's voice and the article's argument, keep the piece grounded in the researcher's findings at {research_path} where relevant, and do not introduce a new problem while fixing another — your edit re-enters the length gate and then the tone/style gate next. Do not touch passages no finding names.

Keep the author's own signature phrasing intact wherever it already appears, if this project documents one — those connectors and asides are the author's fingerprint, not something to smooth away while fixing an unrelated finding.
