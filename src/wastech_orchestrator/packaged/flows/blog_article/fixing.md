Apply the outstanding findings to the article in the working tree, resuming the author's lineage. There are two possible sources:

- The tone-of-voice critic's findings at {review_path} — each names a `path`, a `what`, and a suggested `fix`.
- The deterministic length gate's report at {length_path} — a JSON `data.violations` map telling you which article is below its character or paragraph minimum, and by how much.

Make the smallest edits that resolve each finding. If the length gate flagged the piece as too short, grow it by deepening the existing argument — add a real example, follow a thread further, meet another angle — never by padding with filler, repetition, or throat-clearing (that would fail the tone gate). Preserve the author's voice and the article's argument, keep the piece grounded in the researcher's findings at {research_path}, and do not introduce a new problem while fixing another — your edit re-enters the length gate and then the tone/style gate next. Do not rewrite passages that already work.
