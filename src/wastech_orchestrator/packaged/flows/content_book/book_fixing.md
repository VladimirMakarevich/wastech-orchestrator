Apply the outstanding findings to the assembled book in the working tree, resuming the assembler's lineage. The sources are:

- The book critic's findings at {review_path} — each names a `path`, a `what`, and a suggested `fix`.
- The deterministic gate's report at {constraints_path} — any assembled page that breaks the structural or length limits.

Edit the assembled book directly — do NOT re-stitch from the source chapters (that would discard these fixes). Make the smallest change that resolves each finding, keep the unified voice, and do not introduce a new violation (your edit re-enters the critic and the gate).
