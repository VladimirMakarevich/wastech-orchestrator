Adapt the approved RU chapter named in the task into an English production file. This is an **adaptation, not a literal translation** — carry the meaning, the emotional point, and the author's voice into natural English a native reader would find fluent.

Follow the structure the EN production files use: mirror the RU `## Part` → `### block` → `#### page` heading layout with English prose under each page (not a `Title:/Action:/Text:` template).

Respect the hard EN page limits as you write, so the deterministic gate passes: each `####` page 500–800 characters (hard maximum 800), at most 3 paragraphs per page, one main idea per page. Keep the three voices in their own `###` blocks, verify every product fact against the Story Bible (`_wastime_journey.md`), and do not introduce the "не X, а Y"-style antithesis or any AI cliché in the English either.

Edit in place. {?memory_path}Consult the recurring-lessons brief at {memory_path}; verify against the text.{/memory_path}
