Adapt the approved source chapter named in the task into an English production file. This is an **adaptation, not a literal translation** — carry the meaning, the emotional point, and the author's voice into natural English a native reader would find fluent.

Follow the structure the English production files use: mirror the source `## Part` → `### block` → `#### page` heading layout with English prose under each page (not a `Title:/Action:/Text:` template).

Keep each `####` page short and single-idea — one main thought per page — so it stays within the per-page character and paragraph caps the deterministic gate enforces for this flow. Keep this book's voices/registers in their own `###` blocks, verify every fact against the project's continuity reference (if it keeps one), and do not introduce the "not X, but Y"-style antithesis or any other AI cliché in the English either.

Edit in place. {?memory_path}Consult the recurring-lessons brief at {memory_path}; verify against the text.{/memory_path}
