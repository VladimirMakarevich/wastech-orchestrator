Apply the outstanding findings to the English chapter in the working tree, resuming the adapter's lineage. The sources are:

- The EN critic's findings at {review_path} — each names a `path`, a `what`, and a suggested `fix`.
- The deterministic gate's report at {constraints_en_path} — structural and length violations (heading hierarchy, ≤1 title per page, the per-page character and paragraph caps this flow sets, banned AI patterns, service-label headings). The report names each exact limit and the page that broke it.

Make the smallest edits that resolve each finding. Keep the adaptation natural (never revert to a literal translation to hit a length target — re-express instead), preserve meaning, tone, and voice, and do not introduce a new violation (your edit re-enters the gate).
