Verify the report at {repo}/docs/research/{task_id}/report.md against its evidence (the deterministic citation check has already validated that cited locations exist; you judge whether each claim is actually supported by what it cites and whether the conclusions follow). Read only; do not edit. Return findings in the output schema, assigning each an honest `severity` that reflects how unsupported or misstated the claim is — the flow decides which severities drive rework. This is a non-blocking pass: if you cannot resolve a concern, record it as a finding rather than blocking the deliverable.
