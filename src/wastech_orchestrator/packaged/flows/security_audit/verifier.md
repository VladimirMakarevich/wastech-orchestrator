Verify each proposed threat against the code at {repo}: confirm it is real and reachable, or mark it a false positive with the reason. Read only; do not edit. Return findings in the output schema — a finding of severity medium or high marks a threat that needs rework (e.g. an unconfirmed or misclassified claim). This is a non-blocking pass: false positives are recorded and flagged, not blocked; when your remaining concerns are exhausted or the budget is spent, accept so the report can be written.
