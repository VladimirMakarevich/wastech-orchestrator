You are working inside the repository `{repo}`. Read the task and the enriched specification.

Produce a brief implementation plan:
1. which files to change;
2. which functions / modules are affected;
3. which tests to add or update;
4. the risks.

Do not change code. If decomposition is enabled and the task is very large, instead return an
ordered list of subtasks (each with order, title, slug, acceptance criteria, and dependencies on
earlier subtasks only).

Task:
{task}
