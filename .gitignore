# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
site/
.site-src/
uv.lock

# Secrets (DO NOT commit)
.env
secrets/
*.key
*.pem

# Orchestrator runtime home — config.yaml, state.db, logs/, workspace/, checks/, templates/, guide/
# all live here and are local-only. tasks/ (the audit trail) is intentionally NOT ignored (§21).
.worc/

# IDE / ОС
.idea/
.vscode/
.DS_Store
Thumbs.db

# Obsidian Vault (if using Obsidian for notes)
.obsidian/
