# Implementation follow-ups

IMPORTANT: This is a list of open items that have been identified for further discussion. These should not be taken as mandatory implementation or official requirements; they are simply a list of open items.

Open **implementation** next-steps discovered while building — distinct from [README.md](README.md), which tracks _product features_. Use this for "we deferred X; here's the hook and the context" items, and for findings from real test runs. Record new ones via `/sync-docs` as you create them (date them absolute, link where they're referenced, mark status).

| Date | Item | Context & next step | Referenced in | Status |
| --- | --- | --- | --- | --- |
| 2026-06-12 | **Single umbrella `upgrade`/`doctor` command (config + docs + schema migration)** | Post-upgrade currently needs two steps — `upgrade-config` (materialize new config keys) and `upgrade-docs` (refresh the installed `guide/` docs). Both resolve the install location the same way (`resolve_config_path`) and are fail-closed/idempotent/`--dry-run`. Fold them into one `upgrade` (or `doctor`) that runs both. Fold in the schema-migration remainder too: config value _transforms_ (today's config merge is add-only, not rename/restructure), and a single command that migrates **both** `config.yaml` and `state.db` at once. | `cli.py` `cmd_upgrade_config`/`cmd_upgrade_docs`; `config/upgrade.py`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-12 | **Git-native pre-commit hook** | The Stop docs-sync gate (`.claude/hooks/docs_sync_gate.py`) only covers turns inside Claude Code. For commits made outside the agent, add a real `pre-commit` hook (tracked script + `core.hooksPath`) that enforces the same "code changed ⇒ docs changed" rule. | `.claude/hooks/docs_sync_gate.py`; [.agents/rules/git-workflow.md](../../.agents/rules/git-workflow.md) | candidate |
| 2026-06-12 | **Publish to (Test)PyPI** | Distribution is currently `pipx`/`pip` from a Git tag. To allow `pipx install --pre wastech-orchestrator`, add a publish step to the release workflow using `pypa/gh-action-pypi-publish` with **trusted publishing (OIDC, no tokens)** — TestPyPI for pre-releases first. | [.github/workflows/release.yml](../../.github/workflows/release.yml) | candidate |
| 2026-06-13 | **Verify `worc` is free on PyPI before release** | The `worc` console-script alias is fine for local `pip install -e .`, but before publishing to (Test)PyPI confirm `worc` does not collide with an existing distribution or a common third-party binary; fall back to `worch` or make the alias opt-in if it does. Depends on the (Test)PyPI publish item above. | `pyproject.toml` `[project.scripts]` | candidate |
| 2026-06-13 | **Defense-in-depth: forbid `--admin` in `find_forbidden_args`** | `merge_pr` builds a fixed argv that never includes `--admin` (so branch protection is respected), and a unit test asserts its absence. As extra hardening, add `--admin` (and other protection-bypassing `gh` flags) to `security/forbidden_args.py` so the prohibition is centralized and also covers any future `gh` call path / `extra_args`. | `security/forbidden_args.py`; `git_manager.py` `merge_pr` | candidate |
| 2026-06-13 | **Capture real merge SHA in `--auto` auto-merge mode** | ~~With `git.auto_merge_wait_for_checks: true` the orchestrator arms GitHub-native auto-merge (`gh pr merge --auto`) and returns immediately, so the ledger records `merge_outcome: "armed"` rather than the real merge SHA.~~ **Subsumed by the "Task dependencies (`depends_on`)" task (implemented 2026-06-22)** — the real `mergeCommit.oid` is backfilled into the `state.db` `pr_merge` publish op as a byproduct of the dependency readiness probe (armed→merged), for any task that has a dependent. The append-only ledger keeps its point-in-time `"armed"` record. A general reconciler that backfills _every_ armed PR (even those with no dependent) for pure audit completeness remains an optional, unscheduled extra. | [task-dependencies.md](task-dependencies.md); `git_manager.py` `merge_pr`/`_merge_commit_sha`; `ledger.py` `LedgerRecord` | subsumed |
| 2026-06-22 | **Task dependencies (`depends_on`): non-blocking merge-gated scheduling** | No cross-task dependency model exists today (`depends_on` is sub-task-only). Add a task front-matter `depends_on: [<task-id>, …]` list and gate scheduling on it: a pending task is eligible only when every dependency has **merged**; ineligible tasks are skipped while independent tasks run (non-blocking — the slot never waits on CI), and re-evaluated on later `watch` ticks. Readiness = `gh pr view → state==MERGED` (PR mode) or terminal `DONE` + commit on base (local-commit mode), probed via the safe runner; the probe backfills the real merge SHA (absorbs the row above). Unsatisfiable dep (failed/closed) → waits forever until the operator intervenes (no auto-fail); cycles / unknown refs → fail-closed reject; explicit `run` of a blocked dependent → refused. **Implemented 2026-06-22** (`depends_on` front-matter + `INVALID_DEPENDS_ON` gate; `Orchestrator.dependency_eligibility`/`reject_dependency`; `GitManager.pr_merge_state`/`backfill_merge_sha`; `watch_once` skip/reject + `cmd_run` refusal). SHA backfill writes the SQLite publish op only (the append-only ledger keeps its point-in-time `"armed"`). Full design in the detail file. | [task-dependencies.md](task-dependencies.md); `task/{model,parser,validation_gate}.py`; `core/orchestrator.py`; `git_manager.py`; `cli.py` `watch_once`/`cmd_run` | done |
| 2026-06-13 | ~~**Check discovery: deeper CI parsing**~~ | **Obsolete (2026-06-23):** the entire automatic check-discovery subsystem was deleted by the checks-monorepo change (row below). The gate is now operator-authored `checks.command_sets`; there is no CI-parsing discovery to deepen. | ~~`checks/resolver.py`; `checks/inspect.py`~~ | obsolete |
| 2026-06-13 | **Config-level per-node model/reasoning defaults (`node_defaults`)** | Per-task `nodes:` overrides shipped (disable-only). The complement lets operators set per-node defaults under each provider in `config.yaml` (`agents.providers.<p>.node_defaults.<node-id>.{model,reasoning}`), inserting a level between task-wide and provider-default in the resolution chain. Touches the config schema (+`schema_version` bump), loader, validation. Also listed in [README.md](README.md) "Other deferred features". | `config/{schema,loader,validation}.py`; `task/model.py` | backlog / not scheduled |
| 2026-06-13 | ~~**Generalize discovery model/reasoning into per-stage config**~~ | **Obsolete (2026-06-23):** agent check discovery (`checks.discovery.{model,reasoning}`, `checks/agent.py`, `checks/discovery_factory.py`) was deleted by the checks-monorepo change — there is no agent-assisted discovery knob left to generalize. | ~~`checks/agent.py`; `checks/discovery_factory.py`~~ | obsolete |
| 2026-06-14 | **Known gap: planning is not shown the skill inventory** | Planning-selected skill references shipped, but the planning agent is not given the scanned inventory (`name`+`description` of available skills). The `{skills_path}` variable is empty during planning (it is filled with the agent's chosen skills only _after_ planning), so the planner must read `.claude/skills/` itself or guess names — unknown guesses are dropped as `dropped_unknown`. Next step: add a read-only `{skills_inventory_path}` (an allowlisted prompt variable) pointing at a generated, redacted name+description listing, injected into the planning role. | `core/skills.py` `SkillInventoryScanner`; `core/orchestrator.py` `_resolve_and_render_skills`/`_prompt_variables`; `core/prompts.py` `ALLOWED_PROMPT_VARS` | known gap / candidate |
| 2026-06-14 | **Provider-specific skills + per-stage routing (Codex skills, not only Claude)** | The skill-reference mechanism scans one tree (`<repo>/.claude/skills`, Claude's convention) and surfaces the same chosen skills to every downstream stage regardless of which provider runs it. Codex has its own convention. Next: (a) support a Codex skill source (config: a second `scan_root`, or a per-provider mapping); (b) route per stage using the resolved route (`AgentRouter` primary/fallback) so reference paths match the provider that reads them; (c) keep it provider-neutral (paths only). When a stage falls back to the other provider mid-run, the skill set should follow the provider that actually ran. | `core/skills.py`; `routing/router.py`; `config/schema.py` `SkillsConfig` | backlog / not scheduled |
| 2026-06-13 | **Prompt customization deferred extras** | The prompt-customization feature shipped (later replaced by node `role_file`s). Still deferred from its original open questions: a global shared preamble across all stages, a per-stage `mode`, and authoring/managing project agent stubs (`AGENTS.md`/`CLAUDE.md`/skills) via a separate `agent_instructions:` feature. The skill-_reference_ half shipped; _authoring_ the stubs is the remaining piece (also in [README.md](README.md) "Agent instruction stubs in target repo"). | `core/prompts.py`; `core/skills.py` | backlog / not scheduled |
| 2026-06-14 | **`rerun` deferred extras: `--allow-done` + versioned attempt branches** | `rerun` accepts only `failed`/`manual_action_required`. Deferred: (a) an opt-in `--allow-done` to re-do an already-merged task (a foot-gun — needs a stronger confirm + the remote/PR gate); (b) a versioned-branch option (`agent/<id>-<slug>-attempt-N`) as an alternative to the same-name reset, which would change the branch/PR naming contract and downstream assumptions. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task` | backlog / not scheduled |
| 2026-06-16 | **Windows test-suite portability gap** | The quality-gate suite assumes POSIX (the gate is intended to run on POSIX/WSL). Still failing on Windows only: `tests/test_process_control.py` references `signal.SIGKILL` directly and asserts a SIGTERM→SIGKILL escalation that can't be represented without `SIGKILL`; `test_skills.py`/`test_orchestrator.py` assert `path.endswith(".../SKILL.md")` and `test_cli_init.py` asserts `"worc/README.md"` (both break on `\` separators); `test_cli_upgrade_docs.py` compares bytes that differ only by CRLF vs LF. Next step (if Windows support is wanted): use `os.sep`/`pathlib` in path assertions, normalize line endings in the docs byte-compare, and gate/parametrize the SIGKILL escalation test on `hasattr(signal, "SIGKILL")`. | `tests/test_process_control.py`; `tests/core/test_skills.py`; `tests/core/test_orchestrator.py`; `tests/test_cli_init.py`; `tests/test_cli_upgrade_docs.py` | known gap / backlog (not scheduled) |
| 2026-06-17 | **Flow-engine: custom `tool` nodes (P5)** | Operator Python/executable as a typed `tool` node, run out-of-process under the ceiling (subprocess via `run_process`, side-effect-free, allowlisted JSON stdin/stdout — like Claude Code hooks). **Deferred, not v1** — start only after P1–P4 stabilize (now done). The reserved seams are in place (engine dispatch-by-`kind` registry, per-kind validator tables, reusable context assembly, separate `validate_flow_against_config`); ~1 week, dominated by the security contract + threat tests. Does not violate the no-code-node invariant: `tool` is ceiling-bound, not in-process arbitrary code. | [p5-custom-tool-nodes.md](archive/outdated/flows/p5-custom-tool-nodes.md); `core/flow/schema.py` (no `ToolNode` yet); also [README.md](README.md) "Other deferred features" | open |
| 2026-06-19 | **Supervisor durable `resume_own_lineage` session** | The supervisor layer's own session is still in-memory (survives one task cycle, not a restart). Now that P3 added the `node_lineage` table (`state.db` v10), the supervisor's durable session can reuse it — the remaining design choice is a supervisor-scoped lineage row vs reusing `node_lineage`/`editing_lineage`. No blocking test (the supervisor's finalize is idempotent on resume). | `core/supervisor.py`; `state_store.py` (`node_lineage`); [p2-implementation.md](archive/outdated/flows/p2-implementation.md) | deferred |
| 2026-06-19 | **`fixing`-skipped → immediate `MANUAL` as a conditional flow edge** | With `fixing` skipped, the engine runs the bounded `test_fix` loop to its cap before `MANUAL` rather than going straight to `MANUAL` (terminal outcome preserved; re-tests N× pointlessly). Immediate-manual should be a conditional flow edge (`testing fail→manual` when `fixing` disabled), not engine domain knowledge. | `core/flow/engine.py`; the packaged `implementation` flow; [p1-audit.md](archive/outdated/flows/p1-audit.md) (Additional #7) | deferred / not scheduled |
| 2026-06-21 | **Codex read-only + network sandbox combo** | Codex enables sandbox network only for `workspace-write`, so a `read-only` research node that needs network (deep_research `external_research`) is not network-enabled at the Codex level. Revisit when the read-only + network sandbox combo is actually needed. | `providers/codex.py`; [p3-research-audit.md](archive/outdated/flows/p3-research-audit.md) | deferred |
| 2026-06-21 | **Signature/hash flow registry + `network_policy` per-host allowlist** | Deferred from P4 (security-ceiling §8): a signature/hash flow registry to verify operator-flow integrity, and a `network_policy` per-host allowlist (today a node's network grant is all-or-nothing, not per-host). | [security-ceiling §8](archive/outdated/flows/security-ceiling.md); `core/flow/{validator,registry}.py`; [p4-operator.md](archive/outdated/flows/p4-operator.md) | deferred |
| 2026-06-19 | **Full re-sync of functional-map `file:line` refs** | Deferred until P2 completion because the precise line numbers shift each slice. P2–P4 are now complete, so do the comprehensive pass: re-sync the `file:line` references in the functional map (B06/B07/system-flows) and the likec4 model against the current code. **Also covers the `Stage`-enum removal (2026-06-22):** B05/B16/B28/B06 and `flows/implementation.md` still describe `Stage`, `SKIPPABLE_STAGES`, `StageParams`/`stage_params`, `_build_stage_params`, `INVALID_STAGE_OVERRIDE`/`REVIEW_SKIP_NOT_ALLOWED`, and the `config.*_enabled` `when` guards — all deleted; re-derive them as `nodes`/`NodeOverride`/`node_overrides`/`disabled_nodes`, `validate_disabled_nodes`, `INVALID_NODE_OVERRIDE`, and node-id disable. **Also folds in the per-node `network_access` override (2026-06-22, done):** B30/B29 and B25's network note still describe network as a pure flow-wide switch (`network_access = doc.network_policy is not None`); re-derive them as `resolve_network_access(node.network_access, doc.network_policy)` — a per-node override on top of the flow-wide default. **Also folds in task dependencies (`depends_on`, 2026-06-22, done):** B16 must add the `depends_on` front-matter field + the `INVALID_DEPENDS_ON` reason (and drop the stale `stages`/`StageParams`/`SKIPPABLE_STAGES` text above); B01/B06 must describe `watch_once`'s eligibility skip/terminal-reject and `cmd_run`'s refusal (`Orchestrator.dependency_eligibility`/`reject_dependency`); B07/B08 the read-only `GitManager.pr_merge_state` probe + the armed→merged `backfill_merge_sha` (SQLite publish op only; ledger keeps `"armed"`). **Also folds in operator-authored decomposition (`subtasks:`, 2026-06-22, done):** B16 must add the `subtasks` front-matter field + the new reasons (`invalid_subtasks`, `invalid_subtask_path`, `subtask_file_missing`, `subtask_malformed`, `subtask_count_out_of_range`, `subtask_depends_forward`, `flow_cannot_decompose`); B06 must describe the two decomposition sources (agent-proposed vs operator-authored) collapsing into the same execution region — `Orchestrator._validate_operator_subtasks` (pre-slot reject) + `_persist_decomposition` materialized at preflight, `decide_operator_decomposition`, and `SubtaskSpec.spec_body` (verbatim spec). | [docs/functional/index.md](../functional/index.md); `docs/functional/blocks/`; `docs/likec4/` | candidate |
| 2026-06-22 | **Flow-engine: `Stage`-enum-removal minor follow-ups** | After the `Stage`-enum identity removal, three minor leftovers remain: (a) `hitl.node_interaction_path` (the standalone `hitl` gate, `node-<id>.json`) is now near-redundant with the node-keyed `interaction_path` — could unify; (b) ~~the `Stage` enum still physically lives in `providers/base.py` though it is no longer part of the provider contract — could relocate to `config`/`core` for cohesion~~ — **subsumed by the full enum-deletion task above** (the enum is deleted outright, not relocated); (c) the supervisor calls `create_attempt_dir` with `node_run_id=0` every turn, so a 2nd+ observe/finalize collides on `stages/supervisor/run-000000/` (FileExistsError, swallowed by the advisory guard) — pre-existing, worth fixing. | `core/flow/{wiring,observability}.py`; `providers/base.py`; `core/supervisor.py`; [2026-06-21-audit-remediation.md](archive/outdated/2026-06-21-audit-remediation.md) | minor / backlog |
| 2026-06-22 | **Deduplicate the `.worc` home literal (`_WORC_HOME`)** | Audit #23 (benign — values match): `core/orchestrator.py` `_WORC_HOME` re-declares `cli.WORC_HOME = ".worc"` to avoid a circular import (the core must not import the CLI). To resolve: move `WORC_HOME = ".worc"` to a genuine leaf module that both import, verifying mypy reports no new import cycle. | `cli.py` `WORC_HOME`; `core/orchestrator.py` `_WORC_HOME`; [2026-06-21-audit-remediation.md](archive/outdated/2026-06-21-audit-remediation.md) (#23) | backlog / not scheduled |
| 2026-06-23 | **Remove `checks.discovery`; monorepo checks via `command_sets` + diff-selection** | Accepted direction (design first): delete the whole automatic check-discovery subsystem (`checks/{inspect,detect,probe,agent,fingerprint,schema_validate}.py`, `discovery_factory.py`, the `auto`/`deterministic`/`disabled` modes + cache/fingerprint in `checks/resolver.py`, `CheckDiscoveryConfig`, `ChecksNode.discovery`/`ChecksDiscovery`, the orchestrator re-resolve path, and `propose_default_commands` install-seeding). Single behavior: operator lists commands; empty = no gate (`disabled` collapses into empty). For polyglot monorepos (`.NET`/Angular/Python/iOS/Android in subdirs), add operator-owned `checks.command_sets` (named sets = `paths` globs + `commands[].{name,argv,cwd}`) in `config.yaml` — keeps the "flow never supplies commands" ceiling invariant. The checks node/runner selects the **union** of sets whose `paths` match `git diff --name-only base..HEAD` (paths outside all sets → run all; fail-safe). Per-set `timeout_seconds` override; per-set `skip_if_unavailable` (operator opt-in) turns a missing host toolchain into a loud `skipped` instead of a hard launch-fail — never counted as `passed`, and if every selected check skipped (or a required toolchain is absent) → `manual`, and any skip blocks `git.auto_merge`. Runner runs **all** checks (no fail-fast) and aggregates: quality-fails → `fixing`; incomplete gate → `manual`. Keep argv-no-shell / env-allowlist / timeout / launch-vs-quality / mutation-guard. `schema_version` 14→15. Heterogeneous toolchains surface a single-host constraint (iOS needs macOS+Xcode, etc.) — external-CI build delegation is a separate, orthogonal backlog item. Full design + rejected alternatives in the detail file. **Implemented 2026-06-23** (Р1–Р5 + Р3a; suite green). Two operator-confirmed deltas vs the original sketch: an **empty diff runs nothing** (vacuous pass — not the ADR's run-all; "nothing ran → manual" is scoped to a non-empty selection where every check was skipped), and the glob matcher supports **extension patterns anywhere** (`**/*.md`) so an operator can wire a Markdown linter that runs only on `.md` changes. Diff source is the working tree (`GitManager.changed_code_paths`), not `base..HEAD` (which is empty at testing time, before the publish-node commit). `install` writes `command_sets: {}` (no `--check` flag); state.db v11→v12 (`check_runs.skipped`). | [checks-monorepo.md](checks-monorepo.md); `config/schema.py`; `check_runner.py`; `checks/{model,resolver,selection}.py`; `core/flow/{schema,snapshot,nodes/checks}.py`; `core/orchestrator.py`; `state_store.py`; `cli.py` | done |
| 2026-06-23 | **Monorepo checks: deferred sub-items (exclude-globs, per-command timeout, external-CI delegation)** | Left out of the first checks-monorepo pass (intentional): (a) **exclude-globs** so e.g. editing `ios/docs/**` does not trigger the iOS build (today selection is include-only at set granularity); (b) **per-command timeout** (today the override granularity is the set via `command_sets.<name>.timeout_seconds`); (c) **external-CI / remote-runner build delegation** — the orchestrator is single-host (checks run as a local subprocess in the clone), so a polyglot monorepo needs every toolchain on the host (iOS⇒macOS+Xcode, Android⇒SDK/JDK, .NET⇒dotnet). `skip_if_unavailable` (Р4) is the host escape-hatch; true delegation of the build to CI is orthogonal. | [checks-monorepo.md](checks-monorepo.md) (§"Масштабирование"/"Оставлено на потом"); `checks/selection.py`; `check_runner.py` | deferred / not scheduled |
| 2026-06-23 | **`upgrade-flows`: re-sync the seeded `.worc/flows/` to the packaged version** | `install` now seeds editable copies of the built-in flows + their shared `roles/` prompts into `.worc/flows/`, and those copies **override** the packaged built-ins (`_copy_packaged_flows`) — so a package upgrade never reaches them. Today the only refresh path is `install --reconfigure`, which snapshots the dir to `flows.bak-<UTC>` (`_backup_flows_dir`) and then overwrites **all** packaged-named files, discarding operator edits to the built-ins. Add an `upgrade-flows` (or fold it into the umbrella `upgrade`/`doctor` in the row above) that does a smarter refresh: update only the built-in files the operator has not edited, report drift on the ones they have, and never touch custom operator flows. Mirror `upgrade-docs` (`resolve_config_path`, fail-closed, idempotent, `--dry-run`). | `cli.py` `cmd_install`/`_copy_packaged_flows`/`_backup_flows_dir`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-23 | **`install`-able task-authoring skills (`worc-task`, `worc-deco-task`)** | Shipped two copy-ready Claude Code skills under `skills/` that convert raw user tasks into the orchestrator task format (single task) and operator-authored decomposition (root + subtask specs). For now they are reference templates the user copies into `~/.claude/skills/` or a project `.claude/skills/` by hand. Next step: a dedicated CLI command (e.g. `worc install-skills [--global]`) that materializes them into the right skills location — mirror `cmd_install`/`upgrade-docs`, which already resolve install locations and copy packaged assets. Decide packaging (ship `skills/` inside `src/wastech_orchestrator/templates/` like the other installed assets vs. copy from the repo). | `skills/`; `cli.py` `cmd_install`/`cmd_upgrade_docs` | candidate |
