# Implementation follow-ups

IMPORTANT: This is a list of open items that have been identified for further discussion. These should not be taken as mandatory implementation or official requirements; they are simply a list of open items.

Open **implementation** next-steps discovered while building — distinct from [README.md](README.md), which tracks _product features_. Use this for "we deferred X; here's the hook and the context" items, and for findings from real test runs. Record new ones via `/sync-docs` as you create them (date them absolute, link where they're referenced, mark status).

| Date | Item | Context & next step | Referenced in | Status |
| --- | --- | --- | --- | --- |
| 2026-07-25 | **VF-10 shipped (Variant B) — no-progress fix loops cut to manual; review sees the fixer's report** | Chosen approach avoids putting any instruction in operator role prompts. Primary path is the existing deterministic `no_file_change` stall guard (engine), which cuts a no-progress fix loop short to `manual_action_required` before `max_fix_cycles` — flow-agnostic, prompt-free; its operator-facing terminal reason now points at the fixer's latest output + review findings (`notify/interface.py`). Kept: the evaluator resolves its `rework`-edge target's latest `<node>.out.md` from the exchange into `AgentRunRequest.rework_report_path` → the `prior_fix` context-footer slot (Core-injected, no operator burden); `review.md` tells the reviewer to read it. An explicit per-node blocked signal was built first, then **reverted** (its instruction lived in each author prompt — operator boilerplate, VF-5 smell). | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-10); `core/flow/nodes/evaluator.py`, `providers/base.py`, `notify/interface.py` | shipped 2026-07-25 |
| 2026-07-25 | **VF-10 Variant A (future option) — Core-inject a "blocked" signal instead of operator prompts** | If the crisper explicit-signal behavior is ever wanted — a clean `reason` field, first-cycle + first-node coverage (not only inside a fix loop), and a distinguishable `node_runs.outcome=blocked` — do NOT put the instruction in role prompts (the reason Variant B was chosen). Instead inject a Core-owned, provider-neutral "author I/O protocol" for **agent nodes only**, mirroring the VF-7 `security_preamble` seam (`core/flow/security_preamble.py` + `build_effective_prompt`): `AgentNodeRunner` appends the protocol to the rendered role prompt, the runner parses `{"blocked": true, "reason": …}` from `structured_output`/a fenced ```json block in the final message, records `outcome=blocked`, and raises `NodeManualRequired`. The operator never writes it; it applies uniformly across every flow and both providers. Keep the `no_file_change` backstop regardless. Reverted implementation is in git history (this branch) if a starting point is wanted. | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-10); `core/flow/nodes/agent.py`, `core/flow/security_preamble.py` | open (deferred) |
| 2026-07-25 | **VF-12 shipped — `provider_attempts` / `check_runs` record the real interval, not a zero-width stamp** | `observability.py` takes the attempt's `started_at`/`finished_at` from the result (clock fallback only for a result-less attempt); `check_runner.py` / `dependency_scan.py` capture a wall-clock bracket around each launched check/scanner (on `CheckRunResult`/`ScannerRun`, threaded via the node's `clock`), and `checks.py` `_run_citation` brackets its in-process validation. No schema change (columns existed). Regression tests assert `finished_at > started_at`. | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-12); `core/flow/observability.py`, `check_runner.py` | shipped 2026-07-25 |
| 2026-07-25 | **VF-13 shipped — an operator-killed task's orphan node runs are reconciled, not stranded** | New `StateStore.reconcile_open_node_runs` closes any `status='running'`/`finished_at IS NULL` row to `aborted` (stamping `finished_at`/`error_class='cancelled'`/reason) and returns the pre-update rows. Orchestrator `_reconcile_open_node_runs` (called from `finalize_task` and `_go_terminal`) also records a `provider_attempts` row per killed provider node (`provider=route_primary`, `usage_delta_status='unknown'`, `usage_cost` NULL) and logs one `WARNING` naming the reconciled nodes. No-op on a clean terminal. **Deferred:** (a) reconciliation runs on the finalize/clean-terminal paths — a killed task recovered directly via `rerun` (which archives the prior attempt) does not itself reconcile the prior attempt's orphan rows; (b) the killed attempt's usage is `unknown` (partial `stdout.log` is not parsed for tokens). | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-13); `state_store.py`, `core/orchestrator.py` | shipped 2026-07-25 |
| 2026-07-25 | **VF-21 shipped — a terminal task's blocked cleanup no longer freezes the `watch` queue** | `_resume_cleanup` now mirrors the primary terminal path via a shared `_resume_terminal_cleanup(task_id, status)`: it honors the WRI-012 quiescence barrier (`_exchange_active_unsafe`) and computes `preserve_own_wip = MANUAL_ACTION_REQUIRED and _worktree_is_task_output(...)` so a resumable manual park keeps its own WIP (VF-1) instead of the retry fail-closing on "unaccounted changes". When the cleanup stays blocked it returns `None` (the task is `_NON_ACTIVE` and holds no slot), so `resume()` reads "slot free" and `watch_once` scans `pending/` instead of returning on a spurious MAR — the block is recorded (`cleanup_last_error`), logged as one `WARNING`, and re-elected each tick so it self-heals once the tree clears. Two new recovery tests (preserve-WIP-on-resume; two-tick blocked-cleanup keeps scanning). **Deferred:** `find_incomplete_cleanup` re-election is left unbounded — the "proceed to pending" option was chosen over "stop re-electing after the first failed retry" (which would need a new state column); revisit only if the every-tick retry proves noisy. | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-21); `core/orchestrator.py` (`_resume_cleanup` / `_resume_terminal_cleanup` / `resume`); `docs/operations.md` (watch queue); `tests/core/test_recovery.py` | shipped 2026-07-25; unbounded re-election deferred |
| 2026-07-25 | **VF-22 shipped — the terminal Telegram notification is actionable (glyph + title + stop node + prose reason + blocking finding + report path)** | Terminal notifications lead with a severity glyph (`✅`/`🛑`/`❌`, new `_STATUS_EMOJI` beside `_TRACE_EMOJI`); `done` stays a terse one-liner, while `failed`/`manual_action_required` expand into an actionable body (title, `Stopped at: <node> (<loop> loop), after N fix rounds`, `Why:` prose, `Blocking (<severity>): <one line>` + `Paths:`, `Branch:`, `Details: <stuck.md>`). The reason→prose table + `TerminalDetails`/`TerminalFinding` + `terminal_reason_prose()` live in the shared notify vocabulary (`notify/interface.py`, beside `TRACE_REWORK_EXHAUSTED`); an unmapped token prints verbatim. `send_notification` gains an optional `details` param (also `NullNotifier` + the test fakes). `_notify_terminal` assembles `details` centrally from the `TaskRow` + flow checkpoint (stop node) + `failure_report.json` (loop + most-severe finding), keyed by `task_id`, so the four call sites are unchanged and every path degrades to the terse line when context is absent (validation reject / clean done → `None`). No `state.db` bump. **Scope note:** the "what's required next" line is the `stuck.md` report path only — no fabricated `rerun` command (VF-1/2/3 precedent of plausible-but-refusing suggestions). | [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-22); `notify/{interface,telegram}.py`; `core/orchestrator.py` (`_notify_terminal` / `_terminal_details` / `_top_blocking_finding` / `_read_failure_report` / `_stuck_report_path`); `docs/operations.md` (notification section); `tests/notify/test_send.py`; `tests/core/test_orchestrator.py` | shipped 2026-07-25 |
| 2026-07-25 | **VF-20 shipped — governance/instruction write-deny removed; a change is reported to the operator, not blocked** | Removed the instruction write-deny (dropped `ProviderWriteGuardPolicy.instruction_files` + its threading through `git_manager.resolve_control_paths` and `nodes/agent`); reworded the VF-7 preamble ("read-only this run" → ordinary editable files). A run whose net-task diff touches a governance path (`AGENTS.md`/`AGENTS.override.md`/`CLAUDE.md`/`.agents/rules/**`, matched by the new `governance_changed_paths` helper) is captured once at finalize (before terminal cleanup restores the tree, via `changed_code_paths_since_task_base`) and reported on **four** surfaces: a run-log/console `WARNING`, a `## Governance files changed` section in the committed PR/commit summary, a `governance_changed` field on the completed-ledger record, and a `governance=` mark on the Telegram completion message — and the task still completes normally (no approval gate, no state-machine change; no notice on ordinary runs). The audit `instruction_manifest_digest` is kept (observe drift, don't prevent it); operators wanting an always-ask step on these paths use `security.protected_paths`. **Deferred:** the real-target end-to-end verification — run `wastech-mdlint` task `p9-10-01-governance-docs` and confirm its `AGENTS.md` deliverable lands (the one unchecked acceptance box). | [vf20-governance-writes-notify-not-block.md](issues/vf20-governance-writes-notify-not-block.md); [runtime-validation-findings.md](issues/runtime-validation-findings.md) (VF-20 / VF-5 amendment); `runtime_layout.py`; `git_manager.py`; `core/flow/{instruction_bundle,security_preamble,nodes/agent,nodes/base}.py`; `core/orchestrator.py`; `ledger.py`; `notify/{interface,telegram}.py`; `providers/{base,codex}.py`; `docs/{operations,configuration}.md`; `.agents/rules/security.md`; packaged `guide/skills/worc-flow-tune/SKILL.md`; `tests/{core,git,notify,providers}/` | shipped 2026-07-25; real-target verification pending |
| 2026-07-25 | **VF-8 shipped — run cost is measurable: `usage_cost` populated + the supervisor's provider calls are on the `provider_attempts` ledger** | Fixed the two compounding [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) VF-8 gaps, and closed the v16 row's deferred **(a) cost capture** below. **(1) `usage_cost`:** Claude's stream-json terminal `result` event carries `total_cost_usd` as a **sibling** of `usage` (not inside it), so `parse_stream_json` now captures it and threads it into `_normalize_claude_usage` → `NormalizedUsage.cost` (new shared `coerce_usage_cost` float helper beside `coerce_usage_int`). It rides the per-invocation scope, so `compute_usage_delta` preserves it with no baseline subtraction. **Codex emits no dollar figure** — its `usage_cost` stays NULL by design (documented, never guessed). **(2) supervisor spend:** `provider_attempts` gains a `task_id` column and its `node_run_id` becomes **nullable** (state.db v18→v19, additive) — so a whole-task cost/usage roll-up is `WHERE task_id = ?` (no `node_runs` join) and the constant supervisor layer records its own billable calls with `node_run_id` NULL (fabricating a `node_runs` row was rejected — `recorder.hydrate_run_state` builds the resume trace from **all** `get_node_runs`, so a synthetic `supervisor` row would corrupt resume). `record_provider_attempts` was decoupled from `NodeServices` (takes `store` + `clock` + `task_id` + `node_run_id`) so both the node runners and the supervisor reuse one recorder; the supervisor's per-turn usage is reduced to a **summation-safe delta** against its own resumed-session baseline (new `usage_snapshot` persisted on the `__supervisor__` lineage, read via `_resume_context`) exactly like a graph node — correct on a cumulative provider (Codex), not just Claude. New `get_provider_attempts_for_task` reader. All best-effort in the advisory supervisor (a store error is logged, never breaks a turn). **Still deferred (unchanged):** the v16 row's **(b) read/report surface** — there is still no `worc` cost/token report or PR-summary line; the roll-up is SQL / `get_provider_attempts_for_task` only. A `worc cost <task>` (or an `analyze-task-run` cost section) reading `SUM(usage_cost) … WHERE task_id=?` is the natural next step. | [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) (VF-8); `providers/{base,_adapter_base,claude,codex}.py`; `state_store.py`; `core/flow/observability.py`; `core/{supervisor,orchestrator}.py`; `docs/operations.md`; `tests/{providers,core,state}/` | shipped 2026-07-25; read/report surface deferred |
| 2026-07-24 | **VF-7 shipped — provider-neutral orchestrator security preamble (defense-in-depth)** | Implemented the [vf7-security-preamble-investigation.md](agent-worc-read-isolation/vf7-security-preamble-investigation.md) design: a short, fixed, **Core-owned** security block the orchestrator prepends to every provider prompt (agent, evaluator, and each supervisor turn) telling the agent not to read/mutate the orchestrator's service files (`.worc`/`.worc-io`/`.git`/`tasks/`, the tracked instruction files, credential/env files) and never to commit/push. **Advisory only** — it does NOT replace the sandbox + deny projection (ADR §3); it is the compensating soft barrier that matters most when read-isolation is off (VF-6, now the shipped default). Content built once in new `core/flow/security_preamble.py` (`build_orchestrator_security_preamble`), deriving path tokens from the layout constants (`CONTROL_HOME_DIRNAME`/`EXCHANGE_HOME_DIRNAME`/`REPO_INSTRUCTION_NAMES`) so it cannot drift from the denies. **Always-on baseline** + a read-restraint **reinforcement paragraph iff** effective read-isolation is off (`SecurityConfig.read_isolation_off`). Carried on a new neutral `AgentRunRequest.security_preamble` field and prepended at the single provider-neutral seam `build_effective_prompt` (order `preamble → role prompt → context footer`); **no adapter/CLI-syntax change** (both providers inherit it through `_stdin_text`). Resolved once in the orchestrator (`_security_preamble()`) and threaded via `NodeServices` (agent/evaluator) + the supervisor's own wiring. **Per user decision: no operator toggle** — the block is advisory text that restricts nothing, so it is outside the § MANDATORY escape-hatch rule (which targets functionality-limiting isolation). Not reachable/overridable from a task, `extra_args`, or a flow (not a prompt variable, not a config/flow/task field); secret-free; appears in the redacted rendered-prompt/prompt-audit/`request.json` for all three request kinds. Does not revive VF-5 (no repository-instruction injection, no `--append-system-prompt*`, native discovery untouched). | [vf7-security-preamble-investigation.md](agent-worc-read-isolation/vf7-security-preamble-investigation.md); [decision record README](agent-worc-read-isolation/README.md) §3; [.agents/rules/security.md](../../.agents/rules/security.md); `providers/base.py`; `core/flow/security_preamble.py` (new); `core/flow/{wiring,nodes/base,nodes/agent,nodes/evaluator}.py`; `core/{orchestrator,supervisor}.py`; `docs/worc_architecture.md`; `tests/{providers,core}/` | shipped 2026-07-24 |
| 2026-07-24 | **VF-6 shipped — operator escape hatch `security.disable_read_isolation` (fully disable read-isolation)** | Implemented the [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) VF-6 requirement, closing the drift where [.agents/rules/security.md](../../.agents/rules/security.md) §MANDATORY + rule #3 already specified the flag but no code existed. New **operator-config** key `security.disable_read_isolation` (bool; **shipped default `true`** — read-isolation off out of the box, a deliberate operator deployment-posture decision that departs from § MANDATORY default-safe and the original "off by default"; set `false` to keep it on), sibling of `strict_isolation`. The effective state is computed **once** on `SecurityConfig.read_isolation_off` = `disable_read_isolation OR NOT strict_isolation` (master switch `strict_isolation` always wins toward relaxation; adapters read the property and never recompute the formula). Covers the **read side only**: (a) native discovery re-enabled — **Claude** uses `--setting-sources project` (not `""`) and drops `--strict-mcp-config` (native `CLAUDE.md` + project settings/hooks/MCP/skills; the F37 native-memory deny is lifted), **Codex** drops `--ignore-user-config`, trusts the project, and re-enables the `hooks` feature (native user + `.codex` config/rules/hooks; the heavier autonomous surfaces multi_agent/computer/browser/apps/plugins stay disabled); (b) the private `InternalDenyPolicy` read-deny projection is lifted (Claude tool `Read()` denies + sandbox `denyRead` dropped; Codex profile `deny`→`read`). **Write side unchanged** (write-guard, WRI-009 commit/staging gates, PR control) and the public `denied_read_paths` blacklist **stays enforced** (operator decision — symmetric across providers). The per-attempt Codex canary keeps proving the write boundary (private read flips to a positive control; a private **write** must still be denied). Operator-config ONLY (node-override validator rejects it from a task/flow; not an argv flag for `extra_args`); loud `read-isolation: OFF` warning in `worc preflight` + the run log. Byte-identical argv / deny-sets at defaults (regression-tested). | [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) (VF-6); [.agents/rules/security.md](../../.agents/rules/security.md) §MANDATORY + rule #3; `config/{schema,loader}.py`; `providers/{claude,codex,codex_profile,codex_canary}.py`; `security/isolation.py`; `core/orchestrator.py`; `cli.py`; `docs/{configuration,operations}.md`; packaged `config.example.yaml` + `guide/config/reference.md`; `tests/{providers,config,security,core,task}/` | shipped 2026-07-24 |
| 2026-07-24 | **VF-5 shipped — rolled back repository-instruction discovery-disable + freeze-and-inject to native/agent-read discovery** | Implemented the [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) VF-5 rollback (deferred by the VF-1/2/3 + VF-4 rows below). The orchestrator no longer disables provider-native repository-instruction discovery and no longer injects a frozen `AGENTS.md`/`CLAUDE.md` snapshot into prompts. **Codex** reverts to native `AGENTS.md` discovery (dropped `-c project_doc_max_bytes=0` + the `<repository-instructions>` stdin block; the `.codex` trust / `--ignore-user-config` / `--disable` controls stay). **Claude** keeps `--setting-sources ""` as the security lockdown — verified against Claude Code docs that there is **no** memory-only load path (the switch gates `CLAUDE.md` **and** hooks/MCP/skills together, so re-enabling native memory would weaken the envelope, a hard invariant) — and drops the `--append-system-prompt-file` injection; the agent reads the repo's root files itself (role-prompt-directed). **Reproducibility** moves from freeze-and-inject to a filesystem primitive: the tracked root instruction files are added to `ProviderWriteGuardPolicy.denied_write_paths` (populated by the core node runner via `discover_repository_instructions`, keeping `git_manager` free of a `core` import), so they are readable-but-immutable for the run — identical instructions on every node/resume/fallback, provider-agnostic, no discovery code — and the instruction **digest** is kept (per-source freeze folds into the manifest; the injected concat payload is gone). **Kept unchanged:** the whole filesystem read/write sandbox, task-packet freeze+publish, and skill-package freeze. **Deferred (own analysis needed):** the same "disable-native-then-reimplement" review of skill discovery-disable + the task-packet snapshot; and the optional deeper simplification of hashing live files instead of keeping the per-source freeze-copy for the digest. | [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) (VF-5); [decision record README](agent-worc-read-isolation/README.md); [wri-011](agent-worc-read-isolation/wri-011-freeze-agent-inputs.md); [wri-002](agent-worc-read-isolation/wri-002-claude-private-home-read-deny.md); [wri-003](agent-worc-read-isolation/wri-003-codex-permission-profile-isolation.md); `providers/{base,claude,codex,_adapter_base,exchange}.py`; `runtime_layout.py`; `git_manager.py`; `core/{orchestrator,supervisor}.py`; `core/flow/{instruction_bundle,nodes/agent,nodes/base,nodes/evaluator}.py`; `docs/{operations,worc_architecture}.md`; `.agents/rules/{architecture,security}.md`; packaged guide + role prompts; `tests/` | shipped 2026-07-24; skill-discovery + task-packet review + hash-live simplification deferred |
| 2026-07-24 | **VF-4 shipped — `finalize` re-syncs the operator-facing fix-loop counters from the flow checkpoint** | Fixed the [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) VF-4 counter discrepancy (deferred by the VF-1/2/3 row below). **Confirmed a real defect, not expected semantics:** the `tasks.*_fix_total` / `*_fix_cycles` columns are mirrored **only** at a clean orchestrator terminal (`_sync_counters_from_run_state`), while `fix_iterations` is mirrored on every checkpoint — so a run **killed mid-flow** and finished with `finalize` left the totals stale (`review_fix_total=1`) against an authoritative `flow_run_counters` that held the true churn. `finalize_task` now hydrates the checkpoint and re-mirrors the columns (`LoopCounters.from_run_state`) before recording the terminal; the run-state→mirror mapping is consolidated on `LoopCounters.from_run_state` (shared by the terminal sync + finalize). **Confirmed semantics (no change needed):** the `*_total` are cumulative **within** an attempt (preserved across `rerun --continue`), reset on a fresh rerun, and are **audit-only — they never gate the fix-loop budget** (budgets key off `flow_run_counters`), so the "exceed the round cap across reruns" worry does not apply. | [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md) (VF-4); `core/loop_control.py`; `core/orchestrator.py`; `tests/core/{test_loop_control,test_cli_finalize}.py`; `docs/operations.md` | shipped 2026-07-24 |
| 2026-07-24 | **VF-1/VF-2/VF-3 shipped — `rerun --continue` adopts an operator-edited control plane; dirty-tree/masking fix; stale-`running` recovery** | Fixed three `rerun --continue` findings from [runtime-validation-findings.md](agent-worc-read-isolation/runtime-validation-findings.md). **VF-3:** operator `continue_task` now re-freezes the control plane from the live flow (adopts between-run flow/role/tool edits) and re-binds the instruction manifest to the new control digest; a one-shot `_continue_adopt` marker scopes this to the operator CLI so **automatic crash-recovery keeps the WRI-010 refuse** (a crash can follow an agent mutation). `_apply_continue_controls` rebaselines the checkpoint fingerprint on flow-YAML drift; `plan_rerun` validates the resume node still exists and accepts a stale `running` row (killed-task recovery — safe because `cmd_rerun` refuses when a daemon holds the slot). **VF-2:** the dry-run drift note keys off bundle-level drift (flow+roles+tools) and is now truthful. **VF-1:** `terminal_cleanup(preserve_own_wip=)` preserves the task's own WIP on a resumable manual park instead of failing "unaccounted changes", and `_go_terminal` no longer masks the node's manual reason with the cleanup error. **Deferred (out of scope):** the own-vs-foreign WIP discriminator + `--commit-wip` (still #7 below); VF-4 counter semantics; VF-5 discovery rollback. **Residual trust boundary:** an operator `--continue` trusts the whole live control plane (own-vs-foreign control discrimination is deferred), surfaced by the drift note; agent in-run tamper detection is unchanged. | `core/orchestrator.py`; `git_manager.py`; [architecture.md](../../.agents/rules/architecture.md); [wri-010](agent-worc-read-isolation/wri-010-protect-control-plane.md); `docs/operations.md`; worc-flow-tune SKILL | shipped 2026-07-24 |
| 2026-07-23 | **WRI-006 shipped + remaining cluster review findings closed (H2/H4/H5/H6/H7 + M1–M9 + lows)** | Landed [WRI-006](agent-worc-read-isolation/wri-006-cross-platform-isolation-verification.md) and closed every remaining item from the [final review](agent-worc-read-isolation/final-review-2026-07-23.md) (blockers C1/C2/H1/H3 were done in the row below). **WRI-006 CI:** `.github/workflows/ci.yml` split into a Ubuntu `static` job (ruff/format/mypy/lint-imports/interrogate/vulture/deptry) + a required `test` matrix running the deterministic suite natively on `ubuntu`(3.12+3.13)/`macos`(3.13)/`windows`(3.13), `fail-fast: false`. Per operator decision, **no real-CLI host smokes run in hosted CI** (runners ship no Codex/Claude CLI); real OS-enforcement is a local/manual gate + `worc preflight`. **H2** (`providers/claude.py`): added `_preflight_capability_error` (probes `claude --help` for `--permission-mode`/`--setting-sources`/`--strict-mcp-config`/`--tools` → block before a paid call on flag drift) + `_preflight_degraded_reasons` (`--resume`/`--append-system-prompt`, fallback-aware), mirroring Codex; verified against real claude 2.1.217. **H4** (`providers/codex_canary.py`): the per-attempt canary now (a) runs probes under a throwaway `CODEX_HOME` (reproduces `exec --ignore-user-config`, which `codex sandbox` cannot take as a flag), (b) requires ≥1 satisfied **positive control** (an allowed read) before it may certify — a broken probe harness can no longer read as "all denied → enforcing", (c) supports alias/repo/repo-write probes; the false "canary proves empty MCP inventory" comment in `codex.py` was corrected. **New `run_codex_capability_smoke`** (fixture-based: private denied direct+shell+symlink-alias, repo-read positive control, repo-write per profile, exchange read/write, + `codex mcp list` inventory) classifies `passed`/`unsupported`(`CAPABILITY_UNAVAILABLE`)/`policy-failed`(`CONFIGURATION_ERROR`); all verified against real codex 0.144.4 on macOS. **H7** (`cli.py` + a neutral `IsolationCapabilityReport` hook on `_adapter_base`): `worc preflight` (only, via `capability_smoke=True`; the installer's auto-preflight stays offline) runs the live Codex smoke and surfaces OK/WARN(no-fallback→FAIL)/FAIL(policy leak, unconditional); + operations.md troubleshooting rows for both runtime failure classes. **H5:** `denied_commands`→Codex-execpolicy stays **descoped** (operator-decided); reconciled the contradiction by recording the honest per-provider coverage in [security.md rule #11](../../.agents/rules/security.md) and the decision-record/happy-path/WRI-003 doc (commands are contained: network off in every profile, local `git commit` caught by WRI-009's fingerprint, orchestrator-only commit/push/PR). **H6:** Linux/WSL2 `bubblewrap`+`socat` upgrade note in operations.md. **Mediums:** **M5** (`git_manager.py`) — `_assert_no_untrusted_filters` now also gates `reset_branch_to_base` (raise), `terminal_cleanup` (→ unsafe outcome), and `refresh_base` (best-effort skip, never crashes the watch loop); `ManualActionRequired` added to `cli.main()`'s clean-exit handler. **M7** (`orchestrator._seal_terminal_exchange`) — catches bare `OSError` (ENOSPC/EACCES) on both seal + quarantine paths as unsafe-teardown, honoring "Never raises". **M8** — added an isolated Claude workspace-write run test (deny_policy present → writes the OS-sandbox settings + `--settings`), closing the "run tests use deny_policy=None" gap. **M6** — doc drift fixed (gitignore now `.worc/`+`.worc-io/` in operations/configuration/cookbook; guide schema 30→31; output slots three→four+`report` in guide README/roles; glossary `capability_unavailable` added). **M2** — explicit decision comment at `_run_merge_flow` (merge agent intentionally un-frozen). **Lows** — flow loader now rejects duplicate node ids (`snapshot.py` → `FlowLoadError`); the two packaged `repository_analysis.md` prompts no longer point the agent at now-read-denied `.worc/logs/`. **Accepted residuals (deliberate, prod-safe):** **M1** (`config.yaml`/`.env` not frozen/digested) — config is loaded once and immutable in-memory for the process/daemon, and a new process reading a poisoned `config.yaml` is prevented by the `control_home` write-deny; digesting it is deferred. **M3** (only root-level `AGENTS.md`/`CLAUDE.md` frozen; nested/`@`-ref files not) — harmless for repos that read rules by path on demand (like this one); a target repo relying on nested `AGENTS.md` would not get them under strict isolation — expand the inventory if that ever bites. **M4** (`assert_no_required_secret` fail-closes on known secret _values_, not pattern-only matches) — a conscious compromise to avoid false positives on hyphenated task-ids; the redaction net still scrubs artifacts. **M9** (Claude adapter does not itself fail-closed when `needs_sandbox` and `deny_policy is None`) — a can't-happen-in-prod case (composition always injects the deny policy); an adapter-level raise would ripple through many `deny_policy=None` unit harnesses, so deferred. **Low residuals:** WRI-012 POSIX tracker-poll granularity + Windows Job Object real-behaviour (the latter provable via the now-Windows-runnable Codex smoke / a real host); WRI-003 `CODEX_HOME`-for-deny from parent env (loud auth failure, not a silent leak); WRI-004 `create_attempt_dir` under `logs/` + AST-guard literal-form; WRI-002 `--permission-mode auto` not flow-flagged (no real escalation) + host classification by binary presence; the within-stage-fallback + router profile-strictness defense-in-depth notes. | [wri-006](agent-worc-read-isolation/wri-006-cross-platform-isolation-verification.md); [final-review](agent-worc-read-isolation/final-review-2026-07-23.md); `.github/workflows/ci.yml`; `providers/{claude,codex,codex_canary,_adapter_base}.py`; `cli.py`; `git_manager.py`; `core/orchestrator.py`; `core/flow/snapshot.py`; `tests/providers/`, `tests/git/`, `tests/core/`, `tests/test_cli_preflight.py`; `.agents/rules/security.md`; `docs/{operations,configuration,cookbook,glossary}.md`; packaged guide + `security_audit`/`deep_research` prompts | shipped 2026-07-23; M1/M3/M4/M9 + listed lows = accepted residuals |
| 2026-07-23 | **Pre-WRI-006 blockers C1/C2/H1/H3 fixed (final-review 2026-07-23)** | Closed the four must-fix blockers from the cluster [final review](agent-worc-read-isolation/final-review-2026-07-23.md) before the WRI-006 CI gate. **C1** (`core/orchestrator.py`): the infra error-class dispatch now precedes the evaluator-vs-node split (`_terminal_infra_manual` + `_CONTAINMENT_MANUAL_CLASSES`), so a `CONTAINMENT_UNVERIFIED`/`CAPABILITY_UNAVAILABLE` failure from an **evaluator** node (e.g. `review`) fails closed exactly like an agent node — the earlier order let the evaluator branch degrade to manual **without** flagging `exchange_active_unsafe`, so the seal ran and the un-quiesced tree leaked to the next task; and the terminal **commit/push** (`_fail`) + **cleanup checkout** (`_go_terminal`) are now gated on proven quiescence (`_exchange_active_unsafe`) so no Git action runs against a tree an unknown descendant might still be writing (architecture.md barrier). **C2** (`providers/codex.py`): added `--full-auto`, `-a`/`--ask-for-approval` to `_RESERVED_CODEX_FLAGS` — a `--sandbox` mode selector makes Codex drop the `default_permissions="worc"` profile (losing the private-file read denials). **Decision:** these are _not_ added to the core flow validator — the "core knows no CLI syntax" invariant forbids provider-specific flag names in `core/`; the run-time provider gate (`build_codex_argv`, both config- and node-level `extra_args`) and the offline `isolation_reasons` preflight fully cover them, exactly as for every other reserved Codex flag. **H1** (`core/orchestrator.py`): `assert_exchange_current_task_only` is wrapped so a stale/foreign/locked exchange → `manual_action_required` (naming the entry) instead of a bare `ExchangeError` that crash-looped the daemon. **H3** (`core/flow/instruction_bundle.py` + `core/orchestrator.py`): `load_instruction_bundle` now returns the verified file entries and the resume path repopulates `p.instruction_entries`, so `_task_packet_digest` is non-`None` on `rerun --continue` and `commit_audit` runs the lifecycle-vs-packet check on resume as on fresh. Remaining review items (H2 Claude capability-preflight, H4 canary hardening, H5 `denied_commands` decision, H6/H7 + M6 docs, M1–M9) landed with WRI-006 — see the row above. | [final-review](agent-worc-read-isolation/final-review-2026-07-23.md); `core/orchestrator.py`; `core/flow/instruction_bundle.py`; `providers/codex.py`; `tests/core/{test_orchestrator,test_instruction_bundle}.py`; `tests/providers/test_codex_command.py`; `tests/security/test_isolation.py`; `docs/{operations,configuration}.md` | shipped 2026-07-23; H2/H4/H5/H6/H7/M1–M9 closed (row above) |
| 2026-07-23 | **WRI-003 shipped — Codex permission-profile isolation** | Implemented [WRI-003](agent-worc-read-isolation/wri-003-codex-permission-profile-isolation.md) against codex-cli 0.144.4. New `providers/codex_profile.py` generates `[permissions.worc]` (`extends :read-only`/`:workspace`; absolute deny/read carve-outs; `deny > write > read`; network off) rendered as ONE inline-table `-c` value (incremental dotted `-c` keys fail Codex's untagged-enum parse; keeps `CODEX_HOME` unmutated). New `providers/codex_canary.py` proves it via no-model `codex sandbox -P` (private denied direct+shell, exchange read-only): a leak → non-fallback `CONFIGURATION_ERROR`, an undemonstrable sandbox → `CAPABILITY_UNAVAILABLE`, wired through a new base `_pre_launch_check` hook (gated on `deny_policy`, injectable runner). `codex.py` replaced `--sandbox` with the profile + `default_permissions` + `--ignore-user-config` + project-`untrusted` + `--disable hooks/multi_agent/computer_use/browser_use/apps/plugins` + reserved authority flags, with fresh/resume parity. Config v31: validator rejects legacy `sandbox: read-only\|workspace-write`, `upgrade-config` folds it into `permission_profile`, `danger-full-access` stays the escape. | `providers/codex_profile.py`, `providers/codex_canary.py`, `providers/codex.py`, `providers/_adapter_base.py`, `config/` | Done |
| 2026-07-23 | **Codex execpolicy projection of `security.denied_commands` deferred (WRI-003)** | Operator-decided out of scope. The denied commands are already contained — push/PR need network (disabled in every generated profile) and a local `git commit` is caught by WRI-009's git-control fingerprint — and the beta CLI's execpolicy rules are a Starlark DSL with no clean `codex exec` ingestion surface (`exec` has `--ignore-rules` but no `--rules`). Next step if desired: generate an orchestrator `.rules` layer in the real DSL, validate with `codex execpolicy check --rules`, confirm the supported exec-ingestion surface, and add a hard `codex mcp list` inventory gate to the canary (today MCP is neutralized by `--ignore-user-config` + project-untrusted and the inventory is only recorded as evidence). | [wri-003](agent-worc-read-isolation/wri-003-codex-permission-profile-isolation.md) | Open |
| 2026-07-23 | **WRI-002 shipped — enforce Claude isolation with sandbox and tool policy** | Implemented [WRI-002](agent-worc-read-isolation/wri-002-claude-private-home-read-deny.md). `providers/claude.py`: read-only mode → documented `dontAsk` (dropped the legacy `default` alias) + updated `_MODE_ORDER`; a `SandboxCapability`/`default_sandbox_probe` host seam (macOS Seatbelt always / Linux+WSL2 need `bwrap`+`socat` via `shutil.which` / native Windows unsupported) + a `resolve_claude_tools` decision helper → keep Bash + emit the OS Bash-sandbox on macOS/Linux-avail, **drop Bash** on native-Windows strict (restricted mode), **raise pre-model `ErrorClass.CAPABILITY_UNAVAILABLE`** on Linux-missing-deps strict, keep unsandboxed Bash under `strict_isolation:false`; a `--tools` hard allowlist mirroring `--allowedTools` + `--strict-mcp-config` (kept `--setting-sources ""`; deliberately **not** `--safe-mode` to preserve the F37 `allow_native_memory` opt-in); a private `--settings` OS-sandbox JSON (`build_sandbox_settings` — plain-absolute paths, `denyRead`=internal set, `denyWrite`=internal+exchange/gitdir/common/hooks/`tasks/`, `credentials.files` deny-only, network binary; never `mask`/`tlsTerminate`/`enableWeakerNestedSandbox`/`allowUnsandboxedCommands`/`excludedCommands`); `//`-anchored `Read`/`Write`/`Edit` tool denies projecting `InternalDenyPolicy.denied_paths` (each path emits `//p`+`//p/**` for file+dir+dotfile coverage, **excluding the F37-owned `~/.claude`**) plus `write_guard` Write/Edit denies; a `_find_reserved_claude_args` authority-flag validator (tools/settings/MCP/plugins/agents/add-dir/file/Chrome/IDE/remote/worktree/system-prompt/session — hard-rejected regardless of `strict_isolation`, invoked in `build_claude_argv` + `isolation_reasons`); host-aware `isolation_reasons(config, *, capability=)`. Cross-cutting: `ProviderWriteGuardPolicy` (leaf `runtime_layout.py`) + `GitManager.resolve_control_paths` (normal-clone + linked-worktree, both gitdir **and** common-dir) added to `GitPort`, resolved per workspace-write attempt in the node runner's WRI-009 bracket onto `AgentRunRequest.write_guard`; `deny_policy` constructor-injected on `BaseCliProvider` (both adapters); `ErrorClass.CAPABILITY_UNAVAILABLE` (excluded from all fallback sets) with a **host-verified** conditional fallback (`router.fallback_allowed(..., fallback_can_isolate=)` + injected `isolation_checks`/`_can_isolate`, `orchestrator` → `manual_action_required` when none qualifies); parent-held pre/post-attempt exchange-manifest detection (`exchange.diff_exchange_manifests` + `exchange_publish.capture_exchange_manifest`/`assert_exchange_unchanged`) in the agent **and** evaluator brackets (all profiles; mtime-insensitive); `security/forbidden_args` adds `--allow-dangerously-skip-permissions`; `composition` threads it all; `--allow-dangerously-skip-permissions` etc. An autouse `tests/conftest.py` fixture pins the sandbox probe to "available" so the deterministic suite is host-independent (real host proof is the WRI-006 native gate). **Cluster-exit resolved (Claude half):** WRI-009(a) gitdir/`tasks/` write-deny + (e) `gh`-residual, WRI-010(a) `control_home`/bundle deny, WRI-011(a) read-only exchange + (b) bundle deny — all now Claude-projected (Codex halves stay with WRI-003). **Decisions this session:** CAPABILITY_UNAVAILABLE uses host-verified fallback (ADR-faithful); managed policy documented as TCB with automated inspection deferred; one cohesive change. **Deferred:** (a) **enterprise managed-policy** automated offline inspection (`~/.claude` remote-settings) + an operator trust-allowlist (documented as TCB; enterprise-only); (b) the **supervisor** exchange-manifest raise (its advisory swallow-all would eat it — read-only, so read-isolation via the shared adapter deny_policy already applies); (c) resolving the **public `denied_read_paths`** to absolute sandbox `denyRead` so a Bash `cat <workspace>/.env` is OS-blocked (bounded today by redaction; internal-secret scope is covered). **Verify on real hosts (WRI-006):** does an explicit `denyWrite` override the sandbox's built-in linked-worktree `.git` allowance (else record residual + fail strict — WRI-009 fingerprint is the definitive catch regardless); `allowedDomains:[]`==network-deny; `--settings` applies under `--setting-sources ""` with no foreign array merge; `//dir/**` dotfile coverage. | [wri-002](agent-worc-read-isolation/wri-002-claude-private-home-read-deny.md); `providers/{claude,base,_adapter_base,exchange}.py`; `runtime_layout.py`; `git_manager.py`; `routing/router.py`; `core/orchestrator.py`; `core/flow/nodes/{agent,evaluator,base,exchange_publish}.py`; `composition.py`; `security/forbidden_args.py`; `tests/providers/`, `tests/security/`, `tests/routing/`, `tests/core/`, `tests/git/`; `.agents/rules/security.md`; `docs/{configuration,operations}.md`; packaged guide | shipped 2026-07-23; (a)/(b)/(c) deferred |
| 2026-07-22 | **WRI-009 shipped — protect Git control state and commits from provider poisoning** | Implemented [WRI-009](agent-worc-read-isolation/wri-009-protect-git-index-from-exchange.md), all orchestrator-side (the definitive parent-held gate; the provider grants are cluster-exit). In `git_manager.py`: a **Git control-state fingerprint** (`GitControlState`/`HookFacts`/`GitControlDrift` + `capture_git_control_state`/`compare_git_control_state`, new `_parse_ls_files_stage_z`) covering index (incl. intent-to-add zero-sha), HEAD symbolic/detached identity, current-task ref, repo-local/worktree config (value **hashes**, never raw values), `core.hooksPath`+hook file/target/content, and merge/rebase/cherry-pick/revert/bisect markers — storing only identities/hashes so it carries no secret and reporting redacted path/key/name-level drift; a **subprocess-neutralization** prefix at the single `_run` chokepoint (`git --no-pager -c core.hooksPath=<private empty dir> -c core.fsmonitor=false -c core.editor=false -c sequence.editor=false -c commit.gpgsign=false -c tag.gpgsign=false`, `--no-textconv --no-ext-diff` on `diff`, and a `GIT_TERMINAL_PROMPT=0`/`GIT_OPTIONAL_LOCKS=0`/`GIT_EDITOR=false`/`GIT_SEQUENCE_EDITOR=false`/`GCM_INTERACTIVE=Never` child env); a **filter refuse-gate** (`_assert_no_untrusted_filters`) run before `add`/`checkout`/`merge` that stops in manual action on any repo-local/worktree clean/smudge/filter-process/textconv/diff driver or `core.sshCommand`/`credential.helper` (operator global/system config is trusted, not inventoried); the **full staged-set gate** (`assert_staged_allowed` + `assert_exchange_never_staged`) before **every** commit — code/subtask (`set(paths)`), merge-resolution (exclude-mode, artifact endpoints only), audit (`set(stageable)`) — with dir-prefix-aware membership (porcelain reports `dir/`, diff reports files) and a rename-source exemption that still rejects a forbidden-artifact source; the **audit-commit lifecycle digest check** (`commit_audit(..., task_packet_digest=)` verifies the staged `<id>.md` is byte-identical to the WRI-011 frozen packet); and a **clean-index preflight** (`assert_index_clean_at_start` after `prepare_branch`, all modes — refuse a pre-staged non-artifact baseline, never reset; `_prepare_new` extracted). `update_branch_with_base` switched to `git merge --no-commit` so a clean 3-way merge is finalized through the gated `commit_merge_resolution` (called for both clean and conflict branches in `_run_merge_task`). `commit_audit`'s digest threads via `NodeServices.task_packet_digest` (from `p.instruction_entries[TASK_PACKET_KEY]`) → publish node + `_fail`; `GitPort` gained `capture/compare_git_control_state` + the `commit_audit` kwarg. The compare bracket is in `AgentNodeRunner._invoke` (capture before `run_stage`, compare after the trusted result and **before** `_apply_post_edit_guard`'s `git diff`/commit touch the clone) → `NodeManualRequired` on drift; `_is_workspace_write` helper factored. New preflight `require_git_control()` fails closed below git 2.9 (`core.hooksPath` floor), wired at `cmd_run`/`cmd_rerun`/`cmd_watch`; `detect.git_version()` added. **Deferred / cluster-exit:** (a) the **provider grants** — Codex read-only gitdir/common-dir, Claude `Write`/`Edit` denies on the gitdir, git-mutation Bash/execpolicy command denylist, and the `tasks/`-tree write-deny — plus their typed representation (`ProviderWriteGuardPolicy`/`resolve_control_paths`) land with **WRI-002/003** (the fingerprint uses `git rev-parse --git-path`/`--git-common-dir` and needs none of it); (b) the **fake-provider mutation matrix across both providers/profiles** lands with WRI-002/003 (orchestrator-side gate is unit-tested here via `git_manager` + the node-runner drift test); (c) **`SecurityConfig.allowed_git_filters`** operator escape hatch (current contract: any untrusted repo-local driver → manual); (d) **operator-opt-in signed commits** (signing is force-disabled to match current unsigned behavior); (e) the **`gh` re-invokes-git** hooks residual (closed by the WRI-002/003 read-only gitdir grant). | [wri-009](agent-worc-read-isolation/wri-009-protect-git-index-from-exchange.md); `git_manager.py`; `core/flow/nodes/{base,agent,publish}.py`; `core/flow/wiring.py`; `core/orchestrator.py`; `preflight.py`; `install/detect.py`; `tests/git/test_git_manager*.py`; `tests/core/test_flow_node_runners.py`; `.agents/rules/{git-workflow,security}.md`; `docs/operations.md` | shipped 2026-07-22; (a)/(b) cluster-exit (WRI-002/003), (c)/(d)/(e) deferred |
| 2026-07-22 | **WRI-011 shipped — freeze task, skill, and repository instruction inputs** | Implemented [WRI-011](agent-worc-read-isolation/wri-011-freeze-agent-inputs.md): a new `core/flow/instruction_bundle.py` freezes the validated task packet, the selected skill **packages** (whole `SKILL.md` directory closure — tracked regular files only, count/byte caps, link/reparse/hard-link/special/ADS/case-collision refused, root-level package rejected), and the **root-only** repository instruction files (`AGENTS.md`/`AGENTS.override.md`/`CLAUDE.md`, tracked) into a private immutable bundle at `<private_home>/instruction-bundles/<task-id>/` under one composite `instruction_manifest_digest` (which folds in the WRI-010 control digest, In-scope bullet #4). Shared identity/digest/collision primitives were extracted from WRI-010's `control_bundle.py` into a new leaf `core/flow/frozen_bundle.py` (`FrozenBundleError` base + `inspect_frozen_source`/`digest_entries`/`reject_key_collisions`, `error_cls`-parameterized so each freezer keeps its own subclass). `Orchestrator._engine_run` stages the freeze (task+repo-instructions before the supervisor proposal → skill packages during `_resolve_skill_layers` → `_finalize_instruction_bundle` writes the manifest + persists `tasks.instruction_manifest_digest`, state.db v16→v17); continue loads+verifies against the persisted digest and refuses a differing one. Only redacted exchange copies reach the model (`task.md`, `skills/<folder>/…`, `instructions/repository.md`) — immutability is via the canonical digest, not exchange byte-identity; a required input containing a **known secret value** (resolved env / denied / memory secret — precise membership, not the over-matching generic redaction heuristics), or an unrepresentable (non-UTF-8/over-cap) skill/instruction file, fails **before** launch (`assert_no_required_secret` / packaging error). The constant supervisor was de-inlined: `propose_skill_map`/`finalize` read the task from the frozen exchange packet path + bounded (`_SKILL_DESCRIPTION_INLINE_CAP=200`) skill metadata — no inline title/description. Providers: new `AgentRunRequest.repository_instructions_path`; Codex disables live `AGENTS.md` discovery (`-c project_doc_max_bytes=0`) and injects the frozen instructions as a top-of-turn developer block (`_stdin_text`); Claude disables live `CLAUDE.md`/project-settings discovery (`--setting-sources ""`) and injects via `--append-system-prompt-file`. `InternalDenyPolicy.frozen_instruction_bundle` names the deny target. **Deferred / cluster-exit (verified when WRI-002/003 land):** (a) read-only enforcement of the exchange task/skill/instruction copies; (b) provider deny of `control_home` + the frozen bundles; (c) the adapter-isolated supervisor call (AC2 second half). **Scope bounds recorded:** instruction inventory is **root-only** (no nested `AGENTS.md`, no `@`-reference closure, so `.agents/rules/*` are not frozen — revisit if a run needs them); a binary/over-cap skill resource is an unrepresentable-closure failure, not a silent skip; two selected skills whose package directories share a basename fail strict (folder-name collision) rather than clobbering. **Known consequence:** the discovery-disable argv (Codex `project_doc_max_bytes=0` / Claude `--setting-sources ""`) is unconditional, so the ephemeral **merge flow** (`_run_merge_flow`, which by design is not frozen and sets no `repository_instructions_path`) now runs its conflict-resolution agent with neither live nor frozen `AGENTS.md`/`CLAUDE.md` — acceptable for a mechanical marker-resolution + checks pass, but if a merge agent needs repository guidance, freeze + inject a merge-scoped instruction copy there too. **Verify on real CLIs (owner):** the discovery-disable / injection flags are fake-CLI tested only — confirm the installed Codex 0.144.4 honors `-c project_doc_max_bytes=0` (no `AGENTS.md` read) and Claude 2.1.217 accepts `--setting-sources ""` (drops project `CLAUDE.md`/settings) + `--append-system-prompt-file`; adjust the exact key/empty-value form if a real run rejects it. This pairs with the WRI-006 host smokes. | [wri-011](agent-worc-read-isolation/wri-011-freeze-agent-inputs.md); `core/flow/{instruction_bundle,frozen_bundle,control_bundle}.py`; `core/orchestrator.py`; `core/supervisor.py`; `providers/{base,codex,claude,_adapter_base,exchange}.py`; `runtime_layout.py`; `composition.py`; `state_store.py`; `git_manager.py`; `tests/core/test_instruction_bundle.py`; `tests/providers/test_{codex,claude}_command.py`; `tests/providers/test_codex_run.py`; `docs/operations.md`; `.agents/rules/security.md` | shipped 2026-07-22; (a)/(b)/(c) cluster-exit (WRI-002/003) |
| 2026-07-22 | **WRI-010 shipped — freeze + isolate the in-repo control plane** | Implemented [WRI-010](agent-worc-read-isolation/wri-010-protect-control-plane.md): a new `core/flow/control_bundle.py` freezes the exact control inputs a task's flow references (the flow YAML, every node `role_file`, the supervisor block's three role files, and each `tool` node's resolved executable) into a private immutable bundle at `<private_home>/control-bundles/<task-id>/`, reusing the WRI-001 no-follow inspector (`providers.exchange.default_file_inspector`/`FileFacts`), the containment belt (`providers.artifacts.assert_contained_path`), and `sha256_file` — a symlink/reparse, hard link, special file, or NTFS ADS source is refused, and `shutil.copy2` guarantees the frozen copy is a fresh regular file. `_engine_run` freezes on fresh/restart (persisting `bundle_digest` to a new `tasks.control_bundle_digest` column) and loads+verifies on continue; it then binds `NodeInputs.flow_dir`, the supervisor, and a per-task `ToolRegistry` to the frozen bundle so no later consumer reopens live `.worc`. The `_engine_post_node` hook re-hashes the live inputs after each node (WRI-012 has already proven the provider tree quiescent) and routes any drift to `manual_action_required` (`NodeManualRequired` — Core-detected, never fallback/park). `continue` reuses the frozen bundle and treats a live edit made while parked as a conflict requiring fresh/restart. `InternalDenyPolicy` gained `frozen_control_bundle` (the bundle root, `runtime_layout.CONTROL_BUNDLE_DIRNAME`), populated in `composition.build_internal_deny_policy`. **Deferred / cluster-exit:** (a) the **provider-side deny projection** of `control_home` + the frozen bundle is a cluster-exit criterion verified when WRI-002/003 land (WRI-010 only names the deny target); (b) **tool dependency-graph freezing** — only the resolved entry executable is copied + fingerprinted; a tool that loads sibling files is covered by detection (post-node re-hash) + the future provider deny, not by copying its transitive deps; (c) the **merge flow** (`_run_merge_flow`) is ephemeral and intentionally not frozen. | [wri-010](agent-worc-read-isolation/wri-010-protect-control-plane.md); `core/flow/control_bundle.py`; `core/orchestrator.py`; `runtime_layout.py`; `composition.py`; `state_store.py`; `tests/core/test_control_bundle.py`; `tests/core/test_orchestrator.py`; `tests/core/test_composition_layout.py` | shipped 2026-07-22; (a) cluster-exit, (b)/(c) deferred |
| 2026-07-22 | **WRI-012 shipped — provider process-tree quiescence barrier + residual holes to revisit** | Implemented [WRI-012](agent-worc-read-isolation/wri-012-reap-provider-subtree.md): an injected `ProcessContainment` seam in `providers/process.py` (`PosixProcessContainment` = `start_new_session` group + a during-run descendant-tracker thread + a bounded emptiness proof; `WindowsJobObjectContainment` = ctypes kill-on-close Job Object + `QueryInformationJobObject` emptiness proof, behind an injectable `Win32JobApi` seam), a `run_process` quiescence barrier that terminates + proves-empty on **every** exit path and clears the children-file handle **only** once proven, `QuiescenceResult` on `ProcessResult`, a non-fallback/non-park `ErrorClass.CONTAINMENT_UNVERIFIED` (+ `errors._MESSAGES`), an adapter fail-closed gate in `_adapter_base.run()` before parse, and Core routing to `manual_action_required`. Preserves by name (design-review F14): the `(pid,pgid)` children-file hard-stop, `start_new_session` group-leader launch, router cancellation-before-fallback, pending-graceful-stop. **Residual holes to revisit (all deferred, none block the barrier):** (a) **kernel-container north-star** — a `setsid` child that both spawns _and_ reparents to init entirely within one tracker poll gap is unattributable by process-group/ppid alone; fully closed only by cgroup v2 / PID-namespace (Linux) — this is the same "full OS containment" north-star noted in the 2026-07-06 reliable-stop row, now half-picked-up (process-tree barrier) with the kernel-container half still deferred. (b) **Windows Job assign micro-race** — the child is assigned to the Job Object immediately _after_ `Popen` (not at creation via STARTUPINFOEX `PROC_THREAD_ATTRIBUTE_JOB_LIST`), leaving a sub-millisecond window where a grandchild could escape; the job-at-creation path is deferred hardening. (c) **PID-reuse guard** — the proof/kill trust a tracked pid within the short (~5s) quiescence window without a start-time recheck; a `_read_proc_start_time`-style per-pid guard (Linux `/proc` only, as `process_control` already does for the daemon PID) is deferred. (d) **Windows real-behaviour smoke** — the Job Object is unit-tested only via the injected `Win32JobApi` seam (CI is Ubuntu-only); the real ctypes path lands under the [WRI-006](agent-worc-read-isolation/wri-006-cross-platform-isolation-verification.md) native gate. (e) **Downstream integration is a cluster exit criterion** — WRI-009/010/007 plug their pre/post manifest verification and terminal seal in **after** this barrier proves quiescence; the ordering hook exists (`run()` returns only past the gate), the manifest consumers land with those tasks. (f) **Barrier timings are module constants** (`_QUIESCENCE_TIMEOUT_SECONDS=5`, `_QUIESCENCE_POLL_SECONDS=0.1`, `_TRACKER_POLL_SECONDS=2`), not config — intentional (a safety barrier is not an operator knob); revisit only if a real host needs a longer budget. | [wri-012](agent-worc-read-isolation/wri-012-reap-provider-subtree.md); `providers/process.py`; `providers/base.py`; `providers/errors.py`; `providers/_adapter_base.py`; `core/orchestrator.py`; `tests/providers/test_containment.py`; `tests/providers/test_process_quiescence_posix.py`; `docs/operations.md`; `.agents/rules/architecture.md` | shipped 2026-07-22; residuals (a)–(f) deferred |
| 2026-07-22 | **WRI-004 shipped — typed runtime/control/exchange layout** | Implemented [WRI-004](agent-worc-read-isolation/wri-004-centralize-worc-home-seam.md): a stdlib-only leaf `runtime_layout.py` (`RuntimeLayout` with `repo_root`/`control_home`/`private_home`/`exchange_root` + the `*_HOME_DIRNAME` constants) built at the composition/CLI boundary (`cli.layout_for`) and injected into the Orchestrator, providers, memory, and Git footprint. Collapsed the four duplicated literals (`cli.WORC_HOME`, `core._WORC_HOME`, `memory.paths.for_repo`, `providers/artifacts.EXCHANGE_HOME`) and absorbed the WRI-001 inline-exchange shim (`self._exchange_root = layout.exchange_root`). `worc_home_for` now returns `layout.private_home`; control-plane sites use `.control_home`. No on-disk behavior change (control/private both `<repo>/.worc`). **Groundwork, not yet consumed:** `InternalDenyPolicy` (assembled in `composition.build_internal_deny_policy`) carries control/private homes + resolved env-file + provider auth/config homes (shared `claude_config_home`/`codex_config_home` helpers); **frozen control bundles deferred to WRI-010** (documented shim in wri-004/wri-010). Guarded by an AST call-site test (`tests/test_worc_home_call_sites.py`). | [wri-004](agent-worc-read-isolation/wri-004-centralize-worc-home-seam.md); `runtime_layout.py`; `composition.py`; `core/orchestrator.py`; `cli.py`; `git_manager.py`; `memory/paths.py` | shipped 2026-07-22 |
| 2026-07-22 | **WRI-008 shipped — portable artifact path identities; one residual reject-path edge** | Implemented [WRI-008](agent-worc-read-isolation/wri-008-portable-artifact-identities.md): shared reject-not-sanitize identity validators in `security/identifiers.py` (`is_valid_task_id`/`is_valid_node_id`/`is_portable_path_segment`/`is_windows_reserved_name`), task-id strengthened (no trailing dot / no Windows device name) at the §19 gate, every flow node id validated at `load_flow` (all six kinds, before any lookup map/artifact dir/DB run row), one shared containment belt (`providers.artifacts.assert_contained_path` + `PathIdentityError`) at the private write boundary and delegated-to from the exchange, and the exchange relpath grammar delegated to the shared segment validator (the WRI-001 gap is consolidated). **Residual (out of scope, found while building):** the Phase-A reject path `Orchestrator._reject → write_validation_report` derives its dir from the task **file name** (`Path(task_file).stem`), which is not identity-validated — a task file literally named `con.md`/`nul.md` on Windows would fail the `logs/<stem>/` report write (the task is still rejected; only the audit-report write throws). That is the file stem, not the front-matter identity WRI-008 governs. Next step if it ever bites: write the reject report under a portable fallback dir name when the stem is non-portable. | [wri-008](agent-worc-read-isolation/wri-008-portable-artifact-identities.md); `core/orchestrator.py` (`_reject`); `task/validation_gate.py` (`write_validation_report`) | shipped 2026-07-22; residual candidate |
| 2026-07-22 | **WRI-001 exchange — deferred hardening + atomic-write consolidation** | Landed [WRI-001](agent-worc-read-isolation/wri-001-two-root-exchange-layout.md): the `<repo>/.worc-io/<task-id>/` exchange root, its redaction/path-safety publisher (`providers/exchange.py`), routing of every allowlisted writer/resolver, the live `{checks_path}` fix + recovery parity, the `security_audit` report-node migration to structured-output capture, `.worc-io/` gitignore + runtime-artifact classification, and fresh/restart/continue + an **interim** terminal teardown. **Deferred:** (a) the WRI-009 commit-survival proof (a force-added exchange file cannot reach an orchestrator commit) is a cluster exit criterion — verified when WRI-009 lands, not a gate for closing WRI-001; (b) WRI-001 added a 5th temp+`os.replace` atomic writer (`providers/exchange._atomic_write_text`) beside `memory/_io`, `core/hitl`, `core/decomposition`, `cli` — consolidating the ~5 copies into one shared helper is a separate cleanup (a `providers` leaf cannot import `memory`, so the shared helper must sit in the interface cluster); (c) WRI-001 was built decoupled from WRI-004/WRI-008 — the per-task gap notes to close are recorded inline in `wri-004`, `wri-008`, `wri-011`, `wri-007`, `wri-002`, `wri-003`. **Re-verified 2026-07-22** (after WRI-004/WRI-008 landed): both consolidations are confirmed in code — the exchange root flows from `layout.exchange_root` (no inline `EXCHANGE_HOME`), `git_manager` builds its ignore footprint from the shared `runtime_layout` constants, and both roots share one `assert_contained_path` belt + the `security/identifiers` segment grammar — so (c) is resolved for WRI-004/008 and WRI-001's task doc is now marked implemented. Closed a test-coverage gap found in the re-review: the `security_audit` report-slot structured-output capture had no regression guard and `tests/core/test_flow_security_audit.py`'s fake agent still modelled the old agent-written `.worc/security-reports/` contract — added a report-slot test to `tests/core/test_flow_postprocess.py` and de-staled the integration fake. Optional hardening still open: a single end-to-end HITL test asserting the published `hitl/*.answer.json` is answer-only _in the exchange_ while the durable transport handle never appears there (met today by construction + the `assert_request_contained` guard). | [wri-001](agent-worc-read-isolation/wri-001-two-root-exchange-layout.md) | core landed + re-verified 2026-07-22; (a) cluster-exit (WRI-009) + (b) atomic-writer cleanup deferred |
| 2026-07-16 | **Normalized token-usage accounting — provider-aware usage persisted per attempt (state.db v16)** | Implemented [normalized-usage-accounting.md](token-optimization/normalized-usage-accounting.md). Providers now map their raw `usage` to a typed `NormalizedUsage` (cumulative for Codex, per-invocation for Claude) at the parse return site (`providers/base.py` `NormalizedUsage`/`UsageScope`, `codex.py`/`claude.py`, `_adapter_base.py` `coerce_usage_int`). The orchestrator subtracts the resumed session's previous cumulative snapshot to persist a summation-safe **per-run delta** on `provider_attempts` (10 new nullable columns + `get_provider_attempts` reader) and keeps the running cumulative on `editing_lineage`/`node_lineage` `usage_snapshot` (state.db v15→v16, additive) — delta math in `core/flow/usage_accounting.py` (`compute_usage_delta`, session-identity-gated, negative→`unknown`+warn), threaded through `observability.py` + `agent.py`/`evaluator.py` (resolve-once, incl. the intra-node HITL Codex baseline). Fixed `_produced_no_work` to fire on a resumed Codex node's zero per-run output delta (gated on `request.session_id`). **Deferred:** (a) **cost capture** — the `cost` field ships unpopulated; capturing Claude `total_cost_usd` is a separate small pass. (b) **read/report surface** — a `worc` token report / summary section; today usage is readable via SQL / `get_provider_attempts` only. (c) **Codex resume-id assumption** — the delta gate compares the emitted `session_id` to the baseline's stored id, relying on `codex exec resume` re-emitting the same thread id (the negative-guard is the backstop); if a run shows Codex minting a new id per resume, stamp the effective resume id onto `ProviderAttempt` in the router and compare that. (d) `result.json` carries the provider's **cumulative** normalized record while SQLite carries the per-run **delta** — the `scope` field is self-describing; no artifact rewrite. | token-optimization/normalized-usage-accounting.md; `providers/{base,_adapter_base,codex,claude}.py`; `state_store.py`; `core/flow/{usage_accounting,observability}.py`; `core/flow/nodes/{agent,evaluator}.py`; `docs/operations.md`; `tests/{providers,core,state}/` | shipped 2026-07-16 |
| 2026-07-16 | **Content-flow token hygiene — session-scope cost docs + `blog_article_revise` `polish` → `fresh_disposable`** | Implemented [content-flow-token-hygiene.md](token-optimization/content-flow-token-hygiene.md). Added an explicit token-cost warning to the session-scope docs (`docs/flow-authoring.md` canonical; `packaged/guide/flows/{reference,README}.md` cross-reference it). Flipped the packaged `blog_article_revise` `polish` node to `fresh_disposable` (dropped `lineage_affinity: revise`) — it is terminal and receives the diff + reviewer findings as artifacts, so it no longer re-reads `revise`'s whole transcript; `revise → fixing` lineage is unchanged. Rewrote the `polish`/`fixing` YAML comments and added a read-once / one-patch / one-diff operational budget to the `revise`/`polish` role prompts (polish now points at its context files rather than assuming in-session memory). **Deferred:** (a) the **A/B confirmation** of the ~60–80k saving (and "quality no worse") now has its measurement substrate (row above) — measure next. (b) existing installed `.worc/` copies (e.g. the operator's `WastimeApp/.worc/`) need a **manual re-seed** — there is no `upgrade-flows` mechanism yet; only new installs pick this up. (c) per-node model/effort tuning on the target flow is a config/A-B decision, not a packaged-default change. | token-optimization/content-flow-token-hygiene.md; `docs/flow-authoring.md`; `packaged/flows/blog_article_revise.yaml`; `packaged/flows/blog_article_revise/{revise,polish}.md`; `packaged/guide/flows/{reference,README}.md` | shipped 2026-07-16 |
| 2026-07-16 | **`checks_path` has no live channel for a `kind: checks` node's own-run output — `security_audit`'s `dependency_scan` findings are unreachable by any role-prompt variable** | Found while backporting field-tested prompt improvements from a target repo's seeded `.worc/flows/security_audit/*.md` into the packaged defaults. Traced why the seeded prompts' `{?checks_path}…{/checks_path}` references to "the dependency-scan advisories at {checks_path}" would render empty on a fresh run: `NodeInputs.checks_path` (`core/flow/nodes/base.py`) is only ever set at construction from the always-`None` `_Pipeline.check_log`, or on **resume** from `latest_failed_check_log` (`state_store.py`), which requires a `passed=0` check-run row — and the `dependency_scan` checker always records `passed=True` (`checks/dependency_scan.py`). Unlike evaluator nodes (`EvaluatorNodeRunner.run()` live-mutates `self._in.review_path` on every completed run, role-agnostic — `core/flow/nodes/evaluator.py`), `ChecksNode` has no equivalent live write, and `node_output_vars()` (`core/flow/context_paths.py`) explicitly excludes `checks`/`evaluator`/`human` kinds from the generic `{<id>_path}` channel. Net effect: a `kind: checks` node's structured findings are never exposed to a downstream `agent` node by any prompt variable in a normal run — only pass/fail routing crosses the edge. Removed the broken `{?checks_path}` references from the packaged `security_audit/{threat_analysis,verifier,report}.md` (reverted to plain prose, matching the original's pre-existing vagueness here) rather than leave misleading dead template syntax. **Next step:** give `ChecksNode` a live output channel analogous to `evaluator.py`'s `review_path` write — either a dedicated `checks_path` live-mutation on every completed checks-node run (mirroring `review_path`), or extend `node_output_vars()` to grant `checks`-kind nodes their own `{<id>_path}` (parallel to `tool` nodes, which already get this) — then restore an explicit dependency-scan reference in `security_audit/threat_analysis.md`. | `core/flow/nodes/{base,evaluator,checks}.py`; `core/flow/context_paths.py`; `core/orchestrator.py` (`_restore_engine_inputs`); `checks/dependency_scan.py`; `packaged/flows/security_audit/{threat_analysis,verifier,report}.md` | candidate |
| 2026-07-16 | **Packaged content-authoring flows de-branded + generalized; `blog_article` / `blog_article_revise` / `check_length` promoted from a target repo's custom flow to packaged built-ins** | The packaged `content_chapter` / `content_translate` flows and the `check_journey` tool were, in effect, one operator's book project (Wastime Journey) shipped under a generic-sounding name — role prompts named the product, hardcoded that repo's file paths (`.rules/wastime-journey-rules.md`, `_wastime-app/_idea-app.md`, `mobile/roadmap …`), and content_chapter's packaged copy had drifted behind that repo's own field-tested `.worc/flows/content_chapter/*`. Reworded every content-flow role prompt to drop the product name and hardcoded paths in favor of project-optional conventions (a continuity reference "if this project keeps one", a style/voice guide "if it defines one", etc.) while keeping the field-tested mechanics (the Purpose/Emotional-point structure `check_journey` enforces, the multi-voice separation, the scout → editor → gate → critic → style → fixing shape). Dropped `content_chapter`'s `product_accuracy` node (and its role file): field use found fact-checking prose against a real product doesn't generalize to most content projects and overlapped with the story critic; raised `critic_fix` 3 → 6 to match the field-tested finding that a subjective narrative-critic loop needs more headroom than a mechanical gate. Ported the same repo's per-node model/reasoning tuning rationale (strongest model for creative/judgment nodes, lighter+high-reasoning for the understanding-only scout, same model at lower reasoning for bounded polish/fix passes) in as commented-out suggestions — matching the existing packaged convention of never hardcoding `provider`/`model` in a shipped default — rather than as active pins. Separately, promoted that same target repo's `blog_article` / `blog_article_revise` custom flows (well-crafted authorial-blog-post prompts) to packaged built-ins, generalized the same way; they had never actually run there (no `.worc/logs/` history) because their `tool: check_length` had no packaged or local implementation — added `packaged/tools/check_length` (+ `.cmd` wrapper, `tests/test_check_length.py`) as a new generic minimum-size-floor tool, mirroring `check_journey`'s delivered-tool contract. Built the field-tested F3 fix (see the row below) into `check_length` from the start: diff-only scope, no task-text fallback, so it can never repeat the "measured a reference doc instead of the deliverable" failure. **Next step:** `check_journey`'s domain constants (banned RU service-label headings, the antithesis regex, the EN/book char bands) stay hardcoded for one book's language/format, per its own docstring's "fork and tune" guidance — parametrize them via `args` if a second book-format project needs a different rule set without forking the script. **Same-day correction:** `content_book` (the once-per-book assembly flow) was removed from the packaged set entirely, not just de-branded — dropped `packaged/flows/content_book.yaml` + its `content_book/` prompt folder, and its mentions in the docs/tests below. | `packaged/flows/{content_chapter,content_translate,blog_article,blog_article_revise}.yaml`; `packaged/flows/{content_chapter,content_translate,blog_article,blog_article_revise}/*.md`; `packaged/tools/{check_journey,check_length}{,.cmd}`; `tests/test_check_length.py`; `tests/test_cli_install.py`; `tests/core/test_flow_tools_registry.py`; `docs/glossary.md`; `docs/how-it-works.md`; `docs/flow-authoring.md`; `packaged/guide/flows/README.md` | shipped 2026-07-16 |
| 2026-07-14 | **Codex Windows-sandbox false-success post-mortem — F1/F3/F4/F5 fixes; F2 (host) deferred** | From [2026-07-14-codex-windows-sandbox-false-success.md](../analysis/2026-07-14-codex-windows-sandbox-false-success.md) (run `blog-happy-in-my-misfortunes-2`): the codex `draft` node exited 0 while the sandbox could not spawn any child (`CreateProcessWithLogonW failed: 2`), so nothing was written yet the run was counted `succeeded`. **Shipped:** **F1** — extended `_HELPER_LAUNCH_FAILED_PATTERN` (`providers/codex.py`) to also match the runtime sandbox-launch failures (`CreateProcessWithLogonW failed` / `fs sandbox helper failed` / `windows sandbox:` prefix), so the post-success guard + the nonzero-exit signature both flip the false success into `PERMISSION_DENIED` → Router falls over to Claude (regression tests in `test_codex_run.py`). **F5** — `build_codex_argv` now appends `-c web_search="disabled"` for `network_access=false` nodes so an offline node cannot reach the web through the backend-side `web_search` tool (the incident's writer ran 9 web searches offline); tests in `test_codex_command.py`. **Caveat:** on older codex, disabling web_search conflicts with `reasoning_effort=minimal` (openai/codex#5002) — offline codex nodes in `blog_article` run xhigh/medium, so safe there; revisit if a minimal-reasoning offline codex node is added, and confirm the exact key against the installed codex. **F3/F4 (target repo `.worc/`, custom `blog_article` flow):** F3 — `.worc/tools/check_length` now scopes from the diff only (dropped the `_named_in_task` task-text fallback that measured `.rules/*` etc. on an empty diff) → empty diff = vacuous pass; F4 — `.worc/flows/blog_article/draft.md` no longer injects `{task_path}` (it told the writer to write into the task file); it now writes to the path the task body declares, and the diff carries it downstream. **F2 deferred (host, owner):** seclogon is Running, so the cause is the WindowsApps `pwsh.exe` execution-alias the codex sandbox child cannot launch; remediation is host-side (disable the pwsh App-execution-alias / put a real pwsh ahead of WindowsApps on PATH; if needed reinstall the codex standalone package). F1 makes the pipeline robust regardless (codex infra-fails → Claude fallover). **2026-07-16 addendum:** F3/F4 are now also baked into the packaged built-ins (see the row below) — `packaged/tools/check_length` ships diff-only from day one (no task-text fallback ever existed for it to regress), and `packaged/flows/blog_article/draft.md` never injects `{task_path}`. | 2026-07-14-codex-windows-sandbox-false-success.md; `providers/codex.py`; `tests/providers/test_codex_{run,command}.py`; target `.worc/tools/check_length`, `.worc/flows/blog_article/draft.md` | F1/F3/F4/F5 shipped 2026-07-14; F2 deferred (host, owner) |
| 2026-07-14 | **Windows stop-ladder orphaned-daemon post-mortem — defects A/B/C** | [Post-mortem](../analysis/2026-07-14-windows-stop-ladder-orphaned-daemon.md). **Shipped:** idle stop-file polling remains bounded to ~1s; the same cancellation predicate now reaches FlowEngine, which parks at the untouched next-node checkpoint; an unconfirmed Windows soft stop retains PID/stop/children so escalation never loses its target; timeout automatically invokes the injected `taskkill /F /T` tree killer; a repeated stop with no PID on Windows preserves the live sentinel; and `restart` refuses to spawn a replacement after an unconfirmed stop. No new task status or config. A live Windows smoke confirmed timeout escalation against a real temporary parent/child process tree and verified no surviving process or runtime file. **Still open:** owner-level end-to-end smoke with a real watcher/coding agent and `CTRL_BREAK`; a richer user-facing `cancelled` task status remains separate. | `core/flow/engine.py`; `core/orchestrator.py`; `process_control.py`; `cli.py`; stop/cancellation regression tests | A/B/C shipped 2026-07-14; synthetic Windows tree smoke passed; owner E2E pending |
| 2026-07-14 | **Terminal-cleanup base-branch switch-back is now configurable (`repo.checkout_base_on_cleanup`)** | Operators running every task on one shared branch hit a pointless `git checkout base` at the end of each task. Added the instance-only tri-state `repo.checkout_base_on_cleanup` (bool \| null, default null → per-`branch_mode`: `new` returns, `existing`/`current` stay; `false` = global off; `true` = force `new`+`existing` back) resolved by `GitManager.returns_to_base(mode)` and consumed in `terminal_cleanup`; threaded the persisted mode into the `finalize`/`_resume_cleanup` cleanup calls so all terminal paths agree, and made the `finalize --dry-run` cleanup line mode-aware. **Change of default:** `existing` mode no longer returns to base by default (was: it did). Bumped `CONFIG_SCHEMA_VERSION` → 30. **Deferred sub-question:** the flag is instance-only; if a repo needs per-task control (mirroring `branch_mode`/`auto_merge`'s tri-state task field), add a `checkout_base_on_cleanup` task-model field + resolver — not built now (no demand). | `config/schema.py` (`RepoConfig`); `config/loader.py` (`_opt_bool`); `git_manager.py` (`returns_to_base`/`terminal_cleanup`); `core/orchestrator.py` (`finalize_task`/`_resume_cleanup`/`FinalizePlan`); `cli.py` (`_report_finalize_plan`); `docs/configuration.md`; `docs/task-authoring.md` | shipped 2026-07-14 |
| 2026-07-14 | **Rerun dead-end Option C — resolve the flow pre-slot (prevent the dead-end forming)** | The rerun-dead-end ADR shipped Option A only (restart-in-place makes a pre-checkpoint failure on an operator-owned branch always recoverable). Option C — resolving `task_type` → flow **before** `acquire_slot`/`_register_task` so a missing/invalid flow quarantines to `tasks/rejected/` with no `failed` row (root-cause prevention) — was deferred as not worth the pre-slot machinery now. Hook: add a `_validate_flow_present` pre-slot validator in `run_task` (mirror `_validate_branch_mode` at [orchestrator.py](../../src/wastech_orchestrator/core/orchestrator.py) ~L513, catching `FlowResolutionError`/`FlowValidationError` like `_validate_operator_subtasks` already does), a new `ValidationReason` (e.g. `FLOW_UNRESOLVABLE`), and a pre-slot reject test. Open sub-question from the ADR: hard quarantine (fail-closed, consistent with the other pre-slot checks) vs a retryable defer (friendlier to the concurrent-edit race, but needs new defer machinery + a bound). | [done/rerun-dead-end-pre-checkpoint-existing-branch.md](done/rerun-dead-end-pre-checkpoint-existing-branch.md); `core/orchestrator.py` (`run_task`); `task/validation_gate.py` (`ValidationReason`) | Deferred |
| 2026-07-14 | **`check_length` measured the wrong files; no `repository_document` "must produce the file" engine guard** | From the Windows-10 Codex sandbox-helper incident: when the `draft` agent produced no article, the delivered `check_length` tool measured whatever other `.md` files the diff happened to contain (`.rules/*`, `AGENTS.md`) and drove a pointless `length_fix` loop until the engine's `no_file_change` stall guard tripped — masking the infra root cause. The provider/routing fix (defects #1/#2, shipped 2026-07-14) already resolves the incident (Codex infra-fails → Claude fallback writes the file), so this is defense-in-depth. Two coupled next steps, deferred because they need a concept the task schema lacks: **(a)** a **declared target document** for a task/flow so a check tool can be told which file is the deliverable — today `build_path_context` passes only `repo`/`task_path`/`diff_path`, no target — letting `check_length` return an explicit `missing_target_file` instead of scanning arbitrary changed files; **(b)** an **engine-level guard** for `output_policy: repository_document` agent nodes that treats "supposedly-successful run produced no new file" as a hard failure (today `_apply_output_containment_guard` only checks containment and passes an empty change set vacuously; `resolve_output_policy(REPOSITORY_DOCUMENT)` is hardcoded to `docs/research/<id>` + `report.md`/`sources.json`, which does not describe a blog article, so it cannot be reused as-is). Note `check_length` itself is a project-delivered tool in the target repo's `.worc/tools/`, not orchestrator code. **Update 2026-07-14:** the target-tool half is done — `check_length` now scopes from the diff only (the `_named_in_task` fallback that measured arbitrary task-referenced `.md` was removed; empty diff → vacuous pass), and F1's provider fix closes the incident root cause (see the top row). Still open here as defense-in-depth: **(a)** a declared-target-document concept + **(b)** the engine-level `repository_document` "must produce the file" guard. | [codex on windows 10/INCIDENT.md](codex%20on%20windows%2010/INCIDENT.md); `core/flow/nodes/agent.py` (`_apply_output_containment_guard`); `core/flow/output_policy.py`; `core/flow/context_paths.py` (`build_path_context`); `core/flow/nodes/tool.py` | candidate (a)(b); target `check_length` done 2026-07-14 |
| 2026-07-13 | **Phase-2 lint ratchet burn-down (`C901`/`PLR09xx` per-file-ignore baseline)** | The Phase-2 maintainability ratchets shipped with loose starting thresholds (complexity 15, max-args 10, branches 15, returns 6, statements 50 — see the linting analysis report). Current god-module offenders are baselined in `pyproject.toml` `[tool.ruff.lint.per-file-ignores]` (~20 files) so new/clean code is protected now; the work is to refactor each offender under threshold, delete its code from the baseline, then tighten thresholds (complexity→10, max-args→5). Start with the worst: `core/flow/validator.py` `_check_graph` (complexity 39 / 40 branches / 94 stmts) and `_check_config_consistency` (18); `cli.py` `build_parser` (114 stmts) + `main` (complexity 27 / 23 returns / 26 branches); `core/flow/wiring.py` `build_node_services` (25 args); `task/validation_gate.py` `_check_field_types` (complexity 23). `PLR0911` (too-many-returns) is the widest-spread / lowest-value code and the first candidate to drop from `select` if the baseline proves noisy. | `pyproject.toml` (`[tool.ruff.lint]` select + `[tool.ruff.lint.per-file-ignores]` + `mccabe`/`pylint`); [docs/analysis/linting-and-code-quality-investigation.md](../analysis/linting-and-code-quality-investigation.md) | candidate |
| 2026-07-13 | **Phase-2 residuals: dead-code triage + (archived) functional-map refs for the `composition.py` move** | Loose ends from the linting Phase-2 landing. **(a) Dead-code triage — done 2026-07-14:** deleted the confirmed-dead symbols (`_validate_session_id`, `_artifact_kind`, `_require_human_result` + its only caller `_raise_human_failure`, `_save_counters`, the orphaned `_ARTIFACT_KINDS`/`_SESSION_ID_RE`; `SUBTASK_IN_PROGRESS`; `git_manager.cumulative_committed_diff`); kept the vulture false positives that are used from tests (`SUBTASK_COMMITTED`, `is_terminal`/`is_active`, `symbol_exists`/`write_cache`). **(b) Factory move — done 2026-07-14:** repointed the functional-map factory refs (B04/B06/B18/B22/B24, index, block-registry) from `orchestrator.py:NNNN` to `composition.py`; the remaining bulk file:line drift is moot — the functional map (`docs/functional/`) was archived to `.archives/` on 2026-07-21. **(c) Docstring floor — open (gradual):** `interrogate fail-under` is 70 (actual 70.8%); the analysis deliberately keeps it a threshold (self-documenting Pydantic/config models make a ~100% target wrong), so raise it organically as `notify`/`config`/`install` gain docstrings — not a forced pass. | `pyproject.toml` (`[tool.interrogate]`); `src/wastech_orchestrator/composition.py` | (a)(b) done; (c) gradual |
| 2026-07-13 | **Audit-trail sweep: `tool.py` stdin has no audit artifact at all; memory-packet file goes stale on a node's repeat runs** | Found while fixing the context-footer audit bug (done/agent-prompt-audit-missing-context-footer.md): the footer bug turned out to hit 3 audit surfaces (`request.json`/`rendered-prompt.md`/prompt-audit), fixed together with the supervisor's own total audit gap (observe/finalize/handoff turns never wrote to any audit surface before this fix). Two further gaps the same sweep found were deliberately left out of that fix's scope (confirmed with the operator via the sweep's follow-up section): (a) **`tool.py` nodes have zero audit artifact for their stdin** — they bypass the whole provider/observability spine via a raw `run_process` call, so only redacted `stdout.txt`/`stderr.txt` survive; needs a new artifact type/writer, not a wire-up of an existing one. (b) **The per-node memory-packet file is keyed only by `node_id`** (`memory/packet.py` `write_packet`, dest built in `agent.py`/`evaluator.py` `_memory_path`) and is silently overwritten on a node's 2nd+ run in the same task (e.g. a second `fixing` pass) — an earlier run's `rendered-prompt.md` still references that shared path, which can hold a _later_ run's content by the time anyone reads it; the packet file is also never registered as a tracked artifact. Next step for (a): design a redacted `tool-request.json`-style artifact alongside the existing stdout/stderr streams. Next step for (b): make the packet dest path run-scoped (include `node_run_id`) and call `register_artifact` on it. | done/agent-prompt-audit-missing-context-footer.md; `core/flow/nodes/tool.py`; `memory/packet.py`; `core/flow/nodes/agent.py`/`evaluator.py` (`_memory_path`) | candidate |
| 2026-07-12 | **`rerun --continue` crashed resuming a dirty task branch (`git checkout main failed`)** | `_resume_via_engine`'s branch reattach (the `--continue` resume-in-place path) called `git_manager.prepare_branch(...)` without `mode=`/`branch_ref=`, so it silently took the `BranchMode.NEW` "first creation" branch — which unconditionally did `checkout(base_branch)` before reattaching to the task branch. On a `--continue` at review/fixing/publish (capability #3's "tolerate the task's own uncommitted WIP"), that detour conflicted with the very dirty tree it was supposed to tolerate, aborting on `git checkout main` and terminaling the resume as `failed`/`manual_action_required` with a raw git-stderr reason (RERUN-BUG-REPORT.md). Fixed in `git_manager.prepare_branch`: once the branch already exists (any restart/`--continue`, any `BranchMode`), reattach directly — never via `base_branch` — skipping the base detour entirely; only first creation still fetches/checks out/pulls `base_branch`. Regression test exercises the real `_resume_via_engine` → `prepare_branch` path (not the mocked-`resume` style the existing WIP-tolerance tests use, which never touched this code). **Follow-on UX report (same day):** even after the crash fix, the operator still saw `Rerun ... [continue] from base 'main'? [y/N]` and (reasonably) feared a checkout to base was still about to happen — the confirm prompt used one wording for both modes, while `--dry-run` already correctly said "reuse `<branch>`" for `--continue` with no base mention. Fixed in `cmd_rerun`: the prompt now says `on branch '{plan.branch}'` for `--continue` (base is never touched) and keeps `from base '{plan.base_branch}'` only for a fresh rerun (which really does reset to base). | RERUN-BUG-REPORT.md; `git_manager.py` (`prepare_branch`); `core/orchestrator.py` (`_resume_via_engine`); `cli.py` (`cmd_rerun`); `tests/core/test_cli_rerun.py` | shipped 2026-07-12 |
| 2026-07-12 | **Exhausted-fix-budget confirmation doesn't cover inline per-edge `budget:` loops** | `rerun --continue`'s new interactive exhausted-budget prompt (rerun-flow-drift-and-budget-confirm.md) only checks _named_ loops (`edge.loop is not None`, e.g. `review_fix`/`test_fix`) reachable forward from the resume node — an inline `budget: N` edge with no loop name can still silently re-park on resume, same failure mode this ADR closed for named loops. Deliberately deferred (no flow in the wild uses inline budgets today); if one does, extend `core/loop_control.exhausted_fix_loops` to also walk `edge.budget is not None` edges against their persisted synthetic counter key. | rerun-flow-drift-and-budget-confirm.md; `core/loop_control.py` (`exhausted_fix_loops`) | Deferred |
| 2026-07-11 | **Universal resume/recovery — deferred capabilities** | Phase 0 (`--reset-fix-budget`, WIP tolerance) + Phase 1 `--from <node>` shipped (universal-resume-recovery.md). Deferred: **#5** per-leg provider/model overlay on the resumed leg (reuse the `resolve_node_overrides` seam); **#6** bounded audited force-forward (`--accept-current` / `--skip-node` once, synthetic operator verdict in `evaluations`); **#7** guided reconciliation + `--commit-wip` + the own-vs-foreign WIP discriminator (diff dirty paths against the task's `current.diff`, so Phase 0's "foreign WIP is committed" limitation is closed); **#8** resume-then-finish; plus `--from` under decomposition (per-subtask region). The `--re-resolve` idea listed here for flow-fingerprint drift is superseded — 2026-07-12's rerun-flow-drift-and-budget-confirm.md resolved it differently: `--from <node>` now always resumes through drift (no new flag), rebaselining the checkpoint's fingerprint. | universal-resume-recovery.md; rerun-flow-drift-and-budget-confirm.md | Deferred (drift item resolved) |
| 2026-07-08 | **P5 remediation — SKIPPED / deferred items (closed; residue below)** | Final disposition of the pieces left out of the orchestrator remediation above (p5-remediation-skipped-items.md, archived 2026-07-08). **Done:** A1 substantive — the target `wastech-mdlint/.worc/flows/implementation/review.md` narrowed "missing test coverage" blocking to _zero coverage for new core behavior_, moving completeness to advisory (gitignored local edit, no commit); C2 secondary step — new `ErrorClass.MODEL_REQUEST_INVALID` (a model/schema HTTP 400) split from `process_crashed`, not fallback-eligible, in both adapters (this also fixed a **latent** missing `SESSION_UNAVAILABLE` `_MESSAGES` entry that would `KeyError` in `classify`, now guarded by a new exhaustiveness test); D1 already satisfied (no target task combines `depends_on` with a same-ref shared branch). **Still open — carried here:** (a) **A3 delta-observe (F50)** deliberately stays deferred — skipping re-observation of unchanged node-runs is fuzzier and risks losing supervision signal, and the shipped F50 `high` reasoning cap already cut observe cost; revisit only if observe cost dominates a deep-loop task after the cap (measure `result.json.usage`); lever `core/supervisor.py` observe cycle. (b) **E1/F37** owner live-smoke — prove Claude native-memory isolation is structural (spawn a claude node provoked to write native memory, confirm refusal); lever `providers/claude.py`. | p5-remediation-skipped-items.md; p5-findings-remediation-plan.md; TEST-FINDINGS.md F37/F40/F42/F50; target `wastech-mdlint/.worc/flows/` | A1/C2/D1 done; A3 + E1/F37 open |
| 2026-07-08 | **P5 findings — consolidated remediation plan (memory F43–F48 now tracked + all P5 open findings in one place)** | Single work plan for every OPEN P5 finding, drawn from the phase synthesis, per-node prompt-quality audit, memory audit, and the individual 2026-07-07 rows below: p5-findings-remediation-plan.md. It carries **Цель · Рычаг · Шаги · Тест · Зависимость** per finding with recommended order (P1: F42 review calibration, F43 memory noise, F48 packet relevance; P2: F49/F50/F44–F47/F39-preflight/F40/error_class-triage/F37-verify). **Newly registered here** (were only in the memory audit doc, untracked in follow_ups): **F43** durable semantic/procedural/reviewer lessons stuck in quarantine, never read by the packet (write-only noise duplicating the entity layer) — `supervisor.py:868` finalizer prompt / `packet.py:144-166` read durable quarantine / `lifecycle.py:88-109`; **F48** packet loses path-relevance on a shared chain branch (cumulative `changed_code_paths_since_base` — the untouched tail of the F32 `_diff_base` fix) — `agent.py:598`/`evaluator.py:328`/`git_manager.py:717`; **F44** duplicate entity card per file + empty `last_seen_task_ids` (`service.py:317-336`); **F45** redaction over-fire corrupts a durable lesson `subject` (sibling of F36; `redaction.py:127-134`); **F47** contentless episodes (`orchestrator.py:2145-2158`); **F46** misleading audit merge-rationale (`service.py:281`). Also refines **F49**: the persist point already exists (`_sync_counters_from_run_state`) — the field reads 0 because `engine._reset_loops_at` zeroes the consecutive-loop counter on `accept`; the real fix is a cumulative per-loop-kind total, not a new persist point. The 2026-07-07 rows (F42/F49/F39-preflight/F40/error_class) are the individual sources; this plan supersedes them as the actionable tracker. | p5-findings-remediation-plan.md; p5-phase-synthesis.md; p5-prompt-quality-per-node.md; p5-memory-subsystem-audit.md; TEST-FINDINGS.md F42–F50/F43–F48 | candidate |
| 2026-07-07 | **F42 — calibrate blocking codex-review depth (7 rework cycles on one task)** | From the P5 run p5-04 (`synthesize`) with `review` pinned to codex gpt-5.4: the blocking evaluator returned `rework` **7 times** before `accept` (~2h40m, diff grew to +1547 incl. +802 test lines). Iterations 1–4 were real correctness HIGH bugs (G6-honesty empty `readingOrder`, all-or-nothing `resolveCompileSettings`, per-field leniency, `contentHash` sans provenance) — genuinely valuable; iterations 5–7 drifted into test-coverage/assert completeness and input-bound validation. Not a bug (system worked, converged in budget, all checks green throughout), but the default calibration makes a single big node dominate run time/cost. Options: (1) `review.md` role — ask the reviewer to batch **all** findings of a pass into one verdict and explicitly separate blocking-correctness vs advisory-completeness; (2) `max_rework_per_stage` cap on the review node; (3) a separate **non-blocking** `testing_quality` evaluator for coverage remarks so correctness-review doesn't block on test polish. | p5-04-synthesize-run-analysis.md; TEST-FINDINGS.md F42; `.worc/flows/implementation/review.md` (target) / `packaged/flows/roles/implementation/review.md`; `core/flow/schema.py` (`max_rework_per_stage`) | candidate |
| 2026-07-07 | **`tasks.review_fix_cycles` not persisted (stays 0 despite real review reworks)** | Observed in p5-04: `state.db tasks.review_fix_cycles = 0` after **7** review-driven rework cycles (`fix_iterations = 7` is correct; `test_fix_cycles = 0` correct — no test failures). The global counter captures the loop, but the per-loop `review_fix_cycles` audit field is never incremented/persisted, so per-loop attribution is lost in the audit trail. Minor audit gap, distinct from F42. Next step: find where `review_fix`/`test_fix` loop counters are updated on the `tasks` row and ensure `review_fix_cycles` is written like `fix_iterations`. | p5-04-synthesize-run-analysis.md; TEST-FINDINGS.md F42; `core/orchestrator.py` / `core/state_machine.py` (loop-counter persist) | candidate |
| 2026-07-07 | **F39 — preflight doesn't catch supervisor model↔provider mismatch when provider is inherited** | From p5-02: under codex-primary with `supervisor.model: claude-opus-4-8` and **no** `supervisor.provider`, the supervisor inherited `primary=codex` and sent a claude model to codex → `400 model not supported` on every step (masked by claude fallback). The F39 code fix added `SupervisorConfig.provider` + model↔provider validation, but `preflight` passed `ready` — the validation apparently only fires on an **explicit** `supervisor.provider`, not on the inherited-primary case. Extend the supervisor preflight check to the inherited path: when `supervisor.provider` is unset, validate `supervisor.model` against the resolved global primary's vendor (fatal if no fallback, warn otherwise). Workaround in the field is to set `supervisor.provider` explicitly (done for the campaign). | p5-02-doc-profile-run-analysis.md; codex-primary-correctness.md; TEST-FINDINGS.md F39; `config/schema.py` (`SupervisorConfig`); supervisor provider-resolution + preflight | candidate |
| 2026-07-07 | **F40 — warn when a task's `depends_on` targets a task sharing its own `branch_ref` (contradictory chaining)** | From p5-02: the whole P5 phase blocked at step 2 because every task combined `depends_on: [predecessor]` (a **merge**-gate: `refusing to run … dependency PR is OPEN (unmerged)`) with `branch_mode: existing` + `branch_ref: feat/p5-compile` (a **shared-branch accumulation** whose PR stays open until phase end). The two are mutually exclusive chaining mechanisms; the p4 campaign used shared-branch **without** `depends_on`. Primary fix is task-authoring (don't combine — removed `depends_on` from p5-02..06). Orchestrator UX guard: at task validation, warn (or reject) when a `depends_on` target's branch equals the task's own `branch_ref` — catches the contradiction before a run refuses mid-phase. | p5-02-doc-profile-run-analysis.md; TEST-FINDINGS.md F40; `core/flow/validator.py` / task validation gate; `core/orchestrator.py` (`depends_on` gate) | candidate |
| 2026-07-07 | **Codex exit-2 argparse errors are still classified `unsupported_version` (masks bad-argv); live-verify codex-primary resume nodes** | Deferred, out of the F38/F39 ADR scope. (a) **Misclassification:** the codex adapter's stderr signature (`providers/codex.py` `_CODEX_SIGNATURES`, regex `unknown option` / `unrecognized option` / `unexpected argument`) maps an argparse/exit-2 rejection of our _own_ argv to `unsupported_version`, which hid F38 (a bad-argv construction) behind a version-looking error. Now moot for the resume path (F38 reorders the argv so 0.142.x accepts it), but a latent trap for the next grammar drift — consider a distinct `configuration_error`/`bad_argv` class for exit-2 argparse rejections vs a genuine version gate. (b) **Live-verify (owner):** re-run a `p5-01`-like task under codex-primary on real codex 0.142.5 and confirm supervisor + documentation actually run on codex — `state.db provider_attempts` shows codex `succeeded` on resume nodes (no `unsupported_version`, no claude fallback), and preflight emits the resume-grammar WARN/FAIL only on a real drift. | codex-primary-correctness.md; p5-01-classify-nodes-run-analysis.md; TEST-FINDINGS.md F38/F39; `providers/codex.py` | candidate (a) / **(b) verified 2026-07-07**: p5-02 documentation resume ran on codex succeeded (no `unsupported_version`, no fallback); p5-04 supervisor (all per-step + finalize) ran on codex succeeded. F39 inherited-mismatch preflight gap tracked as its own row above. |
| 2026-07-06 | **⚠️ Reliable-stop subtree kill — Windows `--force-full` real-smoke pending; `--force` narrowed to a pending graceful stop on both platforms (shipped 2026-07-20)** | The reliable-stop ADR mostly shipped: `run_process` timeout/interrupt, daemon-exit cleanup, and `--force-full` use the recorded `(pid, pgid)` handle (`.worc/orchestrator.children`) plus `kill_agent_subtree` (group-kill + POSIX descendant sweep / Windows `taskkill /F /T`), and a stop-killed agent is flagged `ErrorClass.CANCELLED` so the router never falls back and the Core parks the task resumable. **The remaining POSIX soft-stop work shipped 2026-07-20** ([done/reliable-stop-posix-soft-timeout-gap.md](done/reliable-stop-posix-soft-timeout-gap.md)): `--force` now stays a pending graceful stop after timeout (no watcher SIGKILL, every handle retained), and by operator decision the same contract was extended to Windows (`_stop_via_pid_file` no longer tree-kills on a soft timeout) so all hard-kill semantics stay exclusively under `--force-full` on both platforms. **The Windows hard stop is implemented but real-smoke-pending (owner):** start a daemon, run a task, `stop --force-full`, confirm the daemon **and** the full agent subtree (incl. a grandchild that broke into its own group/session) die and `resume()` recovers. Two hooks for later: (1) `is_cancelled`/`ErrorClass.CANCELLED` is the seam a future **cooperative in-stage per-task cancel** (row below) would reuse — it already suppresses fallback + parks; (2) full OS containment (cgroup v2 / PID namespace / Windows Job Object) remains the north-star for "the CLI spawned something we cannot see", deliberately deferred. | `done/reliable-stop-posix-soft-timeout-gap.md`; `providers/process.py`; `process_control.py`; `routing/router.py`; `cli.py` | POSIX + Windows soft-stop pending contract shipped 2026-07-20; Windows `--force-full` owner real-smoke still pending |
| 2026-07-05 | **Live-verify the F37/F28 fixes on a real run** | Two P4 remediations landed with a residual that only a live run can confirm (both fail safe until then). **F37:** `providers/claude.py._native_memory_deny_tools` emits `--disallowedTools "Write(//<config_dir>/**)"` etc. using the documented `//`-absolute glob form; confirm the installed `claude` CLI actually honors that pattern (spawn a run, then assert the agent neither reads nor writes `~/.claude/projects/<target>/memory/`). If the CLI wants `~/`-relative or bare-absolute globs instead, adjust the form. **F28:** the A1/F24 schema fix should let codex review run for real (green codex attempt-1, no claude fallback); confirm on a live run with `review` pinned to codex — until then the "cross-provider review" claim holds only when codex does not fall back. Pairs with the existing F21 planning-allowlist live check. | `providers/claude.py`; TEST-FINDINGS.md F37/F28/F21; `core/flow/nodes/evaluator.py` | validation pending (owner) |
| 2026-07-05 | **Lightweight preflight check: project paths cited in `.worc/flows/*.md` still exist** | Systemic root cause behind F34 (target `planning.md` had cited three non-existent primitive paths): a seeded/edited `.worc/flows/` prompt drifts from the repo with no actuality check. Cheapest guard short of the `upgrade-flows` re-sync (row below): at preflight, extract repository-relative path-looking tokens from each active `.worc/flows/**/*.md` and warn (non-fatal) on any that resolve to nothing in the working tree — catches the F34 class before a run misdirects a weaker planner. Deliberately kept out of the point-fix plan (§0) to avoid scope creep. | p4-prompt-quality-remediation-plan.md §0; `core/flow/validator.py`; `.worc/flows/**/*.md` | candidate |
| 2026-07-03 | **Post-p0 robustness hardening (revive-finalize durability + doc-node capability policy)** | Two deferred hardening decisions surfaced by the p0 campaign, **promoted to a single ADR** — post-p0-robustness-hardening.md. (A) On a revived (`rerun --continue`) task the finalize supervisor turn can fail because the durable `resume_own_lineage` session is unusable after the interrupting stop, so the whole-task summary silently degrades to the minimal fallback (recovery-quality gap adjacent to F14). (B) F17b: worc scopes the documentation node by prompt (F17a), not capability — `network_access:false` hard-blocks only on Codex's OS sandbox, while on Claude it toggles only WebFetch/WebSearch (Bash `npm install` still reaches the network), so p0-04's live install was possible. See the ADR for options + open questions. | post-p0-robustness-hardening.md; test-findings-remediation-plan.md | promoted to ADR |
| 2026-07-02 | **Soft cap on deep/wide subtask handoff graphs** | The intra-task subtask handoff (`{predecessor_context}`) shipped 2026-07-02 (subtask-context-handoff ADR): the deterministic floor + interpretive supervisor brief are assembled per successor from its `depends_on` predecessors. V1 injects the full brief for **every** predecessor. The ADR's one open question is deferred: if a subtask depends on many predecessors, or the chain is long, injecting every brief could grow costly in tokens. Needs a policy (last-N predecessors, a token-budget cap, or summarise-the-summaries). Not built. **Review decision (2026-07-02): deferral confirmed for V1 — no unbounded growth exists today.** Two structural bounds already hold it: (1) `depends_on` lists **direct** predecessors only and the floor iterates `unit.depends_on` (not the transitive closure), so a long chain does **not** accumulate — successor `k` sees only its immediate deps, not `1..k-1`; (2) fan-in width is bounded by the deterministic `max_subtasks` gate (a subtask can depend on at most `max_subtasks-1` others), and the interpretive brief is a single turn regardless of width. So the only real growth vector is a single very-wide fan-in under a large `max_subtasks`. Revisit (last-N predecessors / token-budget cap / summarise-the-summaries) only if a wide decomposition shows the handoff dominating the successor's prompt. Hook: `Orchestrator._assemble_predecessor_context` (bound the `unit.depends_on` iteration) + `Supervisor.handoff`. | subtask-context-handoff.md "Open questions"; `core/orchestrator.py`; `core/supervisor.py` | candidate |
| 2026-07-02 | **Pre-existing broken doc links (backlog link sweep)** | Surfaced while archiving the console/roadmap docs (not caused by the move). (1) [archive/worc-listen-remote-control.md](archive/worc-listen-remote-control.md) has its relative links authored as if the file lived in `docs/backlog/` while it actually sits in `docs/backlog/archive/` — so its sibling-ADR links (`operator-confirmation-gates.md`, `telegram-step-trace.md`, `skills-selection-rework.md`, `archive/done/orchestrator-memory.md` → doubled) and `../../.agents/…`, `../../.claude/…` refs are off by one directory level (the `cli-upgrade.md` refs were repointed to `done/cli-upgrade.md` on 2026-07-02; the rest remain). (2) ~~`README.md` links `memory/adr-0001-memory-subsystem-v1.md` but no such file exists under `docs/backlog/memory/`~~ — **resolved 2026-07-02**: repointed the ADR-0001 links (`README.md`, `memory/index.md`, `memory/plan/index.md`) to the real `memory/adr.md`. (3) several run-remediation rows (now archived in `archive/follow_ups_history.md`) reference `../analysis/{task-023,ion-list-…,watch-autonomy-…}-run-analysis.md` whose files are not present under `docs/analysis/`. Do a bounded relative-link sweep of `docs/backlog/`. | `docs/backlog/archive/worc-listen-remote-control.md`; `docs/backlog/README.md`; `docs/backlog/follow_ups.md` | candidate |
| 2026-07-02 | **Console remediation R2 + R3 (post-review remainders)** | ~~**R2 (invariants):** (H1) blocking `input()` inside the REPL; (M1) POSIX-mode `shlex.split` mangling Windows paths. **R3 (ergonomics):** (M2) default daemon log path; (M3) quit-while-busy detach/stop.~~ **H1/M1/M2/M3 shipped 2026-07-06** by the worc shell reliable control surface ADR (H1 = `--non-interactive` threaded into the forwarded `down`/`restart`; M1 = raw-remainder path parsing + OS-aware `shlex`; M2 = `.worc/logs/daemon.log` default; M3 = `quit` detaches + tells you how to stop). This also closed the standalone 2026-07-01 `shlex.split` row below. **H1 gap found + closed 2026-07-12:** `rerun` was left on the generic `_FORWARD_VERBS` path (no `--non-interactive`) even though `rerun`'s own `--non-interactive`/`--yes` handling already existed for exactly this purpose — a `worc shell` operator running `rerun ... --continue --from <node>` without `--yes` hit the same blocking-`input()`-vs-REPL-stdin fight H1 was meant to close (RERUN-BUG-REPORT.md). Fixed by forwarding `rerun` with `--non-interactive` in `cli_shell.dispatch`, same as `down`/`restart`. **Still open (L-tier ergonomics):** (L1/L2) consolidate the two log tailers / decide on incremental tailing; (L3) console `down`/`restart` reuse the loaded config instead of re-entering `cli.main`; (L4) `logs --daemon`; ~~(L5) add a `docs/functional/` CLI block for `top`/`shell` + the stop ladder~~ — **moot**: the functional map was archived to `.archives/` on 2026-07-21. | cli-upgrade-remediation.md (R2/R3); worc-shell-reliable-control-surface.md; `cli_shell.py`; `cli.py` | L1–L4 candidate; L5 moot (H1/M1/M2/M3 done) |
| 2026-07-06 | **Console spawned-daemon startup-capture log has no rotation; one-shot `run <id>` deferred** | Two intentional scope choices in the worc shell reliable control surface ADR. (a) **Startup-capture growth:** `spawn_detached(capture_path=…)` redirects the spawned daemon's stdout/stderr to `.worc/logs/daemon-startup.log` for crash recovery; it is **truncated per spawn** (never grows across restarts) but has **no rotation within one daemon's lifetime**, so a very-long-lived daemon accumulates a duplicate of its operator log there (the tailed `--log-file` daemon.log _does_ rotate). Cap it (size-truncate / hand off to a `RotatingFileHandler`) if long-running daemons bloat it. (b) **One-shot `run <id>` deferred** (owner decision): the console ships continuous serving (`up`/`watch`) only; a one-shot "run this single task now" verb would add a second claim path needing a slot-guard against the serving daemon. The `run` verb was freed (its old enqueue-alias dropped) so a future one-shot can take the name cleanly. | worc-shell-reliable-control-surface.md; `providers/process.py` `spawn_detached`; `cli_shell.py` `start_watch`/`dispatch` | candidate |
| 2026-07-02 | **Deferred tails from archived run-remediation rows** | Still-open action items carried by the run-remediation rows archived to [archive/follow_ups_history.md](archive/follow_ups_history.md) on 2026-07-02 — kept here so they stay actionable. (task-023) a non-fatal **preflight warning when `checks.command_sets` leave shared/root paths uncovered** (`core/flow/validator.py`). (ion-list) a **live `codex exec` smoke** at preflight instead of the `--help` probe. (PR-merge) **path/area-based merge-flow collection** (a `git.merge_flows` diff-selector), `worc tasks`↔`list --all` dedup, and a watch-startup stale-`MERGE_HEAD` probe. (watch-autonomy) **graceful turn-budget exhaustion** (resume-in-lineage on `error_max_turns`), the **fail-before-commit dirty base tree** (dangling `D`), and the **empty `failure_report.final_diff`** (diff `base..HEAD` of the agent branch). (HITL) **durable across-restart HITL session resume** (persistence keyed by `(task,node,subtask)` + a `state.db` bump) and **per-node HITL autonomy knobs** (`HitlSettings.timeout_s` + bounded auto-proceed for embedded planning/refinement only, never the dangerous-diff guard). (redesign-form-controls) confirm the **silent finalize-tail root cause** on the next run and mark **visual acceptance criteria manual** in `roles/review.md`. (`CLAUDE_CONFIG_DIR` agent-hook isolation and the HITL-wait log signal are tracked by their own rows below.) | [archive/follow_ups_history.md](archive/follow_ups_history.md); `core/flow/validator.py`; `core/orchestrator.py`; `core/hitl.py`; `git_manager.py`; `packaged/flows/roles/review.md` | candidate |
| 2026-06-27 | **Operator confirmation gates: configurable resume cap + empirical fresh-budget check** | Both gates shipped (config v22): the `watch` next-task gate (`orchestrator.auto_mode.confirm_next_task`) and the Claude max-turns continue/stop gate (`agents.providers.<id>.max_turns_gate`), off by default, fail-closed, telegram-required at preflight. Two intentional scope notes. (1) **No hard resume cap** on the max-turns gate (operator decision): each continue needs a fresh approval and timeout→STOP bounds an unattended loop, so a pathological approve-loop is operator-bounded, not orchestrator-bounded. Next step if a hard ceiling is wanted: add a small `max_turns_gate_resumes` config (e.g. 3) checked in `_invoke_with_turn_gate`'s loop with a final forced stop. (2) **Fresh-budget assumption** is confirmed from code (the Claude adapter appends `--max-turns` on every invocation, independent of `--resume`), but not yet empirically verified against a real Claude account hitting a real cap — run that smoke check before relying on the gate in production (set `max_turns` ~5 + gate on, overrun, approve, confirm the resumed run gets a fresh budget). | `core/flow/nodes/agent.py` `_invoke_with_turn_gate`; `cli.py` `_confirm_next_task`; operator-confirmation-gates.md | candidate |
| 2026-06-27 | **Telegram step-trace enrichments (bookend / verbosity / labels)** | The live per-node step-trace shipped (`telegram.trace` bool, config v21; one best-effort `<emoji> <node-id> → <outcome>` message per executed node finish, emitted at the engine post-node hook). Three Decision-deferred enrichments remain, all additive: (1) a **task-start bookend** line for context (terminal status is already covered by `send_notification`); (2) an **`off/milestones/verbose` verbosity enum** to replace the bool if per-node-per-task noise across a busy `watch` queue makes operators turn it off; (3) a **static per-node human-label map** ("implementation — writes the code") layered onto the raw node id. Ship none until real usage shows the bool is too chatty / too terse. | `notify/{interface,telegram}.py` `send_trace`; `core/orchestrator.py` `_engine_post_node`; telegram-step-trace.md (open questions) | candidate |
| 2026-06-27 | **Verify CLI internal 5xx retry behavior (transient-recovery Phase 0 spike)** | Transient provider-failure recovery shipped (`agents.retry`: same-provider retry → symmetric fallback → B-lite soft pause). The ADR's Phase 0 spike — confirming the _pinned_ Codex/Claude CLIs' own internal 5xx/429 retry counts and backoff — was **not** run because it needs the real binaries hitting a real outage (not reproducible in CI). We shipped the ADR's stated defaults (`base_delay_s=2.0`, `max_delay_s=30.0`, `max_attempts=2`); they are operator-tunable, so this is non-blocking. Next step if the defaults prove too tight/loose in practice: measure each CLI's internal retry budget once and re-tune the packaged defaults. | `src/wastech_orchestrator/providers/{codex,claude}.py`; transient-provider-failure-recovery.md (Фаза 0) | candidate |
| 2026-06-27 | **Huge-monorepo skill-inventory context pre-filter** | The supervisor's once-per-task skill proposal is handed the whole discovered inventory (name + description + path per skill). On a repo with hundreds of `SKILL.md` files this becomes large prompt context. Next step if it bites: pre-filter the inventory before feeding the proposal — a `skills.roots` limiter or a diff-scoped `skills.sets` (the `checks.command_sets` pattern), revisited only when the inventory is genuinely large. `resolve_skills` and the per-node threading are unchanged by this. | `core/supervisor.py` `_proposal_prompt`; `core/skills.py` `SkillInventoryScanner`; skills-selection-rework.md (open question 1) | candidate |
| 2026-06-26 | **`worc list [--queue NAME]` command** | The task `queue` tag + per-instance selector shipped (config v18), but there is no `worc list` command to show the pending pool or which instance owns each task. Next step if operators running multiple queues want visibility: add a read-only `list` subcommand that scans `select_pending(folder)`, reads each file's `_scan_pending_meta` (id/priority/queue/depends_on), and prints a table, with an optional `--queue NAME` filter. Read-only, no state change. | `cli.py` `select_pending`/`_scan_pending_meta`; multi-instance-task-queues.md | candidate |
| 2026-06-26 | **`install` only scaffolds the default `tasks/` for a custom `paths.tasks_dir`** | `paths.tasks_dir` (config v17) is honored by the runtime, but `worc install` is config-only — it always writes `tasks_dir: tasks` and creates `tasks/{pending,…}`. An operator who renames the dir must edit `config.yaml` and create the lifecycle subfolders by hand (intentional scope cut — keeps the installer non-interactive). Next step if friction shows up: add a `--tasks-dir NAME` install flag (→ `InstallSpec.tasks_dir`) that writes the value into config **and** scaffolds the matching dirs, surviving `--reconfigure`. | `install/config_writer.py` `build_config_mapping`; `cli.py` `_install_create_dirs`/`REPO_TASK_DIRS`; configurable-tasks-dir.md | candidate |
| 2026-06-12 | **Single umbrella `upgrade`/`doctor` command (config + docs + schema migration)** | Post-upgrade currently needs two steps — `upgrade-config` (materialize new config keys) and `upgrade-docs` (refresh the installed `guide/` docs). Both resolve the install location the same way (`resolve_config_path`) and are fail-closed/idempotent/`--dry-run`. Fold them into one `upgrade` (or `doctor`) that runs both. Fold in the schema-migration remainder too: config value _transforms_ (today's config merge is add-only, not rename/restructure), and a single command that migrates **both** `config.yaml` and `state.db` at once. | `cli.py` `cmd_upgrade_config`/`cmd_upgrade_docs`; `config/upgrade.py`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-12 | **Git-native pre-commit hook** | The Stop docs-sync gate (`.claude/hooks/docs_sync_gate.py`) only covers turns inside Claude Code. For commits made outside the agent, add a real `pre-commit` hook (tracked script + `core.hooksPath`) that enforces the same "code changed ⇒ docs changed" rule. | `.claude/hooks/docs_sync_gate.py`; [.agents/rules/git-workflow.md](../../.agents/rules/git-workflow.md) | candidate |
| 2026-06-12 | **Publish to (Test)PyPI** | Distribution is currently `pipx`/`pip` from a Git tag. To allow `pipx install --pre wastech-orchestrator`, add a publish step to the release workflow using `pypa/gh-action-pypi-publish` with **trusted publishing (OIDC, no tokens)** — TestPyPI for pre-releases first. | [.github/workflows/release.yml](../../.github/workflows/release.yml) | candidate |
| 2026-06-13 | **Verify `worc` is free on PyPI before release** | The `worc` console-script alias is fine for local `pip install -e .`, but before publishing to (Test)PyPI confirm `worc` does not collide with an existing distribution or a common third-party binary; fall back to `worch` or make the alias opt-in if it does. Depends on the (Test)PyPI publish item above. | `pyproject.toml` `[project.scripts]` | candidate |
| 2026-07-06 | **Optional `worc self-update` helper** | The **optional** third phase of the [install-and-upgrade ADR](install-and-upgrade-flow.md) (short-term git-tag recipe is done; medium-term PyPI is the two rows above). A thin `cmd_self_update` that runs the correct `pipx uninstall`+`install` for a requested tag/version **with** the `[shell]` extra — delegating to `pipx` through the process seam as an **argv list, no shell interpolation**, and never touching provider/GitHub credentials. Worth building **only if git-tag installs remain the norm after PyPI lands**; if PyPI covers the common case (`pipx upgrade`/`--pre` work natively), this stays unbuilt so we don't encode the git-tag workaround as a permanent feature. | [install-and-upgrade-flow.md](install-and-upgrade-flow.md); `cli.py`; `providers/process.py` (process seam) | candidate (gated on PyPI outcome) |
| 2026-06-13 | **Defense-in-depth: forbid `--admin` in `find_forbidden_args`** | `merge_pr` builds a fixed argv that never includes `--admin` (so branch protection is respected), and a unit test asserts its absence. As extra hardening, add `--admin` (and other protection-bypassing `gh` flags) to `security/forbidden_args.py` so the prohibition is centralized and also covers any future `gh` call path / `extra_args`. | `security/forbidden_args.py`; `git_manager.py` `merge_pr` | candidate |
| 2026-06-13 | **Config-level per-node model/reasoning defaults (`node_defaults`)** | Per-task `nodes:` overrides shipped (disable-only). The complement lets operators set per-node defaults under each provider in `config.yaml` (`agents.providers.<p>.node_defaults.<node-id>.{model,reasoning}`), inserting a level between task-wide and provider-default in the resolution chain. Touches the config schema (+`schema_version` bump), loader, validation. Also listed in [README.md](README.md) "Other deferred features". | `config/{schema,loader,validation}.py`; `task/model.py` | backlog / not scheduled |
| 2026-06-14 | **Per-provider skill routing on fallback** | Mostly addressed by skills-selection-rework: discovery is now whole-repo (`git ls-files` finds `.claude/skills`, `.agents/skills`, and any other convention), and skills are attached **per node**, not the same set to every stage. Still open: the reference paths are provider-neutral but not provider-_aware_ — when a node falls back to the other provider mid-run, the surfaced skill set does not change. Next step if it matters: derive the per-node set from the resolved route (`AgentRouter` primary/fallback) so it follows the provider that actually ran. | `core/skills.py`; `routing/router.py`; `core/orchestrator.py` `_resolve_skill_layers` | backlog / not scheduled |
| 2026-06-13 | **Prompt customization deferred extras** | The prompt-customization feature shipped (later replaced by node `role_file`s). Still deferred from its original open questions: a global shared preamble across all stages, a per-stage `mode`, and authoring/managing project agent stubs (`AGENTS.md`/`CLAUDE.md`/skills) via a separate `agent_instructions:` feature. The skill-_reference_ half shipped; _authoring_ the stubs is the remaining piece (also in [README.md](README.md) "Agent instruction stubs in target repo"). **Current runtime behaviour documented (2026-06-25):** the orchestrator does not inject these files — each CLI auto-discovers them from the repo-root `cwd` (Codex `AGENTS.md`, Claude `CLAUDE.md`), and because the env allowlist forwards `HOME`, the agent _also_ reads the host operator's **global** `~/.claude/CLAUDE.md` / `~/.codex/AGENTS.md` (outside the per-task reproducibility boundary). The deferred work therefore has two parts: (a) authoring repo-scoped stubs, and (b) optionally isolating/overriding the host-global instruction layer per task. | `core/prompts.py`; `core/skills.py`; `security/env.py`; `providers/{codex,claude}.py` | backlog / not scheduled |
| 2026-06-14 | **`rerun` deferred extras: `--allow-done` + versioned attempt branches** | `rerun` accepts only `failed`/`manual_action_required`. Deferred: (a) an opt-in `--allow-done` to re-do an already-merged task (a foot-gun — needs a stronger confirm + the remote/PR gate); (b) a versioned-branch option (`<current-task-branch>-attempt-N`) as an alternative to the same-name reset, which would change the branch/PR naming contract and downstream assumptions. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task` | backlog / not scheduled |
| 2026-06-26 | **Windows daemon: remaining hardening (`CTRL_BREAK`, macOS start-time, CI matrix)** | The automated Windows timeout backstop is **done 2026-07-14**: `stop_process` receives the existing subprocess-free `hard_kill_fn` seam and escalates to `taskkill /F /T` without importing subprocess logic into process control. Still deferred: (a) a **`CTRL_BREAK_EVENT` handler** so terminal Ctrl+C requests a graceful node-boundary stop; (b) **macOS `_read_proc_start_time`** via `ps -o lstart=` to tighten PID-recycling guards; (c) a **Windows CI runner matrix**. | `src/wastech_orchestrator/process_control.py` (`_can_signal`, `_stop_via_pid_file`, `_read_proc_start_time`); `cli.py` `cmd_watch`; `.github/workflows/ci.yml` | taskkill done; remaining items deferred |
| 2026-06-19 | **`fixing`-skipped → immediate `MANUAL` as a conditional flow edge** | With `fixing` skipped, the engine runs the bounded `test_fix` loop to its cap before `MANUAL` rather than going straight to `MANUAL` (terminal outcome preserved; re-tests N× pointlessly). Immediate-manual should be a conditional flow edge (`testing fail→manual` when `fixing` disabled), not engine domain knowledge. | `core/flow/engine.py`; the packaged `implementation` flow; p1-audit.md (Additional #7) | deferred / not scheduled |
| 2026-06-21 | **Codex read-only + network sandbox combo** | Codex enables sandbox network only for `workspace-write`, so a `read-only` research node that needs network (deep_research `external_research`) is not network-enabled at the Codex level. Revisit when the read-only + network sandbox combo is actually needed. | `providers/codex.py`; p3-research-audit.md | deferred |
| 2026-06-21 | **Signature/hash flow registry + `network_policy` per-host allowlist** | Deferred from P4 (security-ceiling §8): a signature/hash flow registry to verify operator-flow integrity, and a `network_policy` per-host allowlist (today a node's network grant is all-or-nothing, not per-host). The accepted P4 design decision: trust model is **file-based** — operator flows are trusted at `config.yaml` level (single owner, `.worc/flows/`); integrity is guaranteed by the ceiling + fatal validator, not provenance; a signature/hash registry for operator-flow integrity was explicitly deferred. `network_policy` uses binary grant levels (`off`/`advisories`/`research`); a per-host allowlist (so a node's network grant targets specific hosts rather than granting all-or-nothing) was deferred to a later pass. | `core/flow/{validator,registry}.py` | deferred |
| 2026-06-19 | **Full re-sync of functional-map `file:line` refs** | **Moot as of 2026-07-21** — the functional map (`docs/functional/`) was archived to `.archives/`, so the `file:line` re-sync below no longer applies; kept for historical context only. Deferred until P2 completion because the precise line numbers shift each slice. P2–P4 are now complete, so do the comprehensive pass: re-sync the `file:line` references in the functional map (B06/B07/system-flows) and the likec4 model against the current code. **Also covers the `Stage`-enum removal (2026-06-22):** B05/B16/B28/B06 and `flows/implementation.md` still describe `Stage`, `SKIPPABLE_STAGES`, `StageParams`/`stage_params`, `_build_stage_params`, `INVALID_STAGE_OVERRIDE`/`REVIEW_SKIP_NOT_ALLOWED`, and the `config.*_enabled` `when` guards — all deleted; re-derive them as `nodes`/`NodeOverride`/`node_overrides`/`disabled_nodes`, `validate_disabled_nodes`, `INVALID_NODE_OVERRIDE`, and node-id disable. **Also folds in the per-node `network_access` override (2026-06-22, done):** B30/B29 and B25's network note still describe network as a pure flow-wide switch (`network_access = doc.network_policy is not None`); re-derive them as `resolve_network_access(node.network_access, doc.network_policy)` — a per-node override on top of the flow-wide default. **Also folds in task dependencies (`depends_on`, 2026-06-22, done):** B16 must add the `depends_on` front-matter field + the `INVALID_DEPENDS_ON` reason (and drop the stale `stages`/`StageParams`/`SKIPPABLE_STAGES` text above); B01/B06 must describe `watch_once`'s eligibility skip/terminal-reject and `cmd_run`'s refusal (`Orchestrator.dependency_eligibility`/`reject_dependency`); B07/B08 the read-only `GitManager.pr_merge_state` probe + the armed→merged `backfill_merge_sha` (SQLite publish op only; ledger keeps `"armed"`). **Also folds in operator-authored decomposition (`subtasks:`, 2026-06-22, done):** B16 must add the `subtasks` front-matter field + the new reasons (`invalid_subtasks`, `invalid_subtask_path`, `subtask_file_missing`, `subtask_malformed`, `subtask_count_out_of_range`, `subtask_depends_forward`, `flow_cannot_decompose`); B06 must describe the two decomposition sources (agent-proposed vs operator-authored) collapsing into the same execution region — `Orchestrator._validate_operator_subtasks` (pre-slot reject) + `_persist_decomposition` materialized at preflight, `decide_operator_decomposition`, and `SubtaskSpec.spec_body` (verbatim spec). | `docs/likec4/` | moot (functional map archived 2026-07-21) |
| 2026-06-22 | **Flow-engine: `Stage`-enum-removal minor follow-ups** | After the `Stage`-enum identity removal, three minor leftovers remain: (a) `hitl.node_interaction_path` (the standalone `hitl` gate, `node-<id>.json`) is now near-redundant with the node-keyed `interaction_path` — could unify; (b) ~~the `Stage` enum still physically lives in `providers/base.py` though it is no longer part of the provider contract — could relocate to `config`/`core` for cohesion~~ — **subsumed by the full enum-deletion task above** (the enum is deleted outright, not relocated); (c) ~~the supervisor calls `create_attempt_dir` with `node_run_id=0` every turn, so a 2nd+ observe/finalize collides on `stages/supervisor/run-000000/` (FileExistsError, swallowed by the advisory guard)~~ — **done (2026-06-24, ion-list remediation):** each `observe` turn now uses the observed step's `node_run_id` (finalize keeps the `0` sentinel), so successive turns write distinct `run-NNNNNN/` dirs; a failed observation is recorded with `observation_failed=true` (distinct from an empty note). | `core/flow/{wiring,observability}.py`; `providers/base.py`; `core/supervisor.py`; 2026-06-21-audit-remediation.md | (a) minor / backlog; (b)(c) done |
| 2026-06-22 | **Deduplicate the `.worc` home literal (`_WORC_HOME`)** | Audit #23 (benign — values match): `core/orchestrator.py` `_WORC_HOME` re-declared `cli.WORC_HOME = ".worc"` to avoid a circular import. **Resolved 2026-07-22 by [WRI-004](agent-worc-read-isolation/wri-004-centralize-worc-home-seam.md):** the canonical names moved to the stdlib-only leaf `runtime_layout.py` (`CONTROL_HOME_DIRNAME`/`PRIVATE_HOME_DIRNAME`/`EXCHANGE_HOME_DIRNAME`); `cli.WORC_HOME`, `core/orchestrator._WORC_HOME`, `memory.paths.MemoryLayout.for_repo`, and `providers/artifacts.EXCHANGE_HOME` were all removed. Consumers reach the home through the injected `RuntimeLayout` (`lint-imports` confirms no new cycle). | `cli.py`; `core/orchestrator.py`; `runtime_layout.py`; 2026-06-21-audit-remediation.md (#23) | resolved 2026-07-22 (WRI-004) |
| 2026-06-23 | **Monorepo checks: deferred sub-items (exclude-globs, per-command timeout, external-CI delegation)** | Left out of the first checks-monorepo pass (intentional): (a) **exclude-globs** so e.g. editing `ios/docs/**` does not trigger the iOS build (today selection is include-only at set granularity); (b) **per-command timeout** (today the override granularity is the set via `command_sets.<name>.timeout_seconds`); (c) **external-CI / remote-runner build delegation** — the orchestrator is single-host (checks run as a local subprocess in the clone), so a polyglot monorepo needs every toolchain on the host (iOS⇒macOS+Xcode, Android⇒SDK/JDK, .NET⇒dotnet). `skip_if_unavailable` (Р4) is the host escape-hatch; true delegation of the build to CI is orthogonal. | checks-monorepo.md (§"Масштабирование"/"Оставлено на потом"); `checks/selection.py`; `check_runner.py` | deferred / not scheduled |
| 2026-06-23 | **`upgrade-flows`: re-sync the seeded `.worc/flows/` to the packaged version** | `install` now seeds editable copies of the built-in flows + their shared `roles/` prompts into `.worc/flows/` (`_copy_packaged_flows`), and — since the packaged-delivery-only change made `.worc/flows/` the **sole** resolution source (no runtime packaged fallback) — a package upgrade that ships a **new** built-in flow leaves it not merely un-refreshed but **unrunnable** until it is delivered there. This raises the priority of a proper re-sync. Today the only refresh path is `install --reconfigure`, which snapshots the dir to `flows.bak-<UTC>` (`_backup_flows_dir`) and then overwrites **all** packaged-named files, discarding operator edits to the built-ins. Add an `upgrade-flows` (or fold it into the umbrella `upgrade`/`doctor` in the row above) that does a smarter refresh: update only the built-in files the operator has not edited, report drift on the ones they have, and never touch custom operator flows. Mirror `upgrade-docs` (`resolve_config_path`, fail-closed, idempotent, `--dry-run`). **Acceptance case (from the P4 prompt-quality plan §0):** the T1 fix — packaged `review.md` had moved ahead of the seeded target copy (empty-`findings`-array contract) and had to be pulled by hand — is exactly the "packaged advanced → offer to update the unedited block" scenario `upgrade-flows` must reproduce automatically. | `cli.py` `cmd_install`/`_copy_packaged_flows`/`_backup_flows_dir`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-23 | **`install`-able task-authoring skills (`worc-task`, `worc-deco-task`)** | Shipped two copy-ready Claude Code skills under `skills/` that convert raw user tasks into the orchestrator task format (single task) and operator-authored decomposition (root + subtask specs). For now they are reference templates the user copies into `~/.claude/skills/` or a project `.claude/skills/` by hand. Next step: a dedicated CLI command (e.g. `worc install-skills [--global]`) that materializes them into the right skills location — mirror `cmd_install`/`upgrade-docs`, which already resolve install locations and copy packaged assets. Decide packaging (ship `skills/` inside `src/wastech_orchestrator/packaged/` like the other installed assets vs. copy from the repo). | `skills/`; `cli.py` `cmd_install`/`cmd_upgrade_docs` | candidate |
| 2026-06-26 | **🔴 td-be-003 run remediation (first Windows real run): HITL must RESUME the agent session, not recreate it (CRITICAL); empty `command_sets` ⇒ vacuous `testing`; cross-stack task in backend-only flow; review didn't block unmet criterion/destructive migration; no HITL log signal; Windows nuances** | From a post-mortem of `td-be-003-conform-m1-m2-contract-shapes` in `argudebate` (docs/analysis/td-be-003-conform-m1-m2-contract-shapes-run-analysis.md) — clean run (`done`, PR #5 later operator-merged, 0 fix cycles), but `done` over-certifies. **🔴 P0 CRITICAL — operator directive:** HITL **recreates** the agent session instead of resuming it — `planning` ran twice with **different** `result.session_id` (`c8162f4f92ff`→`c5d799a3aef0`), full re-plan, answer delivered as a context file not as conversation continuation. Requirement: regardless of node/timing, after the operator replies the agent MUST continue the SAME session from the point of the Telegram request. Lever: `core/flow/nodes/agent.py` — thread the first run's `session_id` into the 2nd `_invoke` at `:132` (and `_reconsider` `:373`), lifting the `_resume_session_id` "editing_lineage-only" gate (`:491-508`) for the HITL re-invoke so resume works for ANY `session_scope`/node; provider resume already exists (`providers/{claude,_adapter_base}.py`), keep the provider gate (no claude-session-on-codex). Test: 2nd run's `session_id` == 1st. **P0:** (1) target `checks.command_sets: {}` ⇒ `testing` vacuous pass (`check_runs=0`) — add a `dotnet build src/Chat1o1.sln` set (config `checks.command_sets`; `config/schema.py`); (2) cross-stack `/contract-change` task routed to backend-only `implementation_backend` flow whose role forbids `mobile/` ⇒ acceptance criterion #6 (atomic swagger+api-client) structurally unmet, diff backend-only — add/route a cross-stack `task_type`/flow or fix the contradictory criterion. **P1:** (3) `review` accepted with a known-unmet criterion + destructive migration (drops `messages.body` NOT NULL without backfill) as medium not blocking — `roles/implementation_backend/review.md` make those blocking; (4) no console/log line while blocked on HITL approval (2.5-min silent gap) — emit a log line/heartbeat on `human_input` wait (`core/orchestrator.py`/`core/hitl.py`; relates to 2026-06-24 row (c)). **P2:** avoidable HITL on well-specified tasks (planning approval); dangling `subtask  of  …` placeholders when not decomposed (`roles/implementation_backend/{implementation,fixing}.md`); enable `prompt_audit`. **Windows:** `worc` not on PATH (ran `python -m wastech_orchestrator`); Windows env allowlist worked (no `0xC0000409`); `state.db.source_path` stored backslash-style (verify `as_posix()` normalization in `source_path` comparisons); spurious empty dir listing on the `I:` drive. Analysis only — no code/config changed. | td-be-003-conform-m1-m2-contract-shapes-run-analysis.md; `core/flow/nodes/agent.py`; `providers/{claude,_adapter_base}.py`; `config/schema.py`; `core/flow/registry.py`; `roles/implementation_backend/{review,implementation,fixing}.md`; `core/{orchestrator,hitl}.py`; `security/env.py` | candidate (P0 CRITICAL: HITL session resume) |
| 2026-06-26 | **HITL-wait observability & not-decomposed prompt cleanup (td-be-003 findings 5+6)** | Dedicated task carved from the td-be-003 run. **P1:** a node blocked on `human_input` emits no console/log line or heartbeat — operator sees a silent gap (td-be-003: 2.5-min silence, request only in Telegram); emit a log line + heartbeat on the wait and on resolution (`core/hitl.py` / `core/orchestrator.py` ask path + observability). **P2:** when a task is not decomposed the implementation/fixing roles render a dangling "subtask of … spec:" sentence (empty placeholders); make the subtask clause conditional in `roles/implementation_backend/{implementation,fixing}.md` or omit it in the prompt assembler (`core/flow/nodes/agent.py` `_prompt_variables` / `core/flow/prompt.py`). Full design + acceptance in the detail file. | hitl-wait-observability-and-prompt-cleanup.md; td-be-003-conform-m1-m2-contract-shapes-run-analysis.md; `core/hitl.py`; `core/orchestrator.py`; `core/flow/nodes/agent.py`; `roles/implementation_backend/{implementation,fixing}.md` | open — P1/P2 |
| 2026-06-28 | **Richer per-task `cancelled` outcome** | **Boundary cancellation shipped 2026-07-14:** `is_cancelled` now reaches FlowEngine as well as Router; soft `down` finishes the active node, checkpoints the untouched next node, and parks resumably without fallback or failure. Still deferred is the richer product concept: a first-class `cancel` that unwinds the flow and records a terminal `cancelled` task status instead of the current resumable `RUNNING` park. Immediate interruption remains `--force-full`. | `core/flow/engine.py`; `core/orchestrator.py`; `core/state_machine.py`; `routing/router.py`; `cli.py` | boundary stop done; terminal cancelled status deferred |
| 2026-06-28 | **HITL / gate answer in the console** | `worc top`/`worc shell` only **surface** a pending durable gate (e.g. the max-turns `turn-gate-*.json` "awaiting operator"); Telegram stays the answer channel. Bridging the HITL/confirmation-gate prompts into the console prompt (answer, not just show) is a separate feature with its own routing/timeout semantics. From the cli-upgrade ADR. | `cli_shell.py`; `core/hitl.py`; `notify/telegram.py` | backlog / not scheduled |
| 2026-06-28 | **Textual TUI for the console** | The first-cut console is `prompt_toolkit` + `patch_stdout` (live log above a prompt). A full panel/pane/mouse/hotkey TUI (Textual) is the deferred richer option if operators want it. From the cli-upgrade ADR. | `cli_shell.py`; `pyproject.toml` (`[shell]` extra) | backlog / not scheduled |
| 2026-06-28 | **Daemon control socket / IPC** | The console drives the daemon by read-only `state.db` polling + log tailing + file enqueue + the stop ladder. A Unix-socket/IPC channel to push commands into a _running_ task (beyond enqueue + signals) is YAGNI for now; revisit if richer live control is needed. From the cli-upgrade ADR. | `cli_shell.py`; `process_control.py` | backlog / not scheduled |
| 2026-06-28 | **`worc top` raw single-key quit** | `top` quits on `q` **then Enter** (a blocking `readline` reader thread — platform-neutral, no `termios`/`msvcrt`). A raw single-key reader (bare `q`, arrow scrolling) is a deferred nicety. From the cli-upgrade ADR. | `cli.py` (`_stdin_quit_watcher`, `_run_top_loop`) | backlog / not scheduled |
| 2026-07-01 | **Memory V1 complete — post-V1 work (real eval baseline, gated V2/V3/V4, `worc memory add/edit`, (archived) functional-map block)** | The persistent repo-scoped memory subsystem is **fully implemented and merged to `main`** (phases 01–05 + audit remediation, PR #14 / commit `e7aa47a`): store + config, write funnel, read packets, curation, and the safety drills + offline-replay harness. Post-V1 items, none blocking: (a) **record a real eval baseline** — the harness (`tests/eval/harness.py`) and the baseline doc are in place but the numbers are **synthetic** (greenfield: no production task corpus yet); replay real tasks (memory-off vs on vs on-without-entity), record `TaskMetrics`, and **tune the provisional Q1 cleanup-budget / Q3 promotion / Q5 packet-cap integers** against the measured result (the approach is locked, only the integers move). (b) **V2 (SQLite + FTS) / V3 (embeddings) / V4 (entity graph)** stay **gated behind a measured recall/quality lift** (AC-O4 / `Comparison.measured_lift`) — do not ship any of them without the baseline showing the simpler layer measurably fails (blueprint §9/§10). (c) **`worc memory add/edit`** — a curated add/edit verb (V1.x); for now hand-editing the `*.jsonl`/`*.md` files is the authoring path alongside `show/validate/compact/restore`. (d) ~~**functional-map memory block**~~ — **moot as of 2026-07-21**: the functional map was archived to `.archives/`; the subsystem stays documented in `docs/backlog/memory/` + `configuration.md` + `operations.md`. Per-phase V1 scope cuts are in the phase 02/03/04 rows below. | memory/index.md; memory/plan/05-safety-and-evaluation; `tests/eval/`; `tests/memory/test_memory_*_drill.py` | candidate |
| 2026-07-01 | **Memory curation (phase 04) — V1 scope cuts** | The curation path shipped (`DerivedIndex` + `CleanupJob` + `worc memory` CLI + idle-gap hook). Intentional V1 simplifications to tighten later: (a) **staleness is path-only** — `CleanupJob` reconciles **both entities and long-term lessons** by **path** existence (a lesson scoped to a since-vanished path is quarantined, never deleted — F2 existence half resolved 2026-07-01, `_reconcile_lessons`). Still deferred to V2: staleness on a vanished **symbol** (`DerivedIndex.symbol_exists` is built but unwired — F6), and the design §5 **2× explicit contradiction** rule — there is no contradiction ledger in V1, so a lesson contradicted by current reality is not auto-quarantined and **lessons can still accrete contradiction-rot** until a human curates them. Revisit when the steady-state `watch`-a-queue scenario shows lessons drifting from reality (the ADR's headline failure mode). (b) **rename-remap is same-basename only** — a moved file is remapped when exactly one tracked path shares its basename; the design's **content-signature** remap is not built (ambiguous/multi-candidate moves quarantine, fail-closed). (c) **duplicate-merge is within a single tier file** by normalized subject (keeps the first, unions evidence/seen-tasks) — it does not merge across kinds or against quarantine, and the richer §5 merge (oldest-id/newest-wording reconciliation) lives only on the write path. (d) **`DerivedIndex` has no FTS/embeddings/repo-map** (V2+, NFR3) and rebuilds its tracked-set per pass (no incremental cache invalidation) — fine at current scale, revisit for very large monorepos. (e) **the idle hook rate-limits in-memory** (a per-daemon `last-run` monotonic clock), so a restarted daemon may run one extra pass sooner than `cleanup_min_interval_s`; a persisted last-run stamp would make it durable. (f) **`worc memory` has no `add`/`edit` verbs** (hand-editing the `*.jsonl`/`*.md` stays the authoring path; a curated add is a possible V1.x). All additive; none changes the AC-gated behavior (snapshot-before-batch, never-promote, never-edit-code, quarantine-never-delete, audited). | plan/04-curation; `memory/{derived,cleanup,service}.py`; `cli.py` `cmd_memory`/`_build_cleanup_hook`/`watch_loop` | candidate |
| 2026-06-30 | **Memory read path (phase 03) — V1 scope cuts** | The read path shipped (deterministic `PacketBuilder` → `{memory_path}` node-driven injection → packaged role-prompt references). Intentional V1 simplifications to tighten later: (a) **stage-2 semantic rerank is not built** — V1 ships the deterministic stage-1 filter only; the rerank is V3, gated by the offline replay baseline (design §6 / AC-O4). (b) **symbol-scoped retrieval is build-capable but unwired at runtime** — `PacketContext.touched_symbols` is honored by `build()` but the node runner only sources `touched_paths` (from `git changed_code_paths_since_base()`); a symbol source (the `DerivedIndex` symbol index) wires in with phase 04. (c) **planning ranks without touched paths** — before the first commit the base-inclusive diff is empty, so a planning packet ranks by trust/recency/node-scope only (no path overlap); acceptable (precision-first), revisit if planning recall suffers. (d) **the ~15-bullet cap is implicit** — only `packet_max_lines` is enforced as the backstop; one bullet per record + the record caps keep bullets bounded, so no separate bullet knob was added (YAGNI). (e) **per-node weighting is fixed, not config-driven** — `implementation` gets `+2` entity cards and `review`/`fixing` prefer reviewer-kind lessons via ranking; if operators need to tune these, lift them into `MemoryConfig`. (f) **packet caps are provisional** (Q5 / AC-R2 `[refine]`) — tune `packet_max_*` against the eval baseline. All additive; none changes the AC-gated behavior (path-only injection, determinism, caps, empty-state). | plan/03-read-path; `memory/packet.py`; `core/flow/nodes/{agent,base}.py`; `core/flow/wiring.py`; `core/orchestrator.py` `_packet_builder` | candidate |
| 2026-06-30 | **Memory write path (phase 02) — V1 scope cuts** | The write path shipped (candidate-delta parser → `apply_delta` funnel → tier persistence + quarantine → best-effort `evaluations` marker → supervisor emit on the finalize turn + deterministic failure seam). Intentional V1 simplifications to tighten later: (a) **entity-card path existence is now validated at write time** (F1, resolved 2026-07-01) — `apply_delta` resolves each entity card's `paths` against the live repo via an injected `DerivedIndex.path_exists`; an unverifiable path downgrades the card off `repo-observed` → quarantine (best-effort, fail-closed). Still deferred: **symbol** existence at write time (`DerivedIndex.symbol_exists` is built but unwired — F6), and lesson `scope.paths` existence, which is reconciled by `CleanupJob` (F2), not the write path. (b) **contradiction-detection + stable-hotspot promotion gates are V1-off** (`should_promote(has_contradiction=False, stable_hotspot=False)`) — no conflict-quarantine against contradicting active memory yet. (c) **entity merge is a wholesale upsert** by `entity_id` (latest card wins), not a field-union. (d) **`short_term/runs/<task-id>/episode.json`** per-task copy and the **`entities/index.md` / `aliases.json`** derived views are not written (canonical `recent.jsonl` / `entities.jsonl` suffice; aliases live in the card). (e) **episode enrichment** — the per-task episode stores id/task/trust/outcomes/artifact-dir only, not touched paths/symbols or review findings from the diff. (f) the orchestrator now feeds `MemoryService(extra_secrets=…)` the known secret literals (F3, resolved 2026-07-01) — `secret_env_values` (values of non-allowlisted, secret-named parent env vars) + `read_denied_secrets` (the repo's `.env` / `secrets/**`), the same sources the provider adapters scrub from artifacts — so a repo-specific secret value that matches no structural token shape is still redacted (closes the structural-only gap against C1). All additive; none changes the AC-gated behavior (redaction, trust gating, promotion thresholds, audit). | plan/02-write-path; `memory/{service,lifecycle,delta,audit}.py`; `core/{supervisor,orchestrator}.py` | candidate |
| 2026-07-04 | **Live-behavioral confirmation of Claude `read-only` = `default` mode + `Read,Glob,Grep` allowlist blocking Edit/Write** | Run-quality/gating hardening (F19/F20/F21/F22, ADR) implemented F21 by switching Claude's `read-only` profile from `--permission-mode plan` to `default` + an `allowedTools` gate lacking `Edit`/`Write`, so a clarifying question surfaces through the role's `human_input` structured field instead of the CLI's plan-mode UX (`AskUserQuestion`/`ExitPlanMode`). Verified so far: unit tests assert the built argv (`--permission-mode default`, no `Edit`/`Write` in `--allowedTools`) — the ADR's Open Q2. **Not yet verified**: that a real `claude` CLI process actually refuses a write under `default` mode with that allowlist (no live smoke test run, unlike F19's codex 0.139.0 smoke test). Next step: a one-off live smoke run (prompt the model to edit a file under the read-only profile) before leaning on this as a hard security boundary rather than a routing nicety. | run-quality-gating-hardening.md Open Q2; `src/wastech_orchestrator/providers/claude.py` `_PROFILE_MAP`; `tests/providers/test_claude_command.py` | candidate |
| 2026-07-23 | **WRI-007 terminal-exchange sealing — deliberate scope cuts** | The seal/restore/quarantine protocol shipped (`core/flow/exchange_seal.py`; versioned `seal-<NNNNNN>` snapshots; `state.db` v18 guard flags; hooked into `_go_terminal` + operator `finalize_task`). Intentional simplifications to tighten later: (a) **contaminated-tree quarantine is same-filesystem only** — the hostile tree is relocated by an atomic `Path.replace` (never walked/copied by following contents, since it may hold planted links); a genuinely cross-volume `.worc-io` vs `.worc` layout raises `ExchangeCleanupBlocked` → the task is marked `exchange_active_unsafe` (tree left in place, later launches blocked, operator resolves) rather than performing a link-safe cross-device relocation. Revisit only if a deployment actually splits the two roots across filesystems. (b) **the seal/quarantine roots are covered by the transitive `private_home` provider-read deny, not named explicitly in `InternalDenyPolicy`** (unlike the WRI-010/011 frozen bundles) — add `frozen_exchange_seal` there if the WRI-002/003 by-name projection is ever tightened to require every private root be denied by name. (c) **`ensure_current_exchange` tolerates a continue with no active exchange and no seal** (a task that never published) by proceeding with an empty exchange and letting `_restore_engine_inputs` rebuild inputs from the private audit — the pre-launch gate still blocks a stale/foreign tree. | agent-worc-read-isolation/wri-007-seal-terminal-exchange.md; `src/wastech_orchestrator/core/flow/exchange_seal.py`; `runtime_layout.py` (`InternalDenyPolicy`) | deferred |
