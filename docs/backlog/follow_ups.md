# Implementation follow-ups

Append-only log of **implementation** next-steps and tech-debt discovered while building — distinct
from [product_backlog.md](product_backlog.md), which tracks *product features*. Use this for "we
deferred X; here's the hook and the context" items. Record new ones via `/sync-docs` as you create
them (date them absolute, link where they're referenced, mark status). Move an item to `done` (don't
delete) when addressed.

| Date | Item | Context & next step | Referenced in | Status |
|---|---|---|---|---|
| 2026-06-12 | **Wire Telegram HITL into stages** | The Telegram transport now provides `ask_human` for free-form questions and yes/no approvals, but agent stage outputs do not yet request it. Add a typed question/approval signal to refinement/planning and dangerous-action guardrails, then resume the same stage with the answer without changing the single-active-task state machine. | `notify/interface.py`; `notify/telegram.py`; architecture §4.7; [operations.md](../operations.md) | backlog / not scheduled |
| 2026-06-12 | **Schema migration runner** | Config `schema_version` and `state.db` `user_version` are stamped and gated (refuse-newer; adopt legacy `0`), but there is **no migration runner** for the `< current` case. When a backward-incompatible schema change first lands, add a versioned migration step (config and/or DB) and an `upgrade`/`doctor` command that backs up and migrates in place. The version gates are the hooks. | spec §22 "Versioning and compatibility"; [CHANGELOG.md](../../CHANGELOG.md); `config/loader.py` `_check_schema_version`; `state_store.py` `_enforce_schema_version` | backlog / not scheduled |
| 2026-06-12 | **Broken canonical-spec links** | The canonical spec was moved from `docs/orchestrator_final_plan.md` to `docs/implementation_stages/00_orchestrator_final_plan.md` (uncommitted docs reorg), but [CLAUDE.md](../../CLAUDE.md), [AGENTS.md](../../AGENTS.md), [README.md](../../README.md) and several docs still link the old path. Decide the final location, then fix the links repo-wide (or restore the old path). | CLAUDE.md, AGENTS.md, README.md, docs/** | candidate |
| 2026-06-12 | **Git-native pre-commit hook** | The Stop docs-sync gate (`.claude/hooks/docs_sync_gate.py`) only covers turns inside Claude Code. For commits made outside the agent, add a real `pre-commit` hook (tracked script + `core.hooksPath`) that enforces the same "code changed ⇒ docs/CHANGELOG changed" rule. | `.claude/hooks/docs_sync_gate.py`; [docs/rules/git-workflow.md](../rules/git-workflow.md) | candidate |
| 2026-06-12 | **Publish to (Test)PyPI** | Distribution is currently `pipx`/`pip` from a Git tag. To allow `pipx install --pre wastech-orchestrator`, add a publish step to the release workflow using `pypa/gh-action-pypi-publish` with **trusted publishing (OIDC, no tokens)** — TestPyPI for pre-releases first. | [.github/workflows/release.yml](../../.github/workflows/release.yml); [docs/RELEASING.md](../RELEASING.md) | candidate |
| 2026-06-12 | **CI test gate on PRs/pushes** | ~~Only a *release* workflow exists (runs on `v*` tags).~~ **Resolved (2026-06-13)**: added [.github/workflows/ci.yml](../../.github/workflows/ci.yml) running `ruff check` + `ruff format --check` + `mypy src` + `pytest` on pull requests and pushes to `main` (matrix: Python 3.12 / 3.13), mirroring the release gate. | [.github/workflows/ci.yml](../../.github/workflows/ci.yml) | done |
| 2026-06-12 | **Terminal artifacts trail by one audit commit** | ~~Move + final artifacts landed in the *next* task's commit.~~ **Resolved**: finalize now moves the task to `done/`/`failed/` and writes `<id>.summary.md` **before** the commit, so the task + summary ride the task's own `tasks/` commit (§8.3). `logs/` (incl. `terminal-cleanup.json`, `summary.json`) is intentionally never committed. | spec §8.3, §21.3; `core/orchestrator.py` `_finalize_task_artifacts` | done |
| 2026-06-13 | **Session persistence across restarts** | Claude session IDs (`--resume`) are captured and threaded across stages within one run, but they live only in `_Pipeline.session_ids` (in memory). On orchestrator restart every stage begins a fresh session. To survive restarts: persist the active session ID per provider in SQLite `stage_runs` or a sidecar and reload it on recovery. Also consider a configurable opt-out (some operators may prefer fresh sessions per stage). | `core/orchestrator.py` `_Pipeline`; `providers/claude.py` `build_claude_argv`; spec §6 | backlog / not scheduled |
| 2026-06-12 | **`.gitignore` for in-repo runtime files** | Under the in-repo footprint the orchestrator excludes its root runtime files (`state.db`, `config.yaml`) from **its own** scoped commits, but a human running `git add .` in the repo would still stage them. Consider having `init`/`install` append these to the repo's `.gitignore` (or `.git/info/exclude`) for in-repo modes. | `cli.py` `_install_create_dirs`/`cmd_init`; `git_manager.py` `_EXCLUDED_FILES` | candidate |
| 2026-06-13 | **Generalize discovery model/reasoning into per-stage config** | Agent check discovery uses a deliberate one-off `checks.discovery.{model,reasoning}` knob. When per-stage model/reasoning lands, subsume the discovery knob under it. | [per_stage_model_reasoning.md](per_stage_model_reasoning.md); `checks/agent.py`; `checks/discovery_factory.py` | candidate |
| 2026-06-13 | **Consolidate `install/detect.py` onto `checks/detect.py`** | The installer wizard still uses the older first-match `install/detect.detect_checks` for its quick ecosystem hint, while the richer detector lives in `checks/detect.py` (`CheckCandidateDetector`). Consolidate to one source of truth to avoid drift. | `install/detect.py`; `checks/detect.py`; `install/wizard.py` `_resolve_checks` | candidate |
| 2026-06-13 | **Agent check discovery at `preflight`; deeper CI parsing** | Agent fallback runs only at `install`; `preflight` resolves deterministically (no provider spend). Consider an explicit `preflight --discover` flag. Also: discovery currently treats CI workflow files as presence-only evidence — parse GitHub Actions/others to derive the real PR gate, and settle the §17 open decisions (confidence threshold, wrapper-vs-native precedence). | [automatic_check_discovery.md §6, §17](automatic_check_discovery.md); `cli.py` `_install_resolve_checks`; `checks/inspect.py` `_ci_workflows` | candidate |
