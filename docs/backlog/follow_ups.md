# Implementation follow-ups

Append-only log of **implementation** next-steps and tech-debt discovered while building — distinct from [product_backlog.md](product_backlog.md), which tracks _product features_. Use this for "we deferred X; here's the hook and the context" items. Record new ones via `/sync-docs` as you create them (date them absolute, link where they're referenced, mark status). Move an item to `done` (don't delete) when addressed.

| Date | Item | Context & next step | Referenced in | Status |
| --- | --- | --- | --- | --- |
| 2026-06-12 | **Wire Telegram HITL into stages** | **Resolved (2026-06-13):** `refinement`/`planning` now emit strict typed human-input signals; the Core persists a durable handle/answer artifact and repeats the same stage through `human_input_path`. Deletion/dependency diffs are approved before tests, with exact planning reuse and one denial reconsideration. Timeout/transport/ambiguity/repeated signals fail closed without adding a task status. | [telegram.md](../telegram.md); `core/hitl.py`; `core/dangerous_diff.py` | done |
| 2026-06-12 | **Schema migration runner** | Config `schema_version` and `state.db` `user_version` are stamped and gated (refuse-newer; adopt legacy `0`). **Mostly addressed (2026-06-13/14):** (a) the first in-place DB migration — `state_store._migrate` runs idempotent `ALTER TABLE ADD COLUMN` for `state.db` v1→v2 on the writable open path; (b) the **config** migration — the `upgrade-config` command (`cli.cmd_upgrade_config` + `config/upgrade.py`) adds new keys, preserves operator values, stamps `schema_version`, and backs up before writing. Remaining: a single umbrella `doctor`/`upgrade` command that migrates **both** at once; value _transforms_ (today's config merge is add-only, not rename/restructure); and migrating a stale **read-only** DB open (the orchestrator always opens writable first, so `status` sees the migrated DB in practice). | spec §22 "Versioning and compatibility"; `cli.py` `cmd_upgrade_config`; `config/upgrade.py`; `state_store.py` `_migrate`/`_enforce_schema_version` | candidate |
| 2026-06-12 | **Broken canonical-spec links** | **Resolved (2026-06-13):** the canonical location was `docs/implementation_stages/00_orchestrator_final_plan.md`; root instructions, README, rules, backlog documents, and internal spec links targeted that path. **Superseded (2026-06-16):** the `implementation_stages/` docs were removed and all links repointed to the [Functional Map](../functional/index.md) and current docs — see the 2026-06-16 row below. | CLAUDE.md, AGENTS.md, README.md, docs/\*\* | done |
| 2026-06-12 | **Git-native pre-commit hook** | The Stop docs-sync gate (`.claude/hooks/docs_sync_gate.py`) only covers turns inside Claude Code. For commits made outside the agent, add a real `pre-commit` hook (tracked script + `core.hooksPath`) that enforces the same "code changed ⇒ docs changed" rule. | `.claude/hooks/docs_sync_gate.py`; [.agents/rules/git-workflow.md](../../.agents/rules/git-workflow.md) | candidate |
| 2026-06-12 | **Publish to (Test)PyPI** | Distribution is currently `pipx`/`pip` from a Git tag. To allow `pipx install --pre wastech-orchestrator`, add a publish step to the release workflow using `pypa/gh-action-pypi-publish` with **trusted publishing (OIDC, no tokens)** — TestPyPI for pre-releases first. | [.github/workflows/release.yml](../../.github/workflows/release.yml) | candidate |
| 2026-06-12 | **CI test gate on PRs/pushes** | ~~Only a _release_ workflow exists (runs on `v*` tags).~~ **Resolved (2026-06-13)**: added [.github/workflows/ci.yml](../../.github/workflows/ci.yml) running `ruff check` + `ruff format --check` + `mypy src` + `pytest` on pull requests and pushes to `main` (matrix: Python 3.12 / 3.13), mirroring the release gate. | [.github/workflows/ci.yml](../../.github/workflows/ci.yml) | done |
| 2026-06-12 | **Terminal artifacts trail by one audit commit** | ~~Move + final artifacts landed in the _next_ task's commit.~~ **Resolved**: finalize now moves the task to `done/`/`failed/` and writes `<id>.summary.md` **before** the commit, so the task + summary ride the task's own `tasks/` commit (§8.3). `logs/` (incl. `terminal-cleanup.json`, `summary.json`) is intentionally never committed. | spec §8.3, §21.3; `core/orchestrator.py` `_finalize_task_artifacts` | done |
| 2026-06-13 | **Session persistence across restarts** | **Design accepted and split (2026-06-14):** persist a per-unit `implementation`/`fixing` editing lineage rather than the provider-wide `_Pipeline.session_ids` map; add Claude/Codex resume parity, provider-aware fallback requests, atomic persistence/recovery, raw-ID redaction, and explicit affinity availability policy. Schema migration uses the next available version (v3→v4 only if implemented first). | [durable_sessions_and_fixing_affinity.md](durable_sessions_and_fixing_affinity.md); `core/orchestrator.py`; `routing/router.py`; `providers/{claude,codex}.py`; `state_store.py`; spec §6 | accepted |
| 2026-06-12 | **`.gitignore` for in-repo runtime files** | **Resolved (2026-06-13):** `init`/`install` (in-repo footprint) append the runtime files (`state.db`, `state.db-wal`, `state.db-shm`, `config.yaml`, `config.yaml.bak-*`, `orchestrator.pid`) to `.git/info/exclude` by default, or `.gitignore` with `--gitignore-tracked`, via `git_manager.append_runtime_excludes`. **Extended (2026-06-14, post-test-run §3.1):** added the generated `checks/` profile dir (it had leaked into a real run's commit) to `EXCLUDED_DIRS`, `_local_only_dirs` (every footprint), and `RUNTIME_GITIGNORE_LINES`; the Git Manager now guarantees these ignores in the clone it runs in (`ensure_runtime_excludes`, called at branch-prep). The local-`.git/info/exclude` default was kept deliberately (no forced tracked-`.gitignore` mutation of the operator's repo). | `cli.py` `cmd_init`/`cmd_install`; `git_manager.py` `append_runtime_excludes`, `ensure_runtime_excludes`, `_EXCLUDED_FILES`, `EXCLUDED_DIRS` | done |
| 2026-06-13 | **`gh auth status` warning at startup** | The `gh` pre-flight gate (`detect.require_gh`) only checks `gh` is on `PATH`, not that it is authenticated. A logged-out `gh` still fails at `gh pr create`. Consider a non-blocking warning at `run`/`watch` startup (or in `preflight`) that runs `gh auth status` through the safe process runner and surfaces "gh present but not logged in". | `install/detect.py` `require_gh`; `cli.py` `cmd_watch`/`cmd_run`; `git_manager.py` `_gh` | candidate |
| 2026-06-13 | **PID-recycle robustness for `stop`/`restart`** | The `watch` daemon's PID file (`process_control`) is a bare PID; between the daemon's death and a `stop`/`watch` read, the OS could recycle that PID for an unrelated process, so `is_running` cannot prove the PID is _ours_. Acceptable for a single-operator local daemon. To harden: also record a creation marker (process start time / boot id) and compare it in `is_running`. | `process_control.py` `is_running`/`stop_process`; `cli.py` `cmd_watch` refuse-to-start | backlog / not scheduled |
| 2026-06-13 | **Verify `worc` is free on PyPI before release** | The `worc` console-script alias is fine for local `pip install -e .`, but before publishing to (Test)PyPI confirm `worc` does not collide with an existing distribution or a common third-party binary; fall back to `worch` or make the alias opt-in if it does. | `pyproject.toml` `[project.scripts]`; the "Publish to (Test)PyPI" follow-up above | candidate |
| 2026-06-13 | **Generalize discovery model/reasoning into per-stage config** | Agent check discovery uses a deliberate one-off `checks.discovery.{model,reasoning}` knob. Per-_task_ per-stage overrides now exist (`stages:` front matter, 2026-06-13); subsuming the discovery knob waits on the config-level `stage_defaults` part below (a `config.yaml` concern, not a task one). | `checks/agent.py`; `checks/discovery_factory.py` | candidate |
| 2026-06-13 | **Config-level per-stage model/reasoning defaults (`stage_defaults`)** | Per-task `stages:` overrides shipped (per_stage_model_reasoning §3). The complementary §4 lets operators set per-stage defaults under each provider in `config.yaml` (`agents.providers.<p>.stage_defaults.<stage>.{model,reasoning}`), inserting a level between task-wide and provider-default in the resolution chain. Touches the config schema (+`schema_version` bump), loader, validation, and `NormalizedTask.model_for`/`reasoning_for` (or the provider resolution). | `config/{schema,loader,validation}.py`; `task/model.py` `model_for`/`reasoning_for` | backlog / not scheduled |
| 2026-06-13 | **Consolidate `install/detect.py` onto `checks/detect.py`** | The installer wizard still uses the older first-match `install/detect.detect_checks` for its quick ecosystem hint, while the richer detector lives in `checks/detect.py` (`CheckCandidateDetector`). Consolidate to one source of truth to avoid drift. | `install/detect.py`; `checks/detect.py`; `install/wizard.py` `_resolve_checks` | candidate |
| 2026-06-13 | **Per-task opt-out of a global stage skip (`force_stages`)** | Stage-skip resolution is a strict **union** (`effective_skip = agents.skip_stages ∪ task disabled`): a task cannot re-enable a stage the global config skipped (deliberate — avoids a task silently re-running a stage the operator disabled system-wide). If per-task opt-out is needed, add a `force`/`enabled: true`-wins mechanism and define the precedence; today task `enabled: true` is a no-op against a global skip. | `core/orchestrator.py` `effective_skip`; `task/model.py` `disabled_stages` | backlog / not scheduled |
| 2026-06-13 | **Agent check discovery at `preflight`; deeper CI parsing** | ~~Agent fallback runs only at `install`; `preflight` resolves deterministically.~~ **Resolved (2026-06-14, post-test-run §1.2):** discovery now runs inside the state machine at task start (`checks.discovery.run_at_task_start`), so `auto` mode resolves/agent-assists in-band (gated on mode + `agent_fallback` + a configured `model`); re-resolution is proof-only (launch failure / fingerprint / low confidence) and a changed command set is human-approved. Still open: deeper CI parsing — discovery treats CI workflow files as presence-only evidence; parse GitHub Actions/others to derive the real PR gate, and settle the §17 open decisions (confidence threshold, wrapper-vs-native precedence). | [configuration.md](../configuration.md) (`checks.discovery`); `checks/resolver.py`; `core/orchestrator.py` `_check_preflight`; `checks/inspect.py` `_ci_workflows` | partially done (CI parsing: candidate) |
| 2026-06-13 | **Capture real merge SHA in `--auto` auto-merge mode** | With `git.auto_merge_wait_for_checks: true` the orchestrator arms GitHub-native auto-merge (`gh pr merge --auto`) and returns immediately, so the merge happens asynchronously and the ledger records `merge_outcome: "armed"` rather than the real merge SHA. To capture it, a later step (e.g. a `watch`-tick reconciler or a `gh pr view --json mergeCommit` poll) could backfill the SHA into the `pr_merge` publish op + ledger once GitHub completes the merge. | `git_manager.py` `merge_pr`/`_merge_commit_sha`; `ledger.py` `LedgerRecord` | candidate |
| 2026-06-13 | **Defense-in-depth: forbid `--admin` in `find_forbidden_args`** | `merge_pr` builds a fixed argv that never includes `--admin` (so branch protection is respected), and a unit test asserts its absence. As extra hardening, add `--admin` (and other protection-bypassing `gh` flags) to `security/forbidden_args.py` so the prohibition is centralized and also covers any future `gh` call path / `extra_args`. | `security/forbidden_args.py`; `git_manager.py` `merge_pr` | candidate |
| 2026-06-13 | **Prompt customization v1 shipped; deferred extras** | **Resolved (2026-06-13):** the `prompts:` block (append/replace, strict/fallback, allowlisted path variables, redacted `rendered-prompt.md` audit artifact) is implemented; packaged `templates/prompts/<stage>.md` are now the single source of truth and `_STAGE_PROMPTS` was removed. **Deferred** (the backlog doc's §10 open questions): a global shared preamble across all stages, per-stage `mode`, and managing project agent stubs (`AGENTS.md`/`CLAUDE.md`/skills) via a separate `agent_instructions:` feature. **Update (2026-06-14):** the _skill-reference_ half of that idea shipped — planning now selects target-repo skills and surfaces them to downstream stages as read-only paths (`{skills_path}`, `skills:` config block, `core/skills.py`; post-test-run §2.1/§2.2). Still deferred: _authoring/managing_ the stubs themselves, the shared preamble, and per-stage `mode`. | `core/prompts.py`; `core/skills.py`; `config/{schema,loader,validation}.py`; `core/orchestrator.py` | done (extras: backlog / not scheduled) |
| 2026-06-14 | **Single umbrella `upgrade` command (config + docs)** | Post-upgrade currently needs two steps — `upgrade-config` (materialize new config keys) and the new `upgrade-docs` (refresh the installed `worc/` agent docs to the packaged version). Both resolve the install location the same way (`resolve_config_path`) and are fail-closed/idempotent/`--dry-run`. Fold them into one umbrella `upgrade` (or `doctor`) that runs both — overlaps with the "Schema migration runner" row's deferred config+`state.db` umbrella above; do them together. **Update (2026-06-14):** `install-templates` (row below) is now a **third** post-upgrade refresh step, so the umbrella should run all three (`upgrade-config` + `upgrade-docs` + `install-templates`); decide whether it runs `install-templates` add-missing by default and how `--force` threads through. | `cli.py` `cmd_upgrade_config`/`cmd_upgrade_docs`/`cmd_install_templates`; [docs/operations.md](../operations.md#upgrading-the-orchestrator); the "Schema migration runner" row above | candidate |
| 2026-06-14 | **Known gap: planning is not shown the skill inventory** | Planning-selected skill references shipped (post-test-run §2.1), but the _planning agent is not given the scanned inventory_ (`name`+`description` of available skills). The `{skills_path}` variable is empty during planning (it is filled with the agent's chosen skills only _after_ planning), so the planner must read `.claude/skills/` itself or guess names — unknown guesses are dropped as `dropped_unknown`. The selection/validation/downstream-reference/dedup machinery all work; only the "surface the inventory to planning so the choice is informed" half of §2.1 is missing. Next step: add a read-only `{skills_inventory_path}` (an allowlisted prompt variable) pointing at a generated, redacted name+description listing, injected into the `planning` template. | `core/skills.py` `SkillInventoryScanner`; `core/orchestrator.py` `_resolve_and_render_skills`/`_prompt_variables`; `core/prompts.py` `ALLOWED_PROMPT_VARS`; `templates/prompts/planning.md` | known gap / candidate |
| 2026-06-14 | **Provider-specific skills + per-stage routing (Codex skills, not only Claude)** | The skill-reference mechanism (§2.1) currently scans one tree (`<repo>/.claude/skills`, Claude's convention) and surfaces the same chosen skills to every downstream stage regardless of which provider actually runs that stage. Codex has its own skill/instruction convention. Next step: (a) support a Codex skill source alongside the Claude one (config: a second `scan_root`, or a per-provider mapping); (b) route per stage — a stage executed by **Codex** gets the **Codex** skills, a stage executed by **Claude** gets the **Claude** skills — using the resolved route (`AgentRouter` primary/fallback) so the reference paths match the provider that will read them; (c) keep it provider-neutral (paths only, never a provider-specific tool) and keep planning's selection deterministic. Note fallback: when a stage falls back to the other provider mid-run, the skill set should follow the provider that actually ran. | `core/skills.py`; `core/orchestrator.py` `_run_stage`/`_prompt_variables`/`selected_skills`; `routing/router.py`; `config/schema.py` `SkillsConfig` | backlog / not scheduled |
| 2026-06-14 | **Re-run a terminal task (`rerun` command)** | ~~No first-class way to re-run a terminal task.~~ **Resolved (2026-06-14):** `worc rerun <id>` ships with two modes — **fresh** (reset branch to base, clear per-attempt state incl. `publish_operations`/`subtasks`, archive prior artifacts to `attempt-<N>/`, drive from the start) and **`--continue`** (reuse the branch, re-enter at the failed stage via the resume engine; `state.db` v3 `tasks.interrupted_status`). Admits the id once via `is_recovery_rerun`; refuses on a live daemon / busy slot / dirty tree / (fresh) prior remote branch+PR; ledger `attempt`/`rerun_of`. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task`/`continue_task`; `state_store.py` reset/revive + `interrupted_status`; `git_manager.py` `reset_branch_to_base` | done |
| 2026-06-14 | **`rerun` deferred extras: `--allow-done` + versioned attempt branches** | `rerun` accepts only `failed`/`manual_action_required`. Deferred: (a) an opt-in `--allow-done` to re-do an already-merged task (a foot-gun — needs a stronger confirm + the remote/PR gate), and (b) a versioned-branch option (`agent/<id>-<slug>-attempt-N`) as an alternative to the same-name reset, which would change the branch/PR naming contract and downstream assumptions. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task` | backlog / not scheduled |
| 2026-06-14 | **Separate `install-templates` command (deliver/refresh the `templates/` tree add-missing-only)** | ~~Only `init` copies the packaged `templates/` tree; `install` never does, so it drifts.~~ **Resolved (2026-06-14):** `worc install-templates` ships — resolves the install dir like `upgrade-docs` (`--config` → `./config.yaml` → registry), delivers the tree beside `config.yaml` **add-missing-only** (absent → write; present → skip to preserve edits; `--force` overwrites; `--dry-run` previews), **never removes** operator-added files (the deliberate asymmetry with `upgrade-docs`), and never touches `config.yaml`/`prompts.overrides`. `init` + `install-templates` share `cli._copy_templates_tree` (parity-tested). **Update (2026-06-14):** config-relative `templates_dir` resolution and the `--only prompts\|skills` selector are **resolved/moot** via the prompt-templates simplification (prompts-only tree; templates_dir anchored to `config.yaml`) — see the row below. Still deferred (`candidate`): umbrella `upgrade` membership. | `cli.py` `cmd_install_templates`/`_copy_templates_tree`/`cmd_init`; `tests/test_cli_install_templates.py` | done |
| 2026-06-14 | **Manually finalize a task (`finalize` command)** | ~~No operator-facing way to reconcile a task the human handled out-of-band.~~ **Resolved (2026-06-14):** `worc finalize <id> --as <done\|failed\|abandoned>` records + tidies (status, terminal cleanup, file move, HITL close, a `manual` ledger record) **without running the pipeline or committing/pushing/PR-ing**. PR-URL provenance (`--pr-url` > recorded publish-op > none+warn); default read-only `gh pr view` merge check; fail-closed on a dirty tree; refuses while the daemon is live or on a re-finalize; `abandoned` = `manual_action_required` + `outcome: abandoned`. | `cli.py` `cmd_finalize`; `core/orchestrator.py` `plan_finalize`/`finalize_task`/`_relocate_task_file`; `git_manager.py` `verify_pr_state`/`delete_branch`; `hitl.py` `consume_pending_interactions`; `ledger.py` `LedgerRecord` | done |
| 2026-06-14 | **Simplify prompt templates: deliver prompts-only + auto-detect resolution (drop `overrides`)** | ~~Decided 2026-06-14 (operator).~~ **Resolved (2026-06-14):** shipped. **(A)** `init`/`install-templates` now deliver **only** `templates/prompts/` — `skills/`, `AGENTS.md`, `CLAUDE.md`, `task.md` were deleted from the packaged source. **(B)** A `<stage>.md` present in `templates_dir` is auto-detected by file presence (no `overrides` map); packaged defaults are a per-stage **fallback**; `mode` defaults to **`replace`**; **`prompts.overrides` and `prompts.strict` are removed** (legacy keys tolerated fail-open with a warning, stripped by `upgrade-config`); a relative `templates_dir` resolves from the `config.yaml` directory; `""` forces packaged defaults; `schema_version` bumped **5 → 6**. **(C)** `worc/examples/task-rich.md` rewritten as a maximal example (every front-matter field); `decision-guide.md`/`best-practices.md` gained `pr_title` coverage. Subsumes the deferred "config-relative `templates_dir`" and resolves the install-templates design doc "Scope of the tree" (prompts-only; the `--only prompts\|skills` selector is moot). | `core/prompts.py`; `config/{schema,loader,validation,upgrade}.py`; `cli.py` `cmd_upgrade_config`; `src/wastech_orchestrator/templates/`; `src/wastech_orchestrator/worc/`; tests under `tests/{core,config}/` + `tests/test_cli_install_templates.py` | done |
| 2026-06-14 | **Known gap (deep-research): Codex session continuity** | **Research resolved (2026-06-14):** current Codex CLI supports `codex exec resume <SESSION_ID>` and JSONL exposes `thread.started.thread_id`. The remaining adapter/persistence/redaction implementation is tracked by the durable-sessions backlog item. | [durable_sessions_and_fixing_affinity.md](durable_sessions_and_fixing_affinity.md); [official Codex CLI reference](https://developers.openai.com/codex/cli/reference#codex-exec); `providers/codex.py`; spec §6 | done |
| 2026-06-14 | **Agent quality + continuity design split** | **Resolved (2026-06-14):** the combined doc 20 design record was split into three independently implementable backlog contracts with a shared ownership/dependency map. Low-level invocation/audit plumbing may be reused; supervisor and testing remain separate domains; sessions/affinity owns editing lineage. | [backlog program context](README.md#agent-quality-and-continuity-program); [durable sessions](durable_sessions_and_fixing_affinity.md); [hybrid testing](hybrid_agent_testing.md); [supervisor](supervisor_quality_gate.md) | done |
| 2026-06-14 | **Workflow execution foundation** | Add a narrow prerequisite for all four planned changes: transitional built-in `implementation-v1` selection, immutable resolved-profile recovery identity, execution role/session/output vocabulary, and shared audit/path/delta primitives. Preserve current behavior in the foundation release, then support a one-way cutover to required-supervisor `implementation-v2` without permanent dual-profile execution. | [workflow_execution_foundation.md](workflow_execution_foundation.md); [task workflow profiles](task_workflow_profiles.md); [durable sessions](durable_sessions_and_fixing_affinity.md); [hybrid testing](hybrid_agent_testing.md); [supervisor](supervisor_quality_gate.md) | accepted |
| 2026-06-14 | **Hybrid agent testing** | Add an optional once-per-unit workspace-writing testing agent before deterministic checks. Persist an idempotent checkpoint, validate only its test-owned diff delta, keep the approved resolved profile immutable, and leave `CheckRunner` exit codes authoritative. | [hybrid_agent_testing.md](hybrid_agent_testing.md); `core/orchestrator.py`; `check_runner.py`; `checks/agent.py`; `state_store.py`; `templates/prompts/testing.md` | accepted |
| 2026-06-14 | **Supervisor quality-gate** | Add a read-only, fresh-session evaluator with bounded structured findings. Persist verdict/application separately, count every applied rework in local and global budgets, and leave all transitions in the Core. **Decisions 2026-06-15:** supervisor is mandatory with no `enabled` field; required implementation/fixing verdicts fail closed; rollout is a one-way cutover to sole executable `implementation-v2`; when summary output is enabled, its non-blocking final synthesis pass replaces the summary provider call; when summary is disabled, no handoff call, summary files/fallback, task sidecar, or PR summary body is created. | [supervisor_quality_gate.md](supervisor_quality_gate.md); `core/supervisor.py` (new); `core/loop_control.py`; `core/state_machine.py`; `state_store.py`; `templates/prompts/supervisor{,-final}.md` | accepted |
| 2026-06-16 | **`implementation_stages/` docs removed; relink references** | The numbered `docs/implementation_stages/` design docs (incl. `00_orchestrator_final_plan.md`) and `docs/codex_git_orchestrator_architecture.md` were deleted; every remaining link in root instructions, README, `docs/rules/`, the top-level docs, the backlog, and the sync-docs skill was repointed to the [Functional Map](../functional/index.md), [worc_architecture.md](../worc_architecture.md), [configuration.md](../configuration.md), or [telegram.md](../telegram.md) as appropriate. Supersedes the 2026-06-12 'Broken canonical-spec links' row. | CLAUDE.md, AGENTS.md, README.md, docs/ (rules, backlog, top-level), .claude/skills/sync-docs/SKILL.md | done |
| 2026-06-16 | **Унификация исполнения в flow-движок** | Выкристаллизованные требования: убрать хардкодную state-машину (`core/state_machine.py`), заменить универсальным flow-движком с декларативным графом из конфига (YAML+MD), переходы остаются за Core и валидируются заранее; виды узлов `agent`/`checks`/`evaluator`/`hitl`/`publish`; supervisor = bounded routing в объявленных точках ветвления; resume обязателен в v1 (универсальный персистентный чекпоинт). Первый и доказательный пример — перенос текущего `implementation` 1:1 (existing-тесты как golden-harness). Переосмысливает foundation/profiles-программу в сторону полной унификации. **Investigate crewAI выполнен (2026-06-16):** §12 фиксирует, что берём (engine-owned dispatch, router-`emit` = объявленные рёбра, сериализуемый `FlowDefinition`-контракт, YAML-модель агентов/задач, `FlowPersistence`-схема), чем вдохновляемся (pending/persist/resume HITL, checkpoint re-entry, supervisor-роль) и чего НЕ копируем (поверхностная валидация, state-only resume без идемпотентности, неограниченный иерархический LLM-роутинг, отсутствие Git-ownership/security-потолка). Следующий шаг: детальный дизайн. | [refactoring-flow/index.md](refactoring-flow/index.md); [workflow_execution_foundation.md](workflow_execution_foundation.md); [task_workflow_profiles.md](task_workflow_profiles.md); [supervisor_quality_gate.md](supervisor_quality_gate.md); `core/state_machine.py`; `core/orchestrator.py` | backlog / не запланировано |
| 2026-06-16 | **Windows test-suite portability gap** | The quality-gate suite assumes POSIX. Fixed at source: `process_control.stop_process` defaulted `kill_sig` to `signal.SIGKILL`, evaluated at import time → `AttributeError` on Windows (no `SIGKILL`), crashing module import and collecting every CLI test as an error; now `getattr(signal, "SIGKILL", signal.SIGTERM)` (identical on POSIX). Still failing on Windows only (deferred — gate is intended to run on POSIX/WSL): `tests/test_process_control.py` references `signal.SIGKILL` directly and asserts a SIGTERM→SIGKILL escalation that can't be represented without `SIGKILL`; `test_skills.py`/`test_orchestrator.py` assert `path.endswith(".../SKILL.md")` and `test_cli_init.py` asserts `"worc/README.md"` (both break on `\` separators); `test_cli_upgrade_docs.py` compares bytes that differ only by CRLF vs LF. Next step (if Windows support is wanted): use `os.sep`/`pathlib` in path assertions, normalize line endings in the docs byte-compare, and gate/parametrize the SIGKILL escalation test on `hasattr(signal, "SIGKILL")`. | `src/wastech_orchestrator/process_control.py`; `tests/test_process_control.py`; `tests/core/test_skills.py`; `tests/core/test_orchestrator.py`; `tests/test_cli_init.py`; `tests/test_cli_upgrade_docs.py` | known gap / backlog (not scheduled) |
| 2026-06-16 | **worc-home consolidation: footprint modes / registry / init / sibling-workspace retired** | **Resolved (2026-06-16):** the git footprint collapsed to one canonical layout. The `git.footprint.location`/`.tracking`/`.external_root` keys and the `FootprintLocation`/`FootprintTracking` enums, the `--git-mode` flag and the friendly mode names (external/in_repo_exclude/in_repo_commit), the `init` command (`install --non-interactive` replaces scripted `init`), the per-user registry (`install/registry.py`, the `repo-root -> config.yaml` binding, `WASTECH_ORCHESTRATOR_HOME`), and the sibling control workspace (`<repo>-orchestrator`) were all **removed**. Everything the orchestrator generates/installs now lives under a single gitignored `<repo>/.worc/` home (config.yaml, templates/, guide/, state.db, logs/, workspace/, checks/, tasks/rejected); the `tasks/` lifecycle dirs stay at the repo root, git-tracked, and the task file + `<id>.summary.md` are the audit trail, committed by a **task-scoped** audit commit (only the current task's files — never `git add -- tasks/` wholesale). `install` appends a single `.worc/` line to the repo's tracked `.gitignore`; config discovery walks up from cwd to the Git root for `<root>/.worc/config.yaml` (no registry, no `./config.yaml` fallback). `git.footprint` now carries only `audit_commit_message` + `audit_on_branch` (`AuditBranch` enum kept). `CONFIG_SCHEMA_VERSION` bumped 6 → 7. **Open choices kept:** the config key is still named `git.footprint` (could be renamed to `git.audit` in a later schema bump); the installed docs dir is named `guide/` (packaged source dir stays `worc/`). | `cli.py` (`worc_home_for`/`tasks_root_for`/`resolve_config_path`/`cmd_install`); `config/schema.py` (`FootprintConfig`, `CONFIG_SCHEMA_VERSION=7`); `git_manager.py` (`commit_audit`, `RUNTIME_GITIGNORE_LINES`, `append_runtime_excludes`); `install/{config_writer,wizard}.py`; README.md, docs/{configuration,operations,cookbook,worc_architecture,task-authoring}.md, docs/worc/, docs/functional/, docs/likec4/ | done |
| 2026-06-16 | **Dev rules relocated to `.agents/rules/`** | The five development rule docs moved from `docs/rules/` to `.agents/rules/` (`architecture`, `coding-style`, `git-workflow`, `security`, `testing`). All inbound links (root instructions, README, `docs/`, `.claude/` skills, `src/` docstrings) and the moved files' own outbound links were repointed; the docs-sync gate (`docs_sync_gate.py`) now also treats `.agents/` as a documentation change. | CLAUDE.md, AGENTS.md, README.md, docs/, .claude/, src/ | done |
| 2026-06-17 | **Flow-engine: config-aware validation deferred to P4.2** | The flow validator (`core/flow/validator.py`) is config-free: it cannot yet check that node `model`/`reasoning`/providers ∈ `config.yaml` allowlist, that `permission_ceiling` ≤ configured provider capabilities, or that flow `budgets` ≤ `agents.max_*` caps (security-ceiling §4 "Согласованность с config.yaml"). **Decided (2026-06-17):** lands as a separate `validate_flow_against_config(snapshot, config)` called by `FlowRegistry` after structural validation (keeps `validate_flow` config-free for unit tests). Deferred until the shrunk config schema exists. Next step: [p4-operator.md](flows/p4-operator.md) §P4.2. | `core/flow/validator.py`, `core/flow/registry.py`, flows/p4-operator.md, flows/security-ceiling.md | open |
| 2026-06-17 | **Flow-engine: flow `budgets` ↔ config-cap authority** | Packaged `implementation.yaml` carries `budgets.global_fix_iterations: 30` while config carries `agents.max_total_fix_iterations`. **Decided (2026-06-17):** flow `budgets` parameterize `LoopController`; config cap is the upper, non-weakenable safety bound — flow may not exceed it (a ceiling check added with the config-aware validator above). Next step: implement in [p1-engine.md](flows/p1-engine.md) §P1.1. | `core/loop_control.py`, flows/p1-engine.md | open |
| 2026-06-17 | **Flow-engine: custom tool nodes reserved as deferred P5** | Operator Python/executable as a typed `tool` node, run out-of-process under the ceiling (subprocess via `run_process`, side-effect-free, allowlisted JSON stdin/stdout — like Claude Code hooks). **Deferred, not v1** — start only after P1–P4 stabilize. "Reserve now" = P1–P4 must keep the seams (engine dispatch-by-`kind` as a registry, per-kind validator tables, reusable context assembly, separate `validate_flow_against_config`) so P5 drops in (~1 week, dominated by the security contract + threat tests). Does not violate the no-code-node invariant: `tool` is ceiling-bound, not in-process arbitrary code. Spec: [p5-custom-tool-nodes.md](flows/p5-custom-tool-nodes.md). | flows/p5-custom-tool-nodes.md, flows/index.md §1, flows/flow-contract.md §10, flows/plan.md | open |
| 2026-06-17 | **Flow-engine: P1–P4 engineering specs authored** | The granular plan's P1–P4 sections were expanded into per-phase engineering specs (touchpoints with signatures, new types/state.db tables, named tests, exit criteria, open questions) and `plan.md` shrunk to an index. P0 hardening (P0.5: fail-closed unknown fields, checker/network enums, enum-error wrapping, `when.fact` namespace, terminal co-reachability) closed design↔code gaps found while reviewing the "done" P0. | flows/plan.md, flows/p1-engine.md, flows/p2-implementation.md, flows/p3-research-audit.md, flows/p4-operator.md; `core/flow/{snapshot,validator,contracts}.py` | done |
| 2026-06-17 | **Flow-engine P1.1+P1.2 built (engine + persistence, additive)** | **P1.1:** `core/flow/engine.py` (`FlowEngine` engine-owned transitions, generic bounded-loop budgets with zero domain knowledge, terminality) + `core/flow/run_state.py` (`FlowRunState`). **P1.2 (additive — legacy `_drive`/`stage_runs`/granular statuses stay until P1.5 because P1.4's golden-harness dual-runs both):** `state.db` **v4** — `node_runs` table + `NodeRunRow` + `record/complete/skip/get_node_runs` + `tasks.{current_node,flow_run_counters,flow_fingerprint}` + `save/get_flow_checkpoint`; `core/flow/recorder.py` (`StateStoreRunRecorder` + `hydrate_run_state`); generic `Status.RUNNING`; flow-neutral `ledger.write_failure_report` (optional `node_id`); engine resumes at hydrated `current_node`. **Deferred to P1.4** (entangled with wiring the engine into `run_task`/`resume`): the orchestrator-level `RecoveryReconciler` node-progress dispatch, decomposed-resume, and `rerun --continue` at the interrupted node. Not yet wired into the production path. | `core/flow/{engine,run_state,recorder}.py`; `state_store.py`; `core/state_machine.py`; `ledger.py`; flows/p1-engine.md; tests under `tests/core/test_flow_{engine,recorder}.py`, `tests/state/test_node_runs.py` | P1.1/P1.2 done; P1.3–P1.5 in progress |
| 2026-06-17 | **Flow-engine P1.3 (agentic + deterministic node wrappers)** | Prompt assembly from `role_file` (`core/flow/prompt.py`: `render_role_prompt`/`read_role_file`, path-contained; `render_prompt` unchanged; `repo` added to `ALLOWED_PROMPT_VARS`) + thin adapters in `core/flow/nodes/`: `base.py` (`NodeServices`/`NodeInputs` contracts, Protocol collaborator ports, `NodeInfraError`), `agent.py` (`AgentNodeRunner`→router), `evaluator.py` (`EvaluatorNodeRunner` role=review → accept/rework/done), `checks.py` (`ChecksNodeRunner`→CheckRunner, launch_failed→`CheckLaunchError`). Runners constructed per-unit with services/inputs, so the generic engine is untouched. **Folded into P1.4 wiring** (most orchestrator-coupled): `publish`/`hitl` runners, embedded refinement/planning HITL (`node.hitl` + `_run_typed_stage`), dangerous-diff guard, review-artifact + `{review_path}`, prompt_audit/heartbeat, and building `NodeServices`/`NodeInputs` from `_Pipeline` + the `node_id→Stage` map. | `core/flow/prompt.py`, `core/flow/nodes/{__init__,base,agent,evaluator,checks}.py`; `core/prompts.py`; flows/p1-engine.md; `tests/core/test_flow_{prompt,node_runners}.py` | P1.3 core done; publish/hitl/wiring in P1.4 |
| 2026-06-17 | **Flow-engine P1.4 Step A (wiring-first): publish runner + engine driver + review artifact** | `publish.py` (`PublishNodeRunner` → idempotent git via `GitPublishPort`); `engine_driver.py` (`build_node_runners` per-kind registry + `drive_flow` = engine↔orchestrator seam); `EvaluatorNodeRunner` now writes `review/findings.json`+`summary.md` and sets `{review_path}` (parity `_write_review`). End-to-end `tests/core/test_flow_engine_driver.py` (tiny `refine→impl→testing→publish` flow via `drive_flow` + real `StateStoreRunRecorder`/`StateStore`). **Remaining for Step B** (durable round-trip is only verifiable end-to-end via the golden harness): the one shared durable HITL/approval round-trip — `hitl` runner + embedded refinement/planning HITL (`node.hitl`, `parse_typed_stage_output`, `interaction_path` persist/resume) + dangerous-diff guard (`write_current_diff`+`classify_dangerous_diff`+approval); prompt_audit/heartbeat; building `NodeServices`/`NodeInputs` from `_Pipeline` + the `node_id→Stage` map; the dormant `_drive_via_engine` seam in `run_task`; then the parity fixture + golden-harness dual-run + recovery dispatch. | `core/flow/{publish,engine_driver}.py`, `core/flow/nodes/{base,publish,evaluator}.py`; flows/p1-engine.md; `tests/core/test_flow_engine_driver.py`, `tests/core/test_flow_node_runners.py` | Step A core done; durable-HITL + golden-harness in Step B |
| 2026-06-17 | **Flow-engine P1.4 Step B: dangerous-diff classification + parity fixture** | `AgentNodeRunner` writes `{diff_path}` after a workspace-write edit and runs `classify_dangerous_diff` (core-owned, automatic); a dangerous diff fails closed → `NodeManualRequired` (full durable approval round-trip is the next piece). `GitPublishPort`→`GitPort` (+`write_current_diff`/`changed_code_entries`). Parity fixture `tests/core/flows/implementation_parity.yaml` (+`roles/*.md`): the current pipeline 1:1 (refinement→planning→implementation→testing→review→fixing→summary→publish + `test_fix`/`review_fix` loops + decomposition; NO supervisor/testing_quality). **Remaining Step B:** full durable HITL/approval round-trip (`hitl` runner + embedded refinement/planning HITL + dangerous-diff approval branch); prompt_audit/heartbeat; `_Pipeline`→`NodeServices`/`NodeInputs` builder + `node_id→Stage` map; dormant `_drive_via_engine` seam in `run_task`; the golden-harness dual-run (`_drive`↔`FlowEngine` on fake-CLI scenarios); recovery dispatch. | `core/flow/nodes/{agent,base}.py`; `tests/core/flows/implementation_parity.yaml`; `tests/core/test_implementation_parity_flow.py`; flows/p1-engine.md | dangerous-diff(classify)+parity-fixture done; round-trip+wiring+harness remain |

## Post-test-run review improvements

These improvements were found while reviewing real orchestrator test runs (first review: the `pr_title` task, 2026-06-14); the change shipped. The former dedicated `post_test_run_review.md` document was removed together with `implementation_stages/`; record new test-run findings in this file.

_Refs:_ `notify/telegram.py` (`poll_reply`, `send_prompt`/`_drain_pending`, `wait_for_answer`, `check_polling`, `get_webhook_info`); `notify/interface.py` (`AskHandle.update_offset`); `core/hitl.py`; the `pr_title` guardrail (`logs/task-pr-title-override/hitl/guardrail-fixing-cycle-10.json`).
