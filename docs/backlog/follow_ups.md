# Implementation follow-ups

IMPORTANT: This is a list of open items that have been identified for further discussion. These should not be taken as mandatory implementation or official requirements; they are simply a list of open items.

Open **implementation** next-steps discovered while building — distinct from [README.md](README.md), which tracks _product features_. Use this for "we deferred X; here's the hook and the context" items, and for findings from real test runs. Record new ones via `/sync-docs` as you create them (date them absolute, link where they're referenced, mark status).

| Date | Item | Context & next step | Referenced in | Status |
| --- | --- | --- | --- | --- |
| 2026-07-13 | **Audit-trail sweep: `tool.py` stdin has no audit artifact at all; memory-packet file goes stale on a node's repeat runs** | Found while fixing the context-footer audit bug (agent-prompt-audit-missing-context-footer.md): the footer bug turned out to hit 3 audit surfaces (`request.json`/`rendered-prompt.md`/prompt-audit), fixed together with the supervisor's own total audit gap (observe/finalize/handoff turns never wrote to any audit surface before this fix). Two further gaps the same sweep found were deliberately left out of that fix's scope (confirmed with the operator via the sweep's follow-up section): (a) **`tool.py` nodes have zero audit artifact for their stdin** — they bypass the whole provider/observability spine via a raw `run_process` call, so only redacted `stdout.txt`/`stderr.txt` survive; needs a new artifact type/writer, not a wire-up of an existing one. (b) **The per-node memory-packet file is keyed only by `node_id`** (`memory/packet.py` `write_packet`, dest built in `agent.py`/`evaluator.py` `_memory_path`) and is silently overwritten on a node's 2nd+ run in the same task (e.g. a second `fixing` pass) — an earlier run's `rendered-prompt.md` still references that shared path, which can hold a _later_ run's content by the time anyone reads it; the packet file is also never registered as a tracked artifact. Next step for (a): design a redacted `tool-request.json`-style artifact alongside the existing stdout/stderr streams. Next step for (b): make the packet dest path run-scoped (include `node_run_id`) and call `register_artifact` on it. | agent-prompt-audit-missing-context-footer.md; `core/flow/nodes/tool.py`; `memory/packet.py`; `core/flow/nodes/agent.py`/`evaluator.py` (`_memory_path`) | candidate |
| 2026-07-12 | **`rerun --continue` crashed resuming a dirty task branch (`git checkout main failed`)** | `_resume_via_engine`'s branch reattach (the `--continue` resume-in-place path) called `git_manager.prepare_branch(...)` without `mode=`/`branch_ref=`, so it silently took the `BranchMode.NEW` "first creation" branch — which unconditionally did `checkout(base_branch)` before reattaching to the task branch. On a `--continue` at review/fixing/publish (capability #3's "tolerate the task's own uncommitted WIP"), that detour conflicted with the very dirty tree it was supposed to tolerate, aborting on `git checkout main` and terminaling the resume as `failed`/`manual_action_required` with a raw git-stderr reason (RERUN-BUG-REPORT.md). Fixed in `git_manager.prepare_branch`: once the branch already exists (any restart/`--continue`, any `BranchMode`), reattach directly — never via `base_branch` — skipping the base detour entirely; only first creation still fetches/checks out/pulls `base_branch`. Regression test exercises the real `_resume_via_engine` → `prepare_branch` path (not the mocked-`resume` style the existing WIP-tolerance tests use, which never touched this code). **Follow-on UX report (same day):** even after the crash fix, the operator still saw `Rerun ... [continue] from base 'main'? [y/N]` and (reasonably) feared a checkout to base was still about to happen — the confirm prompt used one wording for both modes, while `--dry-run` already correctly said "reuse `<branch>`" for `--continue` with no base mention. Fixed in `cmd_rerun`: the prompt now says `on branch '{plan.branch}'` for `--continue` (base is never touched) and keeps `from base '{plan.base_branch}'` only for a fresh rerun (which really does reset to base). | RERUN-BUG-REPORT.md; `git_manager.py` (`prepare_branch`); `core/orchestrator.py` (`_resume_via_engine`); `cli.py` (`cmd_rerun`); `tests/core/test_cli_rerun.py` | shipped 2026-07-12 |
| 2026-07-12 | **Exhausted-fix-budget confirmation doesn't cover inline per-edge `budget:` loops** | `rerun --continue`'s new interactive exhausted-budget prompt (rerun-flow-drift-and-budget-confirm.md) only checks _named_ loops (`edge.loop is not None`, e.g. `review_fix`/`test_fix`) reachable forward from the resume node — an inline `budget: N` edge with no loop name can still silently re-park on resume, same failure mode this ADR closed for named loops. Deliberately deferred (no flow in the wild uses inline budgets today); if one does, extend `core/loop_control.exhausted_fix_loops` to also walk `edge.budget is not None` edges against their persisted synthetic counter key. | rerun-flow-drift-and-budget-confirm.md; `core/loop_control.py` (`exhausted_fix_loops`) | Deferred |
| 2026-07-11 | **Universal resume/recovery — deferred capabilities** | Phase 0 (`--reset-fix-budget`, WIP tolerance) + Phase 1 `--from <node>` shipped (universal-resume-recovery.md). Deferred: **#5** per-leg provider/model overlay on the resumed leg (reuse the `resolve_node_overrides` seam); **#6** bounded audited force-forward (`--accept-current` / `--skip-node` once, synthetic operator verdict in `evaluations`); **#7** guided reconciliation + `--commit-wip` + the own-vs-foreign WIP discriminator (diff dirty paths against the task's `current.diff`, so Phase 0's "foreign WIP is committed" limitation is closed); **#8** resume-then-finish; plus `--from` under decomposition (per-subtask region). The `--re-resolve` idea listed here for flow-fingerprint drift is superseded — 2026-07-12's rerun-flow-drift-and-budget-confirm.md resolved it differently: `--from <node>` now always resumes through drift (no new flag), rebaselining the checkpoint's fingerprint. | universal-resume-recovery.md; rerun-flow-drift-and-budget-confirm.md | Deferred (drift item resolved) |
| 2026-07-08 | **P5 remediation — SKIPPED / deferred items (closed; residue below)** | Final disposition of the pieces left out of the orchestrator remediation above (p5-remediation-skipped-items.md, archived 2026-07-08). **Done:** A1 substantive — the target `wastech-mdlint/.worc/flows/implementation/review.md` narrowed "missing test coverage" blocking to _zero coverage for new core behavior_, moving completeness to advisory (gitignored local edit, no commit); C2 secondary step — new `ErrorClass.MODEL_REQUEST_INVALID` (a model/schema HTTP 400) split from `process_crashed`, not fallback-eligible, in both adapters (this also fixed a **latent** missing `SESSION_UNAVAILABLE` `_MESSAGES` entry that would `KeyError` in `classify`, now guarded by a new exhaustiveness test); D1 already satisfied (no target task combines `depends_on` with a same-ref shared branch). **Still open — carried here:** (a) **A3 delta-observe (F50)** deliberately stays deferred — skipping re-observation of unchanged node-runs is fuzzier and risks losing supervision signal, and the shipped F50 `high` reasoning cap already cut observe cost; revisit only if observe cost dominates a deep-loop task after the cap (measure `result.json.usage`); lever `core/supervisor.py` observe cycle. (b) **E1/F37** owner live-smoke — prove Claude native-memory isolation is structural (spawn a claude node provoked to write native memory, confirm refusal); lever `providers/claude.py`. | p5-remediation-skipped-items.md; p5-findings-remediation-plan.md; TEST-FINDINGS.md F37/F40/F42/F50; target `wastech-mdlint/.worc/flows/` | A1/C2/D1 done; A3 + E1/F37 open |
| 2026-07-08 | **P5 findings — consolidated remediation plan (memory F43–F48 now tracked + all P5 open findings in one place)** | Single work plan for every OPEN P5 finding, drawn from the phase synthesis, per-node prompt-quality audit, memory audit, and the individual 2026-07-07 rows below: p5-findings-remediation-plan.md. It carries **Цель · Рычаг · Шаги · Тест · Зависимость** per finding with recommended order (P1: F42 review calibration, F43 memory noise, F48 packet relevance; P2: F49/F50/F44–F47/F39-preflight/F40/error_class-triage/F37-verify). **Newly registered here** (were only in the memory audit doc, untracked in follow_ups): **F43** durable semantic/procedural/reviewer lessons stuck in quarantine, never read by the packet (write-only noise duplicating the entity layer) — `supervisor.py:868` finalizer prompt / `packet.py:144-166` read durable quarantine / `lifecycle.py:88-109`; **F48** packet loses path-relevance on a shared chain branch (cumulative `changed_code_paths_since_base` — the untouched tail of the F32 `_diff_base` fix) — `agent.py:598`/`evaluator.py:328`/`git_manager.py:717`; **F44** duplicate entity card per file + empty `last_seen_task_ids` (`service.py:317-336`); **F45** redaction over-fire corrupts a durable lesson `subject` (sibling of F36; `redaction.py:127-134`); **F47** contentless episodes (`orchestrator.py:2145-2158`); **F46** misleading audit merge-rationale (`service.py:281`). Also refines **F49**: the persist point already exists (`_sync_counters_from_run_state`) — the field reads 0 because `engine._reset_loops_at` zeroes the consecutive-loop counter on `accept`; the real fix is a cumulative per-loop-kind total, not a new persist point. The 2026-07-07 rows (F42/F49/F39-preflight/F40/error_class) are the individual sources; this plan supersedes them as the actionable tracker. | p5-findings-remediation-plan.md; p5-phase-synthesis.md; p5-prompt-quality-per-node.md; p5-memory-subsystem-audit.md; TEST-FINDINGS.md F42–F50/F43–F48 | candidate |
| 2026-07-07 | **F42 — calibrate blocking codex-review depth (7 rework cycles on one task)** | From the P5 run p5-04 (`synthesize`) with `review` pinned to codex gpt-5.4: the blocking evaluator returned `rework` **7 times** before `accept` (~2h40m, diff grew to +1547 incl. +802 test lines). Iterations 1–4 were real correctness HIGH bugs (G6-honesty empty `readingOrder`, all-or-nothing `resolveCompileSettings`, per-field leniency, `contentHash` sans provenance) — genuinely valuable; iterations 5–7 drifted into test-coverage/assert completeness and input-bound validation. Not a bug (system worked, converged in budget, all checks green throughout), but the default calibration makes a single big node dominate run time/cost. Options: (1) `review.md` role — ask the reviewer to batch **all** findings of a pass into one verdict and explicitly separate blocking-correctness vs advisory-completeness; (2) `max_rework_per_stage` cap on the review node; (3) a separate **non-blocking** `testing_quality` evaluator for coverage remarks so correctness-review doesn't block on test polish. | p5-04-synthesize-run-analysis.md; TEST-FINDINGS.md F42; `.worc/flows/implementation/review.md` (target) / `packaged/flows/roles/implementation/review.md`; `core/flow/schema.py` (`max_rework_per_stage`) | candidate |
| 2026-07-07 | **`tasks.review_fix_cycles` not persisted (stays 0 despite real review reworks)** | Observed in p5-04: `state.db tasks.review_fix_cycles = 0` after **7** review-driven rework cycles (`fix_iterations = 7` is correct; `test_fix_cycles = 0` correct — no test failures). The global counter captures the loop, but the per-loop `review_fix_cycles` audit field is never incremented/persisted, so per-loop attribution is lost in the audit trail. Minor audit gap, distinct from F42. Next step: find where `review_fix`/`test_fix` loop counters are updated on the `tasks` row and ensure `review_fix_cycles` is written like `fix_iterations`. | p5-04-synthesize-run-analysis.md; TEST-FINDINGS.md F42; `core/orchestrator.py` / `core/state_machine.py` (loop-counter persist) | candidate |
| 2026-07-07 | **F39 — preflight doesn't catch supervisor model↔provider mismatch when provider is inherited** | From p5-02: under codex-primary with `supervisor.model: claude-opus-4-8` and **no** `supervisor.provider`, the supervisor inherited `primary=codex` and sent a claude model to codex → `400 model not supported` on every step (masked by claude fallback). The F39 code fix added `SupervisorConfig.provider` + model↔provider validation, but `preflight` passed `ready` — the validation apparently only fires on an **explicit** `supervisor.provider`, not on the inherited-primary case. Extend the supervisor preflight check to the inherited path: when `supervisor.provider` is unset, validate `supervisor.model` against the resolved global primary's vendor (fatal if no fallback, warn otherwise). Workaround in the field is to set `supervisor.provider` explicitly (done for the campaign). | p5-02-doc-profile-run-analysis.md; codex-primary-correctness.md; TEST-FINDINGS.md F39; `config/schema.py` (`SupervisorConfig`); supervisor provider-resolution + preflight | candidate |
| 2026-07-07 | **F40 — warn when a task's `depends_on` targets a task sharing its own `branch_ref` (contradictory chaining)** | From p5-02: the whole P5 phase blocked at step 2 because every task combined `depends_on: [predecessor]` (a **merge**-gate: `refusing to run … dependency PR is OPEN (unmerged)`) with `branch_mode: existing` + `branch_ref: feat/p5-compile` (a **shared-branch accumulation** whose PR stays open until phase end). The two are mutually exclusive chaining mechanisms; the p4 campaign used shared-branch **without** `depends_on`. Primary fix is task-authoring (don't combine — removed `depends_on` from p5-02..06). Orchestrator UX guard: at task validation, warn (or reject) when a `depends_on` target's branch equals the task's own `branch_ref` — catches the contradiction before a run refuses mid-phase. | p5-02-doc-profile-run-analysis.md; TEST-FINDINGS.md F40; `core/flow/validator.py` / task validation gate; `core/orchestrator.py` (`depends_on` gate) | candidate |
| 2026-07-07 | **Codex exit-2 argparse errors are still classified `unsupported_version` (masks bad-argv); live-verify codex-primary resume nodes** | Deferred, out of the F38/F39 ADR scope. (a) **Misclassification:** the codex adapter's stderr signature (`providers/codex.py` `_CODEX_SIGNATURES`, regex `unknown option` / `unrecognized option` / `unexpected argument`) maps an argparse/exit-2 rejection of our _own_ argv to `unsupported_version`, which hid F38 (a bad-argv construction) behind a version-looking error. Now moot for the resume path (F38 reorders the argv so 0.142.x accepts it), but a latent trap for the next grammar drift — consider a distinct `configuration_error`/`bad_argv` class for exit-2 argparse rejections vs a genuine version gate. (b) **Live-verify (owner):** re-run a `p5-01`-like task under codex-primary on real codex 0.142.5 and confirm supervisor + documentation actually run on codex — `state.db provider_attempts` shows codex `succeeded` on resume nodes (no `unsupported_version`, no claude fallback), and preflight emits the resume-grammar WARN/FAIL only on a real drift. | codex-primary-correctness.md; p5-01-classify-nodes-run-analysis.md; TEST-FINDINGS.md F38/F39; `providers/codex.py` | candidate (a) / **(b) verified 2026-07-07**: p5-02 documentation resume ran on codex succeeded (no `unsupported_version`, no fallback); p5-04 supervisor (all per-step + finalize) ran on codex succeeded. F39 inherited-mismatch preflight gap tracked as its own row above. |
| 2026-07-06 | **⚠️ Reliable-stop subtree kill — Windows real-smoke pending; a future cooperative in-stage cancel can reuse `ErrorClass.CANCELLED`** | The reliable-stop ADR shipped: every stop route (Ctrl-C, soft `stop`, `--force-full`, `run_process` timeout, daemon exit) now reaps the whole agent subtree via a recorded `(pid, pgid)` handle (`.worc/orchestrator.children`) + one `kill_agent_subtree` primitive (group-kill + POSIX descendant sweep / Windows `taskkill /F /T`), and a stop-killed agent is flagged `ErrorClass.CANCELLED` so the router never falls back and the Core parks the task resumable. **POSIX is the validated acceptance gate** (automated real-topology tests + owner macOS smoke). **The Windows hard stop is implemented but real-smoke-pending (owner):** start a daemon, run a task, `stop --force-full`, confirm the daemon **and** the full agent subtree (incl. a grandchild that broke into its own group/session) die and `resume()` recovers. Two hooks for later: (1) `is_cancelled`/`ErrorClass.CANCELLED` is the seam a future **cooperative in-stage per-task cancel** (row below) would reuse — it already suppresses fallback + parks; (2) full OS containment (cgroup v2 / PID namespace / Windows Job Object) remains the north-star for "the CLI spawned something we cannot see", deliberately deferred. | reliable-stop-no-orphaned-agents.md; `providers/process.py`; `process_control.py`; `routing/router.py`; `cli.py` | validation pending (owner) |
| 2026-07-05 | **Live-verify the F37/F28 fixes on a real run** | Two P4 remediations landed with a residual that only a live run can confirm (both fail safe until then). **F37:** `providers/claude.py._native_memory_deny_tools` emits `--disallowedTools "Write(//<config_dir>/**)"` etc. using the documented `//`-absolute glob form; confirm the installed `claude` CLI actually honors that pattern (spawn a run, then assert the agent neither reads nor writes `~/.claude/projects/<target>/memory/`). If the CLI wants `~/`-relative or bare-absolute globs instead, adjust the form. **F28:** the A1/F24 schema fix should let codex review run for real (green codex attempt-1, no claude fallback); confirm on a live run with `review` pinned to codex — until then the "cross-provider review" claim holds only when codex does not fall back. Pairs with the existing F21 planning-allowlist live check. | `providers/claude.py`; TEST-FINDINGS.md F37/F28/F21; `core/flow/nodes/evaluator.py` | validation pending (owner) |
| 2026-07-05 | **Lightweight preflight check: project paths cited in `.worc/flows/*.md` still exist** | Systemic root cause behind F34 (target `planning.md` had cited three non-existent primitive paths): a seeded/edited `.worc/flows/` prompt drifts from the repo with no actuality check. Cheapest guard short of the `upgrade-flows` re-sync (row below): at preflight, extract repository-relative path-looking tokens from each active `.worc/flows/**/*.md` and warn (non-fatal) on any that resolve to nothing in the working tree — catches the F34 class before a run misdirects a weaker planner. Deliberately kept out of the point-fix plan (§0) to avoid scope creep. | p4-prompt-quality-remediation-plan.md §0; `core/flow/validator.py`; `.worc/flows/**/*.md` | candidate |
| 2026-07-03 | **Post-p0 robustness hardening (revive-finalize durability + doc-node capability policy)** | Two deferred hardening decisions surfaced by the p0 campaign, **promoted to a single ADR** — post-p0-robustness-hardening.md. (A) On a revived (`rerun --continue`) task the finalize supervisor turn can fail because the durable `resume_own_lineage` session is unusable after the interrupting stop, so the whole-task summary silently degrades to the minimal fallback (recovery-quality gap adjacent to F14). (B) F17b: worc scopes the documentation node by prompt (F17a), not capability — `network_access:false` hard-blocks only on Codex's OS sandbox, while on Claude it toggles only WebFetch/WebSearch (Bash `npm install` still reaches the network), so p0-04's live install was possible. See the ADR for options + open questions. | post-p0-robustness-hardening.md; test-findings-remediation-plan.md | promoted to ADR |
| 2026-07-02 | **Soft cap on deep/wide subtask handoff graphs** | The intra-task subtask handoff (`{predecessor_context}`) shipped 2026-07-02 (subtask-context-handoff ADR): the deterministic floor + interpretive supervisor brief are assembled per successor from its `depends_on` predecessors. V1 injects the full brief for **every** predecessor. The ADR's one open question is deferred: if a subtask depends on many predecessors, or the chain is long, injecting every brief could grow costly in tokens. Needs a policy (last-N predecessors, a token-budget cap, or summarise-the-summaries). Not built. **Review decision (2026-07-02): deferral confirmed for V1 — no unbounded growth exists today.** Two structural bounds already hold it: (1) `depends_on` lists **direct** predecessors only and the floor iterates `unit.depends_on` (not the transitive closure), so a long chain does **not** accumulate — successor `k` sees only its immediate deps, not `1..k-1`; (2) fan-in width is bounded by the deterministic `max_subtasks` gate (a subtask can depend on at most `max_subtasks-1` others), and the interpretive brief is a single turn regardless of width. So the only real growth vector is a single very-wide fan-in under a large `max_subtasks`. Revisit (last-N predecessors / token-budget cap / summarise-the-summaries) only if a wide decomposition shows the handoff dominating the successor's prompt. Hook: `Orchestrator._assemble_predecessor_context` (bound the `unit.depends_on` iteration) + `Supervisor.handoff`. | subtask-context-handoff.md "Open questions"; `core/orchestrator.py`; `core/supervisor.py` | candidate |
| 2026-07-02 | **Pre-existing broken doc links (backlog link sweep)** | Surfaced while archiving the console/roadmap docs (not caused by the move). (1) [archive/worc-listen-remote-control.md](archive/worc-listen-remote-control.md) has its relative links authored as if the file lived in `docs/backlog/` while it actually sits in `docs/backlog/archive/` — so its sibling-ADR links (`operator-confirmation-gates.md`, `telegram-step-trace.md`, `skills-selection-rework.md`, `archive/done/orchestrator-memory.md` → doubled) and `../../.agents/…`, `../../.claude/…` refs are off by one directory level (the `cli-upgrade.md` refs were repointed to `done/cli-upgrade.md` on 2026-07-02; the rest remain). (2) ~~`README.md` links `memory/adr-0001-memory-subsystem-v1.md` but no such file exists under `docs/backlog/memory/`~~ — **resolved 2026-07-02**: repointed the ADR-0001 links (`README.md`, `memory/index.md`, `memory/plan/index.md`) to the real `memory/adr.md`. (3) several run-remediation rows (now archived in `archive/follow_ups_history.md`) reference `../analysis/{task-023,ion-list-…,watch-autonomy-…}-run-analysis.md` whose files are not present under `docs/analysis/`. Do a bounded relative-link sweep of `docs/backlog/`. | `docs/backlog/archive/worc-listen-remote-control.md`; `docs/backlog/README.md`; `docs/backlog/follow_ups.md` | candidate |
| 2026-07-02 | **Console remediation R2 + R3 (post-review remainders)** | ~~**R2 (invariants):** (H1) blocking `input()` inside the REPL; (M1) POSIX-mode `shlex.split` mangling Windows paths. **R3 (ergonomics):** (M2) default daemon log path; (M3) quit-while-busy detach/stop.~~ **H1/M1/M2/M3 shipped 2026-07-06** by the worc shell reliable control surface ADR (H1 = `--non-interactive` threaded into the forwarded `down`/`restart`; M1 = raw-remainder path parsing + OS-aware `shlex`; M2 = `.worc/logs/daemon.log` default; M3 = `quit` detaches + tells you how to stop). This also closed the standalone 2026-07-01 `shlex.split` row below. **H1 gap found + closed 2026-07-12:** `rerun` was left on the generic `_FORWARD_VERBS` path (no `--non-interactive`) even though `rerun`'s own `--non-interactive`/`--yes` handling already existed for exactly this purpose — a `worc shell` operator running `rerun ... --continue --from <node>` without `--yes` hit the same blocking-`input()`-vs-REPL-stdin fight H1 was meant to close (RERUN-BUG-REPORT.md). Fixed by forwarding `rerun` with `--non-interactive` in `cli_shell.dispatch`, same as `down`/`restart`. **Still open (L-tier ergonomics):** (L1/L2) consolidate the two log tailers / decide on incremental tailing; (L3) console `down`/`restart` reuse the loaded config instead of re-entering `cli.main`; (L4) `logs --daemon`; (L5) add a `docs/functional/` CLI block for `top`/`shell` + the stop ladder (note: `docs/functional/` is reverse-engineered weekly, so L5 may be moot). | cli-upgrade-remediation.md (R2/R3); worc-shell-reliable-control-surface.md; `cli_shell.py`; `cli.py` | L1–L5 candidate (H1/M1/M2/M3 done) |
| 2026-07-06 | **Console spawned-daemon startup-capture log has no rotation; one-shot `run <id>` deferred** | Two intentional scope choices in the worc shell reliable control surface ADR. (a) **Startup-capture growth:** `spawn_detached(capture_path=…)` redirects the spawned daemon's stdout/stderr to `.worc/logs/daemon-startup.log` for crash recovery; it is **truncated per spawn** (never grows across restarts) but has **no rotation within one daemon's lifetime**, so a very-long-lived daemon accumulates a duplicate of its operator log there (the tailed `--log-file` daemon.log _does_ rotate). Cap it (size-truncate / hand off to a `RotatingFileHandler`) if long-running daemons bloat it. (b) **One-shot `run <id>` deferred** (owner decision): the console ships continuous serving (`up`/`watch`) only; a one-shot "run this single task now" verb would add a second claim path needing a slot-guard against the serving daemon. The `run` verb was freed (its old enqueue-alias dropped) so a future one-shot can take the name cleanly. | worc-shell-reliable-control-surface.md; `providers/process.py` `spawn_detached`; `cli_shell.py` `start_watch`/`dispatch` | candidate |
| 2026-07-02 | **Deferred tails from archived run-remediation rows** | Still-open action items carried by the run-remediation rows archived to [archive/follow_ups_history.md](archive/follow_ups_history.md) on 2026-07-02 — kept here so they stay actionable. (task-023) a non-fatal **preflight warning when `checks.command_sets` leave shared/root paths uncovered** (`core/flow/validator.py`). (ion-list) a **live `codex exec` smoke** at preflight instead of the `--help` probe. (PR-merge) **path/area-based merge-flow collection** (a `git.merge_flows` diff-selector), `worc tasks`↔`list --all` dedup, and a watch-startup stale-`MERGE_HEAD` probe. (watch-autonomy) **graceful turn-budget exhaustion** (resume-in-lineage on `error_max_turns`), the **fail-before-commit dirty base tree** (dangling `D`), and the **empty `failure_report.final_diff`** (diff `base..HEAD` of the agent branch). (HITL) **durable across-restart HITL session resume** (persistence keyed by `(task,node,subtask)` + a `state.db` bump) and **per-node HITL autonomy knobs** (`HitlSettings.timeout_s` + bounded auto-proceed for embedded planning/refinement only, never the dangerous-diff guard). (redesign-form-controls) confirm the **silent finalize-tail root cause** on the next run and mark **visual acceptance criteria manual** in `roles/review.md`. (`CLAUDE_CONFIG_DIR` agent-hook isolation and the HITL-wait log signal are tracked by their own rows below.) | [archive/follow_ups_history.md](archive/follow_ups_history.md); `core/flow/validator.py`; `core/orchestrator.py`; `core/hitl.py`; `git_manager.py`; `packaged/flows/roles/review.md` | candidate |
| 2026-06-27 | **Operator confirmation gates: configurable resume cap + empirical fresh-budget check** | Both gates shipped (config v22): the `watch` next-task gate (`orchestrator.auto_mode.confirm_next_task`) and the Claude max-turns continue/stop gate (`agents.providers.<id>.max_turns_gate`), off by default, fail-closed, telegram-required at preflight. Two intentional scope notes. (1) **No hard resume cap** on the max-turns gate (operator decision): each continue needs a fresh approval and timeout→STOP bounds an unattended loop, so a pathological approve-loop is operator-bounded, not orchestrator-bounded. Next step if a hard ceiling is wanted: add a small `max_turns_gate_resumes` config (e.g. 3) checked in `_invoke_with_turn_gate`'s loop with a final forced stop. (2) **Fresh-budget assumption** is confirmed from code (the Claude adapter appends `--max-turns` on every invocation, independent of `--resume`), but not yet empirically verified against a real Claude account hitting a real cap — run that smoke check before relying on the gate in production (set `max_turns` ~5 + gate on, overrun, approve, confirm the resumed run gets a fresh budget). | `core/flow/nodes/agent.py` `_invoke_with_turn_gate`; `cli.py` `_confirm_next_task`; operator-confirmation-gates.md | candidate |
| 2026-06-27 | **Telegram step-trace enrichments (bookend / verbosity / labels)** | The live per-node step-trace shipped (`telegram.trace` bool, config v21; one best-effort `<emoji> <node-id> → <outcome>` message per executed node finish, emitted at the engine post-node hook). Three Decision-deferred enrichments remain, all additive: (1) a **task-start bookend** line for context (terminal status is already covered by `send_notification`); (2) an **`off/milestones/verbose` verbosity enum** to replace the bool if per-node-per-task noise across a busy `watch` queue makes operators turn it off; (3) a **static per-node human-label map** ("implementation — writes the code") layered onto the raw node id. Ship none until real usage shows the bool is too chatty / too terse. | `notify/{interface,telegram}.py` `send_trace`; `core/orchestrator.py` `_engine_post_node`; telegram-step-trace.md (open questions) | candidate |
| 2026-06-27 | **Verify CLI internal 5xx retry behavior (transient-recovery Phase 0 spike)** | Transient provider-failure recovery shipped (`agents.retry`: same-provider retry → symmetric fallback → B-lite soft pause). The ADR's Phase 0 spike — confirming the _pinned_ Codex/Claude CLIs' own internal 5xx/429 retry counts and backoff — was **not** run because it needs the real binaries hitting a real outage (not reproducible in CI). We shipped the ADR's stated defaults (`base_delay_s=2.0`, `max_delay_s=30.0`, `max_attempts=2`); they are operator-tunable, so this is non-blocking. Next step if the defaults prove too tight/loose in practice: measure each CLI's internal retry budget once and re-tune the packaged defaults. | `src/wastech_orchestrator/providers/{codex,claude}.py`; transient-provider-failure-recovery.md (Фаза 0) | candidate |
| 2026-06-27 | **Huge-monorepo skill-inventory context pre-filter** | The supervisor's once-per-task skill proposal is handed the whole discovered inventory (name + description + path per skill). On a repo with hundreds of `SKILL.md` files this becomes large prompt context. Next step if it bites: pre-filter the inventory before feeding the proposal — a `skills.roots` limiter or a diff-scoped `skills.sets` (the `checks.command_sets` pattern), revisited only when the inventory is genuinely large. `resolve_skills` and the per-node threading are unchanged by this. | `core/supervisor.py` `_proposal_prompt`; `core/skills.py` `SkillInventoryScanner`; skills-selection-rework.md (open question 1) | candidate |
| 2026-06-26 | **`worc list [--queue NAME]` command** | The task `queue` tag + per-instance selector shipped (config v18), but there is no `worc list` command to show the pending pool or which instance owns each task. Next step if operators running multiple queues want visibility: add a read-only `list` subcommand that scans `select_pending(folder)`, reads each file's `_scan_pending_meta` (id/priority/queue/depends_on), and prints a table, with an optional `--queue NAME` filter. Read-only, no state change. | `cli.py` `select_pending`/`_scan_pending_meta`; multi-instance-task-queues.md | candidate |
| 2026-06-26 | **`install` only scaffolds the default `tasks/` for a custom `paths.tasks_dir`** | `paths.tasks_dir` (config v17) is honored by the runtime, but `worc install` is config-only — it always writes `tasks_dir: tasks` and creates `tasks/{pending,…}`. An operator who renames the dir must edit `config.yaml` and create the lifecycle subfolders by hand (intentional scope cut — keeps the installer non-interactive). Next step if friction shows up: add a `--tasks-dir NAME` install flag (→ `InstallSpec.tasks_dir`) that writes the value into config **and** scaffolds the matching dirs, surviving `--reconfigure`. | `install/config_writer.py` `build_config_mapping`; `cli.py` `_install_create_dirs`/`REPO_TASK_DIRS`; configurable-tasks-dir.md | candidate |
| 2026-06-12 | **Single umbrella `upgrade`/`doctor` command (config + docs + schema migration)** | Post-upgrade currently needs two steps — `upgrade-config` (materialize new config keys) and `upgrade-docs` (refresh the installed `guide/` docs). Both resolve the install location the same way (`resolve_config_path`) and are fail-closed/idempotent/`--dry-run`. Fold them into one `upgrade` (or `doctor`) that runs both. Fold in the schema-migration remainder too: config value _transforms_ (today's config merge is add-only, not rename/restructure), and a single command that migrates **both** `config.yaml` and `state.db` at once. | `cli.py` `cmd_upgrade_config`/`cmd_upgrade_docs`; `config/upgrade.py`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-12 | **Git-native pre-commit hook** | The Stop docs-sync gate (`.claude/hooks/docs_sync_gate.py`) only covers turns inside Claude Code. For commits made outside the agent, add a real `pre-commit` hook (tracked script + `core.hooksPath`) that enforces the same "code changed ⇒ docs changed" rule. | `.claude/hooks/docs_sync_gate.py`; [.agents/rules/git-workflow.md](../../.agents/rules/git-workflow.md) | candidate |
| 2026-06-12 | **Publish to (Test)PyPI** | Distribution is currently `pipx`/`pip` from a Git tag. To allow `pipx install --pre wastech-orchestrator`, add a publish step to the release workflow using `pypa/gh-action-pypi-publish` with **trusted publishing (OIDC, no tokens)** — TestPyPI for pre-releases first. | [.github/workflows/release.yml](../../.github/workflows/release.yml) | candidate |
| 2026-06-13 | **Verify `worc` is free on PyPI before release** | The `worc` console-script alias is fine for local `pip install -e .`, but before publishing to (Test)PyPI confirm `worc` does not collide with an existing distribution or a common third-party binary; fall back to `worch` or make the alias opt-in if it does. Depends on the (Test)PyPI publish item above. | `pyproject.toml` `[project.scripts]` | candidate |
| 2026-07-06 | **Optional `worc self-update` helper** | The **optional** third phase of the [install-and-upgrade ADR](install-and-upgrade-flow.md) (short-term git-tag recipe is done; medium-term PyPI is the two rows above). A thin `cmd_self_update` that runs the correct `pipx uninstall`+`install` for a requested tag/version **with** the `[shell]` extra — delegating to `pipx` through the process seam as an **argv list, no shell interpolation**, and never touching provider/GitHub credentials. Worth building **only if git-tag installs remain the norm after PyPI lands**; if PyPI covers the common case (`pipx upgrade`/`--pre` work natively), this stays unbuilt so we don't encode the git-tag workaround as a permanent feature. | [install-and-upgrade-flow.md](install-and-upgrade-flow.md); `cli.py`; `providers/process.py` (process seam) | candidate (gated on PyPI outcome) |
| 2026-06-13 | **Defense-in-depth: forbid `--admin` in `find_forbidden_args`** | `merge_pr` builds a fixed argv that never includes `--admin` (so branch protection is respected), and a unit test asserts its absence. As extra hardening, add `--admin` (and other protection-bypassing `gh` flags) to `security/forbidden_args.py` so the prohibition is centralized and also covers any future `gh` call path / `extra_args`. | `security/forbidden_args.py`; `git_manager.py` `merge_pr` | candidate |
| 2026-06-13 | **Config-level per-node model/reasoning defaults (`node_defaults`)** | Per-task `nodes:` overrides shipped (disable-only). The complement lets operators set per-node defaults under each provider in `config.yaml` (`agents.providers.<p>.node_defaults.<node-id>.{model,reasoning}`), inserting a level between task-wide and provider-default in the resolution chain. Touches the config schema (+`schema_version` bump), loader, validation. Also listed in [README.md](README.md) "Other deferred features". | `config/{schema,loader,validation}.py`; `task/model.py` | backlog / not scheduled |
| 2026-06-14 | **Per-provider skill routing on fallback** | Mostly addressed by skills-selection-rework: discovery is now whole-repo (`git ls-files` finds `.claude/skills`, `.agents/skills`, and any other convention), and skills are attached **per node**, not the same set to every stage. Still open: the reference paths are provider-neutral but not provider-_aware_ — when a node falls back to the other provider mid-run, the surfaced skill set does not change. Next step if it matters: derive the per-node set from the resolved route (`AgentRouter` primary/fallback) so it follows the provider that actually ran. | `core/skills.py`; `routing/router.py`; `core/orchestrator.py` `_resolve_skill_layers` | backlog / not scheduled |
| 2026-06-13 | **Prompt customization deferred extras** | The prompt-customization feature shipped (later replaced by node `role_file`s). Still deferred from its original open questions: a global shared preamble across all stages, a per-stage `mode`, and authoring/managing project agent stubs (`AGENTS.md`/`CLAUDE.md`/skills) via a separate `agent_instructions:` feature. The skill-_reference_ half shipped; _authoring_ the stubs is the remaining piece (also in [README.md](README.md) "Agent instruction stubs in target repo"). **Current runtime behaviour documented (2026-06-25):** the orchestrator does not inject these files — each CLI auto-discovers them from the repo-root `cwd` (Codex `AGENTS.md`, Claude `CLAUDE.md`), and because the env allowlist forwards `HOME`, the agent _also_ reads the host operator's **global** `~/.claude/CLAUDE.md` / `~/.codex/AGENTS.md` (outside the per-task reproducibility boundary). The deferred work therefore has two parts: (a) authoring repo-scoped stubs, and (b) optionally isolating/overriding the host-global instruction layer per task. See the discovery note in [B18](../functional/blocks/B18-agent-providers.md) / [B25](../functional/blocks/B25-security-policy.md). | `core/prompts.py`; `core/skills.py`; `security/env.py` | backlog / not scheduled |
| 2026-06-14 | **`rerun` deferred extras: `--allow-done` + versioned attempt branches** | `rerun` accepts only `failed`/`manual_action_required`. Deferred: (a) an opt-in `--allow-done` to re-do an already-merged task (a foot-gun — needs a stronger confirm + the remote/PR gate); (b) a versioned-branch option (`<current-task-branch>-attempt-N`) as an alternative to the same-name reset, which would change the branch/PR naming contract and downstream assumptions. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task` | backlog / not scheduled |
| 2026-06-26 | **Windows daemon: deferred hardening (`taskkill` backstop, `CTRL_BREAK`, macOS start-time, CI matrix)** | The cross-platform `worc watch`/`stop` daemon shipped file-based on Windows (stop-file + PID-file-disappearance; `os.kill` is unusable cross-process there — see windows-cross-platform-support.md). Deferred: (a) a **`taskkill /F /PID` hard-kill backstop** so `stop` can force-terminate a _wedged_ Windows watcher (today it times out, clears the PID file, and the operator kills the survivor via Task Manager) — argv/no-shell, but it relaxes `process_control`'s no-subprocess design, so it was left out; (b) a **`CTRL_BREAK_EVENT` handler** so `Ctrl+C` in a Windows terminal triggers a graceful between-ticks stop (matches POSIX UX); (c) **macOS `_read_proc_start_time`** via `ps -o lstart=` to tighten the PID-recycling guard off Linux; (d) the **Windows CI runner matrix** (ADR change #3 — explicitly out of scope of the implementation pass). | `src/wastech_orchestrator/process_control.py` (`_can_signal`, `_stop_via_pid_file`, `_read_proc_start_time`); `cli.py` `cmd_watch`; `.github/workflows/ci.yml` | deferred / not scheduled |
| 2026-06-19 | **`fixing`-skipped → immediate `MANUAL` as a conditional flow edge** | With `fixing` skipped, the engine runs the bounded `test_fix` loop to its cap before `MANUAL` rather than going straight to `MANUAL` (terminal outcome preserved; re-tests N× pointlessly). Immediate-manual should be a conditional flow edge (`testing fail→manual` when `fixing` disabled), not engine domain knowledge. | `core/flow/engine.py`; the packaged `implementation` flow; p1-audit.md (Additional #7) | deferred / not scheduled |
| 2026-06-21 | **Codex read-only + network sandbox combo** | Codex enables sandbox network only for `workspace-write`, so a `read-only` research node that needs network (deep_research `external_research`) is not network-enabled at the Codex level. Revisit when the read-only + network sandbox combo is actually needed. | `providers/codex.py`; p3-research-audit.md | deferred |
| 2026-06-21 | **Signature/hash flow registry + `network_policy` per-host allowlist** | Deferred from P4 (security-ceiling §8): a signature/hash flow registry to verify operator-flow integrity, and a `network_policy` per-host allowlist (today a node's network grant is all-or-nothing, not per-host). The accepted P4 design decision: trust model is **file-based** — operator flows are trusted at `config.yaml` level (single owner, `.worc/flows/`); integrity is guaranteed by the ceiling + fatal validator, not provenance; a signature/hash registry for operator-flow integrity was explicitly deferred. `network_policy` uses binary grant levels (`off`/`advisories`/`research`); a per-host allowlist (so a node's network grant targets specific hosts rather than granting all-or-nothing) was deferred to a later pass. | `core/flow/{validator,registry}.py` | deferred |
| 2026-06-19 | **Full re-sync of functional-map `file:line` refs** | Deferred until P2 completion because the precise line numbers shift each slice. P2–P4 are now complete, so do the comprehensive pass: re-sync the `file:line` references in the functional map (B06/B07/system-flows) and the likec4 model against the current code. **Also covers the `Stage`-enum removal (2026-06-22):** B05/B16/B28/B06 and `flows/implementation.md` still describe `Stage`, `SKIPPABLE_STAGES`, `StageParams`/`stage_params`, `_build_stage_params`, `INVALID_STAGE_OVERRIDE`/`REVIEW_SKIP_NOT_ALLOWED`, and the `config.*_enabled` `when` guards — all deleted; re-derive them as `nodes`/`NodeOverride`/`node_overrides`/`disabled_nodes`, `validate_disabled_nodes`, `INVALID_NODE_OVERRIDE`, and node-id disable. **Also folds in the per-node `network_access` override (2026-06-22, done):** B30/B29 and B25's network note still describe network as a pure flow-wide switch (`network_access = doc.network_policy is not None`); re-derive them as `resolve_network_access(node.network_access, doc.network_policy)` — a per-node override on top of the flow-wide default. **Also folds in task dependencies (`depends_on`, 2026-06-22, done):** B16 must add the `depends_on` front-matter field + the `INVALID_DEPENDS_ON` reason (and drop the stale `stages`/`StageParams`/`SKIPPABLE_STAGES` text above); B01/B06 must describe `watch_once`'s eligibility skip/terminal-reject and `cmd_run`'s refusal (`Orchestrator.dependency_eligibility`/`reject_dependency`); B07/B08 the read-only `GitManager.pr_merge_state` probe + the armed→merged `backfill_merge_sha` (SQLite publish op only; ledger keeps `"armed"`). **Also folds in operator-authored decomposition (`subtasks:`, 2026-06-22, done):** B16 must add the `subtasks` front-matter field + the new reasons (`invalid_subtasks`, `invalid_subtask_path`, `subtask_file_missing`, `subtask_malformed`, `subtask_count_out_of_range`, `subtask_depends_forward`, `flow_cannot_decompose`); B06 must describe the two decomposition sources (agent-proposed vs operator-authored) collapsing into the same execution region — `Orchestrator._validate_operator_subtasks` (pre-slot reject) + `_persist_decomposition` materialized at preflight, `decide_operator_decomposition`, and `SubtaskSpec.spec_body` (verbatim spec). | [docs/functional/index.md](../functional/index.md); `docs/functional/blocks/`; `docs/likec4/` | candidate |
| 2026-06-22 | **Flow-engine: `Stage`-enum-removal minor follow-ups** | After the `Stage`-enum identity removal, three minor leftovers remain: (a) `hitl.node_interaction_path` (the standalone `hitl` gate, `node-<id>.json`) is now near-redundant with the node-keyed `interaction_path` — could unify; (b) ~~the `Stage` enum still physically lives in `providers/base.py` though it is no longer part of the provider contract — could relocate to `config`/`core` for cohesion~~ — **subsumed by the full enum-deletion task above** (the enum is deleted outright, not relocated); (c) ~~the supervisor calls `create_attempt_dir` with `node_run_id=0` every turn, so a 2nd+ observe/finalize collides on `stages/supervisor/run-000000/` (FileExistsError, swallowed by the advisory guard)~~ — **done (2026-06-24, ion-list remediation):** each `observe` turn now uses the observed step's `node_run_id` (finalize keeps the `0` sentinel), so successive turns write distinct `run-NNNNNN/` dirs; a failed observation is recorded with `observation_failed=true` (distinct from an empty note). | `core/flow/{wiring,observability}.py`; `providers/base.py`; `core/supervisor.py`; 2026-06-21-audit-remediation.md | (a) minor / backlog; (b)(c) done |
| 2026-06-22 | **Deduplicate the `.worc` home literal (`_WORC_HOME`)** | Audit #23 (benign — values match): `core/orchestrator.py` `_WORC_HOME` re-declares `cli.WORC_HOME = ".worc"` to avoid a circular import (the core must not import the CLI). To resolve: move `WORC_HOME = ".worc"` to a genuine leaf module that both import, verifying mypy reports no new import cycle. | `cli.py` `WORC_HOME`; `core/orchestrator.py` `_WORC_HOME`; 2026-06-21-audit-remediation.md (#23) | backlog / not scheduled |
| 2026-06-23 | **Monorepo checks: deferred sub-items (exclude-globs, per-command timeout, external-CI delegation)** | Left out of the first checks-monorepo pass (intentional): (a) **exclude-globs** so e.g. editing `ios/docs/**` does not trigger the iOS build (today selection is include-only at set granularity); (b) **per-command timeout** (today the override granularity is the set via `command_sets.<name>.timeout_seconds`); (c) **external-CI / remote-runner build delegation** — the orchestrator is single-host (checks run as a local subprocess in the clone), so a polyglot monorepo needs every toolchain on the host (iOS⇒macOS+Xcode, Android⇒SDK/JDK, .NET⇒dotnet). `skip_if_unavailable` (Р4) is the host escape-hatch; true delegation of the build to CI is orthogonal. | checks-monorepo.md (§"Масштабирование"/"Оставлено на потом"); `checks/selection.py`; `check_runner.py` | deferred / not scheduled |
| 2026-06-23 | **`upgrade-flows`: re-sync the seeded `.worc/flows/` to the packaged version** | `install` now seeds editable copies of the built-in flows + their shared `roles/` prompts into `.worc/flows/` (`_copy_packaged_flows`), and — since the packaged-delivery-only change made `.worc/flows/` the **sole** resolution source (no runtime packaged fallback) — a package upgrade that ships a **new** built-in flow leaves it not merely un-refreshed but **unrunnable** until it is delivered there. This raises the priority of a proper re-sync. Today the only refresh path is `install --reconfigure`, which snapshots the dir to `flows.bak-<UTC>` (`_backup_flows_dir`) and then overwrites **all** packaged-named files, discarding operator edits to the built-ins. Add an `upgrade-flows` (or fold it into the umbrella `upgrade`/`doctor` in the row above) that does a smarter refresh: update only the built-in files the operator has not edited, report drift on the ones they have, and never touch custom operator flows. Mirror `upgrade-docs` (`resolve_config_path`, fail-closed, idempotent, `--dry-run`). **Acceptance case (from the P4 prompt-quality plan §0):** the T1 fix — packaged `review.md` had moved ahead of the seeded target copy (empty-`findings`-array contract) and had to be pulled by hand — is exactly the "packaged advanced → offer to update the unedited block" scenario `upgrade-flows` must reproduce automatically. | `cli.py` `cmd_install`/`_copy_packaged_flows`/`_backup_flows_dir`; [operations.md](../operations.md#upgrading-the-orchestrator) | candidate |
| 2026-06-23 | **`install`-able task-authoring skills (`worc-task`, `worc-deco-task`)** | Shipped two copy-ready Claude Code skills under `skills/` that convert raw user tasks into the orchestrator task format (single task) and operator-authored decomposition (root + subtask specs). For now they are reference templates the user copies into `~/.claude/skills/` or a project `.claude/skills/` by hand. Next step: a dedicated CLI command (e.g. `worc install-skills [--global]`) that materializes them into the right skills location — mirror `cmd_install`/`upgrade-docs`, which already resolve install locations and copy packaged assets. Decide packaging (ship `skills/` inside `src/wastech_orchestrator/packaged/` like the other installed assets vs. copy from the repo). | `skills/`; `cli.py` `cmd_install`/`cmd_upgrade_docs` | candidate |
| 2026-06-26 | **🔴 td-be-003 run remediation (first Windows real run): HITL must RESUME the agent session, not recreate it (CRITICAL); empty `command_sets` ⇒ vacuous `testing`; cross-stack task in backend-only flow; review didn't block unmet criterion/destructive migration; no HITL log signal; Windows nuances** | From a post-mortem of `td-be-003-conform-m1-m2-contract-shapes` in `argudebate` (docs/analysis/td-be-003-conform-m1-m2-contract-shapes-run-analysis.md) — clean run (`done`, PR #5 later operator-merged, 0 fix cycles), but `done` over-certifies. **🔴 P0 CRITICAL — operator directive:** HITL **recreates** the agent session instead of resuming it — `planning` ran twice with **different** `result.session_id` (`c8162f4f92ff`→`c5d799a3aef0`), full re-plan, answer delivered as a context file not as conversation continuation. Requirement: regardless of node/timing, after the operator replies the agent MUST continue the SAME session from the point of the Telegram request. Lever: `core/flow/nodes/agent.py` — thread the first run's `session_id` into the 2nd `_invoke` at `:132` (and `_reconsider` `:373`), lifting the `_resume_session_id` "editing_lineage-only" gate (`:491-508`) for the HITL re-invoke so resume works for ANY `session_scope`/node; provider resume already exists (`providers/{claude,_adapter_base}.py`), keep the provider gate (no claude-session-on-codex). Test: 2nd run's `session_id` == 1st. **P0:** (1) target `checks.command_sets: {}` ⇒ `testing` vacuous pass (`check_runs=0`) — add a `dotnet build src/Chat1o1.sln` set (config `checks.command_sets`; `config/schema.py`); (2) cross-stack `/contract-change` task routed to backend-only `implementation_backend` flow whose role forbids `mobile/` ⇒ acceptance criterion #6 (atomic swagger+api-client) structurally unmet, diff backend-only — add/route a cross-stack `task_type`/flow or fix the contradictory criterion. **P1:** (3) `review` accepted with a known-unmet criterion + destructive migration (drops `messages.body` NOT NULL without backfill) as medium not blocking — `roles/implementation_backend/review.md` make those blocking; (4) no console/log line while blocked on HITL approval (2.5-min silent gap) — emit a log line/heartbeat on `human_input` wait (`core/orchestrator.py`/`core/hitl.py`; relates to 2026-06-24 row (c)). **P2:** avoidable HITL on well-specified tasks (planning approval); dangling `subtask  of  …` placeholders when not decomposed (`roles/implementation_backend/{implementation,fixing}.md`); enable `prompt_audit`. **Windows:** `worc` not on PATH (ran `python -m wastech_orchestrator`); Windows env allowlist worked (no `0xC0000409`); `state.db.source_path` stored backslash-style (verify `as_posix()` normalization in `source_path` comparisons); spurious empty dir listing on the `I:` drive. Analysis only — no code/config changed. | td-be-003-conform-m1-m2-contract-shapes-run-analysis.md; `core/flow/nodes/agent.py`; `providers/{claude,_adapter_base}.py`; `config/schema.py`; `core/flow/registry.py`; `roles/implementation_backend/{review,implementation,fixing}.md`; `core/{orchestrator,hitl}.py`; `security/env.py` | candidate (P0 CRITICAL: HITL session resume) |
| 2026-06-26 | **HITL-wait observability & not-decomposed prompt cleanup (td-be-003 findings 5+6)** | Dedicated task carved from the td-be-003 run. **P1:** a node blocked on `human_input` emits no console/log line or heartbeat — operator sees a silent gap (td-be-003: 2.5-min silence, request only in Telegram); emit a log line + heartbeat on the wait and on resolution (`core/hitl.py` / `core/orchestrator.py` ask path + observability). **P2:** when a task is not decomposed the implementation/fixing roles render a dangling "subtask of … spec:" sentence (empty placeholders); make the subtask clause conditional in `roles/implementation_backend/{implementation,fixing}.md` or omit it in the prompt assembler (`core/flow/nodes/agent.py` `_prompt_variables` / `core/flow/prompt.py`). Full design + acceptance in the detail file. | hitl-wait-observability-and-prompt-cleanup.md; td-be-003-conform-m1-m2-contract-shapes-run-analysis.md; `core/hitl.py`; `core/orchestrator.py`; `core/flow/nodes/agent.py`; `roles/implementation_backend/{implementation,fixing}.md` | open — P1/P2 |
| 2026-06-28 | **Clean cooperative per-task cancel** | The console's `cancel` de-queues a pending file or routes an active task to the stop ladder (`down` soft / `--force-full` hard). A richer cancel that unwinds the flow and marks the task `cancelled` **without** a hard group-kill (no new status today) is deferred — distinct from `--force-full`'s "kill now, rely on `resume()`". From the cli-upgrade ADR. **Hook (2026-07-06):** the reliable-stop ADR added `ErrorClass.CANCELLED` + the router's `is_cancelled` seam (checked before fallback) and a `CANCELLED → _park` branch — an in-stage cooperative cancel would reuse exactly this so a mid-stage kill neither respawns a fallback nor reports a failure. | `core/orchestrator.py`; `core/state_machine.py`; `routing/router.py`; `cli.py` | backlog / not scheduled |
| 2026-06-28 | **HITL / gate answer in the console** | `worc top`/`worc shell` only **surface** a pending durable gate (e.g. the max-turns `turn-gate-*.json` "awaiting operator"); Telegram stays the answer channel. Bridging the HITL/confirmation-gate prompts into the console prompt (answer, not just show) is a separate feature with its own routing/timeout semantics. From the cli-upgrade ADR. | `cli_shell.py`; `core/hitl.py`; `notify/telegram.py` | backlog / not scheduled |
| 2026-06-28 | **Textual TUI for the console** | The first-cut console is `prompt_toolkit` + `patch_stdout` (live log above a prompt). A full panel/pane/mouse/hotkey TUI (Textual) is the deferred richer option if operators want it. From the cli-upgrade ADR. | `cli_shell.py`; `pyproject.toml` (`[shell]` extra) | backlog / not scheduled |
| 2026-06-28 | **Daemon control socket / IPC** | The console drives the daemon by read-only `state.db` polling + log tailing + file enqueue + the stop ladder. A Unix-socket/IPC channel to push commands into a _running_ task (beyond enqueue + signals) is YAGNI for now; revisit if richer live control is needed. From the cli-upgrade ADR. | `cli_shell.py`; `process_control.py` | backlog / not scheduled |
| 2026-06-28 | **`worc top` raw single-key quit** | `top` quits on `q` **then Enter** (a blocking `readline` reader thread — platform-neutral, no `termios`/`msvcrt`). A raw single-key reader (bare `q`, arrow scrolling) is a deferred nicety. From the cli-upgrade ADR. | `cli.py` (`_stdin_quit_watcher`, `_run_top_loop`) | backlog / not scheduled |
| 2026-07-01 | **Memory V1 complete — post-V1 work (real eval baseline, gated V2/V3/V4, `worc memory add/edit`, functional-map block)** | The persistent repo-scoped memory subsystem is **fully implemented and merged to `main`** (phases 01–05 + audit remediation, PR #14 / commit `e7aa47a`): store + config, write funnel, read packets, curation, and the safety drills + offline-replay harness. Post-V1 items, none blocking: (a) **record a real eval baseline** — the harness (`tests/eval/harness.py`) and the baseline doc are in place but the numbers are **synthetic** (greenfield: no production task corpus yet); replay real tasks (memory-off vs on vs on-without-entity), record `TaskMetrics`, and **tune the provisional Q1 cleanup-budget / Q3 promotion / Q5 packet-cap integers** against the measured result (the approach is locked, only the integers move). (b) **V2 (SQLite + FTS) / V3 (embeddings) / V4 (entity graph)** stay **gated behind a measured recall/quality lift** (AC-O4 / `Comparison.measured_lift`) — do not ship any of them without the baseline showing the simpler layer measurably fails (blueprint §9/§10). (c) **`worc memory add/edit`** — a curated add/edit verb (V1.x); for now hand-editing the `*.jsonl`/`*.md` files is the authoring path alongside `show/validate/compact/restore`. (d) **functional-map memory block** — the subsystem is documented in `docs/backlog/memory/` + `configuration.md` + `operations.md` but has no `docs/functional/blocks/` block yet; fold it into the existing deferred "Full re-sync of functional-map" row. Per-phase V1 scope cuts are in the phase 02/03/04 rows below. | memory/index.md; memory/plan/05-safety-and-evaluation; `tests/eval/`; `tests/memory/test_memory_*_drill.py` | candidate |
| 2026-07-01 | **Memory curation (phase 04) — V1 scope cuts** | The curation path shipped (`DerivedIndex` + `CleanupJob` + `worc memory` CLI + idle-gap hook). Intentional V1 simplifications to tighten later: (a) **staleness is path-only** — `CleanupJob` reconciles **both entities and long-term lessons** by **path** existence (a lesson scoped to a since-vanished path is quarantined, never deleted — F2 existence half resolved 2026-07-01, `_reconcile_lessons`). Still deferred to V2: staleness on a vanished **symbol** (`DerivedIndex.symbol_exists` is built but unwired — F6), and the design §5 **2× explicit contradiction** rule — there is no contradiction ledger in V1, so a lesson contradicted by current reality is not auto-quarantined and **lessons can still accrete contradiction-rot** until a human curates them. Revisit when the steady-state `watch`-a-queue scenario shows lessons drifting from reality (the ADR's headline failure mode). (b) **rename-remap is same-basename only** — a moved file is remapped when exactly one tracked path shares its basename; the design's **content-signature** remap is not built (ambiguous/multi-candidate moves quarantine, fail-closed). (c) **duplicate-merge is within a single tier file** by normalized subject (keeps the first, unions evidence/seen-tasks) — it does not merge across kinds or against quarantine, and the richer §5 merge (oldest-id/newest-wording reconciliation) lives only on the write path. (d) **`DerivedIndex` has no FTS/embeddings/repo-map** (V2+, NFR3) and rebuilds its tracked-set per pass (no incremental cache invalidation) — fine at current scale, revisit for very large monorepos. (e) **the idle hook rate-limits in-memory** (a per-daemon `last-run` monotonic clock), so a restarted daemon may run one extra pass sooner than `cleanup_min_interval_s`; a persisted last-run stamp would make it durable. (f) **`worc memory` has no `add`/`edit` verbs** (hand-editing the `*.jsonl`/`*.md` stays the authoring path; a curated add is a possible V1.x). All additive; none changes the AC-gated behavior (snapshot-before-batch, never-promote, never-edit-code, quarantine-never-delete, audited). | plan/04-curation; `memory/{derived,cleanup,service}.py`; `cli.py` `cmd_memory`/`_build_cleanup_hook`/`watch_loop` | candidate |
| 2026-06-30 | **Memory read path (phase 03) — V1 scope cuts** | The read path shipped (deterministic `PacketBuilder` → `{memory_path}` node-driven injection → packaged role-prompt references). Intentional V1 simplifications to tighten later: (a) **stage-2 semantic rerank is not built** — V1 ships the deterministic stage-1 filter only; the rerank is V3, gated by the offline replay baseline (design §6 / AC-O4). (b) **symbol-scoped retrieval is build-capable but unwired at runtime** — `PacketContext.touched_symbols` is honored by `build()` but the node runner only sources `touched_paths` (from `git changed_code_paths_since_base()`); a symbol source (the `DerivedIndex` symbol index) wires in with phase 04. (c) **planning ranks without touched paths** — before the first commit the base-inclusive diff is empty, so a planning packet ranks by trust/recency/node-scope only (no path overlap); acceptable (precision-first), revisit if planning recall suffers. (d) **the ~15-bullet cap is implicit** — only `packet_max_lines` is enforced as the backstop; one bullet per record + the record caps keep bullets bounded, so no separate bullet knob was added (YAGNI). (e) **per-node weighting is fixed, not config-driven** — `implementation` gets `+2` entity cards and `review`/`fixing` prefer reviewer-kind lessons via ranking; if operators need to tune these, lift them into `MemoryConfig`. (f) **packet caps are provisional** (Q5 / AC-R2 `[refine]`) — tune `packet_max_*` against the eval baseline. All additive; none changes the AC-gated behavior (path-only injection, determinism, caps, empty-state). | plan/03-read-path; `memory/packet.py`; `core/flow/nodes/{agent,base}.py`; `core/flow/wiring.py`; `core/orchestrator.py` `_packet_builder` | candidate |
| 2026-06-30 | **Memory write path (phase 02) — V1 scope cuts** | The write path shipped (candidate-delta parser → `apply_delta` funnel → tier persistence + quarantine → best-effort `evaluations` marker → supervisor emit on the finalize turn + deterministic failure seam). Intentional V1 simplifications to tighten later: (a) **entity-card path existence is now validated at write time** (F1, resolved 2026-07-01) — `apply_delta` resolves each entity card's `paths` against the live repo via an injected `DerivedIndex.path_exists`; an unverifiable path downgrades the card off `repo-observed` → quarantine (best-effort, fail-closed). Still deferred: **symbol** existence at write time (`DerivedIndex.symbol_exists` is built but unwired — F6), and lesson `scope.paths` existence, which is reconciled by `CleanupJob` (F2), not the write path. (b) **contradiction-detection + stable-hotspot promotion gates are V1-off** (`should_promote(has_contradiction=False, stable_hotspot=False)`) — no conflict-quarantine against contradicting active memory yet. (c) **entity merge is a wholesale upsert** by `entity_id` (latest card wins), not a field-union. (d) **`short_term/runs/<task-id>/episode.json`** per-task copy and the **`entities/index.md` / `aliases.json`** derived views are not written (canonical `recent.jsonl` / `entities.jsonl` suffice; aliases live in the card). (e) **episode enrichment** — the per-task episode stores id/task/trust/outcomes/artifact-dir only, not touched paths/symbols or review findings from the diff. (f) the orchestrator now feeds `MemoryService(extra_secrets=…)` the known secret literals (F3, resolved 2026-07-01) — `secret_env_values` (values of non-allowlisted, secret-named parent env vars) + `read_denied_secrets` (the repo's `.env` / `secrets/**`), the same sources the provider adapters scrub from artifacts — so a repo-specific secret value that matches no structural token shape is still redacted (closes the structural-only gap against C1). All additive; none changes the AC-gated behavior (redaction, trust gating, promotion thresholds, audit). | plan/02-write-path; `memory/{service,lifecycle,delta,audit}.py`; `core/{supervisor,orchestrator}.py` | candidate |
| 2026-07-04 | **Live-behavioral confirmation of Claude `read-only` = `default` mode + `Read,Glob,Grep` allowlist blocking Edit/Write** | Run-quality/gating hardening (F19/F20/F21/F22, ADR) implemented F21 by switching Claude's `read-only` profile from `--permission-mode plan` to `default` + an `allowedTools` gate lacking `Edit`/`Write`, so a clarifying question surfaces through the role's `human_input` structured field instead of the CLI's plan-mode UX (`AskUserQuestion`/`ExitPlanMode`). Verified so far: unit tests assert the built argv (`--permission-mode default`, no `Edit`/`Write` in `--allowedTools`) — the ADR's Open Q2. **Not yet verified**: that a real `claude` CLI process actually refuses a write under `default` mode with that allowlist (no live smoke test run, unlike F19's codex 0.139.0 smoke test). Next step: a one-off live smoke run (prompt the model to edit a file under the read-only profile) before leaning on this as a hard security boundary rather than a routing nicety. | run-quality-gating-hardening.md Open Q2; `src/wastech_orchestrator/providers/claude.py` `_PROFILE_MAP`; `tests/providers/test_claude_command.py` | candidate |
