# Implementation follow-ups — archived history

Frozen historical record of **completed and superseded** entries from [follow_ups.md](../follow_ups.md), as of 2026-06-22. The active tracker now holds only open items.

These rows are kept for traceability. **Do not treat this as a source of truth** — the [Functional Map](../../functional/index.md) is the code-derived reference. Links pointed at the archived design docs under outdated/ (now deleted).

The five "accepted" design rows (Session persistence, Workflow execution foundation, Hybrid agent testing, Supervisor quality-gate) and the flow-engine unification row are archived here because they are now **realized** by the completed flow engine (P1–P4); their statuses below are preserved as originally recorded.

| Date | Item | Context & next step | Referenced in | Status |
| --- | --- | --- | --- | --- |
| 2026-06-22 | **Flow-engine: delete the `Stage` enum (per-task skip → flow node ids)** | **Done (2026-06-22).** The `Stage` enum is deleted; per-task skip is re-founded on the resolved flow's node ids. Front-matter is now `nodes: <id>: { enabled: false }` (`StageParams`→`NodeOverride`, `stage_params`→`node_overrides`, `disabled_stages`→`disabled_nodes`); the gate validates **shape only** (`INVALID_NODE_OVERRIDE`; `REVIEW_SKIP_NOT_ALLOWED` deleted); node existence **and** skip-outcome routing soundness are checked at flow resolution by `validate_disabled_nodes` (unknown id / stranded skip → terminal `failed` before any side effect — the optional routing-soundness hardening was **shipped**, not deferred); the engine skips by node id (`FlowEngine.disabled_nodes`); the `config.*_enabled` facts and the `implementation.yaml` `when` guards are removed; `agents.allow_review_skip` + `SKIPPABLE_STAGES` + the `review`/auto-merge special-case are deleted (config **v13**, old key tolerated + stripped by `upgrade-config`). Operator owns which nodes are safe to disable — no `skippable` guard. Closes sub-point (b) of the minor-follow-ups row. ruff + mypy + full pytest green. | stage-enum-removal.md; `providers/base.py`; `task/{model,parser,validation_gate}.py`; `config/{schema,loader,upgrade}.py`; `core/orchestrator.py`; `core/flow/{engine,engine_driver,validator}.py`; `core/flow/packaged/implementation.yaml` | done |
| 2026-06-12 | **Wire Telegram HITL into stages** | **Resolved (2026-06-13):** `refinement`/`planning` now emit strict typed human-input signals; the Core persists a durable handle/answer artifact and repeats the same stage through `human_input_path`. Deletion/dependency diffs are approved before tests, with exact planning reuse and one denial reconsideration. Timeout/transport/ambiguity/repeated signals fail closed without adding a task status. | [telegram.md](../../telegram.md); `core/hitl.py`; `core/dangerous_diff.py` | done |
| 2026-06-12 | **Broken canonical-spec links** | **Resolved (2026-06-13):** the canonical location was `docs/implementation_stages/00_orchestrator_final_plan.md`; root instructions, README, rules, backlog documents, and internal spec links targeted that path. **Superseded (2026-06-16):** the `implementation_stages/` docs were removed and all links repointed to the [Functional Map](../../functional/index.md) and current docs — see the 2026-06-16 row below. | CLAUDE.md, AGENTS.md, README.md, docs/\*\* | done |
| 2026-06-12 | **CI test gate on PRs/pushes** | ~~Only a _release_ workflow exists (runs on `v*` tags).~~ **Resolved (2026-06-13)**: added [.github/workflows/ci.yml](../../../.github/workflows/ci.yml) running `ruff check` + `ruff format --check` + `mypy src` + `pytest` on pull requests and pushes to `main` (matrix: Python 3.12 / 3.13), mirroring the release gate. | [.github/workflows/ci.yml](../../../.github/workflows/ci.yml) | done |
| 2026-06-12 | **Terminal artifacts trail by one audit commit** | ~~Move + final artifacts landed in the _next_ task's commit.~~ **Resolved**: finalize now moves the task to `done/`/`failed/` and writes `<id>.summary.md` **before** the commit, so the task + summary ride the task's own `tasks/` commit (§8.3). `logs/` (incl. `terminal-cleanup.json`, `summary.json`) is intentionally never committed. | spec §8.3, §21.3; `core/orchestrator.py` `_finalize_task_artifacts` | done |
| 2026-06-13 | **Session persistence across restarts** | **Design accepted and split (2026-06-14):** persist a per-unit `implementation`/`fixing` editing lineage rather than the provider-wide `_Pipeline.session_ids` map; add Claude/Codex resume parity, provider-aware fallback requests, atomic persistence/recovery, raw-ID redaction, and explicit affinity availability policy. Schema migration uses the next available version (v3→v4 only if implemented first). | durable_sessions_and_fixing_affinity.md; `core/orchestrator.py`; `routing/router.py`; `providers/{claude,codex}.py`; `state_store.py`; spec §6 | accepted |
| 2026-06-12 | **`.gitignore` for in-repo runtime files** | **Resolved (2026-06-13):** `init`/`install` (in-repo footprint) append the runtime files (`state.db`, `state.db-wal`, `state.db-shm`, `config.yaml`, `config.yaml.bak-*`, `orchestrator.pid`) to `.git/info/exclude` by default, or `.gitignore` with `--gitignore-tracked`, via `git_manager.append_runtime_excludes`. **Extended (2026-06-14, post-test-run §3.1):** added the generated `checks/` profile dir (it had leaked into a real run's commit) to `EXCLUDED_DIRS`, `_local_only_dirs` (every footprint), and `RUNTIME_GITIGNORE_LINES`; the Git Manager now guarantees these ignores in the clone it runs in (`ensure_runtime_excludes`, called at branch-prep). The local-`.git/info/exclude` default was kept deliberately (no forced tracked-`.gitignore` mutation of the operator's repo). | `cli.py` `cmd_init`/`cmd_install`; `git_manager.py` `append_runtime_excludes`, `ensure_runtime_excludes`, `_EXCLUDED_FILES`, `EXCLUDED_DIRS` | done |
| 2026-06-13 | **PID-recycle robustness for `stop`/`restart`** | **Resolved (2026-06-22, audit #14):** the PID file is now JSON `{pid, start_time}`; `is_running`/`running_daemon_pid`/`stop_process` match the recorded start-time so a recycled PID never reads as our daemon. Start-time is read from Linux `/proc/<pid>/stat` (CI + production); non-Linux POSIX degrades to a bare-PID probe (the no-shell-out invariant forbids `ps`). | `process_control.py` `is_running`/`running_daemon_pid`/`stop_process`/`_read_proc_start_time`; `cli.py` `cmd_watch`/`cmd_rerun`/`cmd_finalize` | done |
| 2026-06-13 | **Consolidate `install/detect.py` onto `checks/detect.py`** | ~~The installer wizard used the older first-match `install/detect.detect_checks` for its quick ecosystem hint, while the richer detector lives in `checks/detect.py` (`CheckCandidateDetector`).~~ **Resolved (2026-06-22, audit #12):** `detect_checks`/`_node_checks` deleted; the wizard now seeds defaults through `checks.detect.propose_default_commands(repo_root)`, which runs the same lockfile-aware `RepositoryInspector`→`CheckCandidateDetector` the runtime resolver uses and renders the highest-confidence candidate per logical check to a shell string via `shlex.join`. One ecosystem-detection source of truth; the installer can no longer seed a command the resolver disagrees with. | `install/detect.py`; `checks/detect.py` `propose_default_commands`; `install/wizard.py` `_resolve_checks` | done |
| 2026-06-13 | **Per-task opt-out of a global stage skip (`force_stages`)** | ~~Stage-skip resolution is a strict union (`effective_skip = agents.skip_stages ∪ task disabled`); a task cannot re-enable a globally-skipped stage.~~ **Moot (2026-06-19):** the global `agents.skip_stages` list was removed (config v10) — stage-skip is now **per-task only** (`stages.<stage>.enabled: false`), so there is no global skip to opt out of and no union/precedence problem. | `core/orchestrator.py` `effective_skip`; `task/model.py` `disabled_stages` | resolved (no longer applicable) |
| 2026-06-13 | **Agent check discovery at `preflight`; deeper CI parsing** | ~~Agent fallback runs only at `install`; `preflight` resolves deterministically.~~ **Resolved (2026-06-14, post-test-run §1.2):** discovery now runs inside the state machine at task start (`checks.discovery.run_at_task_start`), so `auto` mode resolves/agent-assists in-band (gated on mode + `agent_fallback` + a configured `model`); re-resolution is proof-only (launch failure / fingerprint / low confidence) and a changed command set is human-approved. Still open: deeper CI parsing — discovery treats CI workflow files as presence-only evidence; parse GitHub Actions/others to derive the real PR gate, and settle the §17 open decisions (confidence threshold, wrapper-vs-native precedence). | [configuration.md](../../configuration.md) (`checks.discovery`); `checks/resolver.py`; `core/orchestrator.py` `_check_preflight`; `checks/inspect.py` `_ci_workflows` | partially done (CI parsing: candidate) |
| 2026-06-13 | **Prompt customization v1 shipped; deferred extras** | **Resolved (2026-06-13):** the `prompts:` block (append/replace, strict/fallback, allowlisted path variables, redacted `rendered-prompt.md` audit artifact) is implemented; packaged `templates/prompts/<stage>.md` are now the single source of truth and `_STAGE_PROMPTS` was removed. **Deferred** (the backlog doc's §10 open questions): a global shared preamble across all stages, per-stage `mode`, and managing project agent stubs (`AGENTS.md`/`CLAUDE.md`/skills) via a separate `agent_instructions:` feature. **Update (2026-06-14):** the _skill-reference_ half of that idea shipped — planning now selects target-repo skills and surfaces them to downstream stages as read-only paths (`{skills_path}`, `skills:` config block, `core/skills.py`; post-test-run §2.1/§2.2). Still deferred: _authoring/managing_ the stubs themselves, the shared preamble, and per-stage `mode`. | `core/prompts.py`; `core/skills.py`; `config/{schema,loader,validation}.py`; `core/orchestrator.py` | done (extras: backlog / not scheduled) |
| 2026-06-14 | **Re-run a terminal task (`rerun` command)** | ~~No first-class way to re-run a terminal task.~~ **Resolved (2026-06-14):** `worc rerun <id>` ships with two modes — **fresh** (reset branch to base, clear per-attempt state incl. `publish_operations`/`subtasks`, archive prior artifacts to `attempt-<N>/`, drive from the start) and **`--continue`** (reuse the branch, re-enter at the failed stage via the resume engine; `state.db` v3 `tasks.interrupted_status`). Admits the id once via `is_recovery_rerun`; refuses on a live daemon / busy slot / dirty tree / (fresh) prior remote branch+PR; ledger `attempt`/`rerun_of`. | `cli.py` `cmd_rerun`; `core/orchestrator.py` `plan_rerun`/`rerun_task`/`continue_task`; `state_store.py` reset/revive + `interrupted_status`; `git_manager.py` `reset_branch_to_base` | done |
| 2026-06-14 | **Separate `install-templates` command (deliver/refresh the `templates/` tree add-missing-only)** | ~~Only `init` copies the packaged `templates/` tree; `install` never does, so it drifts.~~ **Resolved (2026-06-14):** `worc install-templates` ships — resolves the install dir like `upgrade-docs` (`--config` → `./config.yaml` → registry), delivers the tree beside `config.yaml` **add-missing-only** (absent → write; present → skip to preserve edits; `--force` overwrites; `--dry-run` previews), **never removes** operator-added files (the deliberate asymmetry with `upgrade-docs`), and never touches `config.yaml`/`prompts.overrides`. `init` + `install-templates` share `cli._copy_templates_tree` (parity-tested). **Update (2026-06-14):** config-relative `templates_dir` resolution and the `--only prompts\|skills` selector are **resolved/moot** via the prompt-templates simplification (prompts-only tree; templates_dir anchored to `config.yaml`) — see the row below. Still deferred (`candidate`): umbrella `upgrade` membership. | `cli.py` `cmd_install_templates`/`_copy_templates_tree`/`cmd_init`; `tests/test_cli_install_templates.py` | done |
| 2026-06-14 | **Manually finalize a task (`finalize` command)** | ~~No operator-facing way to reconcile a task the human handled out-of-band.~~ **Resolved (2026-06-14):** `worc finalize <id> --as <done\|failed\|abandoned>` records + tidies (status, terminal cleanup, file move, HITL close, a `manual` ledger record) **without running the pipeline or committing/pushing/PR-ing**. PR-URL provenance (`--pr-url` > recorded publish-op > none+warn); default read-only `gh pr view` merge check; fail-closed on a dirty tree; refuses while the daemon is live or on a re-finalize; `abandoned` = `manual_action_required` + `outcome: abandoned`. | `cli.py` `cmd_finalize`; `core/orchestrator.py` `plan_finalize`/`finalize_task`/`_relocate_task_file`; `git_manager.py` `verify_pr_state`/`delete_branch`; `hitl.py` `consume_pending_interactions`; `ledger.py` `LedgerRecord` | done |
| 2026-06-14 | **Simplify prompt templates: deliver prompts-only + auto-detect resolution (drop `overrides`)** | ~~Decided 2026-06-14 (operator).~~ **Resolved (2026-06-14):** shipped. **(A)** `init`/`install-templates` now deliver **only** `templates/prompts/` — `skills/`, `AGENTS.md`, `CLAUDE.md`, `task.md` were deleted from the packaged source. **(B)** A `<stage>.md` present in `templates_dir` is auto-detected by file presence (no `overrides` map); packaged defaults are a per-stage **fallback**; `mode` defaults to **`replace`**; **`prompts.overrides` and `prompts.strict` are removed** (legacy keys tolerated fail-open with a warning, stripped by `upgrade-config`); a relative `templates_dir` resolves from the `config.yaml` directory; `""` forces packaged defaults; `schema_version` bumped **5 → 6**. **(C)** `worc/examples/task-rich.md` rewritten as a maximal example (every front-matter field); `decision-guide.md`/`best-practices.md` gained `pr_title` coverage. Subsumes the deferred "config-relative `templates_dir`" and resolves the install-templates design doc "Scope of the tree" (prompts-only; the `--only prompts\|skills` selector is moot). | `core/prompts.py`; `config/{schema,loader,validation,upgrade}.py`; `cli.py` `cmd_upgrade_config`; `src/wastech_orchestrator/templates/`; `src/wastech_orchestrator/worc/`; tests under `tests/{core,config}/` + `tests/test_cli_install_templates.py` | done |
| 2026-06-14 | **Known gap (deep-research): Codex session continuity** | **Research resolved (2026-06-14):** current Codex CLI supports `codex exec resume <SESSION_ID>` and JSONL exposes `thread.started.thread_id`. The remaining adapter/persistence/redaction implementation is tracked by the durable-sessions backlog item. | durable_sessions_and_fixing_affinity.md; [official Codex CLI reference](https://developers.openai.com/codex/cli/reference#codex-exec); `providers/codex.py`; spec §6 | done |
| 2026-06-14 | **Agent quality + continuity design split** | **Resolved (2026-06-14):** the combined doc 20 design record was split into three independently implementable backlog contracts with a shared ownership/dependency map. Low-level invocation/audit plumbing may be reused; supervisor and testing remain separate domains; sessions/affinity owns editing lineage. | [backlog program context](../README.md#agent-quality-and-continuity-program); durable sessions; hybrid testing; supervisor | done |
| 2026-06-14 | **Workflow execution foundation** | Add a narrow prerequisite for all four planned changes: transitional built-in `implementation-v1` selection, immutable resolved-profile recovery identity, execution role/session/output vocabulary, and shared audit/path/delta primitives. Preserve current behavior in the foundation release, then support a one-way cutover to required-supervisor `implementation-v2` without permanent dual-profile execution. | workflow_execution_foundation.md; task workflow profiles; durable sessions; hybrid testing; supervisor | accepted |
| 2026-06-14 | **Hybrid agent testing** | Add an optional once-per-unit workspace-writing testing agent before deterministic checks. Persist an idempotent checkpoint, validate only its test-owned diff delta, keep the approved resolved profile immutable, and leave `CheckRunner` exit codes authoritative. | hybrid_agent_testing.md; `core/orchestrator.py`; `check_runner.py`; `checks/agent.py`; `state_store.py`; `templates/prompts/testing.md` | accepted |
| 2026-06-14 | **Supervisor quality-gate** | Add a read-only, fresh-session evaluator with bounded structured findings. Persist verdict/application separately, count every applied rework in local and global budgets, and leave all transitions in the Core. **Decisions 2026-06-15:** supervisor is mandatory with no `enabled` field; required implementation/fixing verdicts fail closed; rollout is a one-way cutover to sole executable `implementation-v2`; when summary output is enabled, its non-blocking final synthesis pass replaces the summary provider call; when summary is disabled, no handoff call, summary files/fallback, task sidecar, or PR summary body is created. | supervisor_quality_gate.md; `core/supervisor.py` (new); `core/loop_control.py`; `core/state_machine.py`; `state_store.py`; `templates/prompts/supervisor{,-final}.md` | accepted |
| 2026-06-16 | **`implementation_stages/` docs removed; relink references** | The numbered `docs/implementation_stages/` design docs (incl. `00_orchestrator_final_plan.md`) and `docs/codex_git_orchestrator_architecture.md` were deleted; every remaining link in root instructions, README, `docs/rules/`, the top-level docs, the backlog, and the sync-docs skill was repointed to the [Functional Map](../../functional/index.md), [worc_architecture.md](../../worc_architecture.md), [configuration.md](../../configuration.md), or [telegram.md](../../telegram.md) as appropriate. Supersedes the 2026-06-12 'Broken canonical-spec links' row. | CLAUDE.md, AGENTS.md, README.md, docs/ (rules, backlog, top-level), .claude/skills/sync-docs/SKILL.md | done |
| 2026-06-16 | **Унификация исполнения в flow-движок** | Выкристаллизованные требования: убрать хардкодную state-машину (`core/state_machine.py`), заменить универсальным flow-движком с декларативным графом из конфига (YAML+MD), переходы остаются за Core и валидируются заранее; виды узлов `agent`/`checks`/`evaluator`/`hitl`/`publish`; supervisor = bounded routing в объявленных точках ветвления; resume обязателен в v1 (универсальный персистентный чекпоинт). Первый и доказательный пример — перенос текущего `implementation` 1:1 (existing-тесты как golden-harness). Переосмысливает foundation/profiles-программу в сторону полной унификации. **Investigate crewAI выполнен (2026-06-16):** §12 фиксирует, что берём (engine-owned dispatch, router-`emit` = объявленные рёбра, сериализуемый `FlowDefinition`-контракт, YAML-модель агентов/задач, `FlowPersistence`-схема), чем вдохновляемся (pending/persist/resume HITL, checkpoint re-entry, supervisor-роль) и чего НЕ копируем (поверхностная валидация, state-only resume без идемпотентности, неограниченный иерархический LLM-роутинг, отсутствие Git-ownership/security-потолка). Следующий шаг: детальный дизайн. | refactoring-flow/index.md; workflow_execution_foundation.md; task_workflow_profiles.md; supervisor_quality_gate.md; `core/state_machine.py`; `core/orchestrator.py` | backlog / не запланировано |
| 2026-06-16 | **worc-home consolidation: footprint modes / registry / init / sibling-workspace retired** | **Resolved (2026-06-16):** the git footprint collapsed to one canonical layout. The `git.footprint.location`/`.tracking`/`.external_root` keys and the `FootprintLocation`/`FootprintTracking` enums, the `--git-mode` flag and the friendly mode names (external/in_repo_exclude/in_repo_commit), the `init` command (`install --non-interactive` replaces scripted `init`), the per-user registry (`install/registry.py`, the `repo-root -> config.yaml` binding, `WASTECH_ORCHESTRATOR_HOME`), and the sibling control workspace (`<repo>-orchestrator`) were all **removed**. Everything the orchestrator generates/installs now lives under a single gitignored `<repo>/.worc/` home (config.yaml, templates/, guide/, state.db, logs/, workspace/, checks/, tasks/rejected); the `tasks/` lifecycle dirs stay at the repo root, git-tracked, and the task file + `<id>.summary.md` are the audit trail, committed by a **task-scoped** audit commit (only the current task's files — never `git add -- tasks/` wholesale). `install` appends a single `.worc/` line to the repo's tracked `.gitignore`; config discovery walks up from cwd to the Git root for `<root>/.worc/config.yaml` (no registry, no `./config.yaml` fallback). `git.footprint` now carries only `audit_commit_message` + `audit_on_branch` (`AuditBranch` enum kept). `CONFIG_SCHEMA_VERSION` bumped 6 → 7. **Open choices kept:** the config key is still named `git.footprint` (could be renamed to `git.audit` in a later schema bump); the installed docs dir is named `guide/` (packaged source dir stays `worc/`). | `cli.py` (`worc_home_for`/`tasks_root_for`/`resolve_config_path`/`cmd_install`); `config/schema.py` (`FootprintConfig`, `CONFIG_SCHEMA_VERSION=7`); `git_manager.py` (`commit_audit`, `RUNTIME_GITIGNORE_LINES`, `append_runtime_excludes`); `install/{config_writer,wizard}.py`; README.md, docs/{configuration,operations,cookbook,worc_architecture,task-authoring}.md, docs/worc/, docs/functional/, docs/likec4/ | done |
| 2026-06-16 | **Dev rules relocated to `.agents/rules/`** | The five development rule docs moved from `docs/rules/` to `.agents/rules/` (`architecture`, `coding-style`, `git-workflow`, `security`, `testing`). All inbound links (root instructions, README, `docs/`, `.claude/` skills, `src/` docstrings) and the moved files' own outbound links were repointed; the docs-sync gate (`docs_sync_gate.py`) now also treats `.agents/` as a documentation change. | CLAUDE.md, AGENTS.md, README.md, docs/, .claude/, src/ | done |
| 2026-06-17 | **Flow-engine: config-aware validation deferred to P4.2** | The flow validator (`core/flow/validator.py`) is config-free: it cannot yet check that node `model`/`reasoning`/providers ∈ `config.yaml` allowlist, that `permission_ceiling` ≤ configured provider capabilities, or that flow `budgets` ≤ `agents.max_*` caps (security-ceiling §4 "Согласованность с config.yaml"). **Decided (2026-06-17):** lands as a separate `validate_flow_against_config(snapshot, config)` called by `FlowRegistry` after structural validation (keeps `validate_flow` config-free for unit tests). **Resolved (2026-06-21, P4.2):** `validate_flow_against_config` ships and runs on every `FlowRegistry.resolve` (and `validate_all`) when a config is present. It checks node `provider` ∈ `agents.allowed`, `reasoning` ∈ the closed level set, and `permission_ceiling` reachable by a configured provider. **Scope narrowed deliberately:** there is **no** per-model allowlist (config carries one `model` per provider, not a list — greenfield/YAGNI); and `budgets`/`publishing` are **not** validated here (see the budget row below + the validator module docstring). The orchestrator's old `_check_flow_providers` stopgap was deleted (folded in). | `core/flow/validator.py`, `core/flow/registry.py`, `core/orchestrator.py`, flows/p4-operator.md, flows/security-ceiling.md | done |
| 2026-06-17 | **Flow-engine: flow `budgets` ↔ config-cap authority** | Packaged `implementation.yaml` carries `budgets.global_fix_iterations: 30` while config carries `agents.max_total_fix_iterations`. **Decided (2026-06-17):** flow `budgets` parameterize `LoopController`; config cap is the upper, non-weakenable safety bound — flow may not exceed it (a ceiling check added with the config-aware validator above). **Resolved (2026-06-21, P4.2) — authority is the runtime clamp, NOT a fatal load-time check.** The engine already clamps every loop to `min(flow_budget, cap)` (`engine.py` `_loop_cap`/`_global_cap`), so the cap always wins. A fatal "budget > cap" check would be a footgun: the packaged flow ships budgets at the **default** caps (30/15), so any operator who _narrows_ the cap (a safe, desirable action) would brick the built-in flow — and every fix-loop test deliberately runs caps below the flow budget so the loop terminates fast. "budget > cap" is therefore the expected clamp case, never an error. Same reasoning retired the planned `publishing`→git fatal check: `git.create_pull_request: false` runs any flow in local-commit mode (a supported config), so it is not an escalation. | `core/loop_control.py`, `core/flow/engine.py`, `core/flow/validator.py`, flows/p1-engine.md | done |
| 2026-06-17 | **Flow-engine: P1–P4 engineering specs authored** | The granular plan's P1–P4 sections were expanded into per-phase engineering specs (touchpoints with signatures, new types/state.db tables, named tests, exit criteria, open questions) and `plan.md` shrunk to an index. P0 hardening (P0.5: fail-closed unknown fields, checker/network enums, enum-error wrapping, `when.fact` namespace, terminal co-reachability) closed design↔code gaps found while reviewing the "done" P0. | flows/plan.md, flows/p1-engine.md, flows/p2-implementation.md, flows/p3-research-audit.md, flows/p4-operator.md; `core/flow/{snapshot,validator,contracts}.py` | done |
| 2026-06-17 | **Flow-engine P1.1+P1.2 built (engine + persistence, additive)** | **P1.1:** `core/flow/engine.py` (`FlowEngine` engine-owned transitions, generic bounded-loop budgets with zero domain knowledge, terminality) + `core/flow/run_state.py` (`FlowRunState`). **P1.2 (additive — legacy `_drive`/`stage_runs`/granular statuses stay until P1.5 because P1.4's golden-harness dual-runs both):** `state.db` **v4** — `node_runs` table + `NodeRunRow` + `record/complete/skip/get_node_runs` + `tasks.{current_node,flow_run_counters,flow_fingerprint}` + `save/get_flow_checkpoint`; `core/flow/recorder.py` (`StateStoreRunRecorder` + `hydrate_run_state`); generic `Status.RUNNING`; flow-neutral `ledger.write_failure_report` (optional `node_id`); engine resumes at hydrated `current_node`. **Deferred to P1.4** (entangled with wiring the engine into `run_task`/`resume`): the orchestrator-level `RecoveryReconciler` node-progress dispatch, decomposed-resume, and `rerun --continue` at the interrupted node. Not yet wired into the production path. | `core/flow/{engine,run_state,recorder}.py`; `state_store.py`; `core/state_machine.py`; `ledger.py`; flows/p1-engine.md; tests under `tests/core/test_flow_{engine,recorder}.py`, `tests/state/test_node_runs.py` | P1.1 done; P1.2 **persistence layer** done (resume/recovery dispatch + its named tests — `test_resume_dedups_publish_ops`/`_arbitrary_graph_idempotent`/`_recovery_decomposed_resume_point`/`_continue_revives_at_interrupted_node` — deferred to P1.4 wiring); P1.3–P1.5 in progress |
| 2026-06-17 | **Flow-engine P1.3 (agentic + deterministic node wrappers)** | Prompt assembly from `role_file` (`core/flow/prompt.py`: `render_role_prompt`/`read_role_file`, path-contained; `render_prompt` unchanged; `repo` added to `ALLOWED_PROMPT_VARS`) + thin adapters in `core/flow/nodes/`: `base.py` (`NodeServices`/`NodeInputs` contracts, Protocol collaborator ports, `NodeInfraError`), `agent.py` (`AgentNodeRunner`→router), `evaluator.py` (`EvaluatorNodeRunner` role=review → accept/rework/done), `checks.py` (`ChecksNodeRunner`→CheckRunner, launch_failed→`CheckLaunchError`). Runners constructed per-unit with services/inputs, so the generic engine is untouched. **Folded into P1.4 wiring** (most orchestrator-coupled): `publish`/`hitl` runners, embedded refinement/planning HITL (`node.hitl` + `_run_typed_stage`), dangerous-diff guard, review-artifact + `{review_path}`, prompt_audit/heartbeat, and building `NodeServices`/`NodeInputs` from `_Pipeline` + the `node_id→Stage` map. | `core/flow/prompt.py`, `core/flow/nodes/{__init__,base,agent,evaluator,checks}.py`; `core/prompts.py`; flows/p1-engine.md; `tests/core/test_flow_{prompt,node_runners}.py` | P1.3 core done; publish/hitl/wiring in P1.4 |
| 2026-06-17 | **Flow-engine P1.4 Step A (wiring-first): publish runner + engine driver + review artifact** | `publish.py` (`PublishNodeRunner` → idempotent git via `GitPublishPort`); `engine_driver.py` (`build_node_runners` per-kind registry + `drive_flow` = engine↔orchestrator seam); `EvaluatorNodeRunner` now writes `review/findings.json`+`summary.md` and sets `{review_path}` (parity `_write_review`). End-to-end `tests/core/test_flow_engine_driver.py` (tiny `refine→impl→testing→publish` flow via `drive_flow` + real `StateStoreRunRecorder`/`StateStore`). **Remaining for Step B** (durable round-trip is only verifiable end-to-end via the golden harness): the one shared durable HITL/approval round-trip — `hitl` runner + embedded refinement/planning HITL (`node.hitl`, `parse_typed_stage_output`, `interaction_path` persist/resume) + dangerous-diff guard (`write_current_diff`+`classify_dangerous_diff`+approval); prompt_audit/heartbeat; building `NodeServices`/`NodeInputs` from `_Pipeline` + the `node_id→Stage` map; the dormant `_drive_via_engine` seam in `run_task`; then the parity fixture + golden-harness dual-run + recovery dispatch. | `core/flow/{publish,engine_driver}.py`, `core/flow/nodes/{base,publish,evaluator}.py`; flows/p1-engine.md; `tests/core/test_flow_engine_driver.py`, `tests/core/test_flow_node_runners.py` | Step A core done; durable-HITL + golden-harness in Step B |
| 2026-06-17 | **Flow-engine P1.4 Step B: dangerous-diff classification + parity fixture** | `AgentNodeRunner` writes `{diff_path}` after a workspace-write edit and runs `classify_dangerous_diff` (core-owned, automatic); a dangerous diff fails closed → `NodeManualRequired` (full durable approval round-trip is the next piece). `GitPublishPort`→`GitPort` (+`write_current_diff`/`changed_code_entries`). Parity fixture `tests/core/flows/implementation_parity.yaml` (+`roles/*.md`): the current pipeline 1:1 (refinement→planning→implementation→testing→review→fixing→summary→publish + `test_fix`/`review_fix` loops + decomposition; NO supervisor/testing_quality). **Remaining Step B:** full durable HITL/approval round-trip (`hitl` runner + embedded refinement/planning HITL + dangerous-diff approval branch); prompt_audit/heartbeat; `_Pipeline`→`NodeServices`/`NodeInputs` builder + `node_id→Stage` map; dormant `_drive_via_engine` seam in `run_task`; the golden-harness dual-run (`_drive`↔`FlowEngine` on fake-CLI scenarios); recovery dispatch. | `core/flow/nodes/{agent,base}.py`; `tests/core/flows/implementation_parity.yaml`; `tests/core/test_implementation_parity_flow.py`; flows/p1-engine.md | dangerous-diff(classify)+parity-fixture done; round-trip+wiring+harness remain |
| 2026-06-17 | **Flow-engine P1.4 Step B: embedded refinement/planning HITL** | `core/flow/nodes/human_gate.py` (`HumanGate` — one durable round-trip: `start_ask`→persist `waiting`→`wait_for_answer`→`write_answer`; `resume` for a restart). `AgentNodeRunner` runs refinement/planning through `_run_with_hitl`: parse `parse_typed_stage_output`, on a `human_input` signal do one round-trip + re-run the stage with `human_input_path`, resume a persisted interaction after restart, fail closed (timeout/transport/invalid) → `NodeManualRequired` — a faithful port of `_run_typed_stage`. `NotifierPort` + `NodeServices.notifier`/`ask_timeout_s` + `NodeInputs.contacts`. **Remaining Step B:** dangerous-diff **approval** branch (durable approve + reconsider-on-denial, `_run_edit_stage_with_guardrail`); standalone `hitl` node; prompt_audit/heartbeat; `_Pipeline`→`NodeServices`/`NodeInputs` builder + `node_id→Stage` map; dormant `_drive_via_engine` seam; golden-harness dual-run; recovery dispatch. | `core/flow/nodes/{human_gate,agent,base}.py`; flows/p1-engine.md; `tests/core/test_flow_node_runners.py` | embedded-HITL done; dangerous-diff-approval + wiring + harness remain |
| 2026-06-18 | **Flow-engine P1 COMPLETE — Slice 7 cutover + legacy deletion** | The FlowEngine is the sole driver: `run_task`→`_drive_via_engine` and `resume`→`_resume_via_engine` (node-based, hydrates `FlowRunState` from the `current_node` checkpoint); dangerous-diff approve/deny/reconsider + decomposition fan-out + recovery/`rerun --continue` all run on the engine. **Standalone `hitl` gate node** added (`core/flow/nodes/hitl.py` `HitlNodeRunner`, registered in `engine_driver.build_node_runners`; approval→route:approve/deny, question→done, fail-closed). Observability (prompt_audit/rendered-prompt/provider_attempts/heartbeat) verified at parity in the engine path — no work needed. **Legacy deleted:** `_drive`/`_run_unit`/`_enter_fixing`/`_run_units_and_finish` + ~35 helper methods; `LoopController`/`LoopDecision`/`FixLoop` (kept `LoopCounters`); `stage_runs` table + `StageRunRow` + its methods (engine writes `node_runs`); `tasks.interrupted_status`; the 11 granular statuses (generic `new→validated→preparing→running→terminal` lifecycle); `PromptTemplateStore` + the whole `prompts:` config block + packaged `templates/prompts/`; the `install-templates` CLI command + `_copy_templates_tree`; `QualityAction`/`LifecycleEffect`. **Renames/bumps:** `provider_attempts.stage_run_id`→`node_run_id`; `state.db` v5→**v7**; config v8→**v9**. **Correction:** the `Stage` enum is **retained** (load-bearing for routing — the router selects a provider by `Stage` via `wiring.build_stage_map`); full `Stage` removal + node-based routing is **P4**. Grep-guard `tests/core/test_no_legacy_symbols.py` keeps the deleted symbols gone. | `core/orchestrator.py`, `core/state_machine.py`, `state_store.py`, `core/loop_control.py`, `core/prompts.py`, `config/{schema,loader,upgrade}.py`, `cli.py`, `core/flow/{nodes/hitl,engine_driver,contracts}.py`; flows/p1-engine.md | **done** |
| 2026-06-19 | **Flow-engine P1 audit remediation (post-audit hardening)** | The P1 audit (flows/p1-audit.md) confirmed 7 findings + 6 coverage gaps + 2 secondary; **all fixed, each with a test**. Highlights: publish-failure-after-finalize → resumable `MANUAL` instead of a stranded `done/`-committed `FAILED` (`PublishNodeRunner` raises `NodeManualRequired` on a post-finalize git error); `task.agents` route override threaded to the engine (`NodeInputs.route_override`); `session_scope=fresh_disposable` honored at runtime (`editing_session_id`/`persists_session`; durable lineage/`lineage_affinity`/`resume_own_lineage` stay P2.2); pre-v7 `state.db` refused fail-closed in `open()` (greenfield — no destructive migrations); active `subtask_spec_path` + `subtask_count` wired into edit-node prompts (`roles/{implementation,fixing}.md`); engine loop counters mirrored to operator-facing `tasks.fix_iterations` (`save_flow_checkpoint`+`_sync_counters_from_run_state`); planning-selected skills persisted to `selected_skills.json` and restored on resume; `done` clears the flow checkpoint; `commit_sha_after`/PR-URL overload documented. **Deferred (audit Additional #7):** with `fixing` skipped, the engine runs the bounded `test_fix` loop to its cap before `MANUAL` rather than going straight to `MANUAL` (terminal outcome preserved; re-tests N× pointlessly). Immediate-manual should be a conditional flow edge (`testing fail→manual` when `fixing` disabled), not engine domain knowledge — not scheduled. | flows/p1-audit.md; `core/flow/nodes/{publish,agent,evaluator,base}.py`, `core/flow/{wiring,recorder}.py`, `core/orchestrator.py`, `core/decomposition.py`, `state_store.py` | done (Additional #7 deferred) |
| 2026-06-19 | **Flow-engine P2.2 — durable sessions** | Landed (flows/p2-implementation.md §P2.2): `editing_lineage` table (`state.db` **v9**) is the only place a raw session id is stored; durable `session_scope` in the agent runner (`_resume_session_id`/`_persist_session` via the store — the in-memory `session_ids` map is gone); Codex `exec resume <id>` + `thread.started` parse (PRE.4 contract); Claude `--resume` wired to the store; `lineage_affinity` realized by the one editing session per execution unit (validator rejects a conflicting explicit `provider`); `session_unavailable` → same-provider fresh-retry in the router (new `ErrorClass.SESSION_UNAVAILABLE`, never falls back / never charges a fix iteration); raw session id redacted in every artifact/argv/log, `normalized_session_id` written to `result.json`. **Deferred:** durable `resume_own_lineage` for the **supervisor layer** — its own session is still in-memory (survives one task cycle, not a restart); no blocking test (the supervisor's finalize is idempotent on resume, P2.1), and persisting it needs a small design choice (a supervisor-scoped lineage row vs reusing `editing_lineage`). Pick it up alongside P3's `resume_own_lineage` research critic. | `state_store.py`; `providers/{codex,claude,redaction,base}.py`; `routing/router.py`; `core/flow/nodes/{agent,evaluator,base}.py`; `core/flow/{validator,wiring}.py`; `core/orchestrator.py`; flows/p2-implementation.md | P2.2 done; supervisor durable session deferred |
| 2026-06-19 | **Flow-engine P2.1 — constant supervisor layer + evaluator primitive** | Landed (flows/p2-implementation.md §P2.1): `core/supervisor.py` (per-step read-only `observe` in its own `resume_own_lineage` session + whole-task `finalize` summary, advisory, wired via the orchestrator post-node hook + `_engine_finalize`); `evaluations` table (immutable, append-only, `state.db` **v8**) + `record_evaluation`/`count_rework_verdicts`; `record_rework` in `core/loop_control.py` (single global-fix increment, called by the engine); `config.yaml: supervisor:{model,reasoning,role_file}` under the node ceiling; `EvaluationKind`/`final_handoff` + the summary graph node + `config.summary_enabled` removed (`summary` dropped from `SKIPPABLE_STAGES`); enriched `Finding(severity/reason/paths)`. **Deferred:** durable `resume_own_lineage` for the supervisor (its own session is in-memory until **P2.2**); a full **re-sync of the functional-map `file:line` refs** (B06/B07/system-flows + likec4) once P2.2–P2.5 land — done incrementally here for B07 entity count + schema v8 + index pipeline/state.db, but the precise line numbers shift each P2 slice, so the comprehensive pass waits for P2 completion. | `core/supervisor.py`; `core/orchestrator.py`; `state_store.py`; `core/loop_control.py`; `config/{schema,loader,validation}.py`; `core/flow/{contracts,schema,snapshot,validator,engine,nodes/evaluator}.py`; flows/p2-implementation.md | P2.1 done; durable session + full map re-sync deferred |
| 2026-06-21 | **Flow-engine P3 — research + audit flows (abstraction proven)** | Landed (flows/p3-research-audit.md): **dispatch** — `task_type` on the clean task (model/parser/§19 gate/normalized round-trip), orchestrator resolves the flow by `task.task_type` (unknown → terminal `failed` before branch). **P3.1 checkers** — `core/flow/checkers/{citation,dependency_scan}.py` + dispatch on `ChecksNode.checker` in `nodes/checks.py`: `citation` is a deterministic, no-LLM manifest validator (broken citation → `fail` gating the synthesis loop; missing/malformed manifest → uncheckable, never a crash), `dependency_scan` runs the core-owned argv scanners as evidence and always emits `pass` (gating is the flow's edges). **P3.2 policies** — `core/flow/output_policy.py` (`ResolvedOutputPolicy` + path-containment); after-stage write-containment guard in `AgentNodeRunner` (writes confined to the resolved report dir; private report = repo byte-for-byte); private-report publish in `PublishNodeRunner` (git untouched, fail-closed if the report would be git-trackable); `network_policy` enforced end-to-end (`AgentRunRequest.network_access`, Codex `sandbox_workspace_write.network_access`, Claude WebFetch/WebSearch tools; absent = no network). **P3.3** `deep_research.yaml` executes (roles/research/\*, `resume_own_lineage` critic via the new `node_lineage` table `state.db` **v10**, `config.external_research` resolved from the flow's network grant). **P3.4** `security_audit.yaml` executes (roles/audit/\*); **co-design gate green** — all three packaged flows drive through one engine + one generic runner registry with no `if task_type` (`test_codesign_all_three_flows_generic` + `test_engine_has_no_domain_knowledge`). **Deferred:** (a) the supervisor's own durable session can now reuse `node_lineage` (still in-memory — see the P2.2 row); (b) Codex enables sandbox network only for `workspace-write`, so a `read-only` research node that needs network (deep\*research `external_research`) is not network-enabled at the Codex level — revisit when the read-only+network sandbox combo is needed; (c) per-node observability/interaction paths — research/audit nodes share a kind-based `Stage` identity (`wiring._stage_identity`), so same-identity nodes reuse the per-stage `rendered-prompt.md` audit dir (run-id-keyed files don't collide); the per-node split lands with the **P4** removal of the `Stage` enum. | flows/p3-research-audit.md, flows/p3.1-core-checkers.md … p3.4-security-audit-yaml.md; `task/{model,parser,validation_gate}.py`; `core/flow/{output_policy,wiring,checkers/*,nodes/{checks,publish,agent,evaluator,base}}.py`; `providers/{base,codex,claude}.py`; `state_store.py`; `core/orchestrator.py`; `core/flow/packaged/{deep_research,security_audit}.yaml`+`roles/{research,audit}/_` | done (a/b/c deferred) |
| 2026-06-21 | **Flow-engine P4 — operator surface (validator C in production)** | Landed (flows/p4-operator.md) — the last stage of the flow-engine refactor. **P4.1:** the `FlowRegistry` is wired to the live path with `operator_flows_dir=<repo>/.worc/flows/` (orchestrator builds it with the config), so an operator drops `<task_type>.yaml` there to add or override a flow; `install`/`preflight` fatally validate **every** flow file (`FlowRegistry.validate_all`→`run_preflight` reports `flow <name>: OK/FAIL`, a broken operator flow blocks startup). **P4.2:** `validate_flow_against_config(snapshot, config)` (config-aware third layer, kept separate from config-free `validate_flow`) runs on every resolve — node `provider`∈`agents.allowed`, `reasoning`∈ closed level set, `permission_ceiling` reachable by a configured provider; the stopgap `_check_flow_providers` was deleted (folded in) and `_resolve_flow` now funnels `FlowValidationError`→`PipelineFailed`. Recovery re-check: resume re-resolves + re-validates against the live config (ceiling only narrows — the fingerprinted flow can't widen rights). Threat model pinned as tests (`tests/core/test_flow_threat_model.py`, one per security-ceiling §1 row incl. `test_recovery_ceiling_only_narrows`). **Scope decisions (see the two resolved rows above):** no per-model allowlist; `budgets`/`publishing` are not fatal (runtime clamp / local-commit degradation handle them). **Still deferred to a later pass:** `Stage`-enum removal + per-node observability paths (P3 row (c)); signature/hash flow registry + `network_policy` per-host allowlist (security-ceiling §8); custom `tool` nodes (P5 row below). | flows/p4-operator.md; `core/flow/{validator,registry}.py`; `core/orchestrator.py`; `cli.py` `run_preflight`; `tests/core/{test_flow_threat_model,test_flow_registry}.py`, `tests/test_cli_preflight.py`; docs/configuration.md, docs/functional/{index,blocks/B06-orchestrator-pipeline}.md | done |
| 2026-06-22 | **Flow-engine: `Stage`-enum identity removed (resolves P3 row (c) + P4 deferred)** | **Done** — the flow-engine request identity is now the flow node's `node_id: str`, not the `Stage` enum (threaded through `AgentRunRequest`/`AgentRunResult`/`ResolvedRoute`/router/providers/observability/HITL). The typed-output schema is selected by a derived per-node `OutputContract` (`none`/`human_input`/`planning`) in `AgentNodeRunner._contract` — `planning` = the decomposition proposer (`decomposition.proposed_by`), `human_input` = any `hitl` node — **no new YAML field** (derived from declared structure, strictly more honest than the old `id=="planning"` heuristic). `wiring.build_stage_map`/`_stage_identity`/`NodeServices.stage_for_node` deleted; `hitl.stage_output_schema`→`typed_output_schema`, `parse_typed_stage_output`→`parse_typed_output`; observability + HITL artifacts keyed by `node_id` (`stages/<node_id>/`), so same-capability research/audit nodes no longer overwrite each other's `rendered-prompt.md` (closes the **P3 row (c)** per-node-audit-dir gap and the **P4** "Stage-enum removal + per-node observability paths" deferral); supervisor identity `"supervisor"`, check-discovery `"check-discovery"`. **`Stage` enum kept** as the skip vocabulary only (`SKIPPABLE_STAGES`/`effective_skip`/`Stage.REVIEW in p.skip`/validation gate). Verified: ruff + mypy (95 files) + full pytest green. **Minor deferred:** (a) `hitl.node_interaction_path` (the standalone `hitl` gate, `node-<id>.json`) is now near-redundant with the node-keyed `interaction_path` — kept both to bound test churn, could unify; (b) the `Stage` enum still physically lives in `providers/base.py` though it is no longer part of the provider contract — could relocate to `config`/`core` for cohesion; (c) the supervisor calls `create_attempt_dir` with `node_run_id=0` every turn, so a 2nd+ observe/finalize collides on `stages/supervisor/run-000000/` (FileExistsError, swallowed by the advisory guard) — pre-existing, now more visible since the dir is `supervisor`. | 2026-06-21-audit-remediation.md (Phase 7 #A, #L1, Q-A); flows/p3-research-audit.md (row (c)); flows/p4-operator.md; `providers/{base,artifacts,claude,codex}.py`, `routing/router.py`, `core/hitl.py`, `core/flow/{wiring,observability,nodes/_}.py`, `core/{supervisor,orchestrator}.py`, `checks/agent.py` | done (a/b/c minor deferred) |
| 2026-06-22 | **Strip dead external-spec `§NN` anchors (audit #B)** | **Done (2026-06-22).** AST-aware sweep: edits only docstring (`ast.Constant`) nodes and `tokenize.COMMENT` tokens — never code, never non-docstring string literals — so documented commands like `git add .` survive and code is untouched. Span-local substitutions (parenthetical citations deleted; `the §N <word>`→`the <word>`; `spec §N`→`spec`; `<file>.md §N`→`<file>.md`); no global whitespace tidy (that was the mangling risk). Result: **0 `§` left in `src/` or `tests/`** (453 src + 132 tests stripped). Hand-fixed: the **4** runtime/operator-facing literals (`recovery.py` `manual_reason`, `orchestrator.py` SlotBusy message, two `git_manager.py` messages — dead-spec suffix dropped, an improvement), 13 author-shorthand tags (`§rerun`/`§finalize`/`§ auto-merge bypass`), and 3 vacuous "see spec". **Correction to the earlier deferral note:** only **4** (not ~238) src `§` were in runtime literals — that count wrongly included docstrings. Live doc references (`.agents/rules/\*`, real `.md` files) deliberately kept. ruff + ruff format + mypy + full pytest green. | 2026-06-21-audit-remediation.md (#B, Q-Bb); src/ + tests/ (~133 files); `core/flow/validator.py` keeps the live `security-ceiling.md` ref | done |
| 2026-06-22 | **Flow-engine: per-node `network_access` override** | **Done (2026-06-22, no config-version bump).** Network was a single flow-wide switch (`network_access = doc.network_policy is not None` in both node runners), so an operator could not grant network to only the implementation/testing node. Added an optional tri-state `network_access: bool \| None` field to `AgentNode`/`EvaluatorNode` (`schema.py`), parsed fail-closed in the loader (`snapshot.py` — added to `_AGENT_FIELDS`/`_EVALUATOR_FIELDS`; `_parse_network_access` guards `None`-is-inherit vs `bool(None)`-is-deny). The runners now call a new pure helper `resolve_network_access(node.network_access, doc.network_policy)` (`contracts.py`): explicit node value wins; absent (`None`) inherits the flow default (byte-for-byte backward compatible — every existing flow parses to `None`). **`network_policy` reframed** from hard ceiling to flow-wide default: a node `true` grants network even in a flow that declares no `network_policy`; a node `false` is an explicit opt-out that beats a granting flow. Network-only — never the filesystem ceiling. Packaged flows unchanged (the two research flows keep granting via `None`-inherit). Codex read-only+network stays a no-op (pre-existing, documented as an asymmetry). Functional-map fold-in (B30/B29/B25) deferred to the pending full `file:line` re-sync pass (noted on that open row). ruff + mypy + full pytest green (new tests: `resolve_network_access` 4-case unit, loader tri-state agent+evaluator, end-to-end runner grant-in-policyless-flow + opt-out + evaluator parity; existing provider-argv network tests reused). | per-node-network-access.md; `core/flow/{schema,snapshot,contracts}.py`; `core/flow/nodes/{agent,evaluator}.py`; `docs/configuration.md`; `tests/core/test_flow_{contracts,snapshot,node_runners}.py` | done |
| 2026-06-22 | **`gh auth status` warning at startup** | **Done (2026-06-22).** The `gh` pre-flight gate (`require_gh`) only checked `gh` is on `PATH`, so a logged-out `gh` passed startup and failed far downstream at `gh pr create` (→ opaque `manual_action_required` after the whole pipeline ran). Added a **non-blocking auth advisory** on top of the unchanged hard gate: `detect.gh_auth_ok() -> bool \| None` (read-only `gh auth status` probe via the safe `run_process` with the operator's own env so an env `GH_TOKEN`/`GITHUB_TOKEN` is honored; `True`=authed / `False`=logged out / `None`=launch-fail/timeout=unknown; stdout→throwaway temp file, stderr discarded — the account login/token scopes never surface); `preflight.warn_if_gh_logged_out(emit=None)` emits a fixed `WARNING` ("run `gh auth login`") only when `has_gh()` **and** `gh_auth_ok() is False` (`None`/`True` stay silent), never raises, defaults to a logger emit. Wired immediately after each `require_gh()` (still under `if config.git.create_pull_request:`) in `cmd_run`/`cmd_rerun`/`cmd_watch`. ruff + mypy + full pytest green (new tests: `gh_auth_ok` tri-state via faked `run_process`; advisory emits-once-when-logged-out, silent-when-authed/unknown/gh-absent, never-raises). **Out of scope (unchanged):** no hard auth gate, no mid-run re-check. | gh-auth-status-warning.md; `install/detect.py`; `preflight.py`; `cli.py` `cmd_run`/`cmd_rerun`/`cmd_watch`; `docs/operations.md`; `tests/install/test_detect.py` | done |
| 2026-06-22 | **Provider config: operator-owned full access (Codex + Claude), drop `max_budget_usd`, explicit model/reasoning defaults** | **Done (2026-06-22, config v14).** Three bundled provider-config changes. (1) **Operator-owned full access** — lifted the _absolute_ ban on each provider's structured full-access selector: `find_forbidden_args` no longer rejects `--sandbox danger-full-access`, the Codex adapter's full-access `raise` guard is gone, and the Claude adapter's runtime `_reject_weaker_permission_override` call is gone (the helper survives as the preflight reporter only). Both are now gated by `security.strict_isolation` instead: the default `strict_isolation: true` rejects them at preflight, `false` opts in. The `--dangerously*`/`--yolo`/`--ignore-rules` bans (incl. Claude `--dangerously-skip-permissions`) stay absolute. **Risk #2 resolved as option (b):** added `find_full_access_args` (detects `--sandbox danger-full-access` / `--permission-mode bypassPermissions`) and wired it into the flow validator's config-consistency layer (a flow node's `extra_args` is gated under `strict_isolation`) **and** the Codex provider `isolation_reasons` (closing the extra_args-vs-`sandbox`-field gap) — a true global gate. (2) **Deleted `max_budget_usd`** — schema field, loader parse (incl. the now-dead `_opt_float` helper), the `config_writer` wizard line, and both `config.example.yaml`; tolerate-on-load + strip-on-upgrade (`_REMOVED_KEYS`). (3) **Explicit defaults** — `claude: claude-sonnet-4-6` / `codex: gpt-5.4` (the operator's installed Codex CLI default; not invented), both `reasoning: high`, in the template + `init` wizard; `""`/`null` stays a valid "use CLI default" sentinel. Also fixed the repo-root `config.example.yaml` which was stale at v12. ruff + mypy + full pytest green (new tests: full-access detector, flipped adapter/validator cases, node-gate threat rows, loader/upgrade/config_writer). | provider-config-cleanup.md; `security/forbidden_args.py`; `providers/{codex,claude}.py`; `core/flow/validator.py`; `config/{schema,loader,upgrade}.py`; `install/config_writer.py`; `templates/config.example.yaml` + repo-root `config.example.yaml`; `.agents/rules/security.md` (#10); `docs/configuration.md` | done |

| 2026-06-22 | **Supervisor durable `resume_own_lineage` session** | **Done (2026-06-22, audit-remediation Phase 2 #10).** The supervisor's own session is now durable: `core/supervisor.py` persists and hydrates the raw session id via the `node_lineage` table (`state.db` v10, introduced in P3) under the reserved sentinel key `_SUPERVISOR_LINEAGE_NODE_ID = "__supervisor__"`. `_resume_session` reads the persisted row on first use (restoring across process restarts); `_persist_session` writes it after each observation turn. The earlier in-memory fallback survives as the initial-state case where no row exists yet. The chosen design: a supervisor-scoped row in the existing `node_lineage` table (not a separate `editing_lineage` row), tagged with a non-colliding node-id sentinel. | `core/supervisor.py`; `state_store.py` (`node_lineage`); flows/p2-implementation.md; 2026-06-21-audit-remediation.md | done | | 2026-07-01 | **Schema/DB version ledger kept during stale-comment cleanup (improvements #4)** | Improvement #4 (remove stale historical comments) shipped: runtime code + packaged flows no longer name the removed `supervise_impl`/`supervise_fix` nodes, the summary-as-node concept, `(2026-06-19 revision)` stamps, or "mirrors/port of the legacy X" heritage phrasings (rewritten to present-tense rationale). Operator-confirmed scope decision: the dated `# vN (date, name): …` schema changelog in `config/schema.py` and the "tolerated (removed vN)" notes in `config/loader.py` were **kept** — they document the loader's _current_ tolerate behavior, not dead architecture. Revisit only if the greenfield config compat layer itself is removed. | `config/schema.py`; `config/loader.py`; `docs/backlog/improvements.md` #4 | done (notes) | | 2026-06-27 | **Per-node task override — model has no ceiling; reasoning now in prompt audit** | Per-node `nodes.<id>.{model,reasoning,provider}` shipped (best-effort overlay, warn + skip invalid fields). Two intentional scope notes: (1) **no model ceiling** — a `model` override is passed through unchecked because model names have no reliable tier ordering (accepted ADR open-Q #2 / simplify note); if a future need to bound model power appears, it would require a config-driven model allowlist or tier map, validated in `resolve_node_overrides` like the `reasoning` check. (2) `reasoning` was added to the prompt-audit record (ADR Q#4) — `model`/provider were already auditable; the `node_lineage`/`node_runs` rows still store provider only, not model/reasoning, so audit of effective effort is via `prompt-audit/` JSON, not `state.db`. | `core/node_overrides.py`; `core/flow/observability.py` `write_prompt_audit`; [task-node-model-override.md](done/task-node-model-override.md) | done (notes) | | 2026-06-13 | **Capture real merge SHA in `--auto` auto-merge mode** | ~~With `git.auto_merge_wait_for_checks: true` the orchestrator arms GitHub-native auto-merge (`gh pr merge --auto`) and returns immediately, so the ledger records `merge_outcome: "armed"` rather than the real merge SHA.~~ **Subsumed by the "Task dependencies (`depends_on`)" task (implemented 2026-06-22)** — the real `mergeCommit.oid` is backfilled into the `state.db` `pr_merge` publish op as a byproduct of the dependency readiness probe (armed→merged), for any task that has a dependent. The append-only ledger keeps its point-in-time `"armed"` record. A general reconciler that backfills _every_ armed PR (even those with no dependent) for pure audit completeness remains an optional, unscheduled extra. | task-dependencies.md; `git_manager.py` `merge_pr`/`_merge_commit_sha`; `ledger.py` `LedgerRecord` | subsumed | | 2026-06-22 | **Task dependencies (`depends_on`): non-blocking merge-gated scheduling** | No cross-task dependency model exists today (`depends_on` is sub-task-only). Add a task front-matter `depends_on: [<task-id>, …]` list and gate scheduling on it: a pending task is eligible only when every dependency has **merged**; ineligible tasks are skipped while independent tasks run (non-blocking — the slot never waits on CI), and re-evaluated on later `watch` ticks. Readiness = `gh pr view → state==MERGED` (PR mode) or terminal `DONE` + commit on base (local-commit mode), probed via the safe runner; the probe backfills the real merge SHA (absorbs the row above). Unsatisfiable dep (failed/closed) → waits forever until the operator intervenes (no auto-fail); cycles / unknown refs → fail-closed reject; explicit `run` of a blocked dependent → refused. **Implemented 2026-06-22** (`depends_on` front-matter + `INVALID_DEPENDS_ON` gate; `Orchestrator.dependency_eligibility`/`reject_dependency`; `GitManager.pr_merge_state`/`backfill_merge_sha`; `watch_once` skip/reject + `cmd_run` refusal). SHA backfill writes the SQLite publish op only (the append-only ledger keeps its point-in-time `"armed"`). Full design in the detail file. | task-dependencies.md; `task/{model,parser,validation_gate}.py`; `core/orchestrator.py`; `git_manager.py`; `cli.py` `watch_once`/`cmd_run` | done | | 2026-06-13 | ~~**Check discovery: deeper CI parsing**~~ | **Obsolete (2026-06-23):** the entire automatic check-discovery subsystem was deleted by the checks-monorepo change (row below). The gate is now operator-authored `checks.command_sets`; there is no CI-parsing discovery to deepen. | ~~`checks/resolver.py`; `checks/inspect.py`~~ | obsolete | | 2026-06-13 | ~~**Generalize discovery model/reasoning into per-stage config**~~ | **Obsolete (2026-06-23):** agent check discovery (`checks.discovery.{model,reasoning}`, `checks/agent.py`, `checks/discovery_factory.py`) was deleted by the checks-monorepo change — there is no agent-assisted discovery knob left to generalize. | ~~`checks/agent.py`; `checks/discovery_factory.py`~~ | obsolete | | 2026-06-14 | **Known gap: planning is not shown the skill inventory** | **Resolved (2026-06-27, skills-selection-rework):** planning no longer selects skills at all. Selection moved to operator pins + the once-per-task supervisor proposal, and the proposal _is_ handed the full discovered inventory (name + description + path per skill) in `_proposal_prompt`. The guess-and-drop problem is gone (provenance-closed resolution). | `core/supervisor.py` `_proposal_prompt`; `core/skills.py` `SkillInventoryScanner`; [skills-selection-rework.md](done/skills-selection-rework.md) | resolved | | 2026-06-16 | **Windows test-suite portability gap** | The quality-gate suite assumed POSIX. **Done (2026-06-26)** as part of [windows-cross-platform-support.md](done/windows-cross-platform-support.md): `tests/test_process_control.py` uses an OS-independent `KILL = getattr(signal, "SIGKILL", 9)` sentinel + explicit injected signals (no bare `signal.SIGKILL`); `SkillRef.path` now stores `Path.as_posix()` (production fix — deterministic cross-platform reference paths), fixing the `endswith(".../SKILL.md")` assertions in `test_skills.py`/`test_orchestrator.py`/`test_recovery.py`; `_install_atomic_write` writes `newline=""` (production fix — installed/templated files stay LF), fixing the `test_cli_upgrade_docs.py` CRLF byte-compare; `tests/test_stage_site_docs.py` resolves its synthetic `/repo` root up front (drive mismatch on Windows). The full `pytest` suite + ruff + mypy pass on Windows 10 / Python 3.14. (`test_cli_init.py` was already green.) **Addendum (2026-07-01):** the **memory subsystem** landed _after_ this run (2026-06-30..07-01) and had never executed on Windows; it has now been verified on real Windows 10 / Python 3.14.5 — the memory suite (127 tests) + the four safety drills are green, `ruff`/`mypy` clean, and a `tests/memory/test_memory_cross_platform.py` round-trip test was added (AC-X1/AC-X2), **with no production fix required** (the `as_posix()`/`newline="\n"` invariants held). The same full-suite run surfaced two **pre-existing, non-memory** portability failures now tracked in the `cli_shell` `shlex.split` row below. AC-SF5 (re-block a hand-edited low-trust _active_ record) stays a separate memory confidence-gap item — see [memory/implementation-audit.md](../memory/implementation-audit.md) F5. | `tests/test_process_control.py`; `tests/core/{test_skills,test_orchestrator,test_recovery}.py`; `tests/test_stage_site_docs.py`; `src/wastech_orchestrator/core/skills.py`; `cli.py` `_install_atomic_write` | done (2026-06-26) | | 2026-06-23 | **Remove `checks.discovery`; monorepo checks via `command_sets` + diff-selection** | Accepted direction (design first): delete the whole automatic check-discovery subsystem (`checks/{inspect,detect,probe,agent,fingerprint,schema_validate}.py`, `discovery_factory.py`, the `auto`/`deterministic`/`disabled` modes + cache/fingerprint in `checks/resolver.py`, `CheckDiscoveryConfig`, `ChecksNode.discovery`/`ChecksDiscovery`, the orchestrator re-resolve path, and `propose_default_commands` install-seeding). Single behavior: operator lists commands; empty = no gate (`disabled` collapses into empty). For polyglot monorepos (`.NET`/Angular/Python/iOS/Android in subdirs), add operator-owned `checks.command_sets` (named sets = `paths` globs + `commands[].{name,argv,cwd}`) in `config.yaml` — keeps the "flow never supplies commands" ceiling invariant. The checks node/runner selects the **union** of sets whose `paths` match `git diff --name-only base..HEAD` (paths outside all sets → run all; fail-safe). Per-set `timeout_seconds` override; per-set `skip_if_unavailable` (operator opt-in) turns a missing host toolchain into a loud `skipped` instead of a hard launch-fail — never counted as `passed`, and if every selected check skipped (or a required toolchain is absent) → `manual`, and any skip blocks `git.auto_merge`. Runner runs **all** checks (no fail-fast) and aggregates: quality-fails → `fixing`; incomplete gate → `manual`. Keep argv-no-shell / env-allowlist / timeout / launch-vs-quality / mutation-guard. `schema_version` 14→15. Heterogeneous toolchains surface a single-host constraint (iOS needs macOS+Xcode, etc.) — external-CI build delegation is a separate, orthogonal backlog item. Full design + rejected alternatives in the detail file. **Implemented 2026-06-23** (Р1–Р5 + Р3a; suite green). Two operator-confirmed deltas vs the original sketch: an **empty diff runs nothing** (vacuous pass — not the ADR's run-all; "nothing ran → manual" is scoped to a non-empty selection where every check was skipped), and the glob matcher supports **extension patterns anywhere** (`**/*.md`) so an operator can wire a Markdown linter that runs only on `.md` changes. Diff source is the working tree (`GitManager.changed_code_paths`), not `base..HEAD` (which is empty at testing time, before the publish-node commit). `install` writes `command_sets: {}` (no `--check` flag); state.db v11→v12 (`check_runs.skipped`). | checks-monorepo.md; `config/schema.py`; `check_runner.py`; `checks/{model,resolver,selection}.py`; `core/flow/{schema,snapshot,nodes/checks}.py`; `core/orchestrator.py`; `state_store.py`; `cli.py` | done | | 2026-06-24 | **task-023 run remediation: drop checks fail-safe-to-full; pending-diff summary; lifecycle-move audit staging; `fixing`/supervisor role bounds** | From a post-mortem of `task-023` in the `debates` repo ([docs/analysis/task-023-run-analysis.md](../../analysis/task-023-run-analysis.md)): a frontend-only task burned 7 test→fix cycles fighting an unrunnable .NET backend gate that the **fail-safe-to-full** selection rule pulled in whenever an unclaimed root path changed. **Done (2026-06-24):** (1) removed fail-safe-to-full in `select_check_sets` — an unclaimed path now runs no set on its account; covering shared/root files is the operator's job (a no-`paths` catch-all set or explicit `paths`); supersedes the fail-safe behavior recorded in the 2026-06-23 checks-monorepo row. (2) `GitManager.diff_stat` now diffs `base` vs the **working tree** — the deterministic minimal summary is built before the publish node's `commit_code`, so `base...HEAD` was empty there and rendered a misleading "(no committed changes)". (3) `commit_audit` stages task-file **deletions** on a lifecycle move (`git add -A` over the candidate pathspecs) so a `failed→done` move of a base-tracked task file no longer leaves a dangling `D` on the base branch. (4) hardened `fixing.md` (stay in scope; do not work around missing/incompatible toolchains or pre-existing failures) and `supervisor.md` (flag repeated identical/out-of-scope failures; always write a non-empty final summary). **Deferred:** (a) **preflight warning when `command_sets` leave shared/root paths uncovered** — now that an unclaimed path runs nothing, a root build-config change can pass unchecked; warn (non-fatal) at preflight when there is no catch-all set (lever: checks-config validation / `core/flow/validator.py`). (b) **supervisor empty-output root-cause** — in this run the supervisor's per-step notes and final summary were empty (`supervisor_final.summary_written=false`, `result.json structured_output=null`); the deterministic fallback now yields a meaningful summary regardless, but diagnosing why the read-only supervisor turn returned no text needs the run's `prompt_audit`/`result.json` (not reproducible from orchestrator source). | [task-023-run-analysis.md](../../analysis/task-023-run-analysis.md); `checks/selection.py`; `git_manager.py` `diff_stat`/`commit_audit`; `packaged/flows/roles/{fixing,supervisor}.md`; `core/flow/validator.py` | done (P0/P1); P2 deferred | | 2026-06-24 | **ion-list run remediation: cross-provider fallback provider pins; evaluator-infra not fatal; infra failure reports; Codex reasoning config override; pending→failed audit staging; supervisor artifacts; gitignore noise** | From a post-mortem of `ion-list-full-lines-and-last-divider` in the `debates` repo ([docs/analysis/ion-list-full-lines-and-last-divider-run-analysis.md](../../analysis/ion-list-full-lines-and-last-divider-run-analysis.md)): a green, on-spec diff (5/5 checks) was lost at `review` because the node pinned `codex/gpt-5.5`, codex rejected `--reasoning-effort`, and the claude fallback was sent codex's `gpt-5.5` → 404, then the infra-failed evaluator killed the task as `failed`. **Done (2026-06-24):** (1) cross-provider fallback resets node-pinned `model`/`reasoning`, `extra_args`, and `session_id` in `router.py` `_build_request` (model ids, CLI args, and sessions are provider-specific). (2) an evaluator that cannot _run_ raises `EvaluatorInfraError` → orchestrator degrades to `manual_action_required` (branch + green diff preserved), not `failed`. (3) `_fail` writes `failure_report.json`/`stuck.md` on every infra terminal (`_write_infra_failure_report`, `loop="infra"`). (4) Codex reasoning is emitted as `-c model_reasoning_effort="..."` (`max` aliases to `xhigh`; `minimal` supported), and preflight probes `codex exec --help` for `-c/--config`, failing fast if config overrides are unavailable. (5) `commit_audit` adds `pending`/`processing` to the staged lifecycle states so a `pending→failed`/`pending→done` move of a base-tracked task file leaves no dangling `D`. (6) supervisor turns use the observed step's `node_run_id` (no `run-000000` collision) + record `observation_failed`. (7) `ensure_runtime_excludes` writes the clone-local `.git/info/exclude`, never the tracked `.gitignore`. (8) provider-specific reasoning validation and Codex `workspace-write + network_access=true` flow rejection prevent known unsafe/misrouted combinations before launch. Target repo: `debates/.worc/flows/implementation.yaml` `review` node unpinned from codex back to the global primary. **Deferred:** the preflight check uses `codex exec --help` (cheap, no model spend) rather than a live `codex exec` smoke as the analysis suggested. | [ion-list-full-lines-and-last-divider-run-analysis.md](../../analysis/ion-list-full-lines-and-last-divider-run-analysis.md); `routing/router.py`; `core/flow/nodes/{base,evaluator}.py`; `core/orchestrator.py` `_fail`/`_write_infra_failure_report`; `providers/{_adapter_base,codex,capabilities}.py`; `core/flow/validator.py`; `config/validation.py`; `git_manager.py` `commit_audit`/`ensure_runtime_excludes`; `core/supervisor.py` | done (P0/P1); Codex smoke deferred | | 2026-06-23 | **Operator-driven PR merge (`worc prs` + `worc merge-task` + `worc tasks`)** | Operator go-ahead to land a reviewed orchestrator PR (the human-in-the-loop counterpart to `git.auto_merge`; flips a `depends_on` dependency to ready). **Implemented 2026-06-27 (suite green, 1580 tests).** The ADR locked a **routine** (Option A); in discussion the owner switched to the **flow** alternative (Option B): conflict resolution is a new operator-editable `merge` flow selected by `git.merge_flow` (default `merge`, seeded editable at `.worc/flows/merge.yaml`: `conflict_resolution` agent → `testing` checks → bounded `merge_fix` loop → no-op `publish` `policy: none` terminal). `worc merge-task <id>` pulls base into the task branch (`update_branch_with_base`); a **clean** base-merge is mechanical (`push_branch_update` + reuse `merge_pr`); a **conflict** runs the flow transactionally via an ephemeral engine executor (`_run_merge_flow`: reuses `build_node_inputs`/`build_node_services`/`drive_flow`, fresh run_state, in-memory `_EphemeralRunRecorder` so no checkpoint collides with `rerun --continue`, no supervisor), then the orchestrator commits the merge (`commit_merge_resolution`, with a `git diff --cached --check` leftover-marker guard) and `merge_pr`; any failure → `merge_abort` + PR left open + non-zero, **`DONE` never downgraded**; a `manual_action_required` task flips to `DONE` via the existing `finalize_task` path. `worc prs` lists open un-merged PRs (`--check` live state; `--sync` reconciles externally-merged PRs via `record_external_merge` + `find_open_pr_tasks`, dry-run unless `--yes`, `sync_external_merges`). `worc tasks` lists all tasks (`--status`). **Safety-only** gate (`merge_pr` stays `--admin`-free); **no new task status, no schema/config-version bump** (additive `git.merge_flow` + reuse `tasks`/`publish_operations`, new `merge_commit` publish-op kind). **Deferred sub-items:** (a) **path/area-based merge-flow collection** — shipped one configurable flow name; a future `git.merge_flows` selector keyed on changed paths (the `checks.command_sets` diff-selector pattern) for per-area resolution (mobile/backend/web) is the seam, not built; (b) `worc tasks` **overlaps `worc list --all`** (the `--status` filter is the only new capability) — fold/dedupe into `list` later; (c) a **watch-startup stale-`MERGE_HEAD` probe** — today `GitManager.prepare_branch` aborts a stale merge before the base checkout (sufficient); a startup-recovery probe is belt-and-braces; (d) the `--admin` forbid hardening (row above) now also covers this path. Full design + the rejected routine alternative (Option A) in the detail file. | [orchestrator-driven-pr-merge.md](done/orchestrator-driven-pr-merge.md); `cli.py` `cmd_prs`/`cmd_merge_task`/`cmd_tasks`; `core/orchestrator.py` `merge_task`/`plan_merge`/`sync_external_merges`/`_run_merge_flow`; `git_manager.py` `update_branch_with_base`/`merge_abort`/`commit_merge_resolution`/`push_branch_update`/`record_external_merge`; `state_store.py` `find_open_pr_tasks`; `packaged/flows/merge.yaml` + `roles/conflict_resolution.md` | done (as flow; collection deferred) | | 2026-06-24 | **watch-autonomy/dependency-chain run remediation: `error_max_turns` misclassification; `max_turns` default 50→400; failed-path dirty tree; HITL log visibility; failure-report diff; agent hook isolation** | From a post-mortem of the autonomous `watch` run of the `unify-input-alert-styling-and-buttons` → `redesign-form-controls-to-match-demo` (`depends_on` + `auto_merge`) chain in the `debates` repo ([docs/analysis/watch-autonomy-dependency-run-analysis.md](../../analysis/watch-autonomy-dependency-run-analysis.md)): all three target mechanisms passed (A → PR #3 auto-squash-merged; B unblocked the next tick on the pulled base), but B failed at `implementation` when the broad refactor exhausted `--max-turns 50` — the CLI emitted a terminal `error_max_turns` (exit 1) that the adapter mislabeled `process_crashed` ("exited abnormally"), losing the real cause. **Done (2026-06-24):** (1) the shared adapter now parses the terminal `result` event **before** treating a non-zero exit as a crash — a parseable terminal event is an agent OUTCOME classified by content (`error_max_turns` → returned `TASK_FAILURE` with the subtype appended to the message), never a raised `PROCESS_CRASHED`; only a non-zero exit with NO parseable terminal event stays `PROCESS_CRASHED` (`providers/_adapter_base.py` `run()` + new `ParsedEvents.failure_subtype`; `providers/claude.py` `parse_stream_json`; regression tests in `tests/providers/test_claude_run.py`/`test_claude_parsing.py`). (2) Claude `max_turns` default raised 50→400 across the loader default, installer, example, and docs (`config/loader.py`, `install/config_writer.py`, `packaged/config.example.yaml`, `docs/configuration.md`, B03); kept the field nullable so an explicit `None` still omits the flag. **Deferred:** (a) **graceful turn-budget exhaustion** — `error_max_turns` still ends the node as a terminal `TASK_FAILURE`; a resume-in-same-`editing_lineage` continuation (or a `manual_action_required` "rerun --continue" terminal) on top of the now-accurate class is the richer fix (lever: node runner in `core/flow/*`). (b) **failed-path dirty base tree** — a base-tracked task file that fails _before any commit_ leaves a dangling `D` after terminal cleanup checks out base (the prior `commit_audit` staging covers commit-bearing moves, not fail-before-commit), and the run logged `cleanup_safe=true` while the tree was dirty (levers: `core/orchestrator.py` `_relocate_task_file` + terminal cleanup; `git_manager.py`). (c) **HITL pause invisible in the operator log** — an agent `human_input` question (planning, ~8 min Telegram wait) emits no info-level line, indistinguishable from a hung node (lever: `core/hitl.py` + `orchestrator.py` ask path; optionally a shorter watch HITL timeout). (d) **`failure_report.final_diff` empty** though the failed branch carries committed partial work — diff it as `base..HEAD` of the agent branch (lever: `_fail`/`_write_infra_failure_report`). (e) **spawned agents inherit the operator's global Claude Code hooks** (RTK rewrote the agent's `npm run lint` during implementation) — isolate the agent's `CLAUDE_CONFIG_DIR` from the operator's (lever: provider child-env build / `security`). | [watch-autonomy-dependency-run-analysis.md](../../analysis/watch-autonomy-dependency-run-analysis.md); `providers/{_adapter_base,claude}.py`; `config/loader.py`; `install/config_writer.py`; `packaged/config.example.yaml`; `core/orchestrator.py`; `git_manager.py`; `core/hitl.py` | done (P0 + max_turns); P1/P2 deferred | | 2026-06-26 | **OS-aware env allowlist (Windows preflight fix) + deferred `upgrade-config` repair** | Real Windows testing: `worc preflight` reported `claude: FAIL — 'claude --version' did not succeed` while the CLI worked in the shell. Cause: the default `security.allowed_environment` was POSIX-only and omitted `SystemRoot`, so the Node-based `claude.exe` launched by `build_child_env` (allowlisted env only) aborted at startup with exit `0xC0000409`. **Done (2026-06-26):** `default_allowed_environment(system)`/`os_essential_env(system)` in `security/env.py` make the default OS-aware (cross-platform base + host-OS launch essentials, e.g. `SystemRoot` on Windows); the loader fallback and the installer both use it; example + docs updated; the allowlist stays the single env gate (security rule #4); WSL reports as Linux → POSIX set (correct). **Deferred:** existing configs carry an **explicit** `allowed_environment` that REPLACES the default, and `upgrade-config` is add-only, so a config written before this change keeps its POSIX-only list and is not auto-repaired on Windows — add an `upgrade-config`/`doctor` repair that re-adds the host OS's launch essentials to an existing allowlist (fold into the umbrella `upgrade`/`doctor` row above). Manual workaround: add `SystemRoot` (and the rest) to `allowed_environment` by hand. | [windows-cross-platform-support.md](done/windows-cross-platform-support.md); `security/env.py`; `config/loader.py`; `install/config_writer.py`; `packaged/config.example.yaml`; `config/upgrade.py`; `cli.py` `cmd_upgrade_config` | done (fix); upgrade-config repair deferred | | 2026-06-26 | **🔴 HITL session resume & planning autonomy (td-be-003 findings 0+4)** | Dedicated task carved from the td-be-003 run. **P0 CRITICAL (operator directive):** HITL must RESUME the agent session, never recreate it — after the operator replies the agent continues the SAME session from the request point, for ANY node/`session_scope` (td-be-003: `planning` re-ran with different `session_id` `c8162f4f92ff`→`c5d799a3aef0`, full re-plan, answer delivered as a file). Lever: `core/flow/nodes/agent.py` — thread first-run `session_id` into the 2nd `_invoke` (`:132`) + `_reconsider` (`:373`), lift the `_resume_session_id` editing-lineage-only gate (`:491-508`) for HITL re-invoke; provider resume already exists (`providers/{claude,_adapter_base}.py`), keep the provider gate. **P2:** planning approval makes the run non-autonomous (8h Telegram wait) — pre-decide scope in the task spec or downgrade to a question-with-default. Full design + acceptance in the detail file. **Done (2026-06-27):** P0 session resume implemented **in-process** — the HITL post-answer re-invoke threads the first run's `session_id` (`_hitl_resume_session_id` + the `resume_session_id` arg on `_invoke`/`_build_request`), same-provider gated, working for ANY `session_scope`; `_reconsider` needed no change (its `editing_lineage` author nodes already resume via `_resume_session_id`). P2 satisfied by the "explicitly documented" branch (task-authoring + operations docs). **Deferred:** (a) **durable across-restart HITL resume** — in-process only today; after a restart a `fresh_disposable` node has no durable session slot, so resume falls back honestly to a fresh session + the answer context file. Adding it needs new persistence keyed by `(task, node, subtask)` + provider gate and a `state.db` bump (the user chose the in-process-only scope). (b) **autonomy code knobs** — a per-node `HitlSettings.timeout_s` for agent nodes (mirror the standalone `HitlNode.timeout_s`), and a data-driven bounded-wait "auto-proceed on the agent's produced default" for embedded planning/refinement HITL only (NEVER the dangerous-diff guard, which stays mandatory human-gated). | [hitl-session-resume-and-autonomy.md](done/hitl-session-resume-and-autonomy.md); [td-be-003-conform-m1-m2-contract-shapes-run-analysis.md](../../analysis/td-be-003-conform-m1-m2-contract-shapes-run-analysis.md); `core/flow/nodes/agent.py`; `providers/{claude,_adapter_base}.py`; `core/hitl.py`; `docs/{task-authoring,operations}.md` | done (P0 in-process; P2 documented); durable-restart + autonomy knobs deferred | | 2026-06-25 | **redesign-form-controls run remediation: `rerun` source resolver; base-inclusive `current.diff` + check selection; finalize-tail logging; doc-node `medium`; skill-clause/supervisor prompt; per-task `decomposition`** | From a post-mortem of `redesign-form-controls-to-match-demo` (attempt 4, rerun) in the `debates` repo ([docs/analysis/redesign-form-controls-to-match-demo-run-analysis.md](../../analysis/redesign-form-controls-to-match-demo-run-analysis.md)): the run itself was clean (auto-decomposed into 6 subtasks, 0 fix cycles, all checks green, content-rich summary), but the failed prior attempt was **not rerunnable** without manual git surgery, `current.diff` understated the change ~18×, and one decomposed subtask shipped committed code that ran **zero** checks. **Done (2026-06-25):** (P0) `plan_rerun` resolves the task source tolerant of lifecycle-folder desync — if the stored `source_path` is missing it searches `tasks/{pending,processing,done,failed}/` by id (then slug) via `_resolve_task_source`; >1 match refuses with an ambiguity message rather than guessing (`core/orchestrator.py`). (P1.1) `write_current_diff` now diffs `base` vs the working tree (`git diff <base>`, like `diff_stat`) so committed-since-base work is captured in `current.diff`/`{diff_path}`/PR body/failure report, not just the trailing uncommitted hunk (`git_manager.py`). (P1.2) the `checks` node selects command sets from a **base-inclusive** changed set — new `changed_code_paths_since_base()` (`git diff --name-only <base>` + untracked) used only for selection, leaving the staging set `changed_code_paths()` working-tree-only — so a subtask already committed on the branch is still checked instead of passing vacuously (`git_manager.py`, `core/flow/nodes/{base,checks}.py`). (P2.1) `_engine_finalize` logs the end-of-run tail (`task finalize: starting` → supervisor summary written with `elapsed_seconds` → publish prep) so the long whole-task-summary step is no longer a silent window (`core/orchestrator.py`). (P2.2) the packaged `documentation` node pinned to `reasoning: medium` (mechanical write-up, less wall-time). (P2.3) removed the dangling "skill reference paths…" clause from the `implementation`/`fixing`/`documentation` role files — `build_context_footer` already advises about skills and omits them when none are selected. (P2.4) `supervisor.md` made decomposition-aware (a subtask step is not whole-task closure). (P2-opt) optional per-task `decomposition` (`true`/`false`) field — task-wins gate override mirroring `prompt_audit`/`auto_merge` (`_decomposition_gate_on(task)`), flips only the gate, never forces a split; an operator `subtasks:` manifest ignores it (`task/{model,parser,validation_gate}.py`, `core/orchestrator.py`). **Out of scope / deferred:** (a) **finding 5 root cause** — the ~22-min silent tail is now instrumented (P2.1) but its cause is only confirmable by observing the next run. (b) **finding 4 (operator `rtk` hook leaked into the agent sandbox)** — already tracked as the 2026-06-24 watch-autonomy row item (e) `CLAUDE_CONFIG_DIR` isolation. (c) **finding 6 (light/dark "by construction")** — marking visual acceptance criteria as manual in `roles/review.md` was excluded from the План исправлений and not implemented. (d) packaged role/flow edits (P2.2/P2.3/P2.4) reach only **new** installs until the `upgrade-flows` re-sync row above ships. | [redesign-form-controls-to-match-demo-run-analysis.md](../../analysis/redesign-form-controls-to-match-demo-run-analysis.md); `core/orchestrator.py`; `git_manager.py`; `core/flow/nodes/{base,checks}.py`; `packaged/flows/implementation.yaml`; `packaged/flows/roles/{implementation,fixing,documentation,supervisor}.md`; `task/{model,parser,validation_gate}.py` | done (P0/P1/P2 + per-task decomposition); findings 4/5/6 out-of-scope/deferred | | 2026-06-28 | **Windows hard stop (`taskkill`)** | **Superseded / done by the console R1 remediation (2026-07-02):** `worc stop --force-full` now hard-kills the Windows daemon tree via an injected `taskkill /F /T` seam (`hard_kill_tree`), no longer degrading to soft. Original gap (now closed): `--force-full` was POSIX-only (`os.killpg` on the process group); on Windows `os.killpg`/`os.getpgid` are unavailable. The `taskkill /T` tree-kill reaches the agent as a daemon descendant, so `CREATE_NEW_PROCESS_GROUP` was not needed (it stays deferred for a future graceful `CTRL_BREAK`). Real Windows smoke-test still pending (see the R1 validation row above). Inherited from [windows-cross-platform-support.md](done/windows-cross-platform-support.md). | `process_control.py` (`stop_process`, `_stop_via_tree_kill`); `providers/process.py` (`hard_kill_tree`, `spawn_detached`) | superseded (done via R1) |

## Post-test-run review improvements

These improvements were found while reviewing real orchestrator test runs (first review: the `pr_title` task, 2026-06-14); the change shipped. The former dedicated `post_test_run_review.md` document was removed together with `implementation_stages/`; record new test-run findings in [follow_ups.md](../follow_ups.md).

_Refs:_ `notify/telegram.py` (`poll_reply`, `send_prompt`/`_drain_pending`, `wait_for_answer`, `check_polling`, `get_webhook_info`); `notify/interface.py` (`AskHandle.update_offset`); `core/hitl.py`; the `pr_title` guardrail (`logs/task-pr-title-override/hitl/guardrail-fixing-cycle-10.json`).
