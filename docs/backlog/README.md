# Backlog

Status: **inventory** Date: 2026-06-21 Owner: Vladimir Makarevich

This folder is the single place for backlog and deferred-product ideas for **wastech-orchestrator**. It is an inventory, not an implementation contract. The source of truth remains the code.

Backlog documents must not override the hard invariants from [../../CLAUDE.md](../../CLAUDE.md), [../../AGENTS.md](../../AGENTS.md), or [../../.agents/rules/](../../.agents/rules/).

Where to find the design detail:

- **Open items** keep their detailed design in a file in this folder, linked from the tables below.
- **Completed backlog items** are archived in [archive/done/README.md](archive/done/README.md).
- **Shipped follow-up history** lives in [archive/follow_ups_history.md](archive/follow_ups_history.md) for traceability.
- **Build-time tech-debt and implementation follow-ups** live in [follow_ups.md](follow_ups.md), not here.

## Other files in this folder

| Document | Purpose |
| --- | --- |
| [follow_ups.md](follow_ups.md) | **Open** implementation follow-ups / tech-debt discovered while building — distinct from product features. Recorded via `/sync-docs`. Completed/superseded entries are archived in [archive/follow_ups_history.md](archive/follow_ups_history.md). |
| [runtime_provider_capacity_gate.md](archive/runtime_provider_capacity_gate.md) | Detailed backlog task for checking Codex and Claude capacity before autonomous `watch` admits a pending task. |
| [archive/concurrent-task-worktrees.md](archive/concurrent-task-worktrees.md) | Decision record for processing tasks in parallel via `git worktree`: chosen as the v2 concurrency primitive (not for isolation), gated on a mandatory provider capacity gate. |
| [archive/token_optimization.md](archive/token_optimization.md) | Detailed backlog task for measuring and reducing token usage. |
| [archive/codex-controlled-provider-home.md](archive/codex-controlled-provider-home.md) | Deferred hardening split out of the read-isolation cluster (WRI-003, review decision F7): run autonomous Codex from an orchestrator-controlled `CODEX_HOME` (auth/session/config by construction) instead of layer-isolating the operator home; requires an explicit one-time operator `codex login` contract. |

## Completed backlog

Completed product backlog items are archived in [archive/done/README.md](archive/done/README.md).

## Open backlog

### Other deferred features

These are deferred by the v1 spec or described in architecture notes; not scheduled.

| Item | Summary | Source / constraint |
| --- | --- | --- |
| [Shell quit safety + staged task-file creation](done/shell-quit-safety-and-task-staging.md) | **Implemented 2026-07-14.** Two independent fixes for an operator-safety gap: (A) a loud `WARNING` + confirmation gate on `worc shell` `quit`/`exit` when the watch daemon is still running (`quit` detaching is intentional — `M3` — and stays; this only makes that state impossible to miss); (B) a new `tasks/preparing/` staging folder that all task-file producers (human, `worc-task`/`worc-deco-task` skills) write to first, plus a `promote` verb (`worc promote <id\|file>` / `--all`, and the same in `worc shell`) that atomically moves a finished file into `tasks/pending/` — closes the race where the still-running daemon picks up a task file mid-write. `enqueue` is now an atomic copy too. | From an operator incident: `up` → task ran → `quit` → daemon (by design) kept polling → a new task file being composed directly in `tasks/pending/` was picked up and quarantined before it was finished. |
| [Agent prompt audit trail is missing the context-files footer](done/agent-prompt-audit-missing-context-footer.md) | **Implemented 2026-07-13** (two sweep-found gaps deferred). The "Context files" footer (`build_context_footer`/`build_effective_prompt`) appended before the prompt reaches stdin was never persisted to `rendered-prompt.md`/`request.json`/prompt-audit, and `core/supervisor.py` turns wrote no audit artifact at all. Both fixed: the footer builders moved to the public `providers/base.py` and are now persisted (incl. the previously-missing `skill_reference_paths`), and the supervisor writes `rendered-prompt.md`/prompt-audit per successful turn. Two further gaps the sweep found are deferred — `tool.py` stdin has no audit artifact, and the memory-packet file goes stale on a node's repeat runs (see [follow_ups.md](follow_ups.md)). | From a live `wastech-mdlint` run (`p6-04-config-writer-schema`, `stages/fixing/run-000124`). |
| [A stopped task stays `running` and silently blocks the slot](done/parked-running-task-blocks-slot-no-signal.md) | **Implemented A + B + C 2026-07-13.** After a clean `down`/`stop`, the in-flight task is deliberately left `running` at its checkpoint (parked for resume) and holds the single slot — but nothing told the operator. Shipped: (A) a stop-time note naming the parked task + the recovery levers (`rerun --continue` / `finalize`), (B) a `parked (no daemon)` label in `status`/`top`/`list`, (C) an enriched `SlotBusyError` naming the blocking task (id + node). Signalling only — recovery model unchanged, `stop` stays daemon-only. | From a live `wastech-mdlint` session (`p7-05-integration-tests-docs-2`, stopped via `down` at `planning`). Sits between [shell-quit-safety-and-task-staging.md](done/shell-quit-safety-and-task-staging.md) (daemon _still_ running) and 🔴 Reliable stop (orphaned _processes_); here the daemon is gone and the survivor is DB state. |
| [Rerun dead-end: fails before its first checkpoint on an operator-owned branch](done/rerun-dead-end-pre-checkpoint-existing-branch.md) | **Implemented 2026-07-14 (Option A; Option C deferred).** A task that failed before its first checkpoint on a `branch_mode: existing`/`current` branch could be re-attempted by **neither** `rerun` mode (fresh refused — won't reset an operator-owned branch; `--continue` refused — no checkpoint recorded). `rerun <id>` now **restarts such a task in place** on the branch as-is (no reset, ledger-linked as the next attempt) whenever it has no checkpoint; a run that _did_ produce work still refuses fresh and points to `--continue`. Option C (resolving the flow pre-slot so the dead-end can't form) was deferred — with A in place the dead-end is always recoverable (see [follow_ups.md](follow_ups.md)). | From a live `wastech-mdlint` run (`p7-03-graph-tools`); sibling of the shipped [rerun-flow-drift-and-budget-confirm.md](done/rerun-flow-drift-and-budget-confirm.md), which only covered tasks that _had_ a checkpoint. `state.db` stays sole authoritative state; branch-ownership invariant unchanged. |
| [Rerun flow-drift reconciliation and fix-budget confirmation](rerun-flow-drift-and-budget-confirm.md) | **Implemented 2026-07-12** (simplified from the original proposal — see the ADR's top note). `rerun --continue --from NODE` used to refuse on any whole-file flow-fingerprint drift, even changes upstream of `NODE`; `--from` now resumes through any drift as long as `NODE` exists (no structural compatibility check, no new flag), rebaselining the checkpoint's fingerprint so the resume actually lands at `NODE`. Plus an interactive confirmation gate, scoped to the whole forward-reachable path, before resuming into an already-exhausted fix-loop budget. | Closes the deferred flow-fingerprint-drift item named in [follow_ups.md](follow_ups.md) (from the now-removed `universal-resume-recovery.md`); `state.db` stays the sole authoritative state, no manual SQL edits. |
| Packaged built-ins are delivery-only | Make the bundled `packaged/` tree a delivery-only source: remove the runtime flow-resolution fallback (`_PACKAGED_DIR` in `FlowRegistry._find`/`_builtin_names`) so flows and their role prompts resolve **exclusively** from `.worc/flows/`; a missing flow becomes an explicit error, not a silent load of the bundled copy. `packaged/` is only ever read to deliver files into `.worc/` (install / upgrade / copy command). | `.worc/` becomes the single source of truth for what runs; strengthens "install seeds editable active copies". Prerequisite/coupling: upgrade must re-seed new built-in flows (no `upgrade-flows` yet). Broader sibling of the shipped validate-flow ADR. |
| [Install and upgrade flow](install-and-upgrade-flow.md) | Fix the git-tag install/upgrade friction found on `v0.8.6a3`: `pipx upgrade` never advances a pinned git ref, `pipx install --force` fails on the uv backend, and the `[shell]` extra needs quoted PEP 508 form. Phased path: document the correct git recipe now → publish to PyPI (real fix) → optional `worc self-update`. | Greenfield (no migration); credentials stay outside the orchestrator; cross-platform quoting (zsh/bash/PowerShell). From `wastech-mdlint` testing. |
| Post-p0 robustness hardening | Two deferred hardening decisions from the p0 campaign: (A) revive-finalize durability — the finalize summary silently degrades to the minimal fallback on `rerun --continue` (session lost); (B) F17b doc-node capability policy — `network_access:false` hard-blocks install only on Codex's OS sandbox, not on Claude (Bash still reaches the network). | Supervisor stays advisory; hard security ceiling unchanged; each decision independently schedulable. Promoted from [follow_ups.md](follow_ups.md); from p0 findings. |
| [Agent read-isolation from the .worc runtime home](agent-worc-read-isolation/README.md) | **Design re-reviewed 2026-07-21; implementation tasks ready.** Split provider-readable current-task inputs into `.worc-io/<task-id>`, freeze and deny the in-repo control plane/instruction inputs, prove the provider process tree is gone, keep private audit/runtime state separate, enforce Codex with current permission profiles, enforce Claude with tool policy plus its supported-host Bash sandbox, and seal every terminal exchange before another task can run. | Codex uses no-model effective-policy canaries on macOS/Linux/WSL/native Windows and never mixes permission profiles with legacy `--sandbox`. Claude uses fail-closed Bash containment on macOS/Linux/WSL2; native-Windows strict workspace-write omits Bash. Typed layout/control freeze and process quiescence precede trust in post-attempt manifests; later private-state relocation is defense in depth, not a security boundary by itself. |
| [Git path parsing hardening for Unicode and quoted names](done/git-path-parsing-hardening.md) | **Implemented 2026-07-15.** Replaced line-based parsing of Git path output with three centralized `-z` helpers in `git_manager.py`, migrated across all six hot-spot call sites (staging, check selection, dangerous-diff logic, handoff, cleanup) so Cyrillic and other non-ASCII file names, spaces, quotes and rename records are all handled correctly. | From a 2026-07-15 publish incident on `blog/20260206-моя-история-(EN).md`; fixed the whole bug class, not only `publish`. |
| [Reliable stop: make POSIX `down --force` a pending graceful stop](done/reliable-stop-posix-soft-timeout-gap.md) | **Implemented 2026-07-20.** A live 2026-07-16 repro proved POSIX `down --force` still turned into a post-timeout kill of the watcher PID — not truly graceful and still incomplete. `--force` now stays soft even after timeout (graceful shutdown stays pending until the current node finishes, every handle retained); by operator decision the same pending-stop contract was extended to **Windows** too, so `--force-full` is the sole hard-kill rung on both platforms. | Narrow remaining slice of the older "Reliable stop" work after the shipped subtree-kill refactor. From `wastech-orchestrator` `watch` testing and the live `rework-ch04-calendars-2` repro. |
| [Runtime provider capacity gate](archive/runtime_provider_capacity_gate.md) | Before autonomous `watch` claims a pending task, query the capacity of the Codex/Claude accounts its resolved routes need and defer the task when configured headroom is unavailable. | Runtime admission control, not install preflight. Deferred tasks stay pending, consume no attempts, retried after provider reset. |
| [Token optimization](archive/token_optimization.md) | Measure and reduce token consumption across stages. | Analysis + candidate levers; not part of v1 scope. |
| [Token optimization (2026-07-16 analysis)](token-optimization/README.md) | Campaign grouping the five backlog items from the 2026-07-16 token investigation under one execution order: **measurement first** (normalized usage accounting — _shipped_), **content-flow hygiene** (packaged `blog_article_revise` defaults + session-scope docs — _shipped_), then the supervisor roadmap **P0** (deterministic `SupervisorPacket` → fresh finalize → skip `tool`/`checks`) → **P1** (`observation_mode` + event cadence + budgets) → **P2** (responsibility split + per-function telemetry). See the folder [README](token-optimization/README.md) for the initial context and dependency table. | Supervisor stays advisory-only; the P0→P1→P2 order is a correctness constraint (cadence must not change before the packet exists). From the 2026-07-16 `blog-review-happy-in-my-misfortunes-4` token analysis + [supervisor-token-optimization-options](../analysis/2026-07-16-supervisor-token-optimization-options.md). |
| Config-level per-node `node_defaults` | The complement to the shipped per-task `nodes:` overrides: per-node `{model,reasoning}` defaults under each provider in `config.yaml`. | Tracked in [follow_ups.md](follow_ups.md). Touches schema (+version bump), loader, validation. |
| Richer task parsing | Extract structured metadata beyond `id`, `title`, `refined`, `decompose`, `agents`, `contacts`, `model`, `reasoning`, `stages`, `branch_name`, `auto_merge`. | Candidate fields: repo binding, commands/hints, priority, labels, issue links. Must stay fail-closed. |
| Parallel and graph decomposition | Graph-shaped subtasks, per-subtask worktrees/branches, parallel execution. | V1 decomposition is linear, sequential, on one task branch. |
| [Concurrent task processing via worktrees](archive/concurrent-task-worktrees.md) | Process multiple independent tasks at once by giving each its own `git worktree`. | Must not share a mutable working copy between active agents. Breaks the single-active-task invariant only behind worktree isolation. Deferred v2; isolation alone is solved by a dedicated clone. **Decision record.** |
| PR template support | Generate PR bodies from a configurable template in addition to `summary.md`. | Should integrate with the existing summary stage and Git Manager. |
| GitHub Issues integration | Link tasks to issues, update status, optionally close on PR creation/merge. | Requires GitHub auth through the existing external credential model. |
| Web UI for tasks and logs | Browser UI for pending/running/done tasks, artifacts, logs, stuck states. | Read-only first; mutations need auth/audit. |
| Per-task budget limit | Task-level budget/time/token limits. | Must not let a task raise global security or cost ceilings. |
| Auto-close stale tasks | Detect and close/archive/escalate stale pending/running/manual tasks. | Needs explicit policy and audit trail. |
| Dry-run without push | Run planning/check/review/publish prep without pushing or opening a PR. | Must clearly mark that no publish operation occurred. **Per-task slice shipped**: a task's `publish: commit`/`push` cap stops before the PR (see branch mode); a global/CLI dry-run flag is the remaining part. |
| Lightweight project memory | Persist small project-level lessons/conventions between tasks. | Optional; must not store secrets or unbounded agent context. The minimal first slice of orchestrator memory. |
| Orchestrator memory (cross-task reuse, defrag, autodream) | Persistent, repo-scoped memory across tasks; design distilled from a research-backed architecture blueprint (files-first `.worc/memory/`; short-term / long-term / entity tiers; **narrow supervisor + deterministic MemoryService/PacketBuilder/CleanupJob/DerivedIndex**; precision-first per-stage packets; trust/quarantine/audit/rollback; SQLite→embeddings→graph roadmap gated by an eval plan). Consolidates two deep-research reports; supersedes an earlier exploratory design. **V1 implemented and merged to `main`** (phases 01–05 + audit remediation, PR #14) — **on out of the box** (a fresh `worc install` ships `enabled: true`; an absent or `false` block = off); V2/V3/V4 gated by a measured lift (AC-O4). | No secrets in memory; not an unbounded context dump; advisory only (Core decides, no enforcement channel); memory ≠ derived index; bounded + audited autonomy that never touches an active task. |
| Dynamic agent selection by model | Let a model choose provider/stage routing dynamically. | Must not bypass the configured allowlist, route audit, or deterministic fallback rules. |
| Provider SDK/API backends | OpenAI API / Claude Agent SDK backends in addition to CLIs. | Must remain behind `AgentProvider`; Core must not learn provider syntax. |
| Automatic CLI installation/authorization | Install or authorize Codex/Claude/GitHub CLIs automatically. | Current policy keeps credentials and auth outside the orchestrator. |
| Vendor session transfer | Transfer an active vendor session between Codex and Claude Code. | Not supported; artifacts, not sessions, are the source of truth. (Per-provider durable resume shipped in the durable-sessions item.) |
| Multi-repo/project binding | Select a configured repository per task (`repo` field / project map). | Current config targets one repository. Requires validation and workspace isolation. |
| Agent instruction stubs in target repo | Seed or update target-repo `AGENTS.md`, `CLAUDE.md`, and skills before a run. | **Partial:** the skill-_reference_ half shipped (whole-repo skill discovery, operator pins + the supervisor proposal, `{skills_path}`; see skills-selection-rework); _authoring/managing_ the stubs themselves is still deferred (see [follow_ups.md](follow_ups.md)). |
| [Remote operator agent over Telegram (`worc listen`)](archive/worc-listen-remote-control.md) | Long-running Telegram interface to drive and interrogate the orchestrator by tagging the bot: a conversational "navigator" agent (NL → proposed actions, answers from redacted state) layered over a deterministic, configured-chat-only command grammar (list/status/logs/run-existing/rerun/stop). Remote twin of the local console; further horizon = full task dictation (compose + create + run new tasks from chat, still via a validated file). **Proposed (exploratory — future vision).** | One bot token = one long-poll (409 Conflict), so a single daemon-owned poll broker is the shared prerequisite (also unblocks confirmation gates + step-trace); bounded to already-vetted artifacts (never authors task content); agent tool surface = the bounded verbs only; configured-chat trust boundary; proposer-proposes/Core-decides; fail-closed. |
