# Token optimization (2026-07-16 analysis)

Status: **open — measurement + content hygiene shipped; P0/P1/P2 all accepted 2026-07-26, none implemented yet** Date: 2026-07-16 Owner: Vladimir Makarevich

Residue from the items that land — open decisions, watch items, operational consequences, deliberate non-goals, and what the `main` docs refresh must pick up — is collected in [follow_ups.md](follow_ups.md) (empty until P0 lands; the two shipped items recorded theirs in their own documents).

This folder groups the backlog items that came out of the 2026-07-16 token investigation into a single campaign with one execution order. The code and the mandatory rules remain authoritative; these documents are the design detail, not an implementation contract, and must not override the hard invariants in [../../../CLAUDE.md](../../../CLAUDE.md) / [../../../AGENTS.md](../../../AGENTS.md) / [../../../.agents/rules/](../../../.agents/rules/).

> **Actualized 2026-07-23** (historical — premise **(a)** was refined and premise **(b)** was voided by the 2026-07-26 review below; read that note as the current state)**.** All five statuses were re-verified against the current code. Items #1–#2 are genuinely shipped (code + tests confirm every acceptance criterion); P0/P1/P2 are still unimplemented proposals (the supervisor code is unchanged since the analysis except for the WRI-011 finalize change). Two premises moved and are reflected in the item docs: **(a)** WRI-011 already made `finalize` read the task from a **frozen exchange packet by path** (context-footer), so P0's `SupervisorPacket` must be authored the same way (a frozen read-only artifact referenced by path in the two-root exchange layout), not as inline JSON; **(b)** the measurement substrate (#1) is merged, so P1's `max_digest_tokens` uses **real tokens from the start** — the char/count interim is dropped. Source line references in the item docs were refreshed (WRI churn shifted `orchestrator.py`/`supervisor.py`); the cited `tests/core/test_supervisor.py` references still hold. Still open and now unblocked: the **A/B** that all the headline numbers are gated on (~60–80k for #2; ~480k→30–60k for the supervisor) has not been run, and there is no read/report surface to observe usage yet — both are the cheapest next moves.

> **Actualized 2026-07-26 — acceptance review done; P0, P1 and P2 are all `accepted`.** Every fork the three items had deferred to implementation time is now closed and recorded in each doc's «Решения приёмки» section (IDs X1–X2, P0-D1…D7, P1-D1…D8, P2-D1…D4 — the tables below carry the reasoning). Three text-level defects were fixed along the way: the in-scope doc lists named `main`-only derived docs (X2), the absolute token thresholds descended from code that no longer exists (X1), and P1's packaged defaults were phrased as per-flow-name matching, which the engine may never do (P1-D4). The review also **cut scope**: P1 loses its budgets (`max_calls` / `max_digest_tokens` / `on_budget_exhausted`) and the observe-session knob, its trigger list shrinks from six to three, and P2 drops the separate handoff/skill budgets and the dominance warning. Nothing is implemented yet — the critical path is unchanged (**P0 → P1 → P2**), and P0's first implementation step is the baseline run from X1.

## Initial context

The trigger was a single content run (`blog-review-happy-in-my-misfortunes-4`) whose token cost was dominated by the supervisor and inflated by broken accounting. Two analyses frame the work: the token analysis (F1–F5) and [../../analysis/2026-07-16-supervisor-token-optimization-options.md](https://github.com/VladimirMakarevich/wastech-orchestrator/blob/main/docs/analysis/2026-07-16-supervisor-token-optimization-options.md) (variants A–I, target architecture §6, phasing §8).

Three findings drive everything here:

- **Accounting was wrong before anything could be tuned.** Codex resume usage is cumulative for the whole session, so naive per-node summation double-counted (a reported 424 163 vs. a true 282 699 input tokens, +50%); Claude's real input is split across three fields that were never summed. No honest token A/B is possible until usage is normalized and persisted per attempt.
- **The supervisor is the largest Claude-context consumer.** On the analyzed run seven supervisor calls spent 480 293 input tokens (~70% of the task's Claude input); six of the seven were intermediate observations, and the supervisor is advisory-only (it never routes). The savings are large and safe — but only if `finalize` stops depending on the warm session first.
- **Content flows resume heavy sessions for tiny edits.** `polish` re-ran a 31–37k-token transcript four times for a one-word edit because it inherited `revise`'s editing lineage; the session-scope docs never warned that resume grows input tokens on every turn.

## Items in this folder

| Item | What it does | Status |
| --- | --- | --- |
| [Normalized token-usage accounting](normalized-usage-accounting.md) | Provider-aware normalized usage persisted per attempt in SQLite: Codex resume delta stops double-counting, Claude input = sum of its three fields, raw payload kept for audit; also fixes the latent `_produced_no_work` bug on resumed Codex runs. The measurement substrate every token A/B depends on. | **implemented 2026-07-16** |
| [Content-flow token hygiene](content-flow-token-hygiene.md) | Two engine-free tweaks: a token-cost / history-growth warning in the session-scope docs, and packaged `blog_article_revise` defaults (`polish` → `fresh_disposable`, read-once / one-patch / one-diff role-prompt budget). | **implemented 2026-07-16** |
| [Supervisor P0 — SupervisorPacket → fresh finalize → skip tool/checks](supervisor-finalize-packet-and-cadence.md) | Build a deterministic `SupervisorPacket` from durable state, make `finalize` always run fresh seeded by it, then stop observing `tool`/`checks` nodes. The order is mandatory. | **accepted 2026-07-26** (next to implement) |
| [Supervisor P1 — observation cadence](supervisor-observation-cadence-p1.md) | `observe.mode: all\|selected\|events\|none`, three event triggers, and split observe/finalize model+reasoning. The main saver. (Budgets and the observe-session knob were dropped — P1-D6 / P1-D8.) | **accepted 2026-07-26** (implement only after P0 merges) |
| [Supervisor P2 — responsibility split + telemetry](supervisor-responsibility-split-p2.md) | Extract a deterministic `StepRecorder` and persist per-function usage/cost with a supervisor report in the summary. (Separate handoff/skill budgets and the dominance warning were dropped — P2-D1.) | **accepted 2026-07-26** (implement only after P1 merges) |

See [happy-path.md](happy-path.md) for a plain-language **before/after** walk-through of the analyzed `blog_article_revise` run — what the supervisor costs today (7 LLM calls, 480k input) versus after the whole roadmap ships (1 fresh finalize, < 60k), with diagrams and the quality guards that stay in place.

## Execution sequence

The order is not a suggestion — two of the dependencies are correctness constraints, not preferences.

| # | Item | Depends on | Why the order is fixed |
| --- | --- | --- | --- |
| 1 | Normalized usage accounting | — | Foundation. Without a summation-safe per-attempt usage record, every downstream token claim (the A/Bs below, P1 budgets, P2 telemetry) is unmeasurable or wrong. |
| 2 | Content-flow token hygiene | ships independently; A/B confirmation needs #1 | The default flip and docs ship without waiting; the "~60–80k saved, quality no worse" claim is only honest once #1 exists to measure it. |
| 3 | Supervisor P0 | measurement from #1 for its A/B | **`finalize` must become packet-first and fresh before any cadence change.** Cutting observations first strands `finalize` with an empty digest — it re-reads the repo or thins the summary and degrades `follow_ups` / `memory_delta`. |
| 4 | Supervisor P1 | **requires #3**; its A/B uses #1 | `observe.mode: none`/`events` is safe only over the deterministic P0 packet. Budgets are no longer part of P1 (P1-D6), so nothing here depends on token accounting beyond the shared A/B measurement. |
| 5 | Supervisor P2 | **requires #4**; per-function telemetry uses #1 | Structural cleanup after the savings land — extracts the deterministic recorder P1 introduced and reports per-function usage on top of #1. Not on the savings critical path. |

Items 1–2 are done. The live critical path is **P0 → P1 → P2**; do not start a later phase before its predecessor is merged, and never change supervisor cadence before the P0 packet exists.

## From `proposal` to `accepted`

The repo convention (see [../deep-research-postmortem/](../deep-research-postmortem/README.md), where all ten items are accepted) is that an accepted item carries `Status: **accepted**` in its header plus a `## Decision (date)` section recording the chosen option and any explicitly dropped sub-item. Moving P0/P1/P2 out of `proposal` therefore means: close every fork the doc currently defers to implementation time, and make each acceptance criterion checkable as written.

Two rules for this review: **one decision at a time**, and **re-verify against the code before each question** — the 2026-07-23 pass already found two premises that had moved under the docs, so the assumption is that more have.

### Cross-cutting decisions (close before P0)

| ID | Open question | Why it blocks acceptance |
| --- | --- | --- |
| X1 | ~~The A/B baseline and how supervisor usage is read~~ — **decided 2026-07-26: re-measure the baseline once on current `dev`, make the thresholds relative, read the number by SQL, add no CLI surface.** One `blog_article_revise` run before P0 becomes the only baseline (its `task_id` + numbers are recorded in the P0 doc's new "A/B и baseline" section) and is reused by P1; P0 asks for −60% supervisor input, P1 for −85% and exactly one supervisor call. The measurement needs no new code: VF-8 (DB v19) anchors attempts by `task_id` and the supervisor layer's rows are exactly `node_run_id IS NULL` (`state_store.py:103-108`), with the v16 normalized columns already there, so `SUM(usage_input_total) … WHERE task_id = ? AND node_run_id IS NULL` is the whole query. An operator surface (`worc usage`) stays out — that is P2 items 3–4. | Closed — the historical absolutes (480 293 / 104 567 / 44 107) describe code that no longer exists (WRI-011 + content-flow hygiene landed since), so they stay as orientation only. |
| X2 | ~~Doc-sync scope on `dev`~~ — **decided 2026-07-26: name the exact files that exist here, plus a PR doc-impact note.** Each item now lists its real targets instead of the `main`-only derived docs: P0 → `packaged/guide/flows/roles.md` (its `:63` claim that the supervisor "observes **each step**" becomes false); P1 → `packaged/config.example.yaml`, `guide/config/reference.md:173-182` (the flat `supervisor.*` key table), `guide/flows/reference.md:22` (`SupervisorBlock`), `guide/flows/roles.md`, packaged flows; P2 → `guide/flows/roles.md`. `worc_architecture.md` / `configuration.md` are covered by a one-line doc-impact note in the PR description, never created on `dev`. | Closed — [AGENTS.md](../../../AGENTS.md) forbids creating the derived docs on `dev`, and naming the packaged files explicitly keeps the most-often-forgotten half of a doc change in scope. |

### P0 decisions — [supervisor-finalize-packet-and-cadence.md](supervisor-finalize-packet-and-cadence.md)

| ID | Open question |
| --- | --- |
| P0-D1 | ~~Where the packet physically lives and by which mechanism~~ — **decided 2026-07-26: its own request field plus a published copy in the agent-facing root.** Private authoritative `packet.json` in the task's supervisor artifacts → `publish_artifact(…, "supervisor/packet.json", …)` → `.worc-io/<task-id>/supervisor/packet.json`, referenced by a new `AgentRunRequest.supervisor_packet_path` rendered as a `packet` line in `build_context_footer`. **Not** the WRI-011 instruction bundle: that is frozen at task start and its manifest digest is verified before a session resume, so a late artifact would need a carve-out. Also touches the two other places that enumerate context fields (`providers/exchange.py:398` containment, `providers/_adapter_base.py:658` audit). |
| P0-D2 | ~~What "the packet is identical on a normal run and after revive" means~~ — **decided 2026-07-26: determinism means the packet is a pure function of durable state.** No inputs beyond `state.db` + artifacts (no clock, no env, no absolute paths — repo-relative POSIX, steps ordered by `node_runs.id`, canonical `sort_keys` JSON). Two testable claims replace the untestable one: building twice from the same state is byte-identical, and a revive that re-executed nothing yields the same packet (with re-execution it differs by exactly those steps). Timestamps / `stage_attempts` / fallback facts stay **in** the packet — they are deterministic and are the material for the summary's caveats. |
| P0-D3 | ~~The concrete bounded limits~~ — **decided 2026-07-26: strict, as named code constants (not config).** Full diff inlined only at ≤ 4 000 chars (otherwise changed paths + diff stat + path to `current.diff`), `steps[].message` ≤ 500, observation digest ≤ 8 000 — a packet ceiling around 16 KB. A small diff is still inlined because skipping it forces an extra tool round, and every round re-sends the whole prompt as input. The same 500-char cap also fixes a defect found during the review: `final_message` reaches the observe prompt unbounded today (`core/supervisor.py:1235`). |
| P0-D4 | ~~Whether the warm-resume finalize branch is deleted or kept behind a switch~~ — **decided 2026-07-26: deleted outright, together with the `_session_live` flag** (`core/supervisor.py:727`; the flag is read only there, so leaving it write-only would trip `vulture` in CI). No config toggle: greenfield means back-compat owes nothing, a toggle would force a config-schema bump inside P0 and a second code path in tests forever, and P1 already plans the long-term knob as `finalize.session`. A warm auto-fallback "if the packet fails to build" is also rejected — it would restore non-determinism in the one path P0 makes reproducible and mask a build bug; the orchestrator's deterministic minimal summary stays the only fallback. |
| P0-D5 | ~~One PR or two~~ — **decided 2026-07-26: one change for all of P0.** The `tool`/`checks` skip is not split out. The doc's internal contradiction is resolved by restating the hard rule precisely: the packet is required before turning off **all** observations (P1's `none`/`events`), not before skipping deterministic nodes. Consequence: two measurement points (before P0 → after P0), so the skip's and the packet's contributions are not separated. |
| P0-D6 | ~~Whether the packet goes through the same redaction path as the other provider surfaces~~ — **closed by P0-D1 2026-07-26: yes, by construction.** Publication through `publish_artifact` redacts on the way into the exchange, so the only remaining requirement is that the call passes the per-attempt `extra_secrets` — now an acceptance criterion rather than a design fork. |
| P0-D7 | ~~Cross-platform authoring of the packet file~~ — **closed by P0-D2 2026-07-26:** repo-relative POSIX paths inside the packet and `newline=""` on write, the same house pattern the manifests already use (`core/flow/exchange_seal.py:182`). |

### P1 decisions — [supervisor-observation-cadence-p1.md](supervisor-observation-cadence-p1.md)

| ID | Open question |
| --- | --- |
| P1-D1 | ~~Config shape and compatibility~~ — **decided 2026-07-26: hard cut to the nested form, no value migration.** `model` / `reasoning` / `session` move under `observe` / `finalize` / `handoff`; `role_file` and `provider` stay top-level (the former has a flow-local twin, the latter is one per layer). The flat `supervisor.model` / `supervisor.reasoning` leave the schema, the loader rejects them fail-closed naming the new location, and `worc upgrade-config` strips them with a visible `- supervisor.model (removed …)` line. Values are deliberately **not** carried over: one old value would have to be duplicated into both blocks (restoring the expensive-model-on-cheap-notes problem) or guessed. |
| P1-D2 | ~~One name for the setting~~ — **decided 2026-07-26: the same path in both files, `supervisor.observe.mode`.** The flow-local block gains a nested `observe:` sub-mapping (the `defaults.evaluator` pattern in `_parse_supervisor`, `core/flow/snapshot.py:608`), and the flat `observation_mode` name is dropped everywhere — one form in the docs, the `worc-flow-role` skill and every example, with room for a future `include_nodes` without a second rename. |
| P1-D3 | ~~How the schema bump is stated~~ — **fixed in the doc 2026-07-26** (mechanical, not a fork): the number is no longer pinned. The bump is mandatory (flat keys are removed, so an old config stops loading), stated as "increment `CONFIG_SCHEMA_VERSION` from whatever it is at edit time — 31 as of 2026-07-26 — and add the flat keys to `_REMOVED_KEYS`". |
| P1-D4 | ~~Packaged defaults must not be flow-name matching in the engine~~ — **fixed in the doc 2026-07-26** (invariant, not a fork). The per-flow default goes into each packaged flow's own `supervisor:` block, which already exists (`blog_article_revise.yaml:143`, `implementation.yaml:173`); the engine never branches on a flow name. Added as an acceptance criterion too. |
| P1-D5 | ~~The strictness order behind flow-local narrowing~~ — **decided 2026-07-26: a rank order plus `events` as the global default.** `none < events < selected < all` by how many LLM calls a mode can produce; a flow may set a mode whose rank is not higher, checked the way `permission_profile ≤ permission_ceiling` already is (rank table + fail-closed on an unknown value, `security/profiles.py:23-34`). The `selected` loophole is closed explicitly: under a global `events`, only `events` or `none` is accepted. The global default is `events` so user-authored flows without a `supervisor:` block also get the saving while deviations stay observed. |
| P1-D6 | ~~Budget semantics~~ — **decided 2026-07-26: budgets are dropped from P1 entirely.** No `max_calls`, no `max_digest_tokens`, no `on_budget_exhausted`. Once the mode is `events`/`none` the spend already tracks real deviations rather than step count, so a ceiling would cap what is barely spent — while costing a restart-surviving counter and a pre-send token estimator (normalized usage is post-hoc). The digest stays bounded deterministically at 8 000 chars from P0-D3; if a ceiling is ever needed it becomes its own task on top of `FlowRunState.loop_counters`. This also voids the 2026-07-23 "real tokens from the start" premise. |
| P1-D7 | ~~The trigger list against the data actually available in the post-node hook~~ — **decided 2026-07-26: three triggers, closed list.** `rework` (incl. `rework_exhausted`) and `failure` come straight from `outcome.kind`; `fallback` from a cheap `node_runs` read (`provider_used` vs `route_primary`) using the `node_run_id` the hook already has. Dropped: `hitl` and `dangerous_diff` (a human was **already** paused and asked over Telegram — an advisory note adds nothing) and `subtask_boundary` (a `handoff` turn already runs there, `core/orchestrator.py:2891`). The post-node contract does not grow. |
| P1-D8 | ~~Whether the optional `session: fresh_digest` for observe stays in P1~~ — **decided 2026-07-26: dropped; no `session` key at all.** The supervisor's own session grows from its own turns, and under `events` there are only a handful (none under `none`), so the reason for `fresh_digest` is removed by the mode itself. `finalize` gets no `session` key either: after P0-D4 it has exactly one mode, and a single-valued key is dead config — a P0 rollback is a `git revert`. |

### P2 decisions — [supervisor-responsibility-split-p2.md](supervisor-responsibility-split-p2.md)

| ID | Open question |
| --- | --- |
| P2-D1 | ~~Scope trim~~ — **decided 2026-07-26: three items survive.** Keep the `StepRecorder` extraction, per-function usage (the supervisor's rows are already isolated by `node_run_id IS NULL` and already carry the normalized columns — only a function label is missing), and the supervisor report in the summary (`summary.json` is already written every run). Dropped: separate handoff/skill budgets (the campaign has no budgets left after P1-D6, and both functions are off by default — `decomposition.enabled: false`, `skills.dynamic: false`) and the dominance warning (it would need an invented threshold; the report already shows the spend). |
| P2-D2 | ~~Where `StepRecorder` lives and what it owns~~ — **decided 2026-07-26: extend `core/flow/recorder.py`, no new module.** The deterministic step record joins `StateStoreRunRecorder`, which already writes `node_runs` and checkpoints `FlowRunState`; a second module would mean two writers of deterministic facts about the same node. The LLM note keeps its `supervisor_step` row in `evaluations` but now _supplements_ the facts. Terminology fixed in the doc: "ledger" there means those `evaluations` rows, **not** `ledger.py` (terminal transitions). Import contracts unchanged — it stays inside `core/flow/`. |
| P2-D3 | ~~Storage shape for per-function usage~~ — **decided 2026-07-26: one nullable column on `provider_attempts`** (`observe` / `finalize` / `handoff` / `skill`; NULL for graph nodes). A separate table would duplicate the usage columns or force a join into every report, and the "sums reconcile" criterion would stop being one query. Deriving the function from `attempt_dir` is rejected — semantics out of magic numbers in a path string. Migration follows the house rule: additive nullable column in `_migrate`, an older DB refused fail-closed and recreated (as v16 and v19 did). |
| P2-D4 | ~~The dominance-warning threshold and where it surfaces~~ — **void as of 2026-07-26:** P2-D1 dropped that item, so there is no threshold to decide. |

### Acceptance sequence

| # | Step | Note |
| --- | --- | --- |
| 1 | ~~X2 — fix the doc-sync scope in all three items~~ **done 2026-07-26** | Text-only, no dependency; removes a conflict with the branch rule before any code decision |
| 2 | X1 — **contract decided 2026-07-26**; the baseline run itself is the first implementation step of P0 | Everything numeric in P0 and P1 hangs on this |
| 3 | ~~P0-D1 … P0-D7~~ **done 2026-07-26 — P0 is `accepted`** | The only item whose forks could all be closed immediately |
| 4 | Implement P0 (per P0-D5: the observe skip first, then packet + fresh finalize) | Banks the cheap saving immediately |
| 5 | ~~P1-D1 … P1-D8~~ **done 2026-07-26 — P1 is `accepted`** | Closed earlier than planned: D6 (budgets) and D7 (triggers) were answered by shrinking scope, which needs no P0 code. Implementation still waits for P0 to merge. |
| 6 | Implement P1 |  |
| 7 | ~~P2-D1 … P2-D4~~ **done 2026-07-26 — P2 is `accepted`** | Scope trimmed to three items; implementation still waits for P1 to merge. |

## Expected effect

For a comparable `blog_article_revise` pass: P0's `tool`/`checks` skip and fresh finalize alone remove ~44k + a large slice of the 104 567-token final call; P1's finalize-only cadence removes the historical 375 726 observation input tokens. Target: supervisor input falls from ~480k to ~30–60k with zero missed blocking issues — all A/B-gated on the normalized usage from item #1.
