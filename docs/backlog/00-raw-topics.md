- Когда supervisor генерирует summary, так же указывать возможные технический долг и если требуется рефакторинг.
    - т.е. идея в том, что бы проанализировать всю релаизацию + имея весь контекст о том где что происходило и какой агент что делдал и какие пробелмы были, выявлять участки кода которые потенциально требуется переработать.
- нужно добавить в packaged, что бы капировался при установке config.example.yaml вместе со всеми комментариями
- получается что промпт для supervisor и summary общие для всех flow? наверное нужно разделять, или как минимум давать такую возможность.
    - к примеру если в папке конкретного flow мы не находим промпт для супервизора, то тогда делаем fallback на корневой (а если корнекого нету то статику которая указана в самом оркестраторе)
- убрать пометки и комментарии из кода типа `the # supervise_impl/supervise_fix nodes and the summary-as-node were removed from the target` из проекта. Зачем нам знать эти исторически подробности? Что бы что?
    - и все похожие пометки которые не соответствуют текущей реализации - это только засоряет контекст и больше мусора добавляет
- нужно явно зафиксировать все возможныфе переменные в промтах у ролей, по типу {subtask_order}, {subtask_count}, {subtask_spec_path} и так далее, что бы пользователи при создании своих ролей это учитывали и знали что можно использовать.
- изменить структуру как доставляются папка с ролями, implementation нужно положить в соответствующую папку как и другие flow.
    - каждая flow соответственно должна иметь свою папку для агрегации всех промптов.
- зачем нам папка processing которая создается для корневой папки tasks? разве мы туда что то перемещаем? может удалить?
- написать туториал и файл с best practices о том как создавать кастомные flow, как регистрировать и как потом использовать. Нужно что бы пользователи мог создавать свои кастомные flow.
    - также добавить эти файлы в packaged что бы они доставлялись при установки и обновлении оркестратора.
- нужно подумать следующий механизм: что бы когда оркестратор работает с задачами которые связаны и зависят от других, или когда работает decompose - то было бы не плохо сохранять какую то summary именно для следующего агента и следующей задачи. 
    - К примеру есть связка задача A-B-C-D, A это первая задача а все остальные зависят от предыдущей. Когда задача A завершилась, она фиксирует краткое резюме что было сделано именно с техническим уклоном. Затем запускается задача B и резюме от задачи A передается задаче B как часть промта. Когда задача B завершается, она фиксирует что она сделал + что сделал задача A. Следующая задача C получает на старте в промте уже саммари этих двух задача предшественников. И так далее по цепочке.
    - Цель всех этих манимпуляций - улучшить качество выполнения работы, а также ускорить работу агентов. 