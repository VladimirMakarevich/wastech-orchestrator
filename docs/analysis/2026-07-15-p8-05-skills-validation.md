# Разбор прогона: `p8-05` — валидация skills и проверка host-neutrality

- **Целевой репозиторий:** `/Users/a1234/Documents/GitHub/wastech-mdlint`
- **Финальный статус:** `done` · attempt 1 · **`fix_iterations: 6`** (6 review-реворков, 0 test-fix) · PR #14
- **Окно:** 2026-07-15 15:06 → 16:34 UTC (~88 минут)
- **Тип:** код+тесты — CI-валидатор frontmatter, host-neutrality guard, drift-guard skill↔CLI/MCP. **Самый большой diff фазы:** 7 файлов, +861 / −16.
- **Контекст фазы:** см. [обзор фазы P8](2026-07-15-p8-skills-phase-review.md).

## Рамка

6 реворков → 6 fixing → 7 ревью. Проверки зелёные. Ревью 5,99 млн входных / 109 тыс. выходных токенов; fixing 11,79 млн контекста / 68 тыс. выходных; 24 вызова supervisor. Вторая по дороговизне задача фазы после `p8-02`.

Diff: `packages/core/src/skills/parse-static-skill.ts` (новый парсер), `packages/cli/test/skills-surface.e2e.test.ts`, `packages/mcp-server/test/skills-surface.test.ts`, `packages/core/test/{parse-static-skill,skills-validation}.test.ts`, `packages/core/src/index.ts`, phase-doc + index.

## «Чистка луковицы» — здесь особенно наглядно

Ирония задачи: строится **тот самый drift-guard**, который поймал бы ошибки `p8-02/03/04`, — и он итеративно доводится через ревью. Реворки (`evaluations.findings_json`):

1. парсер frontmatter распознаёт только LF-fences (`startsWith("---\n")`) → на Windows/CRLF каждый `SKILL.md` провалит S1; CLI-guard не покрывает `--config`, `graph --format`, `--version`; MCP-guard проверяет лишь имена tool'ов, не форму контракта.
2. дубли ключей frontmatter молча перезаписываются (два `name:` проходят); CLI-guard проверяет только имена опций, не значения (`--on-existing <…>`, `--fail-on off`, `--format json|mermaid|dot`); MCP-guard проверяет лишь `file`, не поля результата.
3. S7-guard режет узкий allowlist bang-команд (`!gh skill install` пройдёт); CLI-drift ведётся ручной матрицей, а не реальными ссылками из SKILL.md; MCP-матрица захардкожена.
4. `init --config` **не существует** в `program.ts`, а init-skill его документирует и guard это пропускает; парсер не закрывает блок `metadata:` после верхнеуровневого ключа (принимает невалидный YAML); closing-fence принимает `----`/`--- extra`.
5. flag-проверка не привязана к команде (prose `--config` проходит, раз флаг есть у кого-то); `parseStaticSkill` протащен в публичный barrel `@wastech-mdlint/core` без прод-потребителя (расширение API ради теста).
6. валидация значений не привязана к владеющей команде (stale-матрица `impact→json` проходит); CLI-half бьёт только `--help`-поверхности, не структурные JSON-контракты, которые агенты потребляют.

Round 7 — `accept`, но **всё ещё с 2 medium-findings** (flag-проверка не command-specific; MCP не доказывает разбор полей результата). Это подтверждает, что порог принятия — на `high`: круги гнали именно blocking/high, а medium/low остаются как след.

## Вывод по задаче

Настоящий глубокий домен (полноценные drift-guards + robust-парсер frontmatter с Windows/CRLF, дублями, битыми fence'ами) — часть слоёв законна. Но раздуто теми же **F1/F2**: автор на `low` выпускал неполные guard'ы (то имена без значений, то значения без привязки к команде, то ручная матрица вместо реальных ссылок), а fixer лечил ровно процитированное, из-за чего «привязка к команде» и «значения, а не только имена» приходили тремя отдельными кругами (2→4→5→6), хотя это один класс «guard проверяет форму, а не только присутствие».

Дополнительно всплыл **самостоятельный дефект скоупа**, пойманный ревью (round 5, medium): `parseStaticSkill` экспортирован в публичный barrel без прод-потребителя — расширение поддерживаемой поверхности core ради теста. Это как раз то, что должен ловить `simplify-review`; здесь его поймало кросс-провайдерное ревью — хороший знак.

**Рычаги:** общие F1/F2/F4 (обзор фазы). Оценка: F2 («class sweep» + `fixing.reasoning → high`) — наибольший эффект именно здесь, сжал бы 6 кругов до ~2–3, объединив однотипные доработки guard'ов.
