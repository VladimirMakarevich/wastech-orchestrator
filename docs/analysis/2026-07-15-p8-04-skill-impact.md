# Разбор прогона: `p8-04` — skill `wastech-mdlint-impact`

- **Целевой репозиторий:** `/Users/a1234/Documents/GitHub/wastech-mdlint`
- **Финальный статус:** `done` · attempt 1 · `fix_iterations: 2` (2 review-реворка, 0 test-fix) · PR #14
- **Окно:** 2026-07-15 14:34 → 15:06 UTC
- **Тип:** авторинг — `skills/wastech-mdlint-impact/SKILL.md` (Markdown) + phase-doc. 2 файла, +209 / −5.
- **Контекст фазы:** см. [обзор фазы P8](2026-07-15-p8-skills-phase-review.md).

## Рамка

2 реворка → 2 fixing → 3 ревью. Проверки зелёные. Ревью 1,80 млн входных / 31 тыс. выходных токенов; 12 вызовов supervisor. Одна из двух «чистых» задач фазы (наряду с `p8-01`).

## Что гоняло круги — неверная интерпретация контракта `slice`/`impact`/`graph`

Из `evaluations.findings_json`:

- **Round 1 (2 × high):** резолвинг таргета неверен — `ContextSliceResult.files` это весь depth-bounded срез, а не разрешённый целевой файл (на дефолтной глубине включит соседей и погонит `impact` не по тому файлу); игнорирует мульти-матч (`starts` может содержать несколько) и переобещает ID-поддержку (только при `settings.idRef`). Плюс: `graph --format summary` **не существует** (реальные форматы: `human`/`json`/`mermaid`/`dot`) → follow-up-команда не запускается.
- **Round 2 (2 × high):** `readingOrder` описан **наоборот** — это топологический порядок подграфа с рёбрами `referrer → referenced`, т.е. для impact сначала идут вышестоящие зависимые, а не зависимости; skill велит читать порядок задом наперёд. Плюс: резолвинг non-file таргета сделан только через CLI, хотя skill в остальном предпочитает MCP → в MCP-only хосте таргет по ID/anchor/heading не разрешить (не host-neutral), хотя `context-slice` с тем же `matchKind`/`starts` уже есть.

Round 3 — `accept`.

## Вывод по задаче

Тот же класс «проза vs реальный контракт продукта»: несуществующий формат `graph --format summary`, перепутанная семантика `readingOrder`, неверная трактовка `slice.files`. Это **F1/F3**. Ревьюер снова точен (цитирует конкретные строки и контракт результата).

Задача завершилась за 2 круга (домен уже, чем у `p8-02`), но обе итерации — предотвратимые: контракты `slice`/`impact`/`graph` лежат в исходниках, которые автор мог сверить. F1 (`implementation.reasoning → high`) + F3 (дисциплина «сверь каждый формат/поле вывода с источником») убрали бы оба круга. Специфичного рычага нет.
