# Разбор прогона: `p8-02` — skill `wastech-mdlint-init` (горячая точка фазы)

- **Целевой репозиторий:** `/Users/a1234/Documents/GitHub/wastech-mdlint`
- **Финальный статус:** `done` · attempt 1 · **`fix_iterations: 8`** (8 review-реворков, 0 test-fix) · PR #14
- **Окно:** 2026-07-15 12:37 → 13:55 UTC (~78 минут — дольше всех)
- **Тип:** авторинг — единственная поставка `skills/wastech-mdlint-init/SKILL.md` (Markdown), плюс phase-doc. 2 файла, +271 / −5.
- **Контекст фазы:** см. [обзор фазы P8](2026-07-15-p8-skills-phase-review.md).

## Рамка

8 реворков → 8 прогонов fixing → 9 запусков ревью. Проверки зелёные все 8 раз (Markdown их не задевает). **Самая дорогая задача фазы:** ревью 5,84 млн входных / 131 тыс. выходных токенов; fixing 5,94 млн контекста; 30 advisory-вызовов supervisor.

## «Чистка луковицы» — каждый круг новый дефект того же класса

Все реворки — про **портируемость бутстрапа и соответствие реальному контракту CLI** (`evaluations.findings_json`):

1. lint-шаг непортируем (`npx` — npm-specific, голый бинарь не на PATH); CI-инструкция не соответствует flow `init` (rerun `--yes` дефолтит в `--on-existing skip` → workflow не пишется); README хардкодит npm вместо vendor-neutral `gh skill install`.
2. no-lockfile ветка (`Package manager: not detected`) не имеет пути завершения install/lint.
3. no-lockfile всё ещё не бутстрапится end-to-end; CI-offer внутренне противоречив.
4. `init [path]` дефолтит в cwd → примеры для `docs/`/`packages/foo/` пишут в корень; `--yes` для npm всё ещё интерактивен (`npx` спрашивает про install); `yarn.lock` → хардкод Berry-only `yarn dlx` ломается на Yarn classic.
5. `cd`-в-таргет ломает `--config docs/…` (двойной путь `docs/docs/…`); недоучёт `bun.lock`/`bun.lockb` и приоритета `bun>pnpm>yarn>npm`.
6. `--yes` тихо перезаписывает подтверждённый конфиг; пути с пробелами не закавычены; раннер для nested-config недоспецифицирован.
7. runner из effective-init-root, а install/lint описаны как repo-root; нет ветки для no-write исходов `init` (skip/`Not written:`); `--on-existing` на интерактивном rerun игнорируется; `compatibility: ">=0.0.0"` не version-coupled (нарушает I7/P9).
8. frontmatter заявляет same-tag compat, а install тянет текущую версию CLI; правило «ask before…» делает обязательный install условным; `merge` vs `overwrite` поданы как взаимозаменяемые.

Round 9 — `accept` (пустой `findings`).

## Вывод по задаче

Это **не** сбой ревью и **не** scope-крип (diff — ровно один файл). Домен объективно глубокий (портируемый бутстрап поверх npm/pnpm/yarn-classic/yarn-berry/bun с nested-конфигами и семантикой `--on-existing`), поэтому 8 слоёв — во многом настоящая последовательная детализация. Но масштаб раздут связкой из обзора фазы:

- **F1:** автор читал `program.ts`/`init-command.ts` (подтверждено `events.jsonl` `run-000218`), но на `reasoning: low` заявил, что «ссылается только на подтверждённые поверхности», и всё равно написал `init --config` (у `init` его нет) и Berry-only раннер. Правдоподобно-но-неверно.
- **F2:** fixer в раундах 1 и 8 (`run-000221`, `run-000242`) открывал **только** `findings.json` + `SKILL.md`, не переоткрывая исходники CLI, — лечил процитированную строку и не подметал класс. Отсюда 8 однопунктовых кругов вместо 2–3.
- **F4:** `plan.md:26` перечислил `--config` во flat-списке без привязки к команде — сид для `init --config`.

**Рычаги (target-only):** F1 (`implementation.reasoning → high`), F2 (`fixing.reasoning → high` + «class sweep» в [`fixing.md`](../../../wastech-mdlint/.worc/flows/implementation/fixing.md)), F3 (авторская дисциплина сверки в [`implementation.md`](../../../wastech-mdlint/.worc/flows/implementation/implementation.md)), F4 (привязка флагов к команде в [`planning.md`](../../../wastech-mdlint/.worc/flows/implementation/planning.md)). Оценка: связка F1+F2 сжала бы задачу с 8 до ~2–3 кругов и сэкономила ~3,9 млн входных токенов только по ревью.
