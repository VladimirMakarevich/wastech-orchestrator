# Разбор прогона: `p8-03` — skill `wastech-mdlint-fix`

- **Целевой репозиторий:** `/Users/a1234/Documents/GitHub/wastech-mdlint`
- **Финальный статус:** `done` · attempt 1 · `fix_iterations: 3` (3 review-реворка, 0 test-fix) · PR #14
- **Окно:** 2026-07-15 13:55 → 14:34 UTC
- **Тип:** авторинг — `skills/wastech-mdlint-fix/SKILL.md` (Markdown) + phase-doc. 2 файла, +257 / −8.
- **Контекст фазы:** см. [обзор фазы P8](2026-07-15-p8-skills-phase-review.md).

## Рамка

3 реворка → 3 fixing → 4 ревью. Проверки зелёные. Ревью 2,47 млн входных / 50 тыс. выходных токенов; 15 вызовов supervisor.

## Что гоняло круги — всё про несоответствие прозы реальному контракту CLI/core

Из `evaluations.findings_json`:

- **Round 1 (4 finding'а):** шаги 2/3/5 используют `lint` с дефолтным `--fail-on error` → `resolveLintExitCode` (`packages/cli/src/commands.ts`) возвращает 1 при оставшихся ошибках, хост может остановиться до разбора findings; блок «Auto-fixed files» невыводим из документированных команд (`applyFixes()` в `core/engine/fix.ts` возвращает `fixedFiles`, но `handleLint()` его отбрасывает, `formatLintResultJson()` эмитит только `summary/messages/files`); политика покрывает лишь встроенные семейства, но v2 имеет декларативные custom-правила (`REQ-OWNER`, `resolveCustomRule`), всегда non-fixable; setup-gate проверяет лишь наличие конфига.
- **Round 2 (high):** workflow не делает структурный снимок **после** `--fix` перед AI/manual-шагом → работает с pre-fix baseline, findings, всплывшие из-за `--fix` (`SEC-001` дописывает TODO → `CTX-001` флагует плейсхолдеры), теряются.
- **Round 3:** политика REF-семейства покрывает лишь REF-001/002/003 и часть 005, но не REF-004/005/006 из README-таблицы; проза утверждает, что `column` всегда присутствует, хотя в `LintMessage` он опционален.

Round 4 — `accept`.

## Вывод по задаче

Тот же класс, что и `p8-02`/`p8-04`: **skill описывает поведение CLI/core, которого нет или которое иное** (`--fail-on` exit-код, `fixedFiles` не в JSON, custom-правила, опциональный `column`, полный набор REF-*). Это **F1/F3** из обзора: автор на `low` не выверил структурные контракты (`fix.ts`, `commands.ts`, таблицу правил) до утверждений в прозе; шаг Verify (`npm test/build`) прозу не валидирует.

Ревью снова корректно и точно (ссылки на символы в исходниках). Специфичного для `p8-03` рычага нет — лечится общими F1/F3, плюс F2 сжал бы round 3 (REF-семейство и `column` — один класс «сверь весь контракт правил», а не по одному).
