# Разбор прогона: `p4-08-graph-tests` (финальный шаг branch-mode chain-теста)

## STATUS

- **Задача:** `p4-08-graph-tests` — «P4.08 — Graph / slice / impact tests & fixtures». Седьмой и последний шаг chain-теста: `branch_mode: current` — оператор вручную сделал `git checkout feat/p4-graph-chain` ДО `worc run`, без `branch_ref`.
- **Final status:** `done` · **PR** [#9](https://github.com/VladimirMakarevich/wastech-mdlint/pull/9) (открыт, не смержен, **7-е и последнее накопление**: p4-02..p4-08, 47 файлов, +2645/−110) · ветка `feat/p4-graph-chain`.
- **finished_at:** 2026-07-04T23:17:09Z · `fix_iterations=0`.
- **Провайдеры:** claude opus-4-8/high; codex на `review` упал (9/9, F24), fallback claude → `accept`.
- **Побочка (важно):** рабочее дерево target-репо **осталось на ветке `feat/p4-graph-chain`** после завершения задачи (не на `main`) — ожидаемо для `current`-mode; перед следующей задачей ИЗ ОЧЕРЕДИ (не из этой цепочки) нужно будет вручную `git checkout main` (или другую нужную ветку).

## Короткий вывод

Последний непротестированный кусок ADR branch-mode — режим `current` — подтверждён двумя ключевыми фактами: (1) оркестратор НЕ трогал checkout (использовал HEAD как есть, ровно как задокументировано); (2) `terminal_cleanup()` **не форсит** возврат на base — дерево осталось на рабочей ветке. Оба факта — прямая эмпирическая проверка "safety invariant" ADR (деструктивные операции/force-checkout — только в `new`). Явный warning про непригодность `current` для unattended-режима появился даже на прямом `worc run` (не только `watch`) — информативно, не блокирует.

**Итог всей цепочки (p4-02..p4-08):** одна ветка, один PR (#9), 7 задач, 1 fix-цикл (p4-05, реальный HIGH-баг), 9/9 детерминированных codex-крашей (F24) все спасены fallback'ом, 0 ручных вмешательств кроме двух заранее спланированных ручных шагов (git checkout для `current`, правка `depends_on` после находки F25/F26).

## Как прошёл прогон

planning → implementation (~7 мин, тесты+фикстуры на весь стек p4-01..p4-07) → testing (green) → review (codex crash #9/9 идентично F24 → claude, `accept`, 2 LOW) → documentation → publish (PR #9 reused 7-й раз). Checks: typecheck/lint/test/build все зелёные.

Review-находки (2 LOW, обе про сам тестовый фикстур/детерминизм-чек, не про продуктовый код): (1) фикстура `design.md` случайно становится вторым orphan-узлом наравне с намеренным `orphan.md`, размывая «один провал = одно поведение»; (2) определинизм-self-check в e2e сравнивает рёбра через конкатенированную строку с `localeCompare`, что теоретически может дать иной порядок, чем числовое сравнение `line` в продуктовом `compareEdges`, при многозначных номерах строк — латентная хрупкость, не сработавшая на этой фикстуре.

## Находки

Новых F-номеров нет. Все протестированные механики branch-mode ADR (`new`+custom name, `existing`+PR-reuse, `current`+no-force-checkout, `publish: push` cap) сработали ТОЧНО как задокументировано.

## Что уже хорошо

- **`current`-mode: оба safety-инварианта подтверждены живьём** — no-op на checkout, no-force-checkout на cleanup.
- **Вся 7-задачная цепочка сошлась в один PR** без единого дублирующего PR, без потери коммитов, без порчи истории.
- **codex-краш (F24) полностью детерминирован на 9/9 попытках** — не флуктуация, railroad-надёжный сигнал для будущего регрессионного теста.
- **Единственный настоящий баг за всю цепочку (p4-05) был найден и исправлен за 1 итерацию**, не просочился в финальный PR.

## Сводная таблица

| Наблюдение | Причина | Рычаг | Зона |
| --- | --- | --- | --- |
| `current`-mode не трогал checkout, не форсил cleanup на base | `prepare_branch`/`terminal_cleanup` gated на `branch_mode==new` для деструктивных/переключающих операций | `git_manager.py` (branch-mode ADR) — работает как задумано | orchestrator (подтверждение) |
| Warning про `current`+unattended появился и на прямом `run` | эмиссия warning не гейтится специфично на `watch` | — (информативно, не баг) | orchestrator |
| PR #9 закрыл всю цепочку одним PR (47 файлов, 7 коммитов) | PR-reuse на каждом шаге корректно находил и продолжал тот же открытый PR | `git_manager.py:992-1015` | orchestrator (итоговое подтверждение) |
| codex review упал 9/9 идентично | F24 (не новое, финально подтверждено) | `core/flow/nodes/evaluator.py:57-78` | orchestrator |
